{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p>Welcome to Jam documentation!</p> <p> </p>"},{"location":"#about","title":"About","text":"<p>Jam (Jam Auth Module) - A universal auth* combine that provides popular auth mechanisms strictly according to the specification.</p>"},{"location":"#installation","title":"Installation","text":"pip install jamlibInstalled!"},{"location":"#quick-example","title":"Quick example","text":"<pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\npayload = {\n    \"user\": 1\n}\n\njwt = jam.jwt_encode(payload=payload)\nsession_id = jam.session_create(\"user@mail.com\", payload)\notp_code = jam.otp_code(secret=\"7K2HVNA3IQCYFFDX76IXKNCZHQ\")\n</code></pre>"},{"location":"#asynchronous-support","title":"Asynchronous support","text":"<p>Note</p> <p>You can use <code>jam.aio</code> module to work with async functions. The methods are the same, but you need to use <code>await</code> keyword.</p> <pre><code>from jam.aio import Jam\n\njam = Jam(config=\"config.toml\")\ntoken = await jam.jwt_encode(\n    iss=\"Jam\",\n    sub=\"username@example.com\"\n)\n</code></pre>"},{"location":"#why-jam","title":"Why Jam?","text":"<p>Jam is a library that provides the most popular AUTH* mechanisms right out of the box.</p> <ul> <li>JOSE</li> <li>PASETO</li> <li>Server side sessions</li> <li>OTP</li> <li>OAuth2</li> <li>SAML</li> </ul>"},{"location":"#framework-integrations","title":"Framework integrations","text":"<p>Jam provides ready-to-use integrations for the most popular frameworks:</p> <ul> <li>FastAPI</li> <li>Starlette</li> <li>Litestar</li> <li>Flask</li> </ul> <p>Each integration offers built-in middleware or plugin support for JWT and session-based authentication.</p>"},{"location":"#why-choose-jam","title":"Why choose Jam?","text":"<p>Jam supports many authentication methods out of the box with minimal dependencies. Here is a comparison with other libraries:</p> Features / Library Jam Authx PyJWT AuthLib OTP Auth JOSE \u2705 \u274c only JWT \u274c only JWT \u2705 \u274c JWT black/white lists \u2705 \u274c \u274c \u274c \u274c PASETO \u2705 \u274c \u274c \u274c \u274c Server side sessions \u2705 \u2705 \u274c \u274c \u274c OTP \u2705 \u274c \u274c \u274c \u2705 OAuth2 \u2705 \u2705 \u274c \u2705 \u274c SAML 2.0 \u2705 \u274c \u274c \u274c \u274c Flexible config \u2705 \u274c \u274c \u274c \u274c Modularity \u2705 \u274c \u274c \u274c \u274c"},{"location":"configuration/","title":"Jam configuration","text":""},{"location":"configuration/#config","title":"Config","text":"<p>The configuration only works for <code>jam.Jam</code>/<code>jam.aio.Jam</code>. Standalone modules such as <code>jam.jwt.JWT</code>, <code>jam.paseto.PASETOv4</code>, etc. are configured simply by the class's <code>__init__</code>. For each model, see the corresponding documentation.</p>"},{"location":"configuration/#instance","title":"Instance","text":"<p>The <code>*.Jam</code> class itself has several parameters: <pre><code>from jam import Jam\n\njam = Jam(\n    config=\"path/to/config/file.toml.yaml.json\", # or python-dict\n    pointer=\"jam\",\n    logger=JamLogger,\n    log_level=\"INFO\",\n    serializer=JsonEncoder\n)\n</code></pre></p>"},{"location":"configuration/#config-str-dictstr-any","title":"config: str | dict[str, Any]","text":"<p>This is the path to your config as a <code>string</code> or dict with the configuration:</p>"},{"location":"configuration/#python-dict","title":"Python dict","text":"<pre><code>import os\n\nfrom jam import Jam\n\nconfig = {\n    \"jose\": {\n        \"jwt\": {\n            \"alg\": \"HS256\",\n            \"secret_key\": os.getenv(\"JWT_SECRET_KEY\")\n        }\n    },\n    \"paseto\": {\n        \"version\": \"v4\",\n        \"purpose\": \"local\",\n        \"key\": os.getenv(\"PASETO_SECRET_KEY\")\n    }\n    }\n}\n\njam = Jam(config=config)\njwt = jam.jwt_encode(payload={\"user\": 1})\npaseto = jam.paseto_create(payload={\"user\": 1}, footer=None)\n</code></pre>"},{"location":"configuration/#toml","title":"TOML","text":"<p><pre><code>[jam.jose.jwt]\nalg = \"HS256\"\nsecret_key = \"$JWT_SECRET_KEY\"\n\n[jam.paseto]\nversion = \"v4\"\npurpose = \"local\"\nkey = \"$PASETO_SECRET_KEY\"\n</code></pre> <pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\njwt = jam.jwt_encode(payload={\"user\": 1})\npaseto = jam.paseto_create(payload={\"user\": 1}, footer=None)\n</code></pre></p>"},{"location":"configuration/#yaml","title":"YAML","text":"<p><pre><code>jam:\n  jose:\n    jwt:\n      alg: HS256\n      secret_key: $JWT_SECRET_KEY\n  paseto:\n    version: v4\n    purpose: local\n    key: $PASETO_SECRET_KEY\n</code></pre> <pre><code>from jam import Jam\n\njam = Jam(config=\"config.yaml\")\njwt = jam.jwt_encode(payload={\"user\": 1})\npaseto = jam.paseto_create(payload={\"user\": 1}, footer=None)\n</code></pre></p>"},{"location":"configuration/#json","title":"Json","text":"<p><pre><code>{\n  \"jose\": {\n    \"jwt\": {\n      \"alg\": \"HS256\",\n      \"secret_key\": \"$JWT_SECRET_KEY\"\n    }\n  },\n  \"paseto\": {\n    \"version\": \"v4\",\n    \"purpose\": \"local\",\n    \"key\": \"$PASETO_SECRET_KEY\"\n  }\n}\n</code></pre> <pre><code>from jam import Jam\n\njam = Jam(config=\"config.json\")\njwt = jam.jwt_encode(payload={\"user\": 1})\npaseto = jam.paseto_create(payload={\"user\": 1}, footer=None)\n</code></pre></p>"},{"location":"configuration/#pointer-str-jam","title":"pointer: str = \"jam\"","text":"<p>This is the point that Jam will read as config.</p> <p>For example, if we do it like this: <pre><code>from jam import Jam\n\njam = Jam(\n    config=\"config.toml\",\n    pointer=\"anotherpointer\" # &lt;- Another pointer\n)\njwt = jam.jwt_encode(payload={\"user\": 1})\npaseto = jam.paseto_create(payload={\"user\": 1}, footer=None)\n</code></pre></p> <p>Our config file will look like this: <pre><code>[anotherpointer.jose.jwt]  # pointer\nalg = \"HS256\"\nsecret_key = \"$JWT_SECRET_KEY\"\n\n[anotherpointer.paseto]  # pointer\nversion = \"v4\"\npurpose = \"local\"\nkey = \"$PASETO_SECRET_KEY\"\n</code></pre></p> <p>Tip</p> <p>This can be useful for configuring two instances of <code>Jam</code> in a single file, for example.</p>"},{"location":"configuration/#logger-typebaselogger-jamlogger","title":"logger: type[BaseLogger] = JamLogger","text":"<p>Logger class. Useful if you need to replace the standard logger. <pre><code>from jam import Jam\n\nfrom yourapp.logger import YourCustomLogger\n\njam = Jam(config=\"config.toml\", logger=YourCustomLogger)\n</code></pre></p> <p>This parameter can also be configured in the config file, but then you need to pass the path to the class in Python style: <pre><code>[jam]\nlogger = \"yourapp.logger.YourCustomLogger\"\n\n[jam.jose.jwt]\nalg = \"HS256\"\nsecret_key = \"$JWT_SECRET_KEY\"\n</code></pre></p> <p>For more details, see the documentation on logging.</p>"},{"location":"configuration/#log_level-str-info","title":"log_level: str = \"INFO\"","text":"<p>Jam logging level.  The takes one of the following values: <code>DEBUG</code>, <code>INFO</code>, <code>WARNING</code>, <code>ERROR</code>, <code>CRITICAL</code>.</p> <p>It can also be passed in the config file: <pre><code>[jam]\nlog_level = \"INFO\"\n\n[jam.jose.jwt]\nalg = \"HS256\"\nsecret_key = \"$JWT_SECRET_KEY\"\n</code></pre></p>"},{"location":"configuration/#serializer-typebaseencoder-jsonencoder","title":"serializer: type[BaseEncoder] = JsonEncoder","text":"<p>JSON object serializer. By default, JsonEncoder is used, which utilizes sdtlib.json. </p> <p>It can also be passed in the config file as a string: <pre><code>[jam]\nserializer = \"jam.encoders.JsonEncoder\"\n\n[jam.jose.jwt]\nalg = \"HS256\"\nsecret_key = \"$JWT_SECRET_KEY\"\n</code></pre></p> <p>For more details, see the documentation on serialization.</p>"},{"location":"configuration/#environment-variables","title":"Environment variables","text":"<p>Jam will automatically search for environment variables if a value begins with <code>$</code> in config files. For python dict, use <code>os.getenv</code>.</p> <p>Example:</p> <pre><code>[jwt]\nlog_level = \"INFO\"\n\n[jam.jose.jwt]\nalg = \"$JWT_ALG\"\nsecret_key = \"$JWT_SECRET\"\n</code></pre> <p>Note</p> <p>Some modules read certain environment variables by default, as described in detail in each module.</p>"},{"location":"contributing/","title":"Contributing","text":"<p>If you want to help develop Jam, there are a few rules to follow.</p>"},{"location":"contributing/#code-style","title":"Code style","text":"<ul> <li>Python &gt;= 3.10</li> <li>Line length: 80 characters, indented 4 spaces</li> <li>Import order: stdlib \u2192 third-party \u2192 library modules (handled by ruff/isort)</li> <li>Docstrings: Google convention (handled by ruff/pydocstyle)</li> </ul> <pre><code># Built-in libraries\nfrom typing import Literal\n\n# Third-party libraries\nfrom cryptography.hazmat.primitives import hashes\n\n# Library modules\nfrom jam.jwt import Token\n\n\nclass SomeClass:\n    \"\"\"Long class description.\"\"\"\n\n    def __init__(self, some_value: str) -&gt; None:\n        \"\"\"Class constructor.\n\n        Args:\n            some_value (str): Some value\n        \"\"\"\n        self.sv = some_value\n\n    def some_method(self, something: int) -&gt; int:\n        \"\"\"Method description.\n\n        Args:\n            something (int): Argument description\n\n        Raises:\n            ValueError: if something &lt; 1\n\n        Returns:\n            int: Something plus one\n        \"\"\"\n        if something &lt; 1:\n            raise ValueError(\"something &lt; 1\")\n\n        return something + 1\n</code></pre>"},{"location":"contributing/#development-tools","title":"Development Tools","text":"<ul> <li>ruff \u2014 linter + formatter (<code>uv run ruff check</code>, <code>uv run ruff format</code>)</li> <li>pytest \u2014 tests (<code>uv run pytest</code>)</li> <li>pyrefly \u2014 type checking (<code>uv run pyrefly check</code>)</li> <li>pre-commit \u2014 <code>uv run pre-commit run --all-files</code></li> </ul>"},{"location":"contributing/#gitflow","title":"Gitflow","text":""},{"location":"contributing/#commits","title":"Commits","text":"<p>The project must have <code>pre-commit</code></p> <ul> <li><code>[+]</code> Adding new functionality</li> <li><code>[-]</code> Removing something, e.g. removing a function or deleting a file</li> <li><code>[*]</code> Changing the logic</li> <li><code>[~]</code> Changes that do not affect the logic, documentation, linters, etc</li> </ul> <p>The keys in the commit body are also used:</p> <ul> <li><code>R</code>(reason): Reason for change, deletion, etc</li> <li><code>FB</code>(fix by): How it was made or fixed</li> <li><code>N</code>(note): A note of some kind</li> </ul> <p>Example of a correct commit:</p> <pre><code>[*] Changed JWT decryption logic\nR: Used a third-party heavy dependency that slowed down the code\nFB: Removed the dependency and wrote independently\n</code></pre>"},{"location":"contributing/#branches","title":"Branches","text":"<p>All branches are created from <code>master</code> and adhere to strict branch naming:</p> <ul> <li>New feature: <code>feature/&lt;id-issue-if-it-is&gt;-&lt;pair-words-about-feature&gt;</code></li> <li>Bug fixes: <code>fix/&lt;id-issue-if-it-is&gt;-&lt;pair-words-about-bug&gt;</code></li> <li>Refactoring: <code>refactor/&lt;id-issue-if-it-is&gt;-&lt;what-exactly-you-do&gt;</code></li> <li>Writing documentation: <code>docs/&lt;what-exactly-you-do&gt;</code></li> </ul> <p>Examples:</p> <ul> <li><code>feature/validate-redis-session</code></li> <li><code>fix/18-jwt-making</code></li> </ul>"},{"location":"contributing/#pull-requests","title":"Pull requests","text":"<p>The title of the pull request must contain a keyword:</p> <ul> <li><code>FEAT</code> \u2014 Adding a new feature</li> <li><code>BUGFIX</code> \u2014 Bug fix</li> <li><code>HOTFIX</code> \u2014 Urgent bug fix</li> <li><code>DOCS</code> \u2014 Edits/additions to documentation</li> </ul> <p>Example:</p> <pre><code>FEAT Validation of a redis session\n\n## What was done\n\n- Brief description of what was done\n\n## Test report\n\n- How you tested\n</code></pre>"},{"location":"installation/","title":"Installation","text":"<p>Stable release from pypi: <pre><code>pip install jamlib\n</code></pre></p> <p>Stable version(but not released) from github: <pre><code>pip install git+https://github.com/mkrdnk/jam.git@master\n</code></pre></p>"},{"location":"philosophy/","title":"Philosophy","text":"<p>Minimalism. Flexibility. Control.</p> <p>Jam is a lightweight Python authorization library designed to put you in full control:</p> <ul> <li>Simple types \u2014 methods return only standard Python objects (<code>str</code>, <code>int</code>, <code>dict</code>, <code>list</code>, etc.).  </li> <li>Fully replaceable modules \u2014 any component, including <code>jam.Jam</code>, can be extended via inheritance.</li> </ul> <p>Trade-offs for your control:</p> Choice Benefit Manual configuration Transparency and control Inheritance over DI Easy extension without hidden magic Simple types Compatibility and easy serialization <p></p>"},{"location":"api/SUMMARY/","title":"API Reference","text":"<ul> <li>jam.base</li> <li>jam.base_encoder</li> <li>jam.defaults</li> <li>jam.deprecated</li> <li>jam</li> <li>jam.aio.base</li> <li>jam.aio</li> <li>jam.aio.instance</li> <li>jam.aio.jwt</li> <li>jam.aio.oauth2.base</li> <li>jam.aio.oauth2</li> <li>jam.aio.oauth2.builtin</li> <li>jam.aio.oauth2.builtin.github</li> <li>jam.aio.oauth2.builtin.gitlab</li> <li>jam.aio.oauth2.builtin.google</li> <li>jam.aio.oauth2.builtin.yandex</li> <li>jam.aio.oauth2.client</li> <li>jam.aio.sessions.base</li> <li>jam.aio.sessions</li> <li>jam.aio.sessions.json</li> <li>jam.aio.sessions.redis</li> <li>jam.cli</li> <li>jam.cli.cli</li> <li>jam.cli.commands</li> <li>jam.cli.commands.keys</li> <li>jam.cli.commands.password</li> <li>jam.encoders</li> <li>jam.exceptions</li> <li>jam.exceptions.base</li> <li>jam.exceptions.jose</li> <li>jam.exceptions.jwt</li> <li>jam.exceptions.oauth2</li> <li>jam.exceptions.paseto</li> <li>jam.exceptions.plugins</li> <li>jam.exceptions.saml</li> <li>jam.exceptions.sessions</li> <li>jam.ext</li> <li>jam.ext.fastapi</li> <li>jam.ext.flask</li> <li>jam.ext.flask.extensions</li> <li>jam.ext.flask.objects</li> <li>jam.ext.litestar</li> <li>jam.ext.litestar.middleware</li> <li>jam.ext.litestar.objects</li> <li>jam.ext.litestar.plugins</li> <li>jam.ext.starlette</li> <li>jam.ext.starlette.backends</li> <li>jam.ext.starlette.objects</li> <li>jam.instance</li> <li>jam.jose.algorithms</li> <li>jam.jose.base</li> <li>jam.jose</li> <li>jam.jose.jwe</li> <li>jam.jose.jwk</li> <li>jam.jose.jws</li> <li>jam.jose.jwt</li> <li>jam.jose.lists.base</li> <li>jam.jose.lists</li> <li>jam.jose.lists.json</li> <li>jam.jose.lists.memory</li> <li>jam.jose.lists.redis</li> <li>jam.jose.utils</li> <li>jam.jwt.algorithms</li> <li>jam.jwt.base</li> <li>jam.jwt</li> <li>jam.jwt.types</li> <li>jam.jwt.lists.base</li> <li>jam.jwt.lists</li> <li>jam.jwt.lists.json</li> <li>jam.jwt.lists.redis</li> <li>jam.jwt.module</li> <li>jam.jwt.utils</li> <li>jam.logger</li> <li>jam.oauth2.base</li> <li>jam.oauth2</li> <li>jam.oauth2.builtin</li> <li>jam.oauth2.builtin.github</li> <li>jam.oauth2.builtin.gitlab</li> <li>jam.oauth2.builtin.google</li> <li>jam.oauth2.builtin.yandex</li> <li>jam.oauth2.client</li> <li>jam.otp.base</li> <li>jam.otp</li> <li>jam.otp.hotp</li> <li>jam.otp.totp</li> <li>jam.paseto.base</li> <li>jam.paseto</li> <li>jam.paseto.utils</li> <li>jam.paseto.v1</li> <li>jam.paseto.v2</li> <li>jam.paseto.v3</li> <li>jam.paseto.v4</li> <li>jam.plugins.base</li> <li>jam.plugins</li> <li>jam.saml.base</li> <li>jam.saml</li> <li>jam.saml.binding</li> <li>jam.saml.encryption</li> <li>jam.saml.metadata</li> <li>jam.saml.saml</li> <li>jam.saml.signature</li> <li>jam.saml.types</li> <li>jam.saml.xml</li> <li>jam.sessions.base</li> <li>jam.sessions</li> <li>jam.sessions.json</li> <li>jam.sessions.redis</li> <li>jam.utils</li> <li>jam.utils.aes</li> <li>jam.utils.await_maybe</li> <li>jam.utils.basic_auth</li> <li>jam.utils.config_maker</li> <li>jam.utils.ed</li> <li>jam.utils.otp_keys</li> <li>jam.utils.rsa</li> <li>jam.utils.salt_hash</li> <li>jam.utils.symmetric</li> <li>jam.utils.version_check</li> <li>jam.utils.xchacha20poly1305</li> <li>jam.utils.xor</li> </ul>"},{"location":"api/jam.__base__/","title":"jam.base","text":""},{"location":"api/jam.__base__/#jam.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseJam</code> <p>Base jam instance.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam","title":"BaseJam","text":"<pre><code>BaseJam(\n    config: str | dict[str, Any] = \"pyproject.toml\",\n    pointer: str = \"jam\",\n    *,\n    logger: type[BaseLogger] = JamLogger,\n    log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ] = \"INFO\",\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    plugins: list[type[BasePlugin]] = [],\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base jam instance.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Union[str, dict[str, Any]]</code> <p>Configuration</p> <code>'pyproject.toml'</code> <code>pointer</code> <code>str</code> <p>Pointer</p> <code>'jam'</code> <code>logger</code> <code>BaseLogger</code> <p>Logger</p> <code>JamLogger</code> <code>log_level</code> <code>Literal['DEBUG', 'INFO', 'WARNING', 'ERROR', 'CRITICAL']</code> <p>Log level</p> <code>'INFO'</code> <code>serializer</code> <code>Union[BaseEncoder, type[BaseBrowser]]</code> <p>Serializer</p> <code>JsonEncoder</code> <code>plugins</code> <code>list[type[BasePlugin]]</code> <p>List of plugins</p> <code>[]</code> <p>Returns:</p> Type Description <code>None</code> <p>None</p> <p>Methods:</p> Name Description <code>emit</code> <p>Emit event.</p> <code>jwe_decrypt</code> <p>Decrypt JWE token.</p> <code>jwe_encrypt</code> <p>Encrypt data using JWE.</p> <code>jws_sign</code> <p>Sign data using JWS.</p> <code>jws_verify</code> <p>Verify JWS token.</p> <code>jwt_decode</code> <p>Verify and decode JWT token.</p> <code>jwt_encode</code> <p>Encode the JWT with the given expire, header, and payload.</p> <code>oauth2_client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>oauth2_fetch_token</code> <p>Exchange authorization code for access token.</p> <code>oauth2_get_authorized_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>oauth2_refresh_token</code> <p>Use refresh token to obtain a new access token.</p> <code>otp_code</code> <p>Generates an OTP.</p> <code>otp_uri</code> <p>Generates an otpauth:// URI for Google Authenticator.</p> <code>otp_verify_code</code> <p>Checks the OTP code, taking into account the acceptable window.</p> <code>paseto_create</code> <p>Create new PASETO.</p> <code>paseto_decode</code> <p>Decode PASETO and return payload and footer.</p> <code>paseto_make_payload</code> <p>Generate payload for PASETO.</p> <code>session_clear</code> <p>Delete all sessions by key.</p> <code>session_create</code> <p>Create new session.</p> <code>session_delete</code> <p>Delete session.</p> <code>session_get</code> <p>Get data from session.</p> <code>session_rework</code> <p>Rework session.</p> <code>session_update</code> <p>Update session data.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.__build_instance","title":"__build_instance","text":"<pre><code>__build_instance(config: dict[str, Any]) -&gt; None\n</code></pre> <p>Build instance.</p> <p>Load modules from configuration and initialize them. Supports both flat modules (name -&gt; path) and nested modules (name -&gt; {subname -&gt; path}).</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any]</code> <p>Configuration</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.__build_main_config","title":"__build_main_config","text":"<pre><code>__build_main_config(\n    config: dict[str, Any],\n    default_logger: type[BaseLogger],\n    default_log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ],\n    default_serializer: BaseEncoder | type[BaseEncoder],\n) -&gt; dict[str, Any]\n</code></pre> <p>Build main params from config like logger, loglevel, etc.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any]</code> <p>Configuration dictionary</p> required <code>default_logger</code> <code>type[BaseLogger]</code> <p>Default logger class</p> required <code>default_log_level</code> <code>Literal</code> <p>Default log level</p> required <code>default_serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>Default serializer</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Dictionary with logger, log_level, and serializer</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.__otp","title":"__otp","text":"<pre><code>__otp(\n    type: Literal[\"totp\", \"hotp\"] | None = None,\n) -&gt; type[BaseOTP] | None\n</code></pre> <p>Get OTP type.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.__setup_plugins","title":"__setup_plugins","text":"<pre><code>__setup_plugins(plugins: list[type[BasePlugin]]) -&gt; None\n</code></pre> <p>Setup plugins.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.emit","title":"emit","text":"<pre><code>emit(event: str, **kwargs) -&gt; Any\n</code></pre> <p>Emit event.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>str</code> <p>Event name,</p> required <code>**kwargs</code> <p>Event data</p> <code>{}</code>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.jwe_decrypt","title":"jwe_decrypt  <code>abstractmethod</code>","text":"<pre><code>jwe_decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE token.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>Decrypted data.</p> <p>Raises:</p> Type Description <code>JamJWEDecryptionError</code> <p>If decryption fails.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.jwe_encrypt","title":"jwe_encrypt  <code>abstractmethod</code>","text":"<pre><code>jwe_encrypt(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt data using JWE.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to encrypt. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWE header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWE token.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.jws_sign","title":"jws_sign  <code>abstractmethod</code>","text":"<pre><code>jws_sign(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Sign data using JWS.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWS header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS token.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.jws_verify","title":"jws_verify  <code>abstractmethod</code>","text":"<pre><code>jws_verify(token: str) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS token.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload.</p> <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If verification fails.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.jwt_decode","title":"jwt_decode  <code>abstractmethod</code>","text":"<pre><code>jwt_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n    check_nbf: bool = False,\n    include_headers: bool = False,\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify and decode JWT token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token</p> required <code>check_exp</code> <code>bool</code> <p>Check expire</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check white/black list. Docs: https://jam.makridenko.ru/jwt/lists/what/</p> <code>True</code> <code>check_nbf</code> <code>bool</code> <p>Check not-before time</p> <code>False</code> <code>include_headers</code> <code>bool</code> <p>Include headers in the decoded payload</p> <code>False</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.jwt_encode","title":"jwt_encode  <code>abstractmethod</code>","text":"<pre><code>jwt_encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    *,\n    payload: dict[str, Any] | None = None,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID. If none use the JTI fabric function.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.oauth2_client_credentials_flow","title":"oauth2_client_credentials_flow  <code>abstractmethod</code>","text":"<pre><code>oauth2_client_credentials_flow(\n    provider: str,\n    scope: list[str] | None = None,\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.oauth2_fetch_token","title":"oauth2_fetch_token  <code>abstractmethod</code>","text":"<pre><code>oauth2_fetch_token(\n    provider: str,\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.oauth2_get_authorized_url","title":"oauth2_get_authorized_url  <code>abstractmethod</code>","text":"<pre><code>oauth2_get_authorized_url(\n    provider: str, scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra ath params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.oauth2_refresh_token","title":"oauth2_refresh_token  <code>abstractmethod</code>","text":"<pre><code>oauth2_refresh_token(\n    provider: str,\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.otp_code","title":"otp_code  <code>abstractmethod</code>","text":"<pre><code>otp_code(\n    secret: str | bytes, factor: int | None = None\n) -&gt; str\n</code></pre> <p>Generates an OTP.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>OTP code (fixed-length string).</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.otp_uri","title":"otp_uri  <code>abstractmethod</code>","text":"<pre><code>otp_uri(\n    secret: str,\n    name: str,\n    issuer: str,\n    counter: int | None = None,\n) -&gt; str\n</code></pre> <p>Generates an otpauth:// URI for Google Authenticator.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str</code> <p>User secret key.</p> required <code>name</code> <code>str</code> <p>Account name (e.g., email).</p> required <code>issuer</code> <code>str</code> <p>Service name (e.g., \"GitHub\").</p> required <code>counter</code> <code>int | None</code> <p>Counter (for HOTP). Default is None.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A string of the form \"otpauth://...\"</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.otp_verify_code","title":"otp_verify_code  <code>abstractmethod</code>","text":"<pre><code>otp_verify_code(\n    secret: str | bytes,\n    code: str,\n    factor: int | None = None,\n    look_ahead: int | None = 1,\n) -&gt; bool\n</code></pre> <p>Checks the OTP code, taking into account the acceptable window.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>code</code> <code>str</code> <p>The code entered.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <code>look_ahead</code> <code>int</code> <p>Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is 1.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the code matches, otherwise False.</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.paseto_create","title":"paseto_create  <code>abstractmethod</code>","text":"<pre><code>paseto_create(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None,\n) -&gt; str\n</code></pre> <p>Create new PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload</p> required <code>footer</code> <code>dict[str, Any] | str | None</code> <p>Payload if needed</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>PASETO</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.paseto_decode","title":"paseto_decode  <code>abstractmethod</code>","text":"<pre><code>paseto_decode(\n    token: str,\n) -&gt; dict[str, dict[str, Any] | str | None]\n</code></pre> <p>Decode PASETO and return payload and footer.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>PASETO</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, dict[str, Any] | str | None]</code> <p>{\"payload\": PAYLOAD, \"footer\": FOOTER}</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.paseto_make_payload","title":"paseto_make_payload  <code>abstractmethod</code>","text":"<pre><code>paseto_make_payload(\n    exp: int | None = None, **data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Generate payload for PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Custom expire if needed</p> <code>None</code> <code>data</code> <code>dict[str, Any]</code> <p>Data in payload</p> <code>{}</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: New payload</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.session_clear","title":"session_clear  <code>abstractmethod</code>","text":"<pre><code>session_clear(session_key: str) -&gt; None\n</code></pre> <p>Delete all sessions by key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key of session</p> required"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.session_create","title":"session_create  <code>abstractmethod</code>","text":"<pre><code>session_create(\n    session_key: str, data: dict[str, Any]\n) -&gt; str\n</code></pre> <p>Create new session.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key for session</p> required <code>data</code> <code>dict[str, Any]</code> <p>Session data</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session ID</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.session_delete","title":"session_delete  <code>abstractmethod</code>","text":"<pre><code>session_delete(session_id: str) -&gt; None\n</code></pre> <p>Delete session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.session_get","title":"session_get  <code>abstractmethod</code>","text":"<pre><code>session_get(session_id: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get data from session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: Session data if exist</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.session_rework","title":"session_rework  <code>abstractmethod</code>","text":"<pre><code>session_rework(old_session_id: str) -&gt; str\n</code></pre> <p>Rework session.</p> <p>Parameters:</p> Name Type Description Default <code>old_session_id</code> <code>str</code> <p>Old session id</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session id</p>"},{"location":"api/jam.__base__/#jam.__base__.BaseJam.session_update","title":"session_update  <code>abstractmethod</code>","text":"<pre><code>session_update(\n    session_id: str, data: dict[str, Any]\n) -&gt; None\n</code></pre> <p>Update session data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <code>data</code> <code>dict[str, Any]</code> <p>New data</p> required"},{"location":"api/jam.__base_encoder__/","title":"jam.base_encoder","text":""},{"location":"api/jam.__base_encoder__/#jam.__base_encoder__","title":"__base_encoder__","text":"<p>Classes:</p> Name Description <code>BaseEncoder</code> <p>Base encoder instance.</p>"},{"location":"api/jam.__base_encoder__/#jam.__base_encoder__.BaseEncoder","title":"BaseEncoder","text":"<p>               Bases: <code>ABC</code></p> <p>Base encoder instance.</p> <p>Methods:</p> Name Description <code>dumps</code> <p>Dump dict.</p> <code>loads</code> <p>Load json.</p>"},{"location":"api/jam.__base_encoder__/#jam.__base_encoder__.BaseEncoder.dumps","title":"dumps  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>dumps(var: dict[str, Any]) -&gt; bytes\n</code></pre> <p>Dump dict.</p>"},{"location":"api/jam.__base_encoder__/#jam.__base_encoder__.BaseEncoder.loads","title":"loads  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>loads(var: str | bytes) -&gt; dict[str, Any]\n</code></pre> <p>Load json.</p>"},{"location":"api/jam.__defaults__/","title":"jam.defaults","text":""},{"location":"api/jam.__defaults__/#jam.__defaults__","title":"__defaults__","text":"<p>Classes:</p> Name Description <code>DEFAULTS</code> <p>Jam defaults vars.</p>"},{"location":"api/jam.__defaults__/#jam.__defaults__.DEFAULTS","title":"DEFAULTS  <code>dataclass</code>","text":"<pre><code>DEFAULTS(\n    DEBUG: bool = getenv(\"JAM_DEBUG\", \"False\") == \"True\",\n    TEMP_DIR: str = getenv(\"JAM_TMP_DIR\", \"/tmp/jam/\"),\n    ENABLE_PLUGINS: bool = getenv(\"JAM_ENABLE_PLUGINS\", \"1\")\n    == \"0\",\n)\n</code></pre> <p>Jam defaults vars.</p> <p>Attributes:</p> Name Type Description <code>DEBUG</code> <code>bool</code> <p>Debug flag. Use only while developing jam module.</p> <code>ENABLE_PLUGINS</code> <code>bool</code> <p>Enabling experimental plugin features.</p> <code>TEMP_DIR</code> <code>str</code> <p>tmp directory for storing temporary files and blobs.</p>"},{"location":"api/jam.__defaults__/#jam.__defaults__.DEFAULTS.DEBUG","title":"DEBUG  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>DEBUG: bool = getenv('JAM_DEBUG', 'False') == 'True'\n</code></pre> <p>Debug flag. Use only while developing jam module.</p>"},{"location":"api/jam.__defaults__/#jam.__defaults__.DEFAULTS.ENABLE_PLUGINS","title":"ENABLE_PLUGINS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ENABLE_PLUGINS: bool = (\n    getenv(\"JAM_ENABLE_PLUGINS\", \"1\") == \"0\"\n)\n</code></pre> <p>Enabling experimental plugin features.</p>"},{"location":"api/jam.__defaults__/#jam.__defaults__.DEFAULTS.TEMP_DIR","title":"TEMP_DIR  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>TEMP_DIR: str = getenv('JAM_TMP_DIR', '/tmp/jam/')\n</code></pre> <p>tmp directory for storing temporary files and blobs.</p>"},{"location":"api/jam.__deprecated__/","title":"jam.deprecated","text":""},{"location":"api/jam.__deprecated__/#jam.__deprecated__","title":"__deprecated__","text":"<p>Functions:</p> Name Description <code>deprecated</code> <p>Mark funcs are deprecated.</p>"},{"location":"api/jam.__deprecated__/#jam.__deprecated__.deprecated","title":"deprecated","text":"<pre><code>deprecated(replacement: str | None = None)\n</code></pre> <p>Mark funcs are deprecated.</p>"},{"location":"api/jam.aio.__base__/","title":"jam.aio.base","text":""},{"location":"api/jam.aio.__base__/#jam.aio.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseAsyncJam</code> <p>Base async jam instance.</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam","title":"BaseAsyncJam","text":"<pre><code>BaseAsyncJam(\n    config: str | dict[str, Any] = \"pyproject.toml\",\n    pointer: str = \"jam\",\n    *,\n    logger: type[BaseLogger] = JamLogger,\n    log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ] = \"INFO\",\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    plugins: list[type[BasePlugin]] = [],\n)\n</code></pre> <p>               Bases: <code>BaseJam</code></p> <p>Base async jam instance.</p> <p>Methods:</p> Name Description <code>jwe_decrypt</code> <p>Decrypt JWE token.</p> <code>jwe_encrypt</code> <p>Encrypt data using JWE.</p> <code>jws_sign</code> <p>Sign data using JWS.</p> <code>jws_verify</code> <p>Verify JWS token.</p> <code>jwt_create</code> <p>Create JWT token.</p> <code>jwt_decode</code> <p>Verify and decode JWT token.</p> <code>jwt_encode</code> <p>Encode the JWT with the given expire, header, and payload.</p> <code>jwt_make_payload</code> <p>Make JWT-specific payload.</p> <code>oauth2_client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>oauth2_fetch_token</code> <p>Exchange authorization code for access token.</p> <code>oauth2_get_authorized_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>oauth2_refresh_token</code> <p>Use refresh token to obtain a new access token.</p> <code>otp_code</code> <p>Generates an OTP.</p> <code>otp_uri</code> <p>Generates an otpauth:// URI for Google Authenticator.</p> <code>otp_verify_code</code> <p>Checks the OTP code, taking into account the acceptable window.</p> <code>paseto_create</code> <p>Create new PASETO.</p> <code>paseto_decode</code> <p>Decode PASETO and return payload and footer.</p> <code>paseto_make_payload</code> <p>Generate payload for PASETO.</p> <code>session_clear</code> <p>Delete all sessions by key.</p> <code>session_create</code> <p>Create new session.</p> <code>session_delete</code> <p>Delete session.</p> <code>session_get</code> <p>Get data from session.</p> <code>session_rework</code> <p>Rework session.</p> <code>session_update</code> <p>Update session data.</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.jwe_decrypt","title":"jwe_decrypt  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>jwe_decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE token.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>Decrypted data.</p> <p>Raises:</p> Type Description <code>JamJWEDecryptionError</code> <p>If decryption fails.</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.jwe_encrypt","title":"jwe_encrypt  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>jwe_encrypt(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt data using JWE.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to encrypt. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWE header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWE token.</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.jws_sign","title":"jws_sign  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>jws_sign(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Sign data using JWS.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWS header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS token.</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.jws_verify","title":"jws_verify  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>jws_verify(token: str) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS token.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload.</p> <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If verification fails.</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.jwt_create","title":"jwt_create  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>jwt_create(payload: dict[str, Any]) -&gt; str\n</code></pre> <p>Create JWT token.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Data payload</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New token</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.jwt_decode","title":"jwt_decode  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>jwt_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n    check_nbf: bool = False,\n    include_headers: bool = False,\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify and decode JWT token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token</p> required <code>check_exp</code> <code>bool</code> <p>Check expire</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check white/black list. Docs: https://jam.makridenko.ru/jwt/lists/what/</p> <code>True</code> <code>check_nbf</code> <code>bool</code> <p>Check not-before time</p> <code>False</code> <code>include_headers</code> <code>bool</code> <p>Include headers in the decoded payload</p> <code>False</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.jwt_encode","title":"jwt_encode  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>jwt_encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    *,\n    payload: dict[str, Any] | None = None,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID. If none use the JTI fabric function.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT.</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.jwt_make_payload","title":"jwt_make_payload  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>jwt_make_payload(\n    exp: int | None, data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Make JWT-specific payload.</p> <p>Deprecated</p> <pre><code>This method is deprecated; the JWT payload is generated automatically in accordance with the specification.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Token expire, if None -&gt; use default</p> required <code>data</code> <code>dict[str, Any]</code> <p>Data to payload</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Payload</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.oauth2_client_credentials_flow","title":"oauth2_client_credentials_flow  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>oauth2_client_credentials_flow(\n    provider: str,\n    scope: list[str] | None = None,\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.oauth2_fetch_token","title":"oauth2_fetch_token  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>oauth2_fetch_token(\n    provider: str,\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.oauth2_get_authorized_url","title":"oauth2_get_authorized_url  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>oauth2_get_authorized_url(\n    provider: str, scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra ath params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.oauth2_refresh_token","title":"oauth2_refresh_token  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>oauth2_refresh_token(\n    provider: str,\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.otp_code","title":"otp_code  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>otp_code(\n    secret: str | bytes, factor: int | None = None\n) -&gt; str\n</code></pre> <p>Generates an OTP.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>OTP code (fixed-length string).</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.otp_uri","title":"otp_uri  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>otp_uri(\n    secret: str,\n    name: str,\n    issuer: str,\n    counter: int | None = None,\n) -&gt; str\n</code></pre> <p>Generates an otpauth:// URI for Google Authenticator.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str</code> <p>User secret key.</p> required <code>name</code> <code>str</code> <p>Account name (e.g., email).</p> required <code>issuer</code> <code>str</code> <p>Service name (e.g., \"GitHub\").</p> required <code>counter</code> <code>int | None</code> <p>Counter (for HOTP). Default is None.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A string of the form \"otpauth://...\"</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.otp_verify_code","title":"otp_verify_code  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>otp_verify_code(\n    secret: str | bytes,\n    code: str,\n    factor: int | None = None,\n    look_ahead: int | None = 1,\n) -&gt; bool\n</code></pre> <p>Checks the OTP code, taking into account the acceptable window.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>code</code> <code>str</code> <p>The code entered.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <code>look_ahead</code> <code>int</code> <p>Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is 1.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the code matches, otherwise False.</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.paseto_create","title":"paseto_create  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>paseto_create(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None,\n) -&gt; str\n</code></pre> <p>Create new PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload</p> required <code>footer</code> <code>dict[str, Any] | str | None</code> <p>Payload if needed</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>PASETO</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.paseto_decode","title":"paseto_decode  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>paseto_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n) -&gt; dict[str, dict | str | dict | None]\n</code></pre> <p>Decode PASETO and return payload and footer.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>PASETO</p> required <code>check_exp</code> <code>bool</code> <p>Check exp in payload</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check token in list</p> <code>True</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, dict | str | dict | None]</code> <p>{<code>payload</code>: PAYLOAD, <code>footer</code>: FOOTER}</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.paseto_make_payload","title":"paseto_make_payload  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>paseto_make_payload(\n    exp: int | None = None, **data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Generate payload for PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Custom expire if needed</p> <code>None</code> <code>data</code> <code>dict[str, Any]</code> <p>Data in payload</p> <code>{}</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: New payload</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.session_clear","title":"session_clear  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>session_clear(session_key: str) -&gt; None\n</code></pre> <p>Delete all sessions by key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key of session</p> required"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.session_create","title":"session_create  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>session_create(\n    session_key: str, data: dict[str, Any]\n) -&gt; str\n</code></pre> <p>Create new session.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key for session</p> required <code>data</code> <code>dict[str, Any]</code> <p>Session data</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session ID</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.session_delete","title":"session_delete  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>session_delete(session_id: str) -&gt; None\n</code></pre> <p>Delete session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.session_get","title":"session_get  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>session_get(session_id: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get data from session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: Session data if exist</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.session_rework","title":"session_rework  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>session_rework(old_session_id: str) -&gt; str\n</code></pre> <p>Rework session.</p> <p>Parameters:</p> Name Type Description Default <code>old_session_id</code> <code>str</code> <p>Old session id</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session id</p>"},{"location":"api/jam.aio.__base__/#jam.aio.__base__.BaseAsyncJam.session_update","title":"session_update  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>session_update(\n    session_id: str, data: dict[str, Any]\n) -&gt; None\n</code></pre> <p>Update session data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <code>data</code> <code>dict[str, Any]</code> <p>New data</p> required"},{"location":"api/jam.aio.instance/","title":"jam.aio.instance","text":""},{"location":"api/jam.aio.instance/#jam.aio.instance","title":"instance","text":"<p>Classes:</p> Name Description <code>Jam</code> <p>Main async Jam instance.</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam","title":"Jam","text":"<pre><code>Jam(\n    config: str | dict[str, Any] = \"pyproject.toml\",\n    pointer: str = \"jam\",\n    *,\n    logger: type[BaseLogger] = JamLogger,\n    log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ] = \"INFO\",\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    plugins: list[type[BasePlugin]] = [],\n)\n</code></pre> <p>               Bases: <code>BaseAsyncJam</code></p> <p>Main async Jam instance.</p> <p>Methods:</p> Name Description <code>jwe_decrypt</code> <p>Decrypt JWE token.</p> <code>jwe_encrypt</code> <p>Encrypt data using JWE.</p> <code>jws_sign</code> <p>Sign data using JWS.</p> <code>jws_verify</code> <p>Verify JWS token.</p> <code>jwt_create</code> <p>Create JWT token.</p> <code>jwt_decode</code> <p>Verify and decode JWT token.</p> <code>jwt_encode</code> <p>Encode the JWT with the given expire, header, and payload.</p> <code>jwt_make_payload</code> <p>Make JWT-specific payload.</p> <code>oauth2_client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>oauth2_fetch_token</code> <p>Exchange authorization code for access token.</p> <code>oauth2_get_authorized_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>oauth2_refresh_token</code> <p>Use refresh token to obtain a new access token.</p> <code>otp_code</code> <p>Generates an OTP.</p> <code>otp_uri</code> <p>Generates an otpauth:// URI for Google Authenticator.</p> <code>otp_verify_code</code> <p>Checks the OTP code, taking into account the acceptable window.</p> <code>paseto_create</code> <p>Create new PASETO.</p> <code>paseto_decode</code> <p>Decode PASETO.</p> <code>paseto_make_payload</code> <p>Generate payload for PASETO.</p> <code>session_clear</code> <p>Delete all sessions by key.</p> <code>session_create</code> <p>Create new session.</p> <code>session_delete</code> <p>Delete session.</p> <code>session_get</code> <p>Get data from session.</p> <code>session_rework</code> <p>Rework session.</p> <code>session_update</code> <p>Update session data.</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.jwe_decrypt","title":"jwe_decrypt  <code>async</code>","text":"<pre><code>jwe_decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE token.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>Decrypted data.</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.jwe_encrypt","title":"jwe_encrypt  <code>async</code>","text":"<pre><code>jwe_encrypt(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt data using JWE.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to encrypt. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWE header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWE token.</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.jws_sign","title":"jws_sign  <code>async</code>","text":"<pre><code>jws_sign(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Sign data using JWS.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWS header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS token.</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.jws_verify","title":"jws_verify  <code>async</code>","text":"<pre><code>jws_verify(token: str) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS token.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload.</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.jwt_create","title":"jwt_create  <code>async</code>","text":"<pre><code>jwt_create(payload: dict[str, Any]) -&gt; str\n</code></pre> <p>Create JWT token.</p> <p>Deprecated</p> <pre><code>Use Jam.jwt_encode\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Data payload</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New token</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.jwt_decode","title":"jwt_decode  <code>async</code>","text":"<pre><code>jwt_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n    check_nbf: bool = False,\n    include_headers: bool = False,\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify and decode JWT token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token</p> required <code>check_exp</code> <code>bool</code> <p>Check expire</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check white/black list. Docs: https://jam.makridenko.ru/jwt/lists/what/</p> <code>True</code> <code>check_nbf</code> <code>bool</code> <p>Check not-before time</p> <code>False</code> <code>include_headers</code> <code>bool</code> <p>Include headers in the decoded payload</p> <code>False</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload</p> <p>Raises:</p> Type Description <code>JamJWTExpired</code> <p>If token is expired</p> <code>JamJWTNotYetValid</code> <p>If token is not yet valid (nbf claim)</p> <code>JamConfigurationError</code> <p>If JWT list is not connected</p> <code>JamJWTNotInWhiteList</code> <p>If token is not in white list</p> <code>JamJWTInBlackList</code> <p>If token is in black list</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.jwt_encode","title":"jwt_encode  <code>async</code>","text":"<pre><code>jwt_encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    *,\n    payload: dict[str, Any] | None = None,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID. If none use the JTI fabric function.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT.</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.jwt_make_payload","title":"jwt_make_payload  <code>async</code>","text":"<pre><code>jwt_make_payload(\n    exp: int | None, data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Make JWT-specific payload.</p> <p>Deprecated</p> <pre><code>This method is deprecated; the JWT payload is generated automatically in accordance with the specification.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Token expire</p> required <code>data</code> <code>dict[str, Any]</code> <p>Data to payload</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Payload</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.oauth2_client_credentials_flow","title":"oauth2_client_credentials_flow  <code>async</code>","text":"<pre><code>oauth2_client_credentials_flow(\n    provider: str,\n    scope: list[str] | None = None,\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>OAuth2 provider</p> required <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Raises:</p> Type Description <code>JamOAuth2EmptyRaw</code> <p>If response is empty</p> <code>JamOAuth2Error</code> <p>HTTP error</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.oauth2_fetch_token","title":"oauth2_fetch_token  <code>async</code>","text":"<pre><code>oauth2_fetch_token(\n    provider: str,\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.oauth2_get_authorized_url","title":"oauth2_get_authorized_url  <code>async</code>","text":"<pre><code>oauth2_get_authorized_url(\n    provider: str, scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra ath params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.oauth2_refresh_token","title":"oauth2_refresh_token  <code>async</code>","text":"<pre><code>oauth2_refresh_token(\n    provider: str,\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.otp_code","title":"otp_code  <code>async</code>","text":"<pre><code>otp_code(\n    secret: str | bytes, factor: int | None = None\n) -&gt; str\n</code></pre> <p>Generates an OTP.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>OTP code (fixed-length string).</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.otp_uri","title":"otp_uri  <code>async</code>","text":"<pre><code>otp_uri(\n    secret: str,\n    name: str,\n    issuer: str,\n    counter: int | None = None,\n) -&gt; str\n</code></pre> <p>Generates an otpauth:// URI for Google Authenticator.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str</code> <p>User secret key.</p> required <code>name</code> <code>str</code> <p>Account name (e.g., email).</p> required <code>issuer</code> <code>str</code> <p>Service name (e.g., \"GitHub\").</p> required <code>counter</code> <code>int | None</code> <p>Counter (for HOTP). Default is None.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A string of the form \"otpauth://...\"</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.otp_verify_code","title":"otp_verify_code  <code>async</code>","text":"<pre><code>otp_verify_code(\n    secret: str | bytes,\n    code: str,\n    factor: int | None = None,\n    look_ahead: int | None = 1,\n) -&gt; bool\n</code></pre> <p>Checks the OTP code, taking into account the acceptable window.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>code</code> <code>str</code> <p>The code entered.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <code>look_ahead</code> <code>int</code> <p>Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is 1.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the code matches, otherwise False.</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.paseto_create","title":"paseto_create  <code>async</code>","text":"<pre><code>paseto_create(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None,\n) -&gt; str\n</code></pre> <p>Create new PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload</p> required <code>footer</code> <code>dict | str | None</code> <p>Footer</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New token</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.paseto_decode","title":"paseto_decode  <code>async</code>","text":"<pre><code>paseto_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n) -&gt; dict[str, dict[str, Any] | str | None]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Token</p> required <code>check_exp</code> <code>bool</code> <p>Check exp in payload</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check token in list</p> <code>True</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, dict[str, Any] | str | None]</code> <p>{'payload' PAYLOAD, 'footer': FOOTER}</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.paseto_make_payload","title":"paseto_make_payload  <code>async</code>","text":"<pre><code>paseto_make_payload(\n    exp: int | None = None, **data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Generate payload for PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Token expire</p> <code>None</code> <code>data</code> <code>dict[str, Any]</code> <p>Custom data</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Payload</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.session_clear","title":"session_clear  <code>async</code>","text":"<pre><code>session_clear(session_key: str) -&gt; None\n</code></pre> <p>Delete all sessions by key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key of session</p> required"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.session_create","title":"session_create  <code>async</code>","text":"<pre><code>session_create(\n    session_key: str, data: dict[str, Any]\n) -&gt; str\n</code></pre> <p>Create new session.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key for session</p> required <code>data</code> <code>dict[str, Any]</code> <p>Session data</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session ID</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.session_delete","title":"session_delete  <code>async</code>","text":"<pre><code>session_delete(session_id: str) -&gt; None\n</code></pre> <p>Delete session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.session_get","title":"session_get  <code>async</code>","text":"<pre><code>session_get(session_id: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get data from session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: Session data if exist</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.session_rework","title":"session_rework  <code>async</code>","text":"<pre><code>session_rework(old_session_id: str) -&gt; str\n</code></pre> <p>Rework session.</p> <p>Parameters:</p> Name Type Description Default <code>old_session_id</code> <code>str</code> <p>Old session id</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session id</p>"},{"location":"api/jam.aio.instance/#jam.aio.instance.Jam.session_update","title":"session_update  <code>async</code>","text":"<pre><code>session_update(\n    session_id: str, data: dict[str, Any]\n) -&gt; None\n</code></pre> <p>Update session data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <code>data</code> <code>dict[str, Any]</code> <p>New data</p> required"},{"location":"api/jam.aio.jwt/","title":"jam.aio.jwt","text":""},{"location":"api/jam.aio.jwt/#jam.aio.jwt","title":"jwt","text":"<p>Async JWT module.</p> <p>Functions:</p> Name Description <code>create_instance</code> <p>Create async-compatible JWT instance.</p>"},{"location":"api/jam.aio.jwt/#jam.aio.jwt.create_instance","title":"create_instance","text":"<pre><code>create_instance(\n    alg: str,\n    secret: Optional[KeyLike] = None,\n    secret_key: Optional[KeyLike] = None,\n    password: Optional[Union[str, bytes]] = None,\n    logger: BaseLogger = logger,\n    serializer: Union[\n        BaseEncoder, type[BaseEncoder]\n    ] = JsonEncoder,\n    **kwargs: Any,\n) -&gt; BaseJWT\n</code></pre> <p>Create async-compatible JWT instance.</p> <p>Note: JWT encoding/decoding itself is synchronous, but this allows for async-compatible list checking if lists are configured.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm (HS256, RS256, etc.)</p> required <code>secret</code> <code>Optional[KeyLike]</code> <p>Secret key (or use secret_key for backward compatibility)</p> <code>None</code> <code>secret_key</code> <code>Optional[KeyLike]</code> <p>Alias for secret (deprecated, use 'secret')</p> <code>None</code> <code>password</code> <code>Optional[Union[str, bytes]]</code> <p>Password for encrypted keys</p> <code>None</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder</p> <code>JsonEncoder</code> <code>**kwargs</code> <code>Any</code> <p>Additional params (e.g., custom_module, list configuration)</p> <code>{}</code> <p>Returns:</p> Type Description <code>BaseJWT</code> <p>JWT instance or custom module</p>"},{"location":"api/jam.aio/","title":"jam.aio","text":""},{"location":"api/jam.aio/#jam.aio","title":"aio","text":"<p>Async Jam package.</p> <p>Modules:</p> Name Description <code>instance</code> <code>jwt</code> <p>Async JWT module.</p> <code>oauth2</code> <p>Async OAuth2 module.</p> <code>sessions</code> <p>Async module for making server auth sessions.</p> <p>Classes:</p> Name Description <code>Jam</code> <p>Main async Jam instance.</p>"},{"location":"api/jam.aio/#jam.aio.Jam","title":"Jam","text":"<pre><code>Jam(\n    config: str | dict[str, Any] = \"pyproject.toml\",\n    pointer: str = \"jam\",\n    *,\n    logger: type[BaseLogger] = JamLogger,\n    log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ] = \"INFO\",\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    plugins: list[type[BasePlugin]] = [],\n)\n</code></pre> <p>               Bases: <code>BaseAsyncJam</code></p> <p>Main async Jam instance.</p> <p>Methods:</p> Name Description <code>jwe_decrypt</code> <p>Decrypt JWE token.</p> <code>jwe_encrypt</code> <p>Encrypt data using JWE.</p> <code>jws_sign</code> <p>Sign data using JWS.</p> <code>jws_verify</code> <p>Verify JWS token.</p> <code>jwt_create</code> <p>Create JWT token.</p> <code>jwt_decode</code> <p>Verify and decode JWT token.</p> <code>jwt_encode</code> <p>Encode the JWT with the given expire, header, and payload.</p> <code>jwt_make_payload</code> <p>Make JWT-specific payload.</p> <code>oauth2_client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>oauth2_fetch_token</code> <p>Exchange authorization code for access token.</p> <code>oauth2_get_authorized_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>oauth2_refresh_token</code> <p>Use refresh token to obtain a new access token.</p> <code>otp_code</code> <p>Generates an OTP.</p> <code>otp_uri</code> <p>Generates an otpauth:// URI for Google Authenticator.</p> <code>otp_verify_code</code> <p>Checks the OTP code, taking into account the acceptable window.</p> <code>paseto_create</code> <p>Create new PASETO.</p> <code>paseto_decode</code> <p>Decode PASETO.</p> <code>paseto_make_payload</code> <p>Generate payload for PASETO.</p> <code>session_clear</code> <p>Delete all sessions by key.</p> <code>session_create</code> <p>Create new session.</p> <code>session_delete</code> <p>Delete session.</p> <code>session_get</code> <p>Get data from session.</p> <code>session_rework</code> <p>Rework session.</p> <code>session_update</code> <p>Update session data.</p>"},{"location":"api/jam.aio/#jam.aio.Jam.jwe_decrypt","title":"jwe_decrypt  <code>async</code>","text":"<pre><code>jwe_decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE token.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>Decrypted data.</p>"},{"location":"api/jam.aio/#jam.aio.Jam.jwe_encrypt","title":"jwe_encrypt  <code>async</code>","text":"<pre><code>jwe_encrypt(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt data using JWE.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to encrypt. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWE header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWE token.</p>"},{"location":"api/jam.aio/#jam.aio.Jam.jws_sign","title":"jws_sign  <code>async</code>","text":"<pre><code>jws_sign(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Sign data using JWS.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWS header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS token.</p>"},{"location":"api/jam.aio/#jam.aio.Jam.jws_verify","title":"jws_verify  <code>async</code>","text":"<pre><code>jws_verify(token: str) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS token.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload.</p>"},{"location":"api/jam.aio/#jam.aio.Jam.jwt_create","title":"jwt_create  <code>async</code>","text":"<pre><code>jwt_create(payload: dict[str, Any]) -&gt; str\n</code></pre> <p>Create JWT token.</p> <p>Deprecated</p> <pre><code>Use Jam.jwt_encode\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Data payload</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New token</p>"},{"location":"api/jam.aio/#jam.aio.Jam.jwt_decode","title":"jwt_decode  <code>async</code>","text":"<pre><code>jwt_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n    check_nbf: bool = False,\n    include_headers: bool = False,\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify and decode JWT token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token</p> required <code>check_exp</code> <code>bool</code> <p>Check expire</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check white/black list. Docs: https://jam.makridenko.ru/jwt/lists/what/</p> <code>True</code> <code>check_nbf</code> <code>bool</code> <p>Check not-before time</p> <code>False</code> <code>include_headers</code> <code>bool</code> <p>Include headers in the decoded payload</p> <code>False</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload</p> <p>Raises:</p> Type Description <code>JamJWTExpired</code> <p>If token is expired</p> <code>JamJWTNotYetValid</code> <p>If token is not yet valid (nbf claim)</p> <code>JamConfigurationError</code> <p>If JWT list is not connected</p> <code>JamJWTNotInWhiteList</code> <p>If token is not in white list</p> <code>JamJWTInBlackList</code> <p>If token is in black list</p>"},{"location":"api/jam.aio/#jam.aio.Jam.jwt_encode","title":"jwt_encode  <code>async</code>","text":"<pre><code>jwt_encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    *,\n    payload: dict[str, Any] | None = None,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID. If none use the JTI fabric function.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT.</p>"},{"location":"api/jam.aio/#jam.aio.Jam.jwt_make_payload","title":"jwt_make_payload  <code>async</code>","text":"<pre><code>jwt_make_payload(\n    exp: int | None, data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Make JWT-specific payload.</p> <p>Deprecated</p> <pre><code>This method is deprecated; the JWT payload is generated automatically in accordance with the specification.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Token expire</p> required <code>data</code> <code>dict[str, Any]</code> <p>Data to payload</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Payload</p>"},{"location":"api/jam.aio/#jam.aio.Jam.oauth2_client_credentials_flow","title":"oauth2_client_credentials_flow  <code>async</code>","text":"<pre><code>oauth2_client_credentials_flow(\n    provider: str,\n    scope: list[str] | None = None,\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>OAuth2 provider</p> required <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Raises:</p> Type Description <code>JamOAuth2EmptyRaw</code> <p>If response is empty</p> <code>JamOAuth2Error</code> <p>HTTP error</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.aio/#jam.aio.Jam.oauth2_fetch_token","title":"oauth2_fetch_token  <code>async</code>","text":"<pre><code>oauth2_fetch_token(\n    provider: str,\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam.aio/#jam.aio.Jam.oauth2_get_authorized_url","title":"oauth2_get_authorized_url  <code>async</code>","text":"<pre><code>oauth2_get_authorized_url(\n    provider: str, scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra ath params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam.aio/#jam.aio.Jam.oauth2_refresh_token","title":"oauth2_refresh_token  <code>async</code>","text":"<pre><code>oauth2_refresh_token(\n    provider: str,\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam.aio/#jam.aio.Jam.otp_code","title":"otp_code  <code>async</code>","text":"<pre><code>otp_code(\n    secret: str | bytes, factor: int | None = None\n) -&gt; str\n</code></pre> <p>Generates an OTP.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>OTP code (fixed-length string).</p>"},{"location":"api/jam.aio/#jam.aio.Jam.otp_uri","title":"otp_uri  <code>async</code>","text":"<pre><code>otp_uri(\n    secret: str,\n    name: str,\n    issuer: str,\n    counter: int | None = None,\n) -&gt; str\n</code></pre> <p>Generates an otpauth:// URI for Google Authenticator.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str</code> <p>User secret key.</p> required <code>name</code> <code>str</code> <p>Account name (e.g., email).</p> required <code>issuer</code> <code>str</code> <p>Service name (e.g., \"GitHub\").</p> required <code>counter</code> <code>int | None</code> <p>Counter (for HOTP). Default is None.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A string of the form \"otpauth://...\"</p>"},{"location":"api/jam.aio/#jam.aio.Jam.otp_verify_code","title":"otp_verify_code  <code>async</code>","text":"<pre><code>otp_verify_code(\n    secret: str | bytes,\n    code: str,\n    factor: int | None = None,\n    look_ahead: int | None = 1,\n) -&gt; bool\n</code></pre> <p>Checks the OTP code, taking into account the acceptable window.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>code</code> <code>str</code> <p>The code entered.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <code>look_ahead</code> <code>int</code> <p>Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is 1.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the code matches, otherwise False.</p>"},{"location":"api/jam.aio/#jam.aio.Jam.paseto_create","title":"paseto_create  <code>async</code>","text":"<pre><code>paseto_create(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None,\n) -&gt; str\n</code></pre> <p>Create new PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload</p> required <code>footer</code> <code>dict | str | None</code> <p>Footer</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New token</p>"},{"location":"api/jam.aio/#jam.aio.Jam.paseto_decode","title":"paseto_decode  <code>async</code>","text":"<pre><code>paseto_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n) -&gt; dict[str, dict[str, Any] | str | None]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Token</p> required <code>check_exp</code> <code>bool</code> <p>Check exp in payload</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check token in list</p> <code>True</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, dict[str, Any] | str | None]</code> <p>{'payload' PAYLOAD, 'footer': FOOTER}</p>"},{"location":"api/jam.aio/#jam.aio.Jam.paseto_make_payload","title":"paseto_make_payload  <code>async</code>","text":"<pre><code>paseto_make_payload(\n    exp: int | None = None, **data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Generate payload for PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Token expire</p> <code>None</code> <code>data</code> <code>dict[str, Any]</code> <p>Custom data</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Payload</p>"},{"location":"api/jam.aio/#jam.aio.Jam.session_clear","title":"session_clear  <code>async</code>","text":"<pre><code>session_clear(session_key: str) -&gt; None\n</code></pre> <p>Delete all sessions by key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key of session</p> required"},{"location":"api/jam.aio/#jam.aio.Jam.session_create","title":"session_create  <code>async</code>","text":"<pre><code>session_create(\n    session_key: str, data: dict[str, Any]\n) -&gt; str\n</code></pre> <p>Create new session.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key for session</p> required <code>data</code> <code>dict[str, Any]</code> <p>Session data</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session ID</p>"},{"location":"api/jam.aio/#jam.aio.Jam.session_delete","title":"session_delete  <code>async</code>","text":"<pre><code>session_delete(session_id: str) -&gt; None\n</code></pre> <p>Delete session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required"},{"location":"api/jam.aio/#jam.aio.Jam.session_get","title":"session_get  <code>async</code>","text":"<pre><code>session_get(session_id: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get data from session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: Session data if exist</p>"},{"location":"api/jam.aio/#jam.aio.Jam.session_rework","title":"session_rework  <code>async</code>","text":"<pre><code>session_rework(old_session_id: str) -&gt; str\n</code></pre> <p>Rework session.</p> <p>Parameters:</p> Name Type Description Default <code>old_session_id</code> <code>str</code> <p>Old session id</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session id</p>"},{"location":"api/jam.aio/#jam.aio.Jam.session_update","title":"session_update  <code>async</code>","text":"<pre><code>session_update(\n    session_id: str, data: dict[str, Any]\n) -&gt; None\n</code></pre> <p>Update session data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <code>data</code> <code>dict[str, Any]</code> <p>New data</p> required"},{"location":"api/jam.aio.oauth2.__base__/","title":"jam.aio.oauth2.base","text":""},{"location":"api/jam.aio.oauth2.__base__/#jam.aio.oauth2.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseAsyncOAuth2Client</code> <p>Base async OAuth2 client instance.</p>"},{"location":"api/jam.aio.oauth2.__base__/#jam.aio.oauth2.__base__.BaseAsyncOAuth2Client","title":"BaseAsyncOAuth2Client","text":"<pre><code>BaseAsyncOAuth2Client(\n    client_id: str,\n    client_secret: str,\n    auth_url: str,\n    token_url: str,\n    redirect_url: str,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base async OAuth2 client instance.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>ID or your client</p> required <code>client_secret</code> <code>str</code> <p>Secret key for your application</p> required <code>auth_url</code> <code>str</code> <p>App auth url</p> required <code>token_url</code> <code>str</code> <p>App token url</p> required <code>redirect_url</code> <code>str</code> <p>Your app url</p> required <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder. Defaults to JsonEncoder.</p> <code>JsonEncoder</code> <p>Methods:</p> Name Description <code>client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>fetch_token</code> <p>Exchange code for access token.</p> <code>get_authorization_url</code> <p>Get OAuth2 url.</p> <code>refresh_token</code> <p>Update access token.</p>"},{"location":"api/jam.aio.oauth2.__base__/#jam.aio.oauth2.__base__.BaseAsyncOAuth2Client.client_credentials_flow","title":"client_credentials_flow  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>client_credentials_flow(\n    scope: list[str] | None = None, **extra_params: Any\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.aio.oauth2.__base__/#jam.aio.oauth2.__base__.BaseAsyncOAuth2Client.fetch_token","title":"fetch_token  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>fetch_token(\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Auth code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token response</p>"},{"location":"api/jam.aio.oauth2.__base__/#jam.aio.oauth2.__base__.BaseAsyncOAuth2Client.get_authorization_url","title":"get_authorization_url  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get_authorization_url(\n    scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Get OAuth2 url.</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra auth params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>URL for auth</p>"},{"location":"api/jam.aio.oauth2.__base__/#jam.aio.oauth2.__base__.BaseAsyncOAuth2Client.refresh_token","title":"refresh_token  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>refresh_token(\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Update access token.</p> <p>Parameters:</p> Name Type Description Default <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>New token response</p>"},{"location":"api/jam.aio.oauth2.builtin.github/","title":"jam.aio.oauth2.builtin.github","text":""},{"location":"api/jam.aio.oauth2.builtin.github/#jam.aio.oauth2.builtin.github","title":"github","text":"<p>Classes:</p> Name Description <code>GitHubOAuth2Client</code> <p>Async ready to use GitHub OAuth2 provider.</p>"},{"location":"api/jam.aio.oauth2.builtin.github/#jam.aio.oauth2.builtin.github.GitHubOAuth2Client","title":"GitHubOAuth2Client","text":"<pre><code>GitHubOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Async ready to use GitHub OAuth2 provider.</p> <p>See: https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>ID of your app</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>URL for your app</p> required"},{"location":"api/jam.aio.oauth2.builtin.gitlab/","title":"jam.aio.oauth2.builtin.gitlab","text":""},{"location":"api/jam.aio.oauth2.builtin.gitlab/#jam.aio.oauth2.builtin.gitlab","title":"gitlab","text":"<p>Classes:</p> Name Description <code>GitLabOAuth2Client</code> <p>Async ready to use GitLab OAuth2 provider.</p>"},{"location":"api/jam.aio.oauth2.builtin.gitlab/#jam.aio.oauth2.builtin.gitlab.GitLabOAuth2Client","title":"GitLabOAuth2Client","text":"<pre><code>GitLabOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Async ready to use GitLab OAuth2 provider.</p> <p>See: https://docs.gitlab.com/api/oauth2/</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>Your app url</p> required"},{"location":"api/jam.aio.oauth2.builtin.google/","title":"jam.aio.oauth2.builtin.google","text":""},{"location":"api/jam.aio.oauth2.builtin.google/#jam.aio.oauth2.builtin.google","title":"google","text":"<p>Classes:</p> Name Description <code>GoogleOAuth2Client</code> <p>Async ready to use Google OAuth2 provider.</p>"},{"location":"api/jam.aio.oauth2.builtin.google/#jam.aio.oauth2.builtin.google.GoogleOAuth2Client","title":"GoogleOAuth2Client","text":"<pre><code>GoogleOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Async ready to use Google OAuth2 provider.</p> <p>See: https://developers.google.com/identity/protocols/oauth2</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>URL for your app</p> required"},{"location":"api/jam.aio.oauth2.builtin/","title":"jam.aio.oauth2.builtin","text":""},{"location":"api/jam.aio.oauth2.builtin/#jam.aio.oauth2.builtin","title":"builtin","text":"<p>Async builtin OAuth2 providers.</p> <p>Modules:</p> Name Description <code>github</code> <code>gitlab</code> <code>google</code> <code>yandex</code>"},{"location":"api/jam.aio.oauth2.builtin.yandex/","title":"jam.aio.oauth2.builtin.yandex","text":""},{"location":"api/jam.aio.oauth2.builtin.yandex/#jam.aio.oauth2.builtin.yandex","title":"yandex","text":"<p>Classes:</p> Name Description <code>YandexOAuth2Client</code> <p>Async ready to use yandex oauth2 client.</p>"},{"location":"api/jam.aio.oauth2.builtin.yandex/#jam.aio.oauth2.builtin.yandex.YandexOAuth2Client","title":"YandexOAuth2Client","text":"<pre><code>YandexOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Async ready to use yandex oauth2 client.</p> <p>See: https://yandex.ru/dev/id/doc</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>Your app URL</p> required"},{"location":"api/jam.aio.oauth2.client/","title":"jam.aio.oauth2.client","text":""},{"location":"api/jam.aio.oauth2.client/#jam.aio.oauth2.client","title":"client","text":"<p>Classes:</p> Name Description <code>OAuth2Client</code> <p>Async universal OAuth2 client implementation.</p>"},{"location":"api/jam.aio.oauth2.client/#jam.aio.oauth2.client.OAuth2Client","title":"OAuth2Client","text":"<pre><code>OAuth2Client(\n    client_id: str,\n    client_secret: str,\n    auth_url: str,\n    token_url: str,\n    redirect_url: str,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n)\n</code></pre> <p>               Bases: <code>BaseAsyncOAuth2Client</code></p> <p>Async universal OAuth2 client implementation.</p> <p>Methods:</p> Name Description <code>client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>fetch_token</code> <p>Exchange authorization code for access token.</p> <code>get_authorization_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>refresh_token</code> <p>Use refresh token to obtain a new access token.</p>"},{"location":"api/jam.aio.oauth2.client/#jam.aio.oauth2.client.OAuth2Client.__http","title":"__http","text":"<pre><code>__http(url: str)\n</code></pre> <p>Create HTTPS connection context manager.</p>"},{"location":"api/jam.aio.oauth2.client/#jam.aio.oauth2.client.OAuth2Client.__post_form","title":"__post_form  <code>async</code>","text":"<pre><code>__post_form(\n    url: str, params: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Send POST form and parse JSON response (async version).</p>"},{"location":"api/jam.aio.oauth2.client/#jam.aio.oauth2.client.OAuth2Client.client_credentials_flow","title":"client_credentials_flow  <code>async</code>","text":"<pre><code>client_credentials_flow(\n    scope: list[str] | None = None, **extra_params: Any\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Raises:</p> Type Description <code>JamOAuth2EmptyRaw</code> <p>If response is empty</p> <code>JamOAuth2Error</code> <p>HTTP error</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.aio.oauth2.client/#jam.aio.oauth2.client.OAuth2Client.fetch_token","title":"fetch_token  <code>async</code>","text":"<pre><code>fetch_token(\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam.aio.oauth2.client/#jam.aio.oauth2.client.OAuth2Client.get_authorization_url","title":"get_authorization_url  <code>async</code>","text":"<pre><code>get_authorization_url(\n    scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra auth params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam.aio.oauth2.client/#jam.aio.oauth2.client.OAuth2Client.refresh_token","title":"refresh_token  <code>async</code>","text":"<pre><code>refresh_token(\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam.aio.oauth2/","title":"jam.aio.oauth2","text":""},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2","title":"oauth2","text":"<p>Async OAuth2 module.</p> <p>Modules:</p> Name Description <code>builtin</code> <p>Async builtin OAuth2 providers.</p> <code>client</code> <p>Classes:</p> Name Description <code>BaseOAuth2Client</code> <p>Base OAuth2 client instance.</p> <code>GitHubOAuth2Client</code> <p>Async ready to use GitHub OAuth2 provider.</p> <code>GitLabOAuth2Client</code> <p>Async ready to use GitLab OAuth2 provider.</p> <code>GoogleOAuth2Client</code> <p>Async ready to use Google OAuth2 provider.</p> <code>OAuth2Client</code> <p>Async universal OAuth2 client implementation.</p> <code>YandexOAuth2Client</code> <p>Async ready to use yandex oauth2 client.</p> <p>Functions:</p> Name Description <code>create_instance</code> <p>Create async OAuth2 clients for configured providers.</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.BaseOAuth2Client","title":"BaseOAuth2Client","text":"<pre><code>BaseOAuth2Client(\n    client_id: str,\n    client_secret: str,\n    auth_url: str,\n    token_url: str,\n    redirect_url: str,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base OAuth2 client instance.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>ID or your client</p> required <code>client_secret</code> <code>str</code> <p>Secret key for your application</p> required <code>auth_url</code> <code>str</code> <p>App auth url</p> required <code>token_url</code> <code>str</code> <p>App token url</p> required <code>redirect_url</code> <code>str</code> <p>Your app url</p> required <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder. Defaults to JsonEncoder.</p> <code>JsonEncoder</code> <p>Methods:</p> Name Description <code>client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>fetch_token</code> <p>Exchange code for access token.</p> <code>get_authorization_url</code> <p>Get OAuth2 url.</p> <code>refresh_token</code> <p>Update access token.</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.BaseOAuth2Client.client_credentials_flow","title":"client_credentials_flow  <code>abstractmethod</code>","text":"<pre><code>client_credentials_flow(\n    scope: list[str] | None = None,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>Optional[list[str]]</code> <p>Auth scope</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.BaseOAuth2Client.fetch_token","title":"fetch_token  <code>abstractmethod</code>","text":"<pre><code>fetch_token(\n    code: str, grant_type: str = \"authorization_code\"\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Auth code</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'authorization_code'</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token response</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.BaseOAuth2Client.get_authorization_url","title":"get_authorization_url  <code>abstractmethod</code>","text":"<pre><code>get_authorization_url(scope: list[str]) -&gt; str\n</code></pre> <p>Get OAuth2 url.</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>URL for auth</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.BaseOAuth2Client.refresh_token","title":"refresh_token  <code>abstractmethod</code>","text":"<pre><code>refresh_token(\n    refresh_token: str, grant_type: str = \"refresh_token\"\n) -&gt; dict[str, Any]\n</code></pre> <p>Update access token.</p> <p>Parameters:</p> Name Type Description Default <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>New token response</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.GitHubOAuth2Client","title":"GitHubOAuth2Client","text":"<pre><code>GitHubOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Async ready to use GitHub OAuth2 provider.</p> <p>See: https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>ID of your app</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>URL for your app</p> required"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.GitLabOAuth2Client","title":"GitLabOAuth2Client","text":"<pre><code>GitLabOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Async ready to use GitLab OAuth2 provider.</p> <p>See: https://docs.gitlab.com/api/oauth2/</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>Your app url</p> required"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.GoogleOAuth2Client","title":"GoogleOAuth2Client","text":"<pre><code>GoogleOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Async ready to use Google OAuth2 provider.</p> <p>See: https://developers.google.com/identity/protocols/oauth2</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>URL for your app</p> required"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.OAuth2Client","title":"OAuth2Client","text":"<pre><code>OAuth2Client(\n    client_id: str,\n    client_secret: str,\n    auth_url: str,\n    token_url: str,\n    redirect_url: str,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n)\n</code></pre> <p>               Bases: <code>BaseAsyncOAuth2Client</code></p> <p>Async universal OAuth2 client implementation.</p> <p>Methods:</p> Name Description <code>client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>fetch_token</code> <p>Exchange authorization code for access token.</p> <code>get_authorization_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>refresh_token</code> <p>Use refresh token to obtain a new access token.</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.OAuth2Client.__http","title":"__http","text":"<pre><code>__http(url: str)\n</code></pre> <p>Create HTTPS connection context manager.</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.OAuth2Client.__post_form","title":"__post_form  <code>async</code>","text":"<pre><code>__post_form(\n    url: str, params: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Send POST form and parse JSON response (async version).</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.OAuth2Client.client_credentials_flow","title":"client_credentials_flow  <code>async</code>","text":"<pre><code>client_credentials_flow(\n    scope: list[str] | None = None, **extra_params: Any\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Raises:</p> Type Description <code>JamOAuth2EmptyRaw</code> <p>If response is empty</p> <code>JamOAuth2Error</code> <p>HTTP error</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.OAuth2Client.fetch_token","title":"fetch_token  <code>async</code>","text":"<pre><code>fetch_token(\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.OAuth2Client.get_authorization_url","title":"get_authorization_url  <code>async</code>","text":"<pre><code>get_authorization_url(\n    scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra auth params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.OAuth2Client.refresh_token","title":"refresh_token  <code>async</code>","text":"<pre><code>refresh_token(\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.YandexOAuth2Client","title":"YandexOAuth2Client","text":"<pre><code>YandexOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Async ready to use yandex oauth2 client.</p> <p>See: https://yandex.ru/dev/id/doc</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>Your app URL</p> required"},{"location":"api/jam.aio.oauth2/#jam.aio.oauth2.create_instance","title":"create_instance","text":"<pre><code>create_instance(\n    providers: dict[str, dict],\n    logger: BaseLogger = logger,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    **kwargs: Any,\n) -&gt; dict[str, BaseOAuth2Client]\n</code></pre> <p>Create async OAuth2 clients for configured providers.</p> <p>Parameters:</p> Name Type Description Default <code>providers</code> <code>dict[str, dict]</code> <p>{provider_name: {client_id, client_secret, redirect_uri, ...}}</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>JSON encoder/decoder</p> <code>JsonEncoder</code> <code>**kwargs</code> <code>Any</code> <p>Additional params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, BaseOAuth2Client]</code> <p>{provider_name: OAuth2Client instance (async)}</p>"},{"location":"api/jam.aio.sessions.__base__/","title":"jam.aio.sessions.base","text":""},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseAsyncSessionModule</code> <p>Abstract base class for async session management modules.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule","title":"BaseAsyncSessionModule","text":"<pre><code>BaseAsyncSessionModule(\n    id_factory: Callable[[], str] = lambda: str(uuid4()),\n    is_session_crypt: bool = False,\n    session_aes_secret: bytes | str | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base class for async session management modules.</p> <p>Methods:</p> Name Description <code>__decode_session_data__</code> <p>Decode session data.</p> <code>__decode_session_id__</code> <p>Decode the session using AES decryption.</p> <code>__decode_session_id_if_needed__</code> <p>Decode the session ID if it is encoded.</p> <code>__encode_session_data__</code> <p>Encode session data.</p> <code>__encode_session_id__</code> <p>Encode the session using AES encryption.</p> <code>__encode_session_id_if_needed__</code> <p>Encode the session ID if it is not already encoded.</p> <code>clear</code> <p>Clear all sessions by key.</p> <code>create</code> <p>Create a new session with the given session key and data.</p> <code>delete</code> <p>Delete a session by its key or ID.</p> <code>get</code> <p>Retrieve a session by its key or ID.</p> <code>rework</code> <p>Rework a session and return its new ID.</p> <code>update</code> <p>Update an existing session with new data.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Return the unique ID use id_factory.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Return the unique ID use id_factory.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.__decode_session_data__","title":"__decode_session_data__","text":"<pre><code>__decode_session_data__(data: str) -&gt; dict\n</code></pre> <p>Decode session data.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.__decode_session_id__","title":"__decode_session_id__","text":"<pre><code>__decode_session_id__(data: str) -&gt; str\n</code></pre> <p>Decode the session using AES decryption.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.__decode_session_id_if_needed__","title":"__decode_session_id_if_needed__","text":"<pre><code>__decode_session_id_if_needed__(data: str) -&gt; str\n</code></pre> <p>Decode the session ID if it is encoded.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.__encode_session_data__","title":"__encode_session_data__","text":"<pre><code>__encode_session_data__(data: dict) -&gt; str\n</code></pre> <p>Encode session data.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.__encode_session_id__","title":"__encode_session_id__","text":"<pre><code>__encode_session_id__(data: str) -&gt; str\n</code></pre> <p>Encode the session using AES encryption.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.__encode_session_id_if_needed__","title":"__encode_session_id_if_needed__","text":"<pre><code>__encode_session_id_if_needed__(data: str) -&gt; str\n</code></pre> <p>Encode the session ID if it is not already encoded.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.clear","title":"clear  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>clear(session_key: str) -&gt; None\n</code></pre> <p>Clear all sessions by key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The key for the sessions to clear.</p> required"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.create","title":"create  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>create(session_key: str, data: dict[str, Any]) -&gt; str\n</code></pre> <p>Create a new session with the given session key and data.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The key for the session.</p> required <code>data</code> <code>dict[str, Any]</code> <p>The data to be stored in the session.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The session ID.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.delete","title":"delete  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>delete(session_id: str) -&gt; None\n</code></pre> <p>Delete a session by its key or ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session.</p> required"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.get","title":"get  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get(session_id: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Retrieve a session by its key or ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session.</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: The session data if found, otherwise None.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.rework","title":"rework  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>rework(session_id: str) -&gt; str\n</code></pre> <p>Rework a session and return its new ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to rework.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If the session with the given ID does not exist.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The new session ID.</p>"},{"location":"api/jam.aio.sessions.__base__/#jam.aio.sessions.__base__.BaseAsyncSessionModule.update","title":"update  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>update(session_id: str, data: dict[str, Any]) -&gt; None\n</code></pre> <p>Update an existing session with new data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to update.</p> required <code>data</code> <code>dict[str, Any]</code> <p>The new data to be stored in the session.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If the session with the given ID does not exist.</p>"},{"location":"api/jam.aio.sessions.json/","title":"jam.aio.sessions.json","text":""},{"location":"api/jam.aio.sessions.json/#jam.aio.sessions.json","title":"json","text":"<p>Classes:</p> Name Description <code>JSONSessions</code> <p>Async session management module for JSON storage.</p>"},{"location":"api/jam.aio.sessions.json/#jam.aio.sessions.json.JSONSessions","title":"JSONSessions","text":"<pre><code>JSONSessions(\n    json_path: str = \"sessions.json\",\n    is_session_crypt: bool = False,\n    session_aes_secret: bytes | None = None,\n    id_factory: Callable[[], str] = lambda: str(uuid4()),\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseAsyncSessionModule</code></p> <p>Async session management module for JSON storage.</p> <p>Parameters:</p> Name Type Description Default <code>json_path</code> <code>str</code> <p>Path to the JSON file where sessions will be stored.</p> <code>'sessions.json'</code> <code>is_session_crypt</code> <code>bool</code> <p>If True, session keys will be encoded.</p> <code>False</code> <code>session_aes_secret</code> <code>Optional[bytes]</code> <p>AES secret for encoding session keys. Required if <code>is_session_crypt</code> is True.</p> <code>None</code> <code>id_factory</code> <code>Callable[[], str]</code> <p>A callable that generates unique IDs. Defaults to a UUID factory.</p> <code>lambda: str(uuid4())</code> <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder. Defaults to JsonEncoder.</p> <code>JsonEncoder</code> <code>logger</code> <code>Optional[BaseLogger]</code> <p>Logger instance. Defaults to None.</p> <code>None</code> <p>Methods:</p> Name Description <code>clear</code> <p>Clear all sessions for a given session key.</p> <code>create</code> <p>Create a new session.</p> <code>delete</code> <p>Delete a session by its ID.</p> <code>get</code> <p>Retrieve session data by session ID.</p> <code>rework</code> <p>Rework (regenerate) a session ID.</p> <code>update</code> <p>Update session data by its ID.</p>"},{"location":"api/jam.aio.sessions.json/#jam.aio.sessions.json.JSONSessions.clear","title":"clear  <code>async</code>","text":"<pre><code>clear(session_key: str) -&gt; None\n</code></pre> <p>Clear all sessions for a given session key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The session key to clear.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p>"},{"location":"api/jam.aio.sessions.json/#jam.aio.sessions.json.JSONSessions.create","title":"create  <code>async</code>","text":"<pre><code>create(session_key: str, data: dict) -&gt; str\n</code></pre> <p>Create a new session.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The session key.</p> required <code>data</code> <code>dict</code> <p>The data to be stored in the session.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The ID of the created session.</p>"},{"location":"api/jam.aio.sessions.json/#jam.aio.sessions.json.JSONSessions.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(session_id: str) -&gt; None\n</code></pre> <p>Delete a session by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to delete.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p>"},{"location":"api/jam.aio.sessions.json/#jam.aio.sessions.json.JSONSessions.get","title":"get  <code>async</code>","text":"<pre><code>get(session_id) -&gt; dict | None\n</code></pre> <p>Retrieve session data by session ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to retrieve.</p> required <p>Returns:</p> Type Description <code>dict | None</code> <p>dict | None: The session data if found, otherwise None.</p>"},{"location":"api/jam.aio.sessions.json/#jam.aio.sessions.json.JSONSessions.rework","title":"rework  <code>async</code>","text":"<pre><code>rework(session_id: str) -&gt; str\n</code></pre> <p>Rework (regenerate) a session ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The current session ID to be reworked.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If session not found</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The new session ID.</p>"},{"location":"api/jam.aio.sessions.json/#jam.aio.sessions.json.JSONSessions.update","title":"update  <code>async</code>","text":"<pre><code>update(session_id: str, data: dict) -&gt; None\n</code></pre> <p>Update session data by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to update.</p> required <code>data</code> <code>dict</code> <p>The new data to store in the session.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If session not found</p> <p>Returns:</p> Type Description <code>None</code> <p>None</p>"},{"location":"api/jam.aio.sessions/","title":"jam.aio.sessions","text":""},{"location":"api/jam.aio.sessions/#jam.aio.sessions","title":"sessions","text":"<p>Async module for making server auth sessions.</p> <p>Modules:</p> Name Description <code>json</code> <code>redis</code> <p>Functions:</p> Name Description <code>create_instance</code> <p>Create async session module instance.</p>"},{"location":"api/jam.aio.sessions/#jam.aio.sessions.create_instance","title":"create_instance","text":"<pre><code>create_instance(\n    session_type: str | None = None,\n    sessions_type: str | None = None,\n    logger: BaseLogger = logger,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    **kwargs: Any,\n) -&gt; BaseAsyncSessionModule\n</code></pre> <p>Create async session module instance.</p> <p>Parameters:</p> Name Type Description Default <code>session_type</code> <code>str | None</code> <p>\"redis\" | \"json\" | \"custom\"</p> <code>None</code> <code>sessions_type</code> <code>str | None</code> <p>Alias for session_type (deprecated, use 'session_type')</p> <code>None</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>JSON encoder/decoder</p> <code>JsonEncoder</code> <code>**kwargs</code> <code>Any</code> <p>Config params specific to session type</p> <code>{}</code> <p>Returns:</p> Type Description <code>BaseAsyncSessionModule</code> <p>BaseSessionModule instance (async-compatible)</p>"},{"location":"api/jam.aio.sessions.redis/","title":"jam.aio.sessions.redis","text":""},{"location":"api/jam.aio.sessions.redis/#jam.aio.sessions.redis","title":"redis","text":"<p>Classes:</p> Name Description <code>RedisSessions</code> <p>Async Redis session management module.</p>"},{"location":"api/jam.aio.sessions.redis/#jam.aio.sessions.redis.RedisSessions","title":"RedisSessions","text":"<pre><code>RedisSessions(\n    redis_uri: str | Redis = \"redis://localhost:6379/0\",\n    redis_sessions_key: str = \"sessions\",\n    default_ttl: int | None = 3600,\n    is_session_crypt: bool = False,\n    session_aes_secret: bytes | str | None = getenv(\n        \"JAM_SESSION_AES_SECRET\", None\n    ),\n    id_factory: Callable[[], str] = lambda: str(uuid4()),\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseAsyncSessionModule</code></p> <p>Async Redis session management module.</p> <p>Parameters:</p> Name Type Description Default <code>redis_uri</code> <code>str | Redis</code> <p>The URI for the Redis server or Redis instance.</p> <code>'redis://localhost:6379/0'</code> <code>redis_sessions_key</code> <code>str</code> <p>The key under which sessions are stored in Redis.</p> <code>'sessions'</code> <code>default_ttl</code> <code>Optional[int]</code> <p>Default time-to-live for sessions in seconds. Defaults to 3600 seconds (1 hour).</p> <code>3600</code> <code>is_session_crypt</code> <code>bool</code> <p>If True, session keys will be encoded.</p> <code>False</code> <code>session_aes_secret</code> <code>Optional[bytes]</code> <p>AES secret for encoding session keys. Required if <code>is_session_key_crypt</code> is True.</p> <code>getenv('JAM_SESSION_AES_SECRET', None)</code> <code>id_factory</code> <code>Callable[[], str]</code> <p>A callable that generates unique IDs. Defaults to a UUID factory.</p> <code>lambda: str(uuid4())</code> <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder. Defaults to JsonEncoder.</p> <code>JsonEncoder</code> <code>logger</code> <code>Optional[BaseLogger]</code> <p>Logger instance. Defaults to None.</p> <code>None</code> <p>Methods:</p> Name Description <code>clear</code> <p>Clear all sessions for a given session key.</p> <code>create</code> <p>Create a new session with the given session key and data.</p> <code>delete</code> <p>Delete a session by its ID.</p> <code>get</code> <p>Retrieve a session by its key or ID.</p> <code>rework</code> <p>Rework a session and return its new ID.</p> <code>update</code> <p>Update an existing session with new data.</p>"},{"location":"api/jam.aio.sessions.redis/#jam.aio.sessions.redis.RedisSessions.clear","title":"clear  <code>async</code>","text":"<pre><code>clear(session_key: str) -&gt; None\n</code></pre> <p>Clear all sessions for a given session key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The session key to clear.</p> required"},{"location":"api/jam.aio.sessions.redis/#jam.aio.sessions.redis.RedisSessions.create","title":"create  <code>async</code>","text":"<pre><code>create(session_key: str, data: dict) -&gt; str\n</code></pre> <p>Create a new session with the given session key and data.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The key for the session.</p> required <code>data</code> <code>dict</code> <p>The data to be stored in the session.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The unique ID of the created session.</p>"},{"location":"api/jam.aio.sessions.redis/#jam.aio.sessions.redis.RedisSessions.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(session_id: str) -&gt; None\n</code></pre> <p>Delete a session by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The session ID.</p> required"},{"location":"api/jam.aio.sessions.redis/#jam.aio.sessions.redis.RedisSessions.get","title":"get  <code>async</code>","text":"<pre><code>get(session_id: str) -&gt; dict | None\n</code></pre> <p>Retrieve a session by its key or ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The session key or ID.</p> required <p>Returns:</p> Type Description <code>dict | None</code> <p>dict | None: The session data if found, otherwise None.</p>"},{"location":"api/jam.aio.sessions.redis/#jam.aio.sessions.redis.RedisSessions.rework","title":"rework  <code>async</code>","text":"<pre><code>rework(session_id: str) -&gt; str\n</code></pre> <p>Rework a session and return its new ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to rework.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If the session with the given ID does not exist.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The new session ID.</p>"},{"location":"api/jam.aio.sessions.redis/#jam.aio.sessions.redis.RedisSessions.update","title":"update  <code>async</code>","text":"<pre><code>update(session_id: str, data: dict) -&gt; None\n</code></pre> <p>Update an existing session with new data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to update.</p> required <code>data</code> <code>dict</code> <p>The new data to be stored in the session.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If the session with the given ID does not exist.</p>"},{"location":"api/jam.cli.cli/","title":"jam.cli.cli","text":""},{"location":"api/jam.cli.cli/#jam.cli.cli","title":"cli","text":"<p>Functions:</p> Name Description <code>cli</code> <p>Jam CLI - Key generation and password utilities.</p>"},{"location":"api/jam.cli.cli/#jam.cli.cli.cli","title":"cli","text":"<pre><code>cli() -&gt; None\n</code></pre> <p>Jam CLI - Key generation and password utilities.</p>"},{"location":"api/jam.cli.commands.keys/","title":"jam.cli.commands.keys","text":""},{"location":"api/jam.cli.commands.keys/#jam.cli.commands.keys","title":"keys","text":"<p>Functions:</p> Name Description <code>aes</code> <p>Generate AES key.</p> <code>ecdsa</code> <p>Generate ECDSA P-384 key pair.</p> <code>ed25519</code> <p>Generate Ed25519 key pair.</p> <code>keys</code> <p>Generate cryptographic keys.</p> <code>rsa</code> <p>Generate RSA key pair.</p> <code>symmetric</code> <p>Generate symmetric key.</p>"},{"location":"api/jam.cli.commands.keys/#jam.cli.commands.keys.aes","title":"aes","text":"<pre><code>aes(out: str) -&gt; None\n</code></pre> <p>Generate AES key.</p>"},{"location":"api/jam.cli.commands.keys/#jam.cli.commands.keys.ecdsa","title":"ecdsa","text":"<pre><code>ecdsa(public_out: str, private_out: str) -&gt; None\n</code></pre> <p>Generate ECDSA P-384 key pair.</p>"},{"location":"api/jam.cli.commands.keys/#jam.cli.commands.keys.ed25519","title":"ed25519","text":"<pre><code>ed25519(public_out: str, private_out: str) -&gt; None\n</code></pre> <p>Generate Ed25519 key pair.</p>"},{"location":"api/jam.cli.commands.keys/#jam.cli.commands.keys.keys","title":"keys","text":"<pre><code>keys() -&gt; None\n</code></pre> <p>Generate cryptographic keys.</p>"},{"location":"api/jam.cli.commands.keys/#jam.cli.commands.keys.rsa","title":"rsa","text":"<pre><code>rsa(public_out: str, private_out: str) -&gt; None\n</code></pre> <p>Generate RSA key pair.</p>"},{"location":"api/jam.cli.commands.keys/#jam.cli.commands.keys.symmetric","title":"symmetric","text":"<pre><code>symmetric(out: str, bytes: int) -&gt; None\n</code></pre> <p>Generate symmetric key.</p>"},{"location":"api/jam.cli.commands/","title":"jam.cli.commands","text":""},{"location":"api/jam.cli.commands/#jam.cli.commands","title":"commands","text":"<p>Modules:</p> Name Description <code>keys</code> <code>password</code>"},{"location":"api/jam.cli.commands.password/","title":"jam.cli.commands.password","text":""},{"location":"api/jam.cli.commands.password/#jam.cli.commands.password","title":"password","text":"<p>Functions:</p> Name Description <code>hash</code> <p>Hash a password.</p> <code>password</code> <p>Password hashing and verification utilities.</p> <code>verify</code> <p>Verify a password against a hash.</p>"},{"location":"api/jam.cli.commands.password/#jam.cli.commands.password.hash","title":"hash","text":"<pre><code>hash(password: str, out: str | None) -&gt; None\n</code></pre> <p>Hash a password.</p>"},{"location":"api/jam.cli.commands.password/#jam.cli.commands.password.password","title":"password","text":"<pre><code>password() -&gt; None\n</code></pre> <p>Password hashing and verification utilities.</p>"},{"location":"api/jam.cli.commands.password/#jam.cli.commands.password.verify","title":"verify","text":"<pre><code>verify(hash: str) -&gt; None\n</code></pre> <p>Verify a password against a hash.</p>"},{"location":"api/jam.cli/","title":"jam.cli","text":""},{"location":"api/jam.cli/#jam.cli","title":"cli","text":"<p>CLI tool for Jam.</p> <p>Modules:</p> Name Description <code>cli</code> <code>commands</code>"},{"location":"api/jam.encoders/","title":"jam.encoders","text":""},{"location":"api/jam.encoders/#jam.encoders","title":"encoders","text":"<p>Classes:</p> Name Description <code>JsonEncoder</code> <p>Json encoder.</p> <code>MsgspecJsonEncoder</code> <p>JSON encoder based on msgspec.</p>"},{"location":"api/jam.encoders/#jam.encoders.JsonEncoder","title":"JsonEncoder","text":"<p>               Bases: <code>BaseEncoder</code></p> <p>Json encoder.</p> <p>Methods:</p> Name Description <code>dumps</code> <p>Dump dict.</p> <code>loads</code> <p>Load json.</p>"},{"location":"api/jam.encoders/#jam.encoders.JsonEncoder.dumps","title":"dumps  <code>classmethod</code>","text":"<pre><code>dumps(var: dict[str, Any]) -&gt; bytes\n</code></pre> <p>Dump dict.</p>"},{"location":"api/jam.encoders/#jam.encoders.JsonEncoder.loads","title":"loads  <code>classmethod</code>","text":"<pre><code>loads(var: str | bytes) -&gt; dict[str, Any]\n</code></pre> <p>Load json.</p>"},{"location":"api/jam.encoders/#jam.encoders.MsgspecJsonEncoder","title":"MsgspecJsonEncoder","text":"<p>               Bases: <code>BaseEncoder</code></p> <p>JSON encoder based on msgspec.</p> <p>To use it, you need to install the optional msgspec: <code>pip install msgspec</code></p> <p>Methods:</p> Name Description <code>dumps</code> <p>Dump dict.</p> <code>loads</code> <p>Load JSON to dict.</p>"},{"location":"api/jam.encoders/#jam.encoders.MsgspecJsonEncoder.dumps","title":"dumps  <code>classmethod</code>","text":"<pre><code>dumps(var: dict[str, Any]) -&gt; bytes\n</code></pre> <p>Dump dict.</p>"},{"location":"api/jam.encoders/#jam.encoders.MsgspecJsonEncoder.loads","title":"loads  <code>classmethod</code>","text":"<pre><code>loads(var: str | bytes) -&gt; dict[str, Any]\n</code></pre> <p>Load JSON to dict.</p>"},{"location":"api/jam.exceptions.base/","title":"jam.exceptions.base","text":""},{"location":"api/jam.exceptions.base/#jam.exceptions.base","title":"base","text":"<p>Classes:</p> Name Description <code>JamConfigurationError</code> <p>Exception raised when a configuration error occurs.</p> <code>JamError</code> <p>Base jam exception.</p> <code>JamValidationError</code> <p>Exception raised when a validation error occurs.</p>"},{"location":"api/jam.exceptions.base/#jam.exceptions.base.JamConfigurationError","title":"JamConfigurationError","text":"<pre><code>JamConfigurationError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when a configuration error occurs.</p>"},{"location":"api/jam.exceptions.base/#jam.exceptions.base.JamError","title":"JamError","text":"<pre><code>JamError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>Base jam exception.</p> <p>Support: - message: human-readable message - error_code: machine-readable error code - details: additional data</p> <p>Attributes:</p> Name Type Description <code>message</code> <code>str</code> <p>Human-readable message.</p>"},{"location":"api/jam.exceptions.base/#jam.exceptions.base.JamError.message","title":"message  <code>property</code>","text":"<pre><code>message: str\n</code></pre> <p>Human-readable message.</p>"},{"location":"api/jam.exceptions.base/#jam.exceptions.base.JamValidationError","title":"JamValidationError","text":"<pre><code>JamValidationError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when a validation error occurs.</p>"},{"location":"api/jam.exceptions.jose/","title":"jam.exceptions.jose","text":""},{"location":"api/jam.exceptions.jose/#jam.exceptions.jose","title":"jose","text":""},{"location":"api/jam.exceptions.jwt/","title":"jam.exceptions.jwt","text":""},{"location":"api/jam.exceptions.jwt/#jam.exceptions.jwt","title":"jwt","text":""},{"location":"api/jam.exceptions/","title":"jam.exceptions","text":""},{"location":"api/jam.exceptions/#jam.exceptions","title":"exceptions","text":"<p>All Jam exceptions</p> <p>Modules:</p> Name Description <code>base</code> <code>plugins</code> <p>Classes:</p> Name Description <code>JamConfigurationError</code> <p>Exception raised when a configuration error occurs.</p> <code>JamError</code> <p>Base jam exception.</p> <code>JamFlaskPluginConfigError</code> <p>Exception raised when a configuration error occurs for a flask plugin.</p> <code>JamFlaskPluginError</code> <p>Exception raised when an error occurs for a flask plugin.</p> <code>JamLitestarPluginConfigError</code> <p>Exception raised when a configuration error occurs for a litestar plugin.</p> <code>JamLitestarPluginError</code> <p>Exception raised when an error occurs for a litestar plugin.</p> <code>JamStarlettePluginConfigError</code> <p>Exception raised when a configuration error occurs for a starlette plugin.</p> <code>JamStarlettePluginError</code> <p>Exception raised when an error occurs for a starlette plugin.</p> <code>JamValidationError</code> <p>Exception raised when a validation error occurs.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamConfigurationError","title":"JamConfigurationError","text":"<pre><code>JamConfigurationError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when a configuration error occurs.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamError","title":"JamError","text":"<pre><code>JamError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>Base jam exception.</p> <p>Support: - message: human-readable message - error_code: machine-readable error code - details: additional data</p> <p>Attributes:</p> Name Type Description <code>message</code> <code>str</code> <p>Human-readable message.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamError.message","title":"message  <code>property</code>","text":"<pre><code>message: str\n</code></pre> <p>Human-readable message.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamFlaskPluginConfigError","title":"JamFlaskPluginConfigError","text":"<pre><code>JamFlaskPluginConfigError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamConfigurationError</code></p> <p>Exception raised when a configuration error occurs for a flask plugin.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamFlaskPluginError","title":"JamFlaskPluginError","text":"<pre><code>JamFlaskPluginError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when an error occurs for a flask plugin.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamLitestarPluginConfigError","title":"JamLitestarPluginConfigError","text":"<pre><code>JamLitestarPluginConfigError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamConfigurationError</code></p> <p>Exception raised when a configuration error occurs for a litestar plugin.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamLitestarPluginError","title":"JamLitestarPluginError","text":"<pre><code>JamLitestarPluginError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when an error occurs for a litestar plugin.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamStarlettePluginConfigError","title":"JamStarlettePluginConfigError","text":"<pre><code>JamStarlettePluginConfigError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamConfigurationError</code></p> <p>Exception raised when a configuration error occurs for a starlette plugin.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamStarlettePluginError","title":"JamStarlettePluginError","text":"<pre><code>JamStarlettePluginError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when an error occurs for a starlette plugin.</p>"},{"location":"api/jam.exceptions/#jam.exceptions.JamValidationError","title":"JamValidationError","text":"<pre><code>JamValidationError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when a validation error occurs.</p>"},{"location":"api/jam.exceptions.oauth2/","title":"jam.exceptions.oauth2","text":""},{"location":"api/jam.exceptions.oauth2/#jam.exceptions.oauth2","title":"oauth2","text":""},{"location":"api/jam.exceptions.paseto/","title":"jam.exceptions.paseto","text":""},{"location":"api/jam.exceptions.paseto/#jam.exceptions.paseto","title":"paseto","text":""},{"location":"api/jam.exceptions.plugins/","title":"jam.exceptions.plugins","text":""},{"location":"api/jam.exceptions.plugins/#jam.exceptions.plugins","title":"plugins","text":"<p>Classes:</p> Name Description <code>JamFlaskPluginConfigError</code> <p>Exception raised when a configuration error occurs for a flask plugin.</p> <code>JamFlaskPluginError</code> <p>Exception raised when an error occurs for a flask plugin.</p> <code>JamLitestarPluginConfigError</code> <p>Exception raised when a configuration error occurs for a litestar plugin.</p> <code>JamLitestarPluginError</code> <p>Exception raised when an error occurs for a litestar plugin.</p> <code>JamStarlettePluginConfigError</code> <p>Exception raised when a configuration error occurs for a starlette plugin.</p> <code>JamStarlettePluginError</code> <p>Exception raised when an error occurs for a starlette plugin.</p>"},{"location":"api/jam.exceptions.plugins/#jam.exceptions.plugins.JamFlaskPluginConfigError","title":"JamFlaskPluginConfigError","text":"<pre><code>JamFlaskPluginConfigError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamConfigurationError</code></p> <p>Exception raised when a configuration error occurs for a flask plugin.</p>"},{"location":"api/jam.exceptions.plugins/#jam.exceptions.plugins.JamFlaskPluginError","title":"JamFlaskPluginError","text":"<pre><code>JamFlaskPluginError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when an error occurs for a flask plugin.</p>"},{"location":"api/jam.exceptions.plugins/#jam.exceptions.plugins.JamLitestarPluginConfigError","title":"JamLitestarPluginConfigError","text":"<pre><code>JamLitestarPluginConfigError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamConfigurationError</code></p> <p>Exception raised when a configuration error occurs for a litestar plugin.</p>"},{"location":"api/jam.exceptions.plugins/#jam.exceptions.plugins.JamLitestarPluginError","title":"JamLitestarPluginError","text":"<pre><code>JamLitestarPluginError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when an error occurs for a litestar plugin.</p>"},{"location":"api/jam.exceptions.plugins/#jam.exceptions.plugins.JamStarlettePluginConfigError","title":"JamStarlettePluginConfigError","text":"<pre><code>JamStarlettePluginConfigError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamConfigurationError</code></p> <p>Exception raised when a configuration error occurs for a starlette plugin.</p>"},{"location":"api/jam.exceptions.plugins/#jam.exceptions.plugins.JamStarlettePluginError","title":"JamStarlettePluginError","text":"<pre><code>JamStarlettePluginError(\n    message: str | None = None,\n    *,\n    error_code: str | None = None,\n    details: Any | None = None,\n)\n</code></pre> <p>               Bases: <code>JamError</code></p> <p>Exception raised when an error occurs for a starlette plugin.</p>"},{"location":"api/jam.exceptions.saml/","title":"jam.exceptions.saml","text":""},{"location":"api/jam.exceptions.saml/#jam.exceptions.saml","title":"saml","text":""},{"location":"api/jam.exceptions.sessions/","title":"jam.exceptions.sessions","text":""},{"location":"api/jam.exceptions.sessions/#jam.exceptions.sessions","title":"sessions","text":""},{"location":"api/jam.ext.fastapi/","title":"jam.ext.fastapi","text":""},{"location":"api/jam.ext.fastapi/#jam.ext.fastapi","title":"fastapi","text":"<p>FastAPI integration.</p> <p>FastAPI docs: https://fastapi.tiangolo.com/</p> <p>Classes:</p> Name Description <code>JWTBackend</code> <p>JWT Backend for litestar.</p> <code>PASETOBackend</code> <p>PASETO auth backend.</p> <code>SessionBackend</code> <p>Session backend.</p>"},{"location":"api/jam.ext.fastapi/#jam.ext.fastapi.JWTBackend","title":"JWTBackend","text":"<pre><code>JWTBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    use_list: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>JWT Backend for litestar.</p>"},{"location":"api/jam.ext.fastapi/#jam.ext.fastapi.PASETOBackend","title":"PASETOBackend","text":"<pre><code>PASETOBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>PASETO auth backend.</p>"},{"location":"api/jam.ext.fastapi/#jam.ext.fastapi.SessionBackend","title":"SessionBackend","text":"<pre><code>SessionBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>Session backend.</p>"},{"location":"api/jam.ext.flask.extensions/","title":"jam.ext.flask.extensions","text":""},{"location":"api/jam.ext.flask.extensions/#jam.ext.flask.extensions","title":"extensions","text":"<p>Classes:</p> Name Description <code>BaseAuthExtension</code> <p>Base Jam authentication extension for Flask.</p> <code>BaseExtension</code> <p>Base Jam extension for Flask.</p> <code>JWTExtension</code> <p>JWT extension for Flask.</p> <code>OAuth2Extension</code> <p>OAuth2 extension for Flask.</p> <code>PASETOExtension</code> <p>PASETO extension for Flask.</p> <code>SessionExtension</code> <p>Session extension for Flask.</p>"},{"location":"api/jam.ext.flask.extensions/#jam.ext.flask.extensions.BaseAuthExtension","title":"BaseAuthExtension","text":"<pre><code>BaseAuthExtension(\n    app: Flask | None = None,\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = True,\n    **kwargs: Any,\n)\n</code></pre> <p>               Bases: <code>BaseExtension</code></p> <p>Base Jam authentication extension for Flask.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>Flask | None</code> <p>Flask application instance</p> <code>None</code> <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>cookie_name</code> <code>str | None</code> <p>Cookie name to read token</p> <code>None</code> <code>header_name</code> <code>str | None</code> <p>Header name to read token</p> <code>None</code> <code>bearer</code> <code>bool</code> <p>Strip \"Bearer \" prefix from header</p> <code>True</code> <code>**kwargs</code> <code>Any</code> <p>Configuration arguments if config=None</p> <code>{}</code> <p>Methods:</p> Name Description <code>init_app</code> <p>Initialize the Flask application.</p>"},{"location":"api/jam.ext.flask.extensions/#jam.ext.flask.extensions.BaseAuthExtension.init_app","title":"init_app","text":"<pre><code>init_app(app: Flask) -&gt; None\n</code></pre> <p>Initialize the Flask application.</p>"},{"location":"api/jam.ext.flask.extensions/#jam.ext.flask.extensions.BaseExtension","title":"BaseExtension","text":"<pre><code>BaseExtension(app: Flask | None = None, **kwargs: Any)\n</code></pre> <p>Base Jam extension for Flask.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>Flask | None</code> <p>Flask application instance</p> <code>None</code> <code>auth</code> <code>Any | None</code> <p>Pre-created auth module instance</p> required <code>**kwargs</code> <code>Any</code> <p>Configuration arguments</p> <code>{}</code> <p>Methods:</p> Name Description <code>init_app</code> <p>Initialize the Flask application.</p>"},{"location":"api/jam.ext.flask.extensions/#jam.ext.flask.extensions.BaseExtension.init_app","title":"init_app","text":"<pre><code>init_app(app: Flask) -&gt; None\n</code></pre> <p>Initialize the Flask application.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>Flask</code> <p>Flask application instance</p> required"},{"location":"api/jam.ext.flask.extensions/#jam.ext.flask.extensions.JWTExtension","title":"JWTExtension","text":"<pre><code>JWTExtension(\n    app: Flask | None = None,\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    use_list: bool = False,\n    bearer: bool = True,\n    **kwargs: Any,\n)\n</code></pre> <p>               Bases: <code>BaseAuthExtension</code></p> <p>JWT extension for Flask.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>Flask | None</code> <p>Flask application instance</p> <code>None</code> <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>cookie_name</code> <code>str | None</code> <p>Cookie name to read token</p> <code>None</code> <code>header_name</code> <code>str | None</code> <p>Header name to read token</p> <code>None</code> <code>use_list</code> <code>bool</code> <p>Use a list for token</p> <code>False</code> <code>bearer</code> <code>bool</code> <p>Strip \"Bearer \" prefix from header</p> <code>True</code> <code>**kwargs</code> <code>Any</code> <p>Configuration arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.flask.extensions/#jam.ext.flask.extensions.OAuth2Extension","title":"OAuth2Extension","text":"<pre><code>OAuth2Extension(\n    app: Flask | None = None,\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    **kwargs: Any,\n)\n</code></pre> <p>               Bases: <code>BaseExtension</code></p> <p>OAuth2 extension for Flask.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>Flask | None</code> <p>Flask application instance</p> <code>None</code> <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>**kwargs</code> <code>Any</code> <p>Configuration arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.flask.extensions/#jam.ext.flask.extensions.PASETOExtension","title":"PASETOExtension","text":"<pre><code>PASETOExtension(\n    app: Flask | None = None,\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = True,\n    **kwargs: Any,\n)\n</code></pre> <p>               Bases: <code>BaseAuthExtension</code></p> <p>PASETO extension for Flask.</p>"},{"location":"api/jam.ext.flask.extensions/#jam.ext.flask.extensions.SessionExtension","title":"SessionExtension","text":"<pre><code>SessionExtension(\n    app: Flask | None = None,\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = True,\n    **kwargs: Any,\n)\n</code></pre> <p>               Bases: <code>BaseAuthExtension</code></p> <p>Session extension for Flask.</p>"},{"location":"api/jam.ext.flask/","title":"jam.ext.flask","text":""},{"location":"api/jam.ext.flask/#jam.ext.flask","title":"flask","text":"<p>Flask integration.</p> <p>Flask docs: https://flask.palletsprojects.com</p> <p>Modules:</p> Name Description <code>extensions</code> <code>objects</code> <p>Classes:</p> Name Description <code>JWTExtension</code> <p>JWT extension for Flask.</p> <code>OAuth2Extension</code> <p>OAuth2 extension for Flask.</p> <code>PASETOExtension</code> <p>PASETO extension for Flask.</p> <code>SessionExtension</code> <p>Session extension for Flask.</p> <code>Token</code> <p>Data for Authresult.</p>"},{"location":"api/jam.ext.flask/#jam.ext.flask.JWTExtension","title":"JWTExtension","text":"<pre><code>JWTExtension(\n    app: Flask | None = None,\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    use_list: bool = False,\n    bearer: bool = True,\n    **kwargs: Any,\n)\n</code></pre> <p>               Bases: <code>BaseAuthExtension</code></p> <p>JWT extension for Flask.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>Flask | None</code> <p>Flask application instance</p> <code>None</code> <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>cookie_name</code> <code>str | None</code> <p>Cookie name to read token</p> <code>None</code> <code>header_name</code> <code>str | None</code> <p>Header name to read token</p> <code>None</code> <code>use_list</code> <code>bool</code> <p>Use a list for token</p> <code>False</code> <code>bearer</code> <code>bool</code> <p>Strip \"Bearer \" prefix from header</p> <code>True</code> <code>**kwargs</code> <code>Any</code> <p>Configuration arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.flask/#jam.ext.flask.OAuth2Extension","title":"OAuth2Extension","text":"<pre><code>OAuth2Extension(\n    app: Flask | None = None,\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    **kwargs: Any,\n)\n</code></pre> <p>               Bases: <code>BaseExtension</code></p> <p>OAuth2 extension for Flask.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>Flask | None</code> <p>Flask application instance</p> <code>None</code> <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>**kwargs</code> <code>Any</code> <p>Configuration arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.flask/#jam.ext.flask.PASETOExtension","title":"PASETOExtension","text":"<pre><code>PASETOExtension(\n    app: Flask | None = None,\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = True,\n    **kwargs: Any,\n)\n</code></pre> <p>               Bases: <code>BaseAuthExtension</code></p> <p>PASETO extension for Flask.</p>"},{"location":"api/jam.ext.flask/#jam.ext.flask.SessionExtension","title":"SessionExtension","text":"<pre><code>SessionExtension(\n    app: Flask | None = None,\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = True,\n    **kwargs: Any,\n)\n</code></pre> <p>               Bases: <code>BaseAuthExtension</code></p> <p>Session extension for Flask.</p>"},{"location":"api/jam.ext.flask/#jam.ext.flask.Token","title":"Token  <code>dataclass</code>","text":"<pre><code>Token(token: str | None)\n</code></pre> <p>Data for Authresult.</p>"},{"location":"api/jam.ext.flask.objects/","title":"jam.ext.flask.objects","text":""},{"location":"api/jam.ext.flask.objects/#jam.ext.flask.objects","title":"objects","text":"<p>Classes:</p> Name Description <code>Token</code> <p>Data for Authresult.</p>"},{"location":"api/jam.ext.flask.objects/#jam.ext.flask.objects.Token","title":"Token  <code>dataclass</code>","text":"<pre><code>Token(token: str | None)\n</code></pre> <p>Data for Authresult.</p>"},{"location":"api/jam.ext.litestar/","title":"jam.ext.litestar","text":""},{"location":"api/jam.ext.litestar/#jam.ext.litestar","title":"litestar","text":"<p>Litestar integration.</p> <p>Litestar docs: https://docs.litestar.dev</p> <p>Modules:</p> Name Description <code>middleware</code> <code>objects</code> <code>plugins</code> <p>Classes:</p> Name Description <code>BasePlugin</code> <p>Base Litestar plugin.</p> <code>BaseUser</code> <p>Abstract user model for AuthResult.</p> <code>JWTPlugin</code> <p>JWT plugin for litestar.</p> <code>OAuth2Plugin</code> <p>OAuth2 plugin for litestar.</p> <code>PASETOPlugin</code> <p>PASETO plugin for litestar.</p> <code>SessionPlugin</code> <p>Sessions plugin for litestar.</p> <code>SimpleUser</code> <p>Simple user with payload.</p> <code>Token</code> <p>Data for Authresult.</p>"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.BasePlugin","title":"BasePlugin","text":"<pre><code>BasePlugin(\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    middleware: bool = True,\n    user: type[BaseUser] | None = None,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>InitPlugin</code></p> <p>Base Litestar plugin.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>cookie_name</code> <code>str | None</code> <p>Cookie name to read token</p> <code>None</code> <code>header_name</code> <code>str | None</code> <p>Header name to read token</p> <code>None</code> <code>bearer</code> <code>bool</code> <p>Use bearer prefix for token (e.g. \"Bearer \")</p> <code>False</code> <code>middleware</code> <code>bool</code> <p>Use middleware?</p> <code>True</code> <code>user</code> <code>type[BaseUser]</code> <p>User for request state. See: DOCUMENTATION</p> <code>None</code> <code>**kwargs</code> <p>Config arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.BaseUser","title":"BaseUser  <code>dataclass</code>","text":"<pre><code>BaseUser()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract user model for AuthResult.</p> <p>Methods:</p> Name Description <code>from_payload</code> <p>Build model from payload.</p>"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.BaseUser.from_payload","title":"from_payload  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>from_payload(payload: dict[str, Any]) -&gt; BaseUser\n</code></pre> <p>Build model from payload.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload for your data.</p> required"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.JWTPlugin","title":"JWTPlugin","text":"<pre><code>JWTPlugin(\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    middleware: bool = True,\n    bearer: bool = False,\n    use_list: bool = False,\n    user: type[BaseUser] | None = None,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BasePlugin</code></p> <p>JWT plugin for litestar.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>cookie_name</code> <code>str | None</code> <p>Cookie name to read token</p> <code>None</code> <code>header_name</code> <code>str | None</code> <p>Header name to read token</p> <code>None</code> <code>middleware</code> <code>bool</code> <p>Use middleware?</p> <code>True</code> <code>bearer</code> <code>bool</code> <p>Use bearer prefix for token (e.g. \"Bearer \")</p> <code>False</code> <code>use_list</code> <code>bool</code> <p>Use token list for authentication?</p> <code>False</code> <code>user</code> <code>type[BaseUser]</code> <p>User for request state. See: DOCUMENTATION</p> <code>None</code> <code>**kwargs</code> <p>Config arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.OAuth2Plugin","title":"OAuth2Plugin","text":"<pre><code>OAuth2Plugin(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BasePlugin</code></p> <p>OAuth2 plugin for litestar.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>**kwargs</code> <p>Config arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.PASETOPlugin","title":"PASETOPlugin","text":"<pre><code>PASETOPlugin(\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    middleware: bool = True,\n    user: type[BaseUser] | None = None,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BasePlugin</code></p> <p>PASETO plugin for litestar.</p>"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.SessionPlugin","title":"SessionPlugin","text":"<pre><code>SessionPlugin(\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    middleware: bool = True,\n    user: type[BaseUser] | None = None,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BasePlugin</code></p> <p>Sessions plugin for litestar.</p>"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.SimpleUser","title":"SimpleUser  <code>dataclass</code>","text":"<pre><code>SimpleUser(payload: dict[str, Any])\n</code></pre> <p>               Bases: <code>BaseUser</code></p> <p>Simple user with payload.</p> <p>Methods:</p> Name Description <code>from_payload</code> <p>Set payload=payload.</p>"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.SimpleUser.from_payload","title":"from_payload  <code>classmethod</code>","text":"<pre><code>from_payload(payload: dict[str, Any]) -&gt; SimpleUser\n</code></pre> <p>Set payload=payload.</p>"},{"location":"api/jam.ext.litestar/#jam.ext.litestar.Token","title":"Token  <code>dataclass</code>","text":"<pre><code>Token(token: str | None)\n</code></pre> <p>Data for Authresult.</p>"},{"location":"api/jam.ext.litestar.middleware/","title":"jam.ext.litestar.middleware","text":""},{"location":"api/jam.ext.litestar.middleware/#jam.ext.litestar.middleware","title":"middleware","text":"<p>Classes:</p> Name Description <code>BaseMiddleware</code> <p>Base Jam middleware for litestar.</p> <code>JWTMiddleware</code> <p>JWT Middleware for litestar.</p> <code>PASETOMiddleware</code> <p>Middleware for PASETO.</p> <code>SessionMiddleware</code> <p>Session middleware for litestar.</p>"},{"location":"api/jam.ext.litestar.middleware/#jam.ext.litestar.middleware.BaseMiddleware","title":"BaseMiddleware","text":"<p>               Bases: <code>AbstractAuthenticationMiddleware</code></p> <p>Base Jam middleware for litestar.</p>"},{"location":"api/jam.ext.litestar.middleware/#jam.ext.litestar.middleware.JWTMiddleware","title":"JWTMiddleware","text":"<p>               Bases: <code>BaseMiddleware</code></p> <p>JWT Middleware for litestar.</p>"},{"location":"api/jam.ext.litestar.middleware/#jam.ext.litestar.middleware.PASETOMiddleware","title":"PASETOMiddleware","text":"<p>               Bases: <code>BaseMiddleware</code></p> <p>Middleware for PASETO.</p>"},{"location":"api/jam.ext.litestar.middleware/#jam.ext.litestar.middleware.SessionMiddleware","title":"SessionMiddleware","text":"<p>               Bases: <code>BaseMiddleware</code></p> <p>Session middleware for litestar.</p>"},{"location":"api/jam.ext.litestar.objects/","title":"jam.ext.litestar.objects","text":""},{"location":"api/jam.ext.litestar.objects/#jam.ext.litestar.objects","title":"objects","text":"<p>Classes:</p> Name Description <code>BaseUser</code> <p>Abstract user model for AuthResult.</p> <code>SimpleUser</code> <p>Simple user with payload.</p> <code>Token</code> <p>Data for Authresult.</p>"},{"location":"api/jam.ext.litestar.objects/#jam.ext.litestar.objects.BaseUser","title":"BaseUser  <code>dataclass</code>","text":"<pre><code>BaseUser()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract user model for AuthResult.</p> <p>Methods:</p> Name Description <code>from_payload</code> <p>Build model from payload.</p>"},{"location":"api/jam.ext.litestar.objects/#jam.ext.litestar.objects.BaseUser.from_payload","title":"from_payload  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>from_payload(payload: dict[str, Any]) -&gt; BaseUser\n</code></pre> <p>Build model from payload.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload for your data.</p> required"},{"location":"api/jam.ext.litestar.objects/#jam.ext.litestar.objects.SimpleUser","title":"SimpleUser  <code>dataclass</code>","text":"<pre><code>SimpleUser(payload: dict[str, Any])\n</code></pre> <p>               Bases: <code>BaseUser</code></p> <p>Simple user with payload.</p> <p>Methods:</p> Name Description <code>from_payload</code> <p>Set payload=payload.</p>"},{"location":"api/jam.ext.litestar.objects/#jam.ext.litestar.objects.SimpleUser.from_payload","title":"from_payload  <code>classmethod</code>","text":"<pre><code>from_payload(payload: dict[str, Any]) -&gt; SimpleUser\n</code></pre> <p>Set payload=payload.</p>"},{"location":"api/jam.ext.litestar.objects/#jam.ext.litestar.objects.Token","title":"Token  <code>dataclass</code>","text":"<pre><code>Token(token: str | None)\n</code></pre> <p>Data for Authresult.</p>"},{"location":"api/jam.ext.litestar.plugins/","title":"jam.ext.litestar.plugins","text":""},{"location":"api/jam.ext.litestar.plugins/#jam.ext.litestar.plugins","title":"plugins","text":"<p>Classes:</p> Name Description <code>BasePlugin</code> <p>Base Litestar plugin.</p> <code>JWTPlugin</code> <p>JWT plugin for litestar.</p> <code>OAuth2Plugin</code> <p>OAuth2 plugin for litestar.</p> <code>PASETOPlugin</code> <p>PASETO plugin for litestar.</p> <code>SessionPlugin</code> <p>Sessions plugin for litestar.</p>"},{"location":"api/jam.ext.litestar.plugins/#jam.ext.litestar.plugins.BasePlugin","title":"BasePlugin","text":"<pre><code>BasePlugin(\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    middleware: bool = True,\n    user: type[BaseUser] | None = None,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>InitPlugin</code></p> <p>Base Litestar plugin.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>cookie_name</code> <code>str | None</code> <p>Cookie name to read token</p> <code>None</code> <code>header_name</code> <code>str | None</code> <p>Header name to read token</p> <code>None</code> <code>bearer</code> <code>bool</code> <p>Use bearer prefix for token (e.g. \"Bearer \")</p> <code>False</code> <code>middleware</code> <code>bool</code> <p>Use middleware?</p> <code>True</code> <code>user</code> <code>type[BaseUser]</code> <p>User for request state. See: DOCUMENTATION</p> <code>None</code> <code>**kwargs</code> <p>Config arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.litestar.plugins/#jam.ext.litestar.plugins.JWTPlugin","title":"JWTPlugin","text":"<pre><code>JWTPlugin(\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    middleware: bool = True,\n    bearer: bool = False,\n    use_list: bool = False,\n    user: type[BaseUser] | None = None,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BasePlugin</code></p> <p>JWT plugin for litestar.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>cookie_name</code> <code>str | None</code> <p>Cookie name to read token</p> <code>None</code> <code>header_name</code> <code>str | None</code> <p>Header name to read token</p> <code>None</code> <code>middleware</code> <code>bool</code> <p>Use middleware?</p> <code>True</code> <code>bearer</code> <code>bool</code> <p>Use bearer prefix for token (e.g. \"Bearer \")</p> <code>False</code> <code>use_list</code> <code>bool</code> <p>Use token list for authentication?</p> <code>False</code> <code>user</code> <code>type[BaseUser]</code> <p>User for request state. See: DOCUMENTATION</p> <code>None</code> <code>**kwargs</code> <p>Config arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.litestar.plugins/#jam.ext.litestar.plugins.OAuth2Plugin","title":"OAuth2Plugin","text":"<pre><code>OAuth2Plugin(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BasePlugin</code></p> <p>OAuth2 plugin for litestar.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any] | str | None</code> <p>Jam config as path/to/file or dict.</p> <code>None</code> <code>pointer</code> <code>str</code> <p>Config pointer</p> <code>GENERIC_POINTER</code> <code>**kwargs</code> <p>Config arguments if config=None</p> <code>{}</code>"},{"location":"api/jam.ext.litestar.plugins/#jam.ext.litestar.plugins.PASETOPlugin","title":"PASETOPlugin","text":"<pre><code>PASETOPlugin(\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    middleware: bool = True,\n    user: type[BaseUser] | None = None,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BasePlugin</code></p> <p>PASETO plugin for litestar.</p>"},{"location":"api/jam.ext.litestar.plugins/#jam.ext.litestar.plugins.SessionPlugin","title":"SessionPlugin","text":"<pre><code>SessionPlugin(\n    config: dict[str, Any] | str | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    middleware: bool = True,\n    user: type[BaseUser] | None = None,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BasePlugin</code></p> <p>Sessions plugin for litestar.</p>"},{"location":"api/jam.ext/","title":"jam.ext","text":""},{"location":"api/jam.ext/#jam.ext","title":"ext","text":"<p>Extensions and integrations.</p> <p>Modules:</p> Name Description <code>fastapi</code> <p>FastAPI integration.</p> <code>flask</code> <p>Flask integration.</p> <code>litestar</code> <p>Litestar integration.</p> <code>starlette</code> <p>Starlette integration.</p>"},{"location":"api/jam.ext.starlette.backends/","title":"jam.ext.starlette.backends","text":""},{"location":"api/jam.ext.starlette.backends/#jam.ext.starlette.backends","title":"backends","text":"<p>Classes:</p> Name Description <code>BaseBackend</code> <p>Base Jam backend.</p> <code>JWTBackend</code> <p>JWT Backend for litestar.</p> <code>PASETOBackend</code> <p>PASETO auth backend.</p> <code>SessionBackend</code> <p>Session backend.</p>"},{"location":"api/jam.ext.starlette.backends/#jam.ext.starlette.backends.BaseBackend","title":"BaseBackend","text":"<pre><code>BaseBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>AuthenticationBackend</code></p> <p>Base Jam backend.</p>"},{"location":"api/jam.ext.starlette.backends/#jam.ext.starlette.backends.JWTBackend","title":"JWTBackend","text":"<pre><code>JWTBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    use_list: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>JWT Backend for litestar.</p>"},{"location":"api/jam.ext.starlette.backends/#jam.ext.starlette.backends.PASETOBackend","title":"PASETOBackend","text":"<pre><code>PASETOBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>PASETO auth backend.</p>"},{"location":"api/jam.ext.starlette.backends/#jam.ext.starlette.backends.SessionBackend","title":"SessionBackend","text":"<pre><code>SessionBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>Session backend.</p>"},{"location":"api/jam.ext.starlette/","title":"jam.ext.starlette","text":""},{"location":"api/jam.ext.starlette/#jam.ext.starlette","title":"starlette","text":"<p>Starlette integration.</p> <p>Starlette docs: https://starlette.dev</p> <p>Modules:</p> Name Description <code>backends</code> <code>objects</code> <p>Classes:</p> Name Description <code>BaseUser</code> <p>BaseUser with builder from payload.</p> <code>JWTBackend</code> <p>JWT Backend for litestar.</p> <code>PASETOBackend</code> <p>PASETO auth backend.</p> <code>SessionBackend</code> <p>Session backend.</p> <code>SimpleUser</code> <p>Simple user with payload only.</p>"},{"location":"api/jam.ext.starlette/#jam.ext.starlette.BaseUser","title":"BaseUser","text":"<p>               Bases: <code>ABC</code>, <code>BaseUser</code></p> <p>BaseUser with builder from payload.</p> <p>Methods:</p> Name Description <code>from_payload</code> <p>Create instance by payload.</p>"},{"location":"api/jam.ext.starlette/#jam.ext.starlette.BaseUser.from_payload","title":"from_payload  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>from_payload(payload: dict[str, Any]) -&gt; BaseUser\n</code></pre> <p>Create instance by payload.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload from auth</p> required <p>Example: <pre><code>class MyUser(JamBaseUser):\n    id: int\n    username: str\n\n    @classmethod\n    def from_payload(cls, payload):\n        return cls(\n            id=payload[\"id\"],\n            username=payload[\"username\"]\n        )\n\npayload = jwt.decode(token)\nuser = MyUser.from_payload(payload)\n</code></pre></p>"},{"location":"api/jam.ext.starlette/#jam.ext.starlette.JWTBackend","title":"JWTBackend","text":"<pre><code>JWTBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    use_list: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>JWT Backend for litestar.</p>"},{"location":"api/jam.ext.starlette/#jam.ext.starlette.PASETOBackend","title":"PASETOBackend","text":"<pre><code>PASETOBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>PASETO auth backend.</p>"},{"location":"api/jam.ext.starlette/#jam.ext.starlette.SessionBackend","title":"SessionBackend","text":"<pre><code>SessionBackend(\n    config: str | dict[str, Any] | None = None,\n    pointer: str = GENERIC_POINTER,\n    cookie_name: str | None = None,\n    header_name: str | None = None,\n    bearer: bool = False,\n    user: type[BaseUser] = SimpleUser,\n    **kwargs,\n)\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>Session backend.</p>"},{"location":"api/jam.ext.starlette/#jam.ext.starlette.SimpleUser","title":"SimpleUser  <code>dataclass</code>","text":"<pre><code>SimpleUser(payload: dict[str, Any])\n</code></pre> <p>               Bases: <code>BaseUser</code></p> <p>Simple user with payload only.</p>"},{"location":"api/jam.ext.starlette.objects/","title":"jam.ext.starlette.objects","text":""},{"location":"api/jam.ext.starlette.objects/#jam.ext.starlette.objects","title":"objects","text":"<p>Classes:</p> Name Description <code>BaseUser</code> <p>BaseUser with builder from payload.</p> <code>SimpleUser</code> <p>Simple user with payload only.</p>"},{"location":"api/jam.ext.starlette.objects/#jam.ext.starlette.objects.BaseUser","title":"BaseUser","text":"<p>               Bases: <code>ABC</code>, <code>BaseUser</code></p> <p>BaseUser with builder from payload.</p> <p>Methods:</p> Name Description <code>from_payload</code> <p>Create instance by payload.</p>"},{"location":"api/jam.ext.starlette.objects/#jam.ext.starlette.objects.BaseUser.from_payload","title":"from_payload  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>from_payload(payload: dict[str, Any]) -&gt; BaseUser\n</code></pre> <p>Create instance by payload.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload from auth</p> required <p>Example: <pre><code>class MyUser(JamBaseUser):\n    id: int\n    username: str\n\n    @classmethod\n    def from_payload(cls, payload):\n        return cls(\n            id=payload[\"id\"],\n            username=payload[\"username\"]\n        )\n\npayload = jwt.decode(token)\nuser = MyUser.from_payload(payload)\n</code></pre></p>"},{"location":"api/jam.ext.starlette.objects/#jam.ext.starlette.objects.SimpleUser","title":"SimpleUser  <code>dataclass</code>","text":"<pre><code>SimpleUser(payload: dict[str, Any])\n</code></pre> <p>               Bases: <code>BaseUser</code></p> <p>Simple user with payload only.</p>"},{"location":"api/jam.instance/","title":"jam.instance","text":""},{"location":"api/jam.instance/#jam.instance","title":"instance","text":"<p>Classes:</p> Name Description <code>Jam</code> <p>Main instance.</p>"},{"location":"api/jam.instance/#jam.instance.Jam","title":"Jam","text":"<pre><code>Jam(\n    config: str | dict[str, Any] = \"pyproject.toml\",\n    pointer: str = \"jam\",\n    *,\n    logger: type[BaseLogger] = JamLogger,\n    log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ] = \"INFO\",\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    plugins: list[type[BasePlugin]] = [],\n)\n</code></pre> <p>               Bases: <code>BaseJam</code></p> <p>Main instance.</p> <p>Methods:</p> Name Description <code>jwe_decrypt</code> <p>Decrypt JWE token.</p> <code>jwe_encrypt</code> <p>Encrypt data using JWE.</p> <code>jws_sign</code> <p>Sign data using JWS.</p> <code>jws_verify</code> <p>Verify JWS token.</p> <code>jwt_create</code> <p>Create JWT token.</p> <code>jwt_decode</code> <p>Verify and decode JWT token.</p> <code>jwt_encode</code> <p>Encode the JWT with the given expire, header, and payload.</p> <code>jwt_make_payload</code> <p>Make JWT-specific payload.</p> <code>oauth2_client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>oauth2_fetch_token</code> <p>Exchange authorization code for access token.</p> <code>oauth2_get_authorized_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>oauth2_refresh_token</code> <p>Use refresh token to obtain a new access token.</p> <code>otp_code</code> <p>Generates an OTP.</p> <code>otp_uri</code> <p>Generates an otpauth:// URI for Google Authenticator.</p> <code>otp_verify_code</code> <p>Checks the OTP code, taking into account the acceptable window.</p> <code>paseto_create</code> <p>Create new PASETO.</p> <code>paseto_decode</code> <p>Decode PASETO.</p> <code>paseto_make_payload</code> <p>Generate payload for PASETO.</p> <code>session_clear</code> <p>Delete all sessions by key.</p> <code>session_create</code> <p>Create new session.</p> <code>session_delete</code> <p>Delete session.</p> <code>session_get</code> <p>Get data from session.</p> <code>session_rework</code> <p>Rework session.</p> <code>session_update</code> <p>Update session data.</p>"},{"location":"api/jam.instance/#jam.instance.Jam.jwe_decrypt","title":"jwe_decrypt","text":"<pre><code>jwe_decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE token.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>Decrypted data.</p>"},{"location":"api/jam.instance/#jam.instance.Jam.jwe_encrypt","title":"jwe_encrypt","text":"<pre><code>jwe_encrypt(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt data using JWE.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to encrypt. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWE header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWE token.</p>"},{"location":"api/jam.instance/#jam.instance.Jam.jws_sign","title":"jws_sign","text":"<pre><code>jws_sign(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Sign data using JWS.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWS header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS token.</p>"},{"location":"api/jam.instance/#jam.instance.Jam.jws_verify","title":"jws_verify","text":"<pre><code>jws_verify(token: str) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS token.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload.</p>"},{"location":"api/jam.instance/#jam.instance.Jam.jwt_create","title":"jwt_create","text":"<pre><code>jwt_create(payload: dict[str, Any]) -&gt; str\n</code></pre> <p>Create JWT token.</p> <p>Deprecated</p> <pre><code>Use Jam.jwt_encode\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Data payload</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New token</p>"},{"location":"api/jam.instance/#jam.instance.Jam.jwt_decode","title":"jwt_decode","text":"<pre><code>jwt_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n    check_nbf: bool = False,\n    include_headers: bool = False,\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify and decode JWT token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token</p> required <code>check_exp</code> <code>bool</code> <p>Check expire</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check white/black list. Docs: https://jam.makridenko.ru/jwt/lists/what/</p> <code>True</code> <code>check_nbf</code> <code>bool</code> <p>Check not-before time</p> <code>False</code> <code>include_headers</code> <code>bool</code> <p>Include headers in the decoded payload</p> <code>False</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload</p> <p>Raises:</p> Type Description <code>JamJWTExpired</code> <p>If token is expired</p> <code>JamJWTNotYetValid</code> <p>If token is not yet valid (nbf claim)</p> <code>JamConfigurationError</code> <p>If JWT list is not connected</p> <code>JamJWTNotInWhiteList</code> <p>If token is not in white list</p> <code>JamJWTInBlackList</code> <p>If token is in black list</p>"},{"location":"api/jam.instance/#jam.instance.Jam.jwt_encode","title":"jwt_encode","text":"<pre><code>jwt_encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    *,\n    payload: dict[str, Any] | None = None,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID. If none use the JTI fabric function.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT.</p>"},{"location":"api/jam.instance/#jam.instance.Jam.jwt_make_payload","title":"jwt_make_payload","text":"<pre><code>jwt_make_payload(\n    exp: int | None, data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Make JWT-specific payload.</p> <p>Deprecated</p> <pre><code>This method is deprecated; the JWT payload is generated automatically in accordance with the specification.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Token expire</p> required <code>data</code> <code>dict[str, Any]</code> <p>Data to payload</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Payload</p>"},{"location":"api/jam.instance/#jam.instance.Jam.oauth2_client_credentials_flow","title":"oauth2_client_credentials_flow","text":"<pre><code>oauth2_client_credentials_flow(\n    provider: str,\n    scope: list[str] | None = None,\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>OAuth2 provider</p> required <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Raises:</p> Type Description <code>JamOAuth2EmptyRaw</code> <p>If response is empty</p> <code>JamOAuth2Error</code> <p>HTTP error</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.instance/#jam.instance.Jam.oauth2_fetch_token","title":"oauth2_fetch_token","text":"<pre><code>oauth2_fetch_token(\n    provider: str,\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam.instance/#jam.instance.Jam.oauth2_get_authorized_url","title":"oauth2_get_authorized_url","text":"<pre><code>oauth2_get_authorized_url(\n    provider: str, scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra ath params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam.instance/#jam.instance.Jam.oauth2_refresh_token","title":"oauth2_refresh_token","text":"<pre><code>oauth2_refresh_token(\n    provider: str,\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam.instance/#jam.instance.Jam.otp_code","title":"otp_code","text":"<pre><code>otp_code(\n    secret: str | bytes, factor: int | None = None\n) -&gt; str\n</code></pre> <p>Generates an OTP.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>OTP code (fixed-length string).</p>"},{"location":"api/jam.instance/#jam.instance.Jam.otp_uri","title":"otp_uri","text":"<pre><code>otp_uri(\n    secret: str,\n    name: str,\n    issuer: str,\n    counter: int | None = None,\n) -&gt; str\n</code></pre> <p>Generates an otpauth:// URI for Google Authenticator.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str</code> <p>User secret key.</p> required <code>name</code> <code>str</code> <p>Account name (e.g., email).</p> required <code>issuer</code> <code>str</code> <p>Service name (e.g., \"GitHub\").</p> required <code>counter</code> <code>int | None</code> <p>Counter (for HOTP). Default is None.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A string of the form \"otpauth://...\"</p>"},{"location":"api/jam.instance/#jam.instance.Jam.otp_verify_code","title":"otp_verify_code","text":"<pre><code>otp_verify_code(\n    secret: str | bytes,\n    code: str,\n    factor: int | None = None,\n    look_ahead: int | None = 1,\n) -&gt; bool\n</code></pre> <p>Checks the OTP code, taking into account the acceptable window.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>code</code> <code>str</code> <p>The code entered.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <code>look_ahead</code> <code>int</code> <p>Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is 1.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the code matches, otherwise False.</p>"},{"location":"api/jam.instance/#jam.instance.Jam.paseto_create","title":"paseto_create","text":"<pre><code>paseto_create(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None,\n) -&gt; str\n</code></pre> <p>Create new PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload</p> required <code>footer</code> <code>dict | str | None</code> <p>Footer</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New token</p>"},{"location":"api/jam.instance/#jam.instance.Jam.paseto_decode","title":"paseto_decode","text":"<pre><code>paseto_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n) -&gt; dict[str, dict[str, Any] | str | None]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Token</p> required <code>check_exp</code> <code>bool</code> <p>Check exp in payload</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check token in list</p> <code>True</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, dict[str, Any] | str | None]</code> <p>{'payload' PAYLOAD, 'footer': FOOTER}</p>"},{"location":"api/jam.instance/#jam.instance.Jam.paseto_make_payload","title":"paseto_make_payload","text":"<pre><code>paseto_make_payload(\n    exp: int | None = None, **data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Generate payload for PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Token expire</p> <code>None</code> <code>data</code> <code>dict[str, Any]</code> <p>Custom data</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Payload</p>"},{"location":"api/jam.instance/#jam.instance.Jam.session_clear","title":"session_clear","text":"<pre><code>session_clear(session_key: str) -&gt; None\n</code></pre> <p>Delete all sessions by key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key of session</p> required"},{"location":"api/jam.instance/#jam.instance.Jam.session_create","title":"session_create","text":"<pre><code>session_create(\n    session_key: str, data: dict[str, Any]\n) -&gt; str\n</code></pre> <p>Create new session.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key for session</p> required <code>data</code> <code>dict[str, Any]</code> <p>Session data</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session ID</p>"},{"location":"api/jam.instance/#jam.instance.Jam.session_delete","title":"session_delete","text":"<pre><code>session_delete(session_id: str) -&gt; None\n</code></pre> <p>Delete session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required"},{"location":"api/jam.instance/#jam.instance.Jam.session_get","title":"session_get","text":"<pre><code>session_get(session_id: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get data from session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: Session data if exist</p>"},{"location":"api/jam.instance/#jam.instance.Jam.session_rework","title":"session_rework","text":"<pre><code>session_rework(old_session_id: str) -&gt; str\n</code></pre> <p>Rework session.</p> <p>Parameters:</p> Name Type Description Default <code>old_session_id</code> <code>str</code> <p>Old session id</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If session with given ID does not exist.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session id</p>"},{"location":"api/jam.instance/#jam.instance.Jam.session_update","title":"session_update","text":"<pre><code>session_update(\n    session_id: str, data: dict[str, Any]\n) -&gt; None\n</code></pre> <p>Update session data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <code>data</code> <code>dict[str, Any]</code> <p>New data</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If session with given ID does not exist.</p>"},{"location":"api/jam.jose.__algorithms__/","title":"jam.jose.algorithms","text":""},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__","title":"__algorithms__","text":"<p>Classes:</p> Name Description <code>AESCBCEncAlgorithm</code> <p>AES-CBC + HMAC content encryption - RFC 7518 Section 5.2.</p> <code>AESGCMEncAlgorithm</code> <p>AES-GCM content encryption - RFC 7518 Section 5.1.</p> <code>AESGCMKeyAlgorithm</code> <p>AES-GCM Key Wrap algorithms - RFC 7518 Section 4.7.</p> <code>AESKeyWrapAlgorithm</code> <p>AES Key Wrap algorithms - RFC 7518 Section 4.4.</p> <code>BaseAlgorithm</code> <p>Base class for JWT signing algorithms.</p> <code>BaseEncAlgorithm</code> <p>Base class for JWE content encryption algorithms - RFC 7518 Section 5.</p> <code>BaseKeyAlgorithm</code> <p>Base class for JWE key management algorithms - RFC 7518 Section 4.</p> <code>ECDHKeyAlgorithm</code> <p>ECDH-ES Key Management algorithms - RFC 7518 Section 4.6.</p> <code>ESAlgorithm</code> <p>ECDSA algorithms (ES256, ES384, ES512).</p> <code>HSAlgorithm</code> <p>HMAC-based algorithms (HS256, HS384, HS512).</p> <code>PBES2KeyAlgorithm</code> <p>PBES2 Key Management algorithms - RFC 7518 Section 4.8.</p> <code>PSAlgorithm</code> <p>RSA PSS algorithms (PS256, PS384, PS512).</p> <code>RSAKeyAlgorithm</code> <p>RSA Key Management algorithms - RFC 7518 Section 4.2.</p> <code>RSAlgorithm</code> <p>RSA PKCS1v15 algorithms (RS256, RS384, RS512).</p> <p>Functions:</p> Name Description <code>create_algorithm</code> <p>Create algorithm instance based on algorithm name.</p> <code>create_enc_algorithm</code> <p>Create JWE content encryption algorithm instance.</p> <code>create_key_algorithm</code> <p>Create JWE key management algorithm instance.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESCBCEncAlgorithm","title":"AESCBCEncAlgorithm","text":"<pre><code>AESCBCEncAlgorithm(enc: str, logger: BaseLogger)\n</code></pre> <p>               Bases: <code>BaseEncAlgorithm</code></p> <p>AES-CBC + HMAC content encryption - RFC 7518 Section 5.2.</p> <p>Methods:</p> Name Description <code>decrypt</code> <p>Decrypt using AES-CBC + HMAC.</p> <code>encrypt</code> <p>Encrypt using AES-CBC + HMAC.</p> <code>get_iv_length</code> <p>Get the IV length for the content encryption algorithm.</p> <code>get_key_length</code> <p>Get the key length for the content encryption algorithm.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESCBCEncAlgorithm.decrypt","title":"decrypt","text":"<pre><code>decrypt(\n    ciphertext: bytes,\n    iv: bytes,\n    tag: bytes,\n    aad: bytes,\n    key: bytes,\n) -&gt; bytes\n</code></pre> <p>Decrypt using AES-CBC + HMAC.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESCBCEncAlgorithm.encrypt","title":"encrypt","text":"<pre><code>encrypt(\n    plaintext: bytes, iv: bytes, aad: bytes, key: bytes\n) -&gt; tuple[bytes, bytes]\n</code></pre> <p>Encrypt using AES-CBC + HMAC.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESCBCEncAlgorithm.get_iv_length","title":"get_iv_length","text":"<pre><code>get_iv_length() -&gt; int\n</code></pre> <p>Get the IV length for the content encryption algorithm.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESCBCEncAlgorithm.get_key_length","title":"get_key_length","text":"<pre><code>get_key_length() -&gt; int\n</code></pre> <p>Get the key length for the content encryption algorithm.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESGCMEncAlgorithm","title":"AESGCMEncAlgorithm","text":"<pre><code>AESGCMEncAlgorithm(enc: str, logger: BaseLogger)\n</code></pre> <p>               Bases: <code>BaseEncAlgorithm</code></p> <p>AES-GCM content encryption - RFC 7518 Section 5.1.</p> <p>Methods:</p> Name Description <code>decrypt</code> <p>Decrypt using AES-GCM.</p> <code>encrypt</code> <p>Encrypt using AES-GCM.</p> <code>get_iv_length</code> <p>Get the IV length for the content encryption algorithm.</p> <code>get_key_length</code> <p>Get the key length for the content encryption algorithm.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESGCMEncAlgorithm.decrypt","title":"decrypt","text":"<pre><code>decrypt(\n    ciphertext: bytes,\n    iv: bytes,\n    tag: bytes,\n    aad: bytes,\n    key: bytes,\n) -&gt; bytes\n</code></pre> <p>Decrypt using AES-GCM.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESGCMEncAlgorithm.encrypt","title":"encrypt","text":"<pre><code>encrypt(\n    plaintext: bytes, iv: bytes, aad: bytes, key: bytes\n) -&gt; tuple[bytes, bytes]\n</code></pre> <p>Encrypt using AES-GCM.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESGCMEncAlgorithm.get_iv_length","title":"get_iv_length","text":"<pre><code>get_iv_length() -&gt; int\n</code></pre> <p>Get the IV length for the content encryption algorithm.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESGCMEncAlgorithm.get_key_length","title":"get_key_length","text":"<pre><code>get_key_length() -&gt; int\n</code></pre> <p>Get the key length for the content encryption algorithm.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESGCMKeyAlgorithm","title":"AESGCMKeyAlgorithm","text":"<pre><code>AESGCMKeyAlgorithm(\n    alg: str,\n    key: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseKeyAlgorithm</code></p> <p>AES-GCM Key Wrap algorithms - RFC 7518 Section 4.7.</p> <p>Methods:</p> Name Description <code>unwrap_key</code> <p>Unwrap CEK using AES-GCM.</p> <code>wrap_key</code> <p>Wrap CEK using AES-GCM.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESGCMKeyAlgorithm.unwrap_key","title":"unwrap_key","text":"<pre><code>unwrap_key(\n    encrypted_key: bytes, header: dict[str, Any]\n) -&gt; bytes\n</code></pre> <p>Unwrap CEK using AES-GCM.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESGCMKeyAlgorithm.wrap_key","title":"wrap_key","text":"<pre><code>wrap_key(cek: bytes) -&gt; tuple[bytes, dict[str, Any]]\n</code></pre> <p>Wrap CEK using AES-GCM.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESKeyWrapAlgorithm","title":"AESKeyWrapAlgorithm","text":"<pre><code>AESKeyWrapAlgorithm(\n    alg: str,\n    key: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseKeyAlgorithm</code></p> <p>AES Key Wrap algorithms - RFC 7518 Section 4.4.</p> <p>Methods:</p> Name Description <code>unwrap_key</code> <p>Unwrap CEK using AES Key Wrap.</p> <code>wrap_key</code> <p>Wrap CEK using AES Key Wrap.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESKeyWrapAlgorithm.unwrap_key","title":"unwrap_key","text":"<pre><code>unwrap_key(\n    encrypted_key: bytes, header: dict[str, Any]\n) -&gt; bytes\n</code></pre> <p>Unwrap CEK using AES Key Wrap.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.AESKeyWrapAlgorithm.wrap_key","title":"wrap_key","text":"<pre><code>wrap_key(cek: bytes) -&gt; tuple[bytes, dict[str, Any]]\n</code></pre> <p>Wrap CEK using AES Key Wrap.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseAlgorithm","title":"BaseAlgorithm","text":"<pre><code>BaseAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base class for JWT signing algorithms.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm name</p> required <code>secret</code> <code>KeyLike</code> <p>Secret key</p> required <code>password</code> <code>bytes | None</code> <p>Password for private key</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> required <p>Methods:</p> Name Description <code>sign</code> <p>Sign data.</p> <code>verify</code> <p>Verify signature.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseAlgorithm.sign","title":"sign  <code>abstractmethod</code>","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseAlgorithm.verify","title":"verify  <code>abstractmethod</code>","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseEncAlgorithm","title":"BaseEncAlgorithm","text":"<pre><code>BaseEncAlgorithm(enc: str, logger: BaseLogger)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base class for JWE content encryption algorithms - RFC 7518 Section 5.</p> <p>Parameters:</p> Name Type Description Default <code>enc</code> <code>str</code> <p>Algorithm name.</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance.</p> required <p>Methods:</p> Name Description <code>decrypt</code> <p>Decrypt ciphertext.</p> <code>encrypt</code> <p>Encrypt plaintext.</p> <code>get_iv_length</code> <p>Return IV length in bytes.</p> <code>get_key_length</code> <p>Return key length in bytes.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseEncAlgorithm.decrypt","title":"decrypt  <code>abstractmethod</code>","text":"<pre><code>decrypt(\n    ciphertext: bytes,\n    iv: bytes,\n    tag: bytes,\n    aad: bytes,\n    key: bytes,\n) -&gt; bytes\n</code></pre> <p>Decrypt ciphertext.</p> <p>Parameters:</p> Name Type Description Default <code>ciphertext</code> <code>bytes</code> <p>Encrypted data.</p> required <code>iv</code> <code>bytes</code> <p>Initialization vector.</p> required <code>tag</code> <code>bytes</code> <p>Authentication tag.</p> required <code>aad</code> <code>bytes</code> <p>Additional authenticated data.</p> required <code>key</code> <code>bytes</code> <p>Encryption key.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Decrypted plaintext.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseEncAlgorithm.encrypt","title":"encrypt  <code>abstractmethod</code>","text":"<pre><code>encrypt(\n    plaintext: bytes, iv: bytes, aad: bytes, key: bytes\n) -&gt; tuple[bytes, bytes]\n</code></pre> <p>Encrypt plaintext.</p> <p>Parameters:</p> Name Type Description Default <code>plaintext</code> <code>bytes</code> <p>Data to encrypt.</p> required <code>iv</code> <code>bytes</code> <p>Initialization vector.</p> required <code>aad</code> <code>bytes</code> <p>Additional authenticated data.</p> required <code>key</code> <code>bytes</code> <p>Encryption key.</p> required <p>Returns:</p> Type Description <code>tuple[bytes, bytes]</code> <p>Tuple of (ciphertext, tag).</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseEncAlgorithm.get_iv_length","title":"get_iv_length  <code>abstractmethod</code>","text":"<pre><code>get_iv_length() -&gt; int\n</code></pre> <p>Return IV length in bytes.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseEncAlgorithm.get_key_length","title":"get_key_length  <code>abstractmethod</code>","text":"<pre><code>get_key_length() -&gt; int\n</code></pre> <p>Return key length in bytes.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseKeyAlgorithm","title":"BaseKeyAlgorithm","text":"<pre><code>BaseKeyAlgorithm(\n    alg: str,\n    key: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base class for JWE key management algorithms - RFC 7518 Section 4.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm name.</p> required <code>key</code> <code>KeyLike</code> <p>Key for wrapping/unwrapping.</p> required <code>password</code> <code>bytes | None</code> <p>Password for encrypted private keys.</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance.</p> required <p>Methods:</p> Name Description <code>unwrap_key</code> <p>Unwrap CEK.</p> <code>wrap_key</code> <p>Wrap CEK (Content Encryption Key).</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseKeyAlgorithm.unwrap_key","title":"unwrap_key  <code>abstractmethod</code>","text":"<pre><code>unwrap_key(\n    encrypted_key: bytes, header: dict[str, Any]\n) -&gt; bytes\n</code></pre> <p>Unwrap CEK.</p> <p>Parameters:</p> Name Type Description Default <code>encrypted_key</code> <code>bytes</code> <p>Wrapped CEK.</p> required <code>header</code> <code>dict[str, Any]</code> <p>JWE header (may contain needed parameters).</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Unwrapped CEK.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.BaseKeyAlgorithm.wrap_key","title":"wrap_key  <code>abstractmethod</code>","text":"<pre><code>wrap_key(cek: bytes) -&gt; tuple[bytes, dict[str, Any]]\n</code></pre> <p>Wrap CEK (Content Encryption Key).</p> <p>Parameters:</p> Name Type Description Default <code>cek</code> <code>bytes</code> <p>Content Encryption Key to wrap.</p> required <p>Returns:</p> Type Description <code>tuple[bytes, dict[str, Any]]</code> <p>Tuple of (encrypted_key, header_updates).</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.ECDHKeyAlgorithm","title":"ECDHKeyAlgorithm","text":"<pre><code>ECDHKeyAlgorithm(\n    alg: str,\n    key: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseKeyAlgorithm</code></p> <p>ECDH-ES Key Management algorithms - RFC 7518 Section 4.6.</p> <p>Methods:</p> Name Description <code>unwrap_key</code> <p>Unwrap CEK using ECDH.</p> <code>wrap_key</code> <p>Wrap CEK using ECDH.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.ECDHKeyAlgorithm.unwrap_key","title":"unwrap_key","text":"<pre><code>unwrap_key(\n    encrypted_key: bytes, header: dict[str, Any]\n) -&gt; bytes\n</code></pre> <p>Unwrap CEK using ECDH.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.ECDHKeyAlgorithm.wrap_key","title":"wrap_key","text":"<pre><code>wrap_key(cek: bytes) -&gt; tuple[bytes, dict[str, Any]]\n</code></pre> <p>Wrap CEK using ECDH.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.ESAlgorithm","title":"ESAlgorithm","text":"<pre><code>ESAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseAlgorithm</code></p> <p>ECDSA algorithms (ES256, ES384, ES512).</p> <p>Methods:</p> Name Description <code>sign</code> <p>Sign data using ECDSA.</p> <code>verify</code> <p>Verify ECDSA signature.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.ESAlgorithm.sign","title":"sign","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data using ECDSA.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.ESAlgorithm.verify","title":"verify","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify ECDSA signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.HSAlgorithm","title":"HSAlgorithm","text":"<pre><code>HSAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseAlgorithm</code></p> <p>HMAC-based algorithms (HS256, HS384, HS512).</p> <p>Methods:</p> Name Description <code>sign</code> <p>Sign data using HMAC.</p> <code>verify</code> <p>Verify HMAC signature.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.HSAlgorithm.sign","title":"sign","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data using HMAC.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.HSAlgorithm.verify","title":"verify","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify HMAC signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.PBES2KeyAlgorithm","title":"PBES2KeyAlgorithm","text":"<pre><code>PBES2KeyAlgorithm(\n    alg: str,\n    key: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseKeyAlgorithm</code></p> <p>PBES2 Key Management algorithms - RFC 7518 Section 4.8.</p> <p>Methods:</p> Name Description <code>unwrap_key</code> <p>Unwrap CEK using PBES2.</p> <code>wrap_key</code> <p>Wrap CEK using PBES2.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.PBES2KeyAlgorithm.unwrap_key","title":"unwrap_key","text":"<pre><code>unwrap_key(\n    encrypted_key: bytes, header: dict[str, Any]\n) -&gt; bytes\n</code></pre> <p>Unwrap CEK using PBES2.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.PBES2KeyAlgorithm.wrap_key","title":"wrap_key","text":"<pre><code>wrap_key(cek: bytes) -&gt; tuple[bytes, dict[str, Any]]\n</code></pre> <p>Wrap CEK using PBES2.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.PSAlgorithm","title":"PSAlgorithm","text":"<pre><code>PSAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseAlgorithm</code></p> <p>RSA PSS algorithms (PS256, PS384, PS512).</p> <p>Methods:</p> Name Description <code>sign</code> <p>Sign data using RSA PSS.</p> <code>verify</code> <p>Verify RSA PSS signature.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.PSAlgorithm.sign","title":"sign","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data using RSA PSS.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.PSAlgorithm.verify","title":"verify","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify RSA PSS signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.RSAKeyAlgorithm","title":"RSAKeyAlgorithm","text":"<pre><code>RSAKeyAlgorithm(\n    alg: str,\n    key: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseKeyAlgorithm</code></p> <p>RSA Key Management algorithms - RFC 7518 Section 4.2.</p> <p>Methods:</p> Name Description <code>unwrap_key</code> <p>Unwrap CEK using RSA.</p> <code>wrap_key</code> <p>Wrap CEK using RSA.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.RSAKeyAlgorithm.unwrap_key","title":"unwrap_key","text":"<pre><code>unwrap_key(\n    encrypted_key: bytes, header: dict[str, Any]\n) -&gt; bytes\n</code></pre> <p>Unwrap CEK using RSA.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.RSAKeyAlgorithm.wrap_key","title":"wrap_key","text":"<pre><code>wrap_key(cek: bytes) -&gt; tuple[bytes, dict[str, Any]]\n</code></pre> <p>Wrap CEK using RSA.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.RSAlgorithm","title":"RSAlgorithm","text":"<pre><code>RSAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseAlgorithm</code></p> <p>RSA PKCS1v15 algorithms (RS256, RS384, RS512).</p> <p>Methods:</p> Name Description <code>sign</code> <p>Sign data using RSA PKCS1v15.</p> <code>verify</code> <p>Verify RSA PKCS1v15 signature.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.RSAlgorithm.sign","title":"sign","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data using RSA PKCS1v15.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.RSAlgorithm.verify","title":"verify","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify RSA PKCS1v15 signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.create_algorithm","title":"create_algorithm","text":"<pre><code>create_algorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n) -&gt; BaseAlgorithm\n</code></pre> <p>Create algorithm instance based on algorithm name.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm name</p> required <code>secret</code> <code>KeyLike</code> <p>Secret key</p> required <code>password</code> <code>bytes | None</code> <p>Password for private key</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> required <p>Returns:</p> Name Type Description <code>BaseAlgorithm</code> <code>BaseAlgorithm</code> <p>Algorithm instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If algorithm is not supported</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.create_enc_algorithm","title":"create_enc_algorithm","text":"<pre><code>create_enc_algorithm(\n    enc: str, logger: BaseLogger\n) -&gt; BaseEncAlgorithm\n</code></pre> <p>Create JWE content encryption algorithm instance.</p> <p>Parameters:</p> Name Type Description Default <code>enc</code> <code>str</code> <p>Algorithm name.</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance.</p> required <p>Returns:</p> Type Description <code>BaseEncAlgorithm</code> <p>BaseEncAlgorithm instance.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If algorithm is not supported.</p>"},{"location":"api/jam.jose.__algorithms__/#jam.jose.__algorithms__.create_key_algorithm","title":"create_key_algorithm","text":"<pre><code>create_key_algorithm(\n    alg: str,\n    key: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n) -&gt; BaseKeyAlgorithm\n</code></pre> <p>Create JWE key management algorithm instance.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm name.</p> required <code>key</code> <code>KeyLike</code> <p>Key for wrapping/unwrapping.</p> required <code>password</code> <code>bytes | None</code> <p>Password for encrypted private keys.</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance.</p> required <p>Returns:</p> Type Description <code>BaseKeyAlgorithm</code> <p>BaseKeyAlgorithm instance.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If algorithm is not supported.</p>"},{"location":"api/jam.jose.__base__/","title":"jam.jose.base","text":""},{"location":"api/jam.jose.__base__/#jam.jose.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseJWE</code> <p>Base JSON Web Encryption - RFC 7516.</p> <code>BaseJWK</code> <p>JSON Web Key - RFC 7517.</p> <code>BaseJWKSet</code> <p>JWK Set - RFC 7517 Section 5.</p> <code>BaseJWKStorage</code> <p>Base JWK Storage.</p> <code>BaseJWS</code> <p>Base JSON Web Signature - RFC 7515.</p> <code>BaseJWT</code> <p>Base JWT.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWE","title":"BaseJWE","text":"<p>               Bases: <code>ABC</code></p> <p>Base JSON Web Encryption - RFC 7516.</p> <p>Methods:</p> Name Description <code>decrypt</code> <p>Decrypt JWE token.</p> <code>encrypt</code> <p>Encrypt plaintext.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWE.decrypt","title":"decrypt  <code>abstractmethod</code>","text":"<pre><code>decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE compact serialization string.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Decrypted plaintext bytes.</p> <p>Raises:</p> Type Description <code>JamJWEDecryptionError</code> <p>If decryption fails.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWE.encrypt","title":"encrypt  <code>abstractmethod</code>","text":"<pre><code>encrypt(\n    plaintext: bytes | str | dict[str, Any],\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt plaintext.</p> <p>Produces JWE Compact Serialization: BASE64URL(header).BASE64URL(encrypted_key).BASE64URL(iv).BASE64URL(ciphertext).BASE64URL(tag)</p> <p>Parameters:</p> Name Type Description Default <code>plaintext</code> <code>bytes | str | dict[str, Any]</code> <p>Data to encrypt. If str, will be encoded to UTF-8. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWE header (must include 'alg' and 'enc').</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>JWE compact serialization string.</p> <p>Raises:</p> Type Description <code>JamJWEEncryptionError</code> <p>If encryption fails.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWK","title":"BaseJWK","text":"<p>               Bases: <code>ABC</code></p> <p>JSON Web Key - RFC 7517.</p> <p>Methods:</p> Name Description <code>from_dict</code> <p>Create JWK from dict.</p> <code>sign</code> <p>Sign data using JWK.</p> <code>to_dict</code> <p>Convert JWK to dict.</p> <code>validate</code> <p>Validate and create JWK from dict.</p> <code>verify</code> <p>Verify JWS token and return payload.</p> <p>Attributes:</p> Name Type Description <code>alg</code> <code>str | None</code> <p>Algorithm (alg) - RS256, ES256, etc.</p> <code>kid</code> <code>str | None</code> <p>Key ID (kid).</p> <code>kty</code> <code>str</code> <p>Key type (kty) - RSA, EC, oct, etc.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWK.alg","title":"alg  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>alg: str | None\n</code></pre> <p>Algorithm (alg) - RS256, ES256, etc.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWK.kid","title":"kid  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>kid: str | None\n</code></pre> <p>Key ID (kid).</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWK.kty","title":"kty  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>kty: str\n</code></pre> <p>Key type (kty) - RSA, EC, oct, etc.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWK.from_dict","title":"from_dict  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; BaseJWK\n</code></pre> <p>Create JWK from dict.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>JWK dict.</p> required <p>Returns:</p> Type Description <code>BaseJWK</code> <p>JWK instance.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWK.sign","title":"sign  <code>abstractmethod</code>","text":"<pre><code>sign(data: bytes, alg: str | None = None) -&gt; str\n</code></pre> <p>Sign data using JWK.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign.</p> required <code>alg</code> <code>str | None</code> <p>Algorithm to use. If None, uses default for kty.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>JWS compact serialization string.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWK.to_dict","title":"to_dict  <code>abstractmethod</code>","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert JWK to dict.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>JWK dict.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWK.validate","title":"validate  <code>abstractmethod</code> <code>staticmethod</code>","text":"<pre><code>validate(data: dict[str, Any]) -&gt; BaseJWK\n</code></pre> <p>Validate and create JWK from dict.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>JWK dict to validate.</p> required <p>Returns:</p> Type Description <code>BaseJWK</code> <p>JWK instance.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If JWK is invalid.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWK.verify","title":"verify  <code>abstractmethod</code>","text":"<pre><code>verify(\n    token: str, alg: str | None = None\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token and return payload.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS compact serialization token.</p> required <code>alg</code> <code>str | None</code> <p>Algorithm to use. If None, uses default for kty.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with 'header' and 'payload' keys.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKSet","title":"BaseJWKSet","text":"<p>               Bases: <code>ABC</code></p> <p>JWK Set - RFC 7517 Section 5.</p> <p>Methods:</p> Name Description <code>filter</code> <p>Filter JWKs by criteria.</p> <code>from_dict</code> <p>Create JWKSet from dict.</p> <code>get_by_kid</code> <p>Get JWK by key ID (kid).</p> <code>get_by_kty</code> <p>Get all JWKs by key type.</p> <code>to_dict</code> <p>Convert JWKSet to dict.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKSet.filter","title":"filter  <code>abstractmethod</code>","text":"<pre><code>filter(**criteria: Any) -&gt; list[dict[str, Any]]\n</code></pre> <p>Filter JWKs by criteria.</p> <p>Parameters:</p> Name Type Description Default <code>**criteria</code> <code>Any</code> <p>Filter criteria (kty, use, alg, key_ops, kid).</p> <code>{}</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: List of matching JWK dicts.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKSet.from_dict","title":"from_dict  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; BaseJWKSet\n</code></pre> <p>Create JWKSet from dict.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>JWKSet dict with 'keys' array.</p> required <p>Returns:</p> Type Description <code>BaseJWKSet</code> <p>JWKSet instance.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKSet.get_by_kid","title":"get_by_kid  <code>abstractmethod</code>","text":"<pre><code>get_by_kid(kid: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get JWK by key ID (kid).</p> <p>Parameters:</p> Name Type Description Default <code>kid</code> <code>str</code> <p>Key ID to search for.</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: JWK dict if found, None otherwise.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKSet.get_by_kty","title":"get_by_kty  <code>abstractmethod</code>","text":"<pre><code>get_by_kty(kty: str) -&gt; list[dict[str, Any]]\n</code></pre> <p>Get all JWKs by key type.</p> <p>Parameters:</p> Name Type Description Default <code>kty</code> <code>str</code> <p>Key type (RSA, EC, oct).</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: List of matching JWK dicts.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKSet.to_dict","title":"to_dict  <code>abstractmethod</code>","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert JWKSet to dict.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: JWKSet dict with 'keys' array.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKStorage","title":"BaseJWKStorage","text":"<p>               Bases: <code>ABC</code></p> <p>Base JWK Storage.</p> <p>Methods:</p> Name Description <code>delete</code> <p>Delete a key by name.</p> <code>get</code> <p>Get a key by name.</p> <code>store</code> <p>Store a JWK with the given name.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKStorage.delete","title":"delete  <code>abstractmethod</code>","text":"<pre><code>delete(name: str) -&gt; None\n</code></pre> <p>Delete a key by name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the key to delete.</p> required"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKStorage.get","title":"get  <code>abstractmethod</code>","text":"<pre><code>get(name: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get a key by name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the key to retrieve.</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: JWK dict if found.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWKStorage.store","title":"store  <code>abstractmethod</code>","text":"<pre><code>store(name: str, jwk: dict[str, Any]) -&gt; None\n</code></pre> <p>Store a JWK with the given name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the key to store.</p> required <code>jwk</code> <code>dict[str, Any]</code> <p>JWK dict to store.</p> required"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWS","title":"BaseJWS","text":"<p>               Bases: <code>ABC</code></p> <p>Base JSON Web Signature - RFC 7515.</p> <p>Methods:</p> Name Description <code>deserialize_compact</code> <p>Parse JWS Compact Serialization.</p> <code>serialize_compact</code> <p>Create JWS Compact Serialization.</p> <code>sign</code> <p>Sign data and return JWS compact serialization.</p> <code>verify</code> <p>Verify JWS token and return header/payload.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWS.deserialize_compact","title":"deserialize_compact  <code>abstractmethod</code>","text":"<pre><code>deserialize_compact(\n    s: str, validate: bool = True\n) -&gt; dict[str, Any]\n</code></pre> <p>Parse JWS Compact Serialization.</p> <p>Parameters:</p> Name Type Description Default <code>s</code> <code>str</code> <p>JWS in compact serialization format.</p> required <code>validate</code> <code>bool</code> <p>Whether to validate signature. Defaults to True.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Parsed JWS with keys: - header: Protected header dict - payload: Decoded payload bytes - signature: Raw signature bytes</p> <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If validation fails.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWS.serialize_compact","title":"serialize_compact  <code>abstractmethod</code>","text":"<pre><code>serialize_compact(\n    protected: dict[str, Any], payload: bytes | str\n) -&gt; str\n</code></pre> <p>Create JWS Compact Serialization.</p> <p>Parameters:</p> Name Type Description Default <code>protected</code> <code>dict[str, Any]</code> <p>Protected header.</p> required <code>payload</code> <code>bytes | str</code> <p>Payload to sign.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS in compact serialization format:  BASE64URL(protected).BASE64URL(payload).BASE64URL(signature)</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWS.sign","title":"sign  <code>abstractmethod</code>","text":"<pre><code>sign(\n    header: dict[str, Any],\n    data: bytes | str | dict[str, Any],\n) -&gt; str\n</code></pre> <p>Sign data and return JWS compact serialization.</p> <p>Parameters:</p> Name Type Description Default <code>header</code> <code>dict[str, Any]</code> <p>JWS header (must include 'alg').</p> required <code>data</code> <code>bytes | str | dict[str, Any]</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS compact serialization string.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWS.verify","title":"verify  <code>abstractmethod</code>","text":"<pre><code>verify(token: str, validate: bool = True) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token and return header/payload.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS compact serialization token.</p> required <code>validate</code> <code>bool</code> <p>Whether to validate signature. Defaults to True.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Parsed JWS with 'header' and 'payload' keys.</p> <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If validation fails.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWT","title":"BaseJWT","text":"<p>               Bases: <code>ABC</code></p> <p>Base JWT.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode the JWT and return the header and payload.</p> <code>decrypt</code> <p>Decrypt JWE token.</p> <code>encode</code> <p>Encode the JWT with the given expire, header, and payload.</p> <code>encrypt</code> <p>Encrypt plaintext.</p> <p>Attributes:</p> Name Type Description <code>jti</code> <code>str</code> <p>The JWT ID.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWT.jti","title":"jti  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>jti: str\n</code></pre> <p>The JWT ID.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWT.decode","title":"decode  <code>abstractmethod</code>","text":"<pre><code>decode(\n    token: str, validate_claims: bool = True\n) -&gt; dict[str, Any]\n</code></pre> <p>Decode the JWT and return the header and payload.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT</p> required <code>validate_claims</code> <code>bool</code> <p>Whether to validate exp/nbf claims.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with 'header' and 'payload' keys (both dicts).</p> <p>Raises:</p> Type Description <code>JamJWTExpired</code> <p>If token is expired.</p> <code>JamJWTNotYetValid</code> <p>If token is not yet valid.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWT.decrypt","title":"decrypt  <code>abstractmethod</code>","text":"<pre><code>decrypt(token: str) -&gt; dict[str, Any] | bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE compact serialization string.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>dict[str, Any] | bytes</code> <p>Decrypted plaintext.</p> <p>Raises:</p> Type Description <code>JamJWEDecryptionError</code> <p>If decryption fails.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWT.encode","title":"encode  <code>abstractmethod</code>","text":"<pre><code>encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    header: dict[str, Any] | None = None,\n    payload: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT.</p>"},{"location":"api/jam.jose.__base__/#jam.jose.__base__.BaseJWT.encrypt","title":"encrypt  <code>abstractmethod</code>","text":"<pre><code>encrypt(\n    plaintext: bytes | str | dict[str, Any],\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt plaintext.</p> <p>Produces JWE Compact Serialization: BASE64URL(header).BASE64URL(encrypted_key).BASE64URL(iv).BASE64URL(ciphertext).BASE64URL(tag)</p> <p>Parameters:</p> Name Type Description Default <code>plaintext</code> <code>bytes | str | dict[str, Any]</code> <p>Data to encrypt. If str, will be encoded to UTF-8. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWE header (must include 'alg' and 'enc').</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>JWE compact serialization string.</p> <p>Raises:</p> Type Description <code>JamJWEEncryptionError</code> <p>If encryption fails.</p>"},{"location":"api/jam.jose.jwe/","title":"jam.jose.jwe","text":""},{"location":"api/jam.jose.jwe/#jam.jose.jwe","title":"jwe","text":"<p>Classes:</p> Name Description <code>JWE</code> <p>JWE (JSON Web Encryption) implementation - RFC 7516.</p>"},{"location":"api/jam.jose.jwe/#jam.jose.jwe.JWE","title":"JWE","text":"<pre><code>JWE(\n    alg: str,\n    enc: str,\n    key: KeyLike | JWK,\n    password: bytes | None = None,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger = logger,\n)\n</code></pre> <p>               Bases: <code>BaseJWE</code></p> <p>JWE (JSON Web Encryption) implementation - RFC 7516.</p> <p>Provides encryption and decryption of data using JWK keys.</p> Example <pre><code>&gt;&gt;&gt; jwk = JWK.from_dict({\"kty\": \"oct\", \"k\": \"your-secret-key\"})\n&gt;&gt;&gt; jwe = JWE(alg=\"A128KW\", enc=\"A128CBC-HS256\", key=jwk)\n&gt;&gt;&gt; token = jwe.encrypt(\"secret data\")\n&gt;&gt;&gt; plaintext = jwe.decrypt(token)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Key management algorithm (e.g., \"A128KW\", \"RSA-OAEP\").</p> required <code>enc</code> <code>str</code> <p>Content encryption algorithm (e.g., \"A128CBC-HS256\", \"A256GCM\").</p> required <code>key</code> <code>KeyLike | JWK</code> <p>Key for encryption/decryption. Can be KeyLike or JWK.</p> required <code>password</code> <code>bytes | None</code> <p>Password for encrypted private keys.</p> <code>None</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>Encoder for serializing/deserializing JSON data.</p> <code>JsonEncoder</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance.</p> <code>logger</code> <p>Methods:</p> Name Description <code>decrypt</code> <p>Decrypt JWE token.</p> <code>encrypt</code> <p>Encrypt plaintext.</p>"},{"location":"api/jam.jose.jwe/#jam.jose.jwe.JWE.decrypt","title":"decrypt","text":"<pre><code>decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE compact serialization string.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Decrypted plaintext bytes.</p> <p>Raises:</p> Type Description <code>JamJWEDecryptionError</code> <p>If decryption fails.</p>"},{"location":"api/jam.jose.jwe/#jam.jose.jwe.JWE.encrypt","title":"encrypt","text":"<pre><code>encrypt(\n    plaintext: bytes | str | dict[str, Any],\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt plaintext.</p> <p>Parameters:</p> Name Type Description Default <code>plaintext</code> <code>bytes | str | dict[str, Any]</code> <p>Data to encrypt. If str, will be encoded to UTF-8. If dict, will be serialized using self._serializer.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>Additional JWE header.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>JWE compact serialization string.</p> <p>Raises:</p> Type Description <code>JamJWEEncryptionError</code> <p>If encryption fails.</p>"},{"location":"api/jam.jose.jwk/","title":"jam.jose.jwk","text":""},{"location":"api/jam.jose.jwk/#jam.jose.jwk","title":"jwk","text":"<p>Classes:</p> Name Description <code>JWK</code> <p>JSON Web Key - RFC 7517.</p> <code>JWKCommon</code> <p>Common JWK parameters shared across all key types - RFC 7517 Section 4.</p> <code>JWKEC</code> <p>Elliptic Curve Key - RFC 7517 Section 6.2.</p> <code>JWKOct</code> <p>Symmetric (Octet Sequence) Key - RFC 7517 Section 6.4.</p> <code>JWKRSA</code> <p>RSA Key - RFC 7517 Section 6.3.</p> <code>JWKSet</code> <p>JWK Set - RFC 7517 Section 5.</p> <p>Attributes:</p> Name Type Description <code>JWKDict</code> <p>Union type representing any valid JWK dict.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKDict","title":"JWKDict  <code>module-attribute</code>","text":"<pre><code>JWKDict = JWKRSA | JWKEC | JWKOct\n</code></pre> <p>Union type representing any valid JWK dict.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWK","title":"JWK","text":"<pre><code>JWK(data: dict[str, Any])\n</code></pre> <p>               Bases: <code>BaseJWK</code></p> <p>JSON Web Key - RFC 7517.</p> <p>Provides JWK validation and signing capabilities.</p> Example <pre><code>&gt;&gt;&gt; jwk = JWK.from_dict({\"kty\": \"oct\", \"k\": \"your-secret-key\"})\n&gt;&gt;&gt; jwk.sign(b\"data\", \"HS256\")\n&gt;&gt;&gt; jwk.verify(token)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Validated JWK dict.</p> required <p>Methods:</p> Name Description <code>from_dict</code> <p>Create JWK from dict.</p> <code>sign</code> <p>Sign data using JWK.</p> <code>to_dict</code> <p>Convert JWK to dict.</p> <code>validate</code> <p>Validate and normalize JWK dict.</p> <code>verify</code> <p>Verify JWS token and return payload.</p> <p>Attributes:</p> Name Type Description <code>alg</code> <code>str | None</code> <p>Get algorithm from JWK if set.</p> <code>kid</code> <code>str | None</code> <p>Get key ID if set.</p> <code>kty</code> <code>str</code> <p>Get key type.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWK.alg","title":"alg  <code>property</code>","text":"<pre><code>alg: str | None\n</code></pre> <p>Get algorithm from JWK if set.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>Algorithm or None.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWK.kid","title":"kid  <code>property</code>","text":"<pre><code>kid: str | None\n</code></pre> <p>Get key ID if set.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>Key ID or None.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWK.kty","title":"kty  <code>property</code>","text":"<pre><code>kty: str\n</code></pre> <p>Get key type.</p> <p>Returns:</p> Type Description <code>str</code> <p>Key type (RSA, EC, oct).</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWK.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; JWK\n</code></pre> <p>Create JWK from dict.</p> <p>Alias for JWK.validate().</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>JWK dict.</p> required <p>Returns:</p> Type Description <code>JWK</code> <p>JWK instance.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWK.sign","title":"sign","text":"<pre><code>sign(data: bytes, alg: str | None = None) -&gt; str\n</code></pre> <p>Sign data using JWK.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign.</p> required <code>alg</code> <code>str | None</code> <p>Algorithm to use. If None, uses default for kty.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>JWS compact serialization string.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If algorithm is not supported or key is invalid.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWK.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert JWK to dict.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>JWK dict.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWK.validate","title":"validate  <code>staticmethod</code>","text":"<pre><code>validate(data: dict[str, Any]) -&gt; JWK\n</code></pre> <p>Validate and normalize JWK dict.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>JWK dict to validate.</p> required <p>Returns:</p> Type Description <code>JWK</code> <p>JWK instance.</p> <p>Raises:</p> Type Description <code>JamJWKValidationError</code> <p>If JWK is invalid.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWK.verify","title":"verify","text":"<pre><code>verify(\n    token: str, alg: str | None = None\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token and return payload.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS compact serialization token.</p> required <code>alg</code> <code>str | None</code> <p>Algorithm to use. If None, uses default for kty.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with 'header' and 'payload' keys.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If signature is invalid.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon","title":"JWKCommon","text":"<p>               Bases: <code>TypedDict</code></p> <p>Common JWK parameters shared across all key types - RFC 7517 Section 4.</p> <p>Attributes:</p> Name Type Description <code>alg</code> <code>str</code> <p>Algorithm. Identifies the algorithm intended for use with the key.</p> <code>key_ops</code> <code>list[str]</code> <p>Key Operations. Identifies the operations for which the key is intended.</p> <code>kid</code> <code>str</code> <p>Key ID. Unique identifier for the key.</p> <code>kty</code> <code>str</code> <p>Key Type. Required. Identifies the cryptographic algorithm family.</p> <code>use</code> <code>Literal['sig', 'enc']</code> <p>Public Key Use. Identifies the intended use of the public key.</p> <code>x5c</code> <code>str</code> <p>X.509 Certificate Chain. Base64-encoded X.509 public key certificate chain.</p> <code>x5t</code> <code>str</code> <p>X.509 Certificate Thumbprint. Base64-encoded SHA-1 thumbprint of the certificate.</p> <code>x5t_S256</code> <code>str</code> <p>X.509 Certificate SHA-256 Thumbprint. Base64-encoded SHA-256 thumbprint.</p> <code>x5u</code> <code>str</code> <p>X.509 URL. URI pointing to an X.509 public key certificate or chain.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon.alg","title":"alg  <code>instance-attribute</code>","text":"<pre><code>alg: str\n</code></pre> <p>Algorithm. Identifies the algorithm intended for use with the key.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon.key_ops","title":"key_ops  <code>instance-attribute</code>","text":"<pre><code>key_ops: list[str]\n</code></pre> <p>Key Operations. Identifies the operations for which the key is intended.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon.kid","title":"kid  <code>instance-attribute</code>","text":"<pre><code>kid: str\n</code></pre> <p>Key ID. Unique identifier for the key.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon.kty","title":"kty  <code>instance-attribute</code>","text":"<pre><code>kty: str\n</code></pre> <p>Key Type. Required. Identifies the cryptographic algorithm family.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon.use","title":"use  <code>instance-attribute</code>","text":"<pre><code>use: Literal['sig', 'enc']\n</code></pre> <p>Public Key Use. Identifies the intended use of the public key.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon.x5c","title":"x5c  <code>instance-attribute</code>","text":"<pre><code>x5c: str\n</code></pre> <p>X.509 Certificate Chain. Base64-encoded X.509 public key certificate chain.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon.x5t","title":"x5t  <code>instance-attribute</code>","text":"<pre><code>x5t: str\n</code></pre> <p>X.509 Certificate Thumbprint. Base64-encoded SHA-1 thumbprint of the certificate.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon.x5t_S256","title":"x5t_S256  <code>instance-attribute</code>","text":"<pre><code>x5t_S256: str\n</code></pre> <p>X.509 Certificate SHA-256 Thumbprint. Base64-encoded SHA-256 thumbprint.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKCommon.x5u","title":"x5u  <code>instance-attribute</code>","text":"<pre><code>x5u: str\n</code></pre> <p>X.509 URL. URI pointing to an X.509 public key certificate or chain.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKEC","title":"JWKEC","text":"<p>               Bases: <code>JWKCommon</code></p> <p>Elliptic Curve Key - RFC 7517 Section 6.2.</p> <p>Represents an elliptic curve public or private key.</p> Example <p>ec_key: JWKEC = { ...     \"kty\": \"EC\", ...     \"crv\": \"P-256\", ...     \"x\": \"f83OJ3D2xF1Bg8v...\", ...     \"y\": \"x_FEzRu9m36HLN_t...\", ... }</p> <p>Attributes:</p> Name Type Description <code>crv</code> <code>Literal['P-256', 'P-384', 'P-521']</code> <p>Elliptic curve name. The curve on which the key is based.</p> <code>d</code> <code>str</code> <p>EC private key value. The private key value. Present only in private keys.</p> <code>x</code> <code>str</code> <p>EC x coordinate. The x coordinate of the elliptic curve point.</p> <code>y</code> <code>str</code> <p>EC y coordinate. The y coordinate of the elliptic curve point.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKEC.crv","title":"crv  <code>instance-attribute</code>","text":"<pre><code>crv: Literal['P-256', 'P-384', 'P-521']\n</code></pre> <p>Elliptic curve name. The curve on which the key is based.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKEC.d","title":"d  <code>instance-attribute</code>","text":"<pre><code>d: str\n</code></pre> <p>EC private key value. The private key value. Present only in private keys.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKEC.x","title":"x  <code>instance-attribute</code>","text":"<pre><code>x: str\n</code></pre> <p>EC x coordinate. The x coordinate of the elliptic curve point.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKEC.y","title":"y  <code>instance-attribute</code>","text":"<pre><code>y: str\n</code></pre> <p>EC y coordinate. The y coordinate of the elliptic curve point.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKOct","title":"JWKOct","text":"<p>               Bases: <code>JWKCommon</code></p> <p>Symmetric (Octet Sequence) Key - RFC 7517 Section 6.4.</p> <p>Represents a symmetric (secret) key.</p> Example <p>oct_key: JWKOct = { ...     \"kty\": \"oct\", ...     \"k\": \"AyM32w-xOvmxxkBq...\", ... }</p> <p>Attributes:</p> Name Type Description <code>k</code> <code>str</code> <p>Key value. The base64url-encoded symmetric key value.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKOct.k","title":"k  <code>instance-attribute</code>","text":"<pre><code>k: str\n</code></pre> <p>Key value. The base64url-encoded symmetric key value.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKRSA","title":"JWKRSA","text":"<p>               Bases: <code>JWKCommon</code></p> <p>RSA Key - RFC 7517 Section 6.3.</p> <p>Represents an RSA public or private key.</p> Example <p>rsa_key: JWKRSA = { ...     \"kty\": \"RSA\", ...     \"n\": \"0vx7agoebGcQSuu...\", ...     \"e\": \"AQAB\", ... }</p> <p>Attributes:</p> Name Type Description <code>d</code> <code>str</code> <p>RSA private exponent d. Present only in private keys.</p> <code>dp</code> <code>str</code> <p>First factor exponent. d mod (p-1). Present only in private keys.</p> <code>dq</code> <code>str</code> <p>Second factor exponent. d mod (q-1). Present only in private keys.</p> <code>e</code> <code>str</code> <p>RSA exponent e. The exponent value for the RSA public key.</p> <code>n</code> <code>str</code> <p>RSA modulus n. The modulus value for the RSA public key.</p> <code>p</code> <code>str</code> <p>First prime p. First prime factor of n. Present only in private keys.</p> <code>q</code> <code>str</code> <p>Second prime q. Second prime factor of n. Present only in private keys.</p> <code>qi</code> <code>str</code> <p>First CRT coefficient. q^(-1) mod p. Present only in private keys.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKRSA.d","title":"d  <code>instance-attribute</code>","text":"<pre><code>d: str\n</code></pre> <p>RSA private exponent d. Present only in private keys.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKRSA.dp","title":"dp  <code>instance-attribute</code>","text":"<pre><code>dp: str\n</code></pre> <p>First factor exponent. d mod (p-1). Present only in private keys.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKRSA.dq","title":"dq  <code>instance-attribute</code>","text":"<pre><code>dq: str\n</code></pre> <p>Second factor exponent. d mod (q-1). Present only in private keys.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKRSA.e","title":"e  <code>instance-attribute</code>","text":"<pre><code>e: str\n</code></pre> <p>RSA exponent e. The exponent value for the RSA public key.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKRSA.n","title":"n  <code>instance-attribute</code>","text":"<pre><code>n: str\n</code></pre> <p>RSA modulus n. The modulus value for the RSA public key.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKRSA.p","title":"p  <code>instance-attribute</code>","text":"<pre><code>p: str\n</code></pre> <p>First prime p. First prime factor of n. Present only in private keys.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKRSA.q","title":"q  <code>instance-attribute</code>","text":"<pre><code>q: str\n</code></pre> <p>Second prime q. Second prime factor of n. Present only in private keys.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKRSA.qi","title":"qi  <code>instance-attribute</code>","text":"<pre><code>qi: str\n</code></pre> <p>First CRT coefficient. q^(-1) mod p. Present only in private keys.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKSet","title":"JWKSet","text":"<pre><code>JWKSet(keys: list[dict[str, Any]] | None = None)\n</code></pre> <p>               Bases: <code>BaseJWKSet</code></p> <p>JWK Set - RFC 7517 Section 5.</p> <p>Represents a set of JWKs. Used to organize and filter collections of keys.</p> Example <pre><code>&gt;&gt;&gt; jwkset = JWKSet(keys=[rsa_key, ec_key])\n&gt;&gt;&gt; jwkset.get_by_kid(\"my-key-id\")\n&gt;&gt;&gt; jwkset.filter(kty=\"RSA\")\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>keys</code> <code>list[dict[str, Any]] | None</code> <p>List of JWK dicts.</p> <code>None</code> <p>Methods:</p> Name Description <code>filter</code> <p>Filter JWKs by criteria.</p> <code>from_dict</code> <p>Create JWKSet from dict.</p> <code>get_by_kid</code> <p>Get JWK by key ID (kid).</p> <code>get_by_kty</code> <p>Get all JWKs by key type.</p> <code>to_dict</code> <p>Convert JWKSet to dict.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKSet.filter","title":"filter","text":"<pre><code>filter(**criteria: Any) -&gt; list[dict[str, Any]]\n</code></pre> <p>Filter JWKs by criteria.</p> <p>Parameters:</p> Name Type Description Default <code>**criteria</code> <code>Any</code> <p>Filter criteria (kty, use, alg, key_ops, kid).</p> <code>{}</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>List of matching JWK dicts.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKSet.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; JWKSet\n</code></pre> <p>Create JWKSet from dict.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Dict with \"keys\" key.</p> required <p>Returns:</p> Type Description <code>JWKSet</code> <p>JWKSet instance.</p> <p>Raises:</p> Type Description <code>JamJWKValidationError</code> <p>If data is invalid.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKSet.get_by_kid","title":"get_by_kid","text":"<pre><code>get_by_kid(kid: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get JWK by key ID (kid).</p> <p>Parameters:</p> Name Type Description Default <code>kid</code> <code>str</code> <p>Key ID to search for.</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>JWK dict if found, None otherwise.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKSet.get_by_kty","title":"get_by_kty","text":"<pre><code>get_by_kty(kty: str) -&gt; list[dict[str, Any]]\n</code></pre> <p>Get all JWKs by key type.</p> <p>Parameters:</p> Name Type Description Default <code>kty</code> <code>str</code> <p>Key type (RSA, EC, oct).</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>List of matching JWK dicts.</p>"},{"location":"api/jam.jose.jwk/#jam.jose.jwk.JWKSet.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert JWKSet to dict.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with \"keys\" key containing list of JWKs.</p>"},{"location":"api/jam.jose.jws/","title":"jam.jose.jws","text":""},{"location":"api/jam.jose.jws/#jam.jose.jws","title":"jws","text":"<p>Classes:</p> Name Description <code>JWS</code> <p>JWS (JSON Web Signature) implementation - RFC 7515.</p>"},{"location":"api/jam.jose.jws/#jam.jose.jws.JWS","title":"JWS","text":"<pre><code>JWS(\n    alg: str,\n    key: KeyLike | JWK,\n    password: bytes | None = None,\n    logger: BaseLogger = logger,\n)\n</code></pre> <p>               Bases: <code>BaseJWS</code></p> <p>JWS (JSON Web Signature) implementation - RFC 7515.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm name</p> required <code>key</code> <code>KeyLike | JWK</code> <p>Key to use for signing/verifying</p> required <code>password</code> <code>bytes | None</code> <p>Password for encrypted private keys</p> <code>None</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <p>Methods:</p> Name Description <code>deserialize_compact</code> <p>Parse JWS Compact Serialization.</p> <code>serialize_compact</code> <p>Create JWS Compact Serialization.</p> <code>sign</code> <p>Sign data and return JWS compact serialization.</p> <code>verify</code> <p>Verify and parse JWS token.</p>"},{"location":"api/jam.jose.jws/#jam.jose.jws.JWS.deserialize_compact","title":"deserialize_compact","text":"<pre><code>deserialize_compact(\n    s: str, validate: bool = True\n) -&gt; dict[str, Any]\n</code></pre> <p>Parse JWS Compact Serialization.</p> <p>Parameters:</p> Name Type Description Default <code>s</code> <code>str</code> <p>JWS in compact serialization format.</p> required <code>validate</code> <code>bool</code> <p>Whether to validate signature. Defaults to True.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Parsed JWS with keys: - header: Protected header dict - payload: Decoded payload bytes - signature: Raw signature bytes</p> <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If validation fails or format is invalid.</p>"},{"location":"api/jam.jose.jws/#jam.jose.jws.JWS.serialize_compact","title":"serialize_compact","text":"<pre><code>serialize_compact(\n    protected: dict[str, Any],\n    payload: bytes | str | dict[str, Any],\n) -&gt; str\n</code></pre> <p>Create JWS Compact Serialization.</p> <p>Parameters:</p> Name Type Description Default <code>protected</code> <code>dict[str, Any]</code> <p>Protected header (must include 'alg').</p> required <code>payload</code> <code>bytes | str</code> <p>Payload to sign. If dict, will be JSON encoded.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS in compact serialization format:  BASE64URL(protected).BASE64URL(payload).BASE64URL(signature)</p>"},{"location":"api/jam.jose.jws/#jam.jose.jws.JWS.sign","title":"sign","text":"<pre><code>sign(\n    header: dict[str, Any],\n    data: bytes | str | dict[str, Any],\n) -&gt; str\n</code></pre> <p>Sign data and return JWS compact serialization.</p> <p>Parameters:</p> Name Type Description Default <code>header</code> <code>dict[str, Any]</code> <p>Protected header (alg will be added automatically).</p> required <code>data</code> <code>bytes | str | dict[str, Any]</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <p>Returns:</p> Type Description <code>str</code> <p>JWS compact serialization string.</p>"},{"location":"api/jam.jose.jws/#jam.jose.jws.JWS.verify","title":"verify","text":"<pre><code>verify(token: str, validate: bool = True) -&gt; dict[str, Any]\n</code></pre> <p>Verify and parse JWS token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS compact serialization string.</p> required <code>validate</code> <code>bool</code> <p>Whether to validate signature.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with 'header', 'payload', 'signature' keys.</p>"},{"location":"api/jam.jose.jwt/","title":"jam.jose.jwt","text":""},{"location":"api/jam.jose.jwt/#jam.jose.jwt","title":"jwt","text":"<p>Classes:</p> Name Description <code>JWT</code> <p>JWT (JSON Web Token) implementation - RFC 7519.</p>"},{"location":"api/jam.jose.jwt/#jam.jose.jwt.JWT","title":"JWT","text":"<pre><code>JWT(\n    alg: str | None = None,\n    enc: str | None = None,\n    secret_key: str | bytes | KeyLike | JWK | None = None,\n    password: str | bytes | None = None,\n    list: dict[str, Any] | None = None,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger = logger,\n    jws: JWS | None = None,\n    jwe: JWE | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWT</code></p> <p>JWT (JSON Web Token) implementation - RFC 7519.</p> <p>Supports JWS (signed), JWE (encrypted), and JWS+JWE (sign then encrypt) tokens.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str | None</code> <p>JWT algorithm name for signing (JWS). Used if jws is not provided.</p> <code>None</code> <code>enc</code> <code>str | None</code> <p>JWE content encryption algorithm. If provided, creates encrypted JWT.</p> <code>None</code> <code>secret_key</code> <code>str | bytes | KeyLike | JWK | None</code> <p>Key for signing/encryption.</p> <code>None</code> <code>password</code> <code>str | bytes | None</code> <p>Password for encrypted private keys.</p> <code>None</code> <code>list</code> <code>dict[str, Any] | None</code> <p>List config for token storage.</p> <code>None</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>JSON encoder/decoder.</p> <code>JsonEncoder</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance.</p> <code>logger</code> <code>jws</code> <code>JWS | None</code> <p>Pre-built JWS instance. If provided, alg is ignored.</p> <code>None</code> <code>jwe</code> <code>JWE | None</code> <p>Pre-built JWE instance. If provided, enc and secret_key are ignored.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If neither alg/enc provided and no jws/jwe provided.</p> <code>ValueError</code> <p>If both alg and jws are provided.</p> <code>ValueError</code> <p>If both enc and jwe are provided.</p> <code>JamJWTUnsupportedAlgorithm</code> <p>If algorithm is not supported.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode the JWT and return the header and payload.</p> <code>decrypt</code> <p>Decrypt JWE or JWS+JWE token.</p> <code>encode</code> <p>Encode the JWT with the given expire, header, and payload (JWS).</p> <code>encrypt</code> <p>Encrypt payload using JWE.</p> <p>Attributes:</p> Name Type Description <code>jti</code> <code>str</code> <p>The JWT ID.</p>"},{"location":"api/jam.jose.jwt/#jam.jose.jwt.JWT.jti","title":"jti  <code>property</code>","text":"<pre><code>jti: str\n</code></pre> <p>The JWT ID.</p>"},{"location":"api/jam.jose.jwt/#jam.jose.jwt.JWT.decode","title":"decode","text":"<pre><code>decode(\n    token: str, validate_claims: bool = True\n) -&gt; dict[str, Any]\n</code></pre> <p>Decode the JWT and return the header and payload.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required <code>validate_claims</code> <code>bool</code> <p>Whether to validate exp/nbf claims. Defaults to True.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with 'header' and 'payload' keys (both dicts).</p> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If alg is not provided.</p> <code>JamJWSVerificationError</code> <p>If token has invalid type.</p> <code>JamJWTExpired</code> <p>If token is expired.</p> <code>JamJWTNotYetValid</code> <p>If token is not yet valid.</p>"},{"location":"api/jam.jose.jwt/#jam.jose.jwt.JWT.decrypt","title":"decrypt","text":"<pre><code>decrypt(token: str) -&gt; dict[str, Any] | bytes\n</code></pre> <p>Decrypt JWE or JWS+JWE token.</p> <p>If token is JWS+JWE (sign then encrypt): 1. Decrypt JWE to get JWS compact serialization 2. Verify JWS to get original payload</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Encrypted JWT token.</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | bytes</code> <p>dict[str, Any]: Decrypted payload.</p> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If enc is not provided.</p>"},{"location":"api/jam.jose.jwt/#jam.jose.jwt.JWT.encode","title":"encode","text":"<pre><code>encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    header: dict[str, Any] | None = None,\n    payload: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload (JWS).</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID. If None, auto-generated.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT (JWS compact serialization).</p> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If alg is not provided.</p>"},{"location":"api/jam.jose.jwt/#jam.jose.jwt.JWT.encrypt","title":"encrypt","text":"<pre><code>encrypt(\n    plaintext: bytes | str | dict[str, Any],\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt payload using JWE.</p> <p>If both jws and jwe are configured, creates JWS+JWE (sign then encrypt): 1. Create JWS compact serialization (sign) 2. Use JWS result as plaintext for JWE (encrypt)</p> <p>Parameters:</p> Name Type Description Default <code>plaintext</code> <code>bytes | str | dict[str, Any]</code> <p>Data to encrypt. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>Additional JWE header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Encrypted JWT (JWE or JWS+JWE).</p> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If jwe is not configured.</p>"},{"location":"api/jam.jose.lists.__base__/","title":"jam.jose.lists.base","text":""},{"location":"api/jam.jose.lists.__base__/#jam.jose.lists.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseJWTList</code> <p>Abstract class for lists manipulation.</p>"},{"location":"api/jam.jose.lists.__base__/#jam.jose.lists.__base__.BaseJWTList","title":"BaseJWTList","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract class for lists manipulation.</p> <p>Methods:</p> Name Description <code>add</code> <p>Add a single token to the list.</p> <code>add_many</code> <p>Add multiple tokens to the list.</p> <code>check</code> <p>Check if a token is present in the list.</p> <code>check_many</code> <p>Check multiple tokens in the list.</p> <code>delete</code> <p>Remove a single token from the list.</p> <code>delete_many</code> <p>Remove multiple tokens from the list.</p>"},{"location":"api/jam.jose.lists.__base__/#jam.jose.lists.__base__.BaseJWTList.add","title":"add  <code>abstractmethod</code>","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Add a single token to the list.</p>"},{"location":"api/jam.jose.lists.__base__/#jam.jose.lists.__base__.BaseJWTList.add_many","title":"add_many  <code>abstractmethod</code>","text":"<pre><code>add_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Add multiple tokens to the list.</p>"},{"location":"api/jam.jose.lists.__base__/#jam.jose.lists.__base__.BaseJWTList.check","title":"check  <code>abstractmethod</code>","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Check if a token is present in the list.</p>"},{"location":"api/jam.jose.lists.__base__/#jam.jose.lists.__base__.BaseJWTList.check_many","title":"check_many  <code>abstractmethod</code>","text":"<pre><code>check_many(tokens: list[str]) -&gt; dict[str, bool]\n</code></pre> <p>Check multiple tokens in the list.</p>"},{"location":"api/jam.jose.lists.__base__/#jam.jose.lists.__base__.BaseJWTList.delete","title":"delete  <code>abstractmethod</code>","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Remove a single token from the list.</p>"},{"location":"api/jam.jose.lists.__base__/#jam.jose.lists.__base__.BaseJWTList.delete_many","title":"delete_many  <code>abstractmethod</code>","text":"<pre><code>delete_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Remove multiple tokens from the list.</p>"},{"location":"api/jam.jose.lists.json/","title":"jam.jose.lists.json","text":""},{"location":"api/jam.jose.lists.json/#jam.jose.lists.json","title":"json","text":"<p>Classes:</p> Name Description <code>JSONList</code> <p>JSON file-based JWT black/white list.</p>"},{"location":"api/jam.jose.lists.json/#jam.jose.lists.json.JSONList","title":"JSONList","text":"<pre><code>JSONList(\n    type: Literal[\"white\", \"black\"],\n    prefix: str = \"jwt_list\",\n    json_path: str = \"whitelist.json\",\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWTList</code></p> <p>JSON file-based JWT black/white list.</p> <p>Not recommended for blacklists - no TTL support, user must manage token lifetime.</p> <p>Dependency required: <code>pip install jamlib[json]</code></p> <p>Attributes:</p> Name Type Description <code>_db</code> <code>TinyDB</code> <p>TinyDB instance.</p> <code>_prefix</code> <code>str</code> <p>Key prefix.</p> <p>Methods:</p> Name Description <code>add</code> <p>add single token to list</p> <code>add_many</code> <p>add multiple tokens to list</p> <code>check</code> <p>check if token exists in list</p> <code>check_many</code> <p>check multiple tokens in list</p> <code>delete</code> <p>remove token from list</p> <code>delete_many</code> <p>remove multiple tokens from list</p> <p>Parameters:</p> Name Type Description Default <code>type</code> <code>Literal['white', 'black']</code> <p>Type of list.</p> required <code>prefix</code> <code>str</code> <p>Key prefix (used for logging).</p> <code>'jwt_list'</code> <code>json_path</code> <code>str</code> <p>Path to JSON file.</p> <code>'whitelist.json'</code> <code>logger</code> <code>BaseLogger | None</code> <p>Logger instance.</p> <code>None</code>"},{"location":"api/jam.jose.lists.json/#jam.jose.lists.json.JSONList.add","title":"add","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Add a single token to the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists.json/#jam.jose.lists.json.JSONList.add_many","title":"add_many","text":"<pre><code>add_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Add multiple tokens to the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists.json/#jam.jose.lists.json.JSONList.check","title":"check","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Check if a token is present in the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if token exists in list.</p>"},{"location":"api/jam.jose.lists.json/#jam.jose.lists.json.JSONList.check_many","title":"check_many","text":"<pre><code>check_many(tokens: list[str]) -&gt; dict[str, bool]\n</code></pre> <p>Check multiple tokens in the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>dict[str, bool]: Mapping of token to presence.</p>"},{"location":"api/jam.jose.lists.json/#jam.jose.lists.json.JSONList.delete","title":"delete","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Remove a token from the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists.json/#jam.jose.lists.json.JSONList.delete_many","title":"delete_many","text":"<pre><code>delete_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Remove multiple tokens from the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists/","title":"jam.jose.lists","text":""},{"location":"api/jam.jose.lists/#jam.jose.lists","title":"lists","text":"<p>Module for managing JWT black and white lists.</p> <p>Modules:</p> Name Description <code>json</code> <code>memory</code> <code>redis</code> <p>Classes:</p> Name Description <code>BaseJWTList</code> <p>Abstract class for lists manipulation.</p> <code>JSONList</code> <p>JSON file-based JWT black/white list.</p> <code>MemoryList</code> <p>In-memory JWT black/white list.</p> <code>RedisList</code> <p>Redis-based JWT black/white list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.BaseJWTList","title":"BaseJWTList","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract class for lists manipulation.</p> <p>Methods:</p> Name Description <code>add</code> <p>Add a single token to the list.</p> <code>add_many</code> <p>Add multiple tokens to the list.</p> <code>check</code> <p>Check if a token is present in the list.</p> <code>check_many</code> <p>Check multiple tokens in the list.</p> <code>delete</code> <p>Remove a single token from the list.</p> <code>delete_many</code> <p>Remove multiple tokens from the list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.BaseJWTList.add","title":"add  <code>abstractmethod</code>","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Add a single token to the list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.BaseJWTList.add_many","title":"add_many  <code>abstractmethod</code>","text":"<pre><code>add_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Add multiple tokens to the list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.BaseJWTList.check","title":"check  <code>abstractmethod</code>","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Check if a token is present in the list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.BaseJWTList.check_many","title":"check_many  <code>abstractmethod</code>","text":"<pre><code>check_many(tokens: list[str]) -&gt; dict[str, bool]\n</code></pre> <p>Check multiple tokens in the list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.BaseJWTList.delete","title":"delete  <code>abstractmethod</code>","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Remove a single token from the list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.BaseJWTList.delete_many","title":"delete_many  <code>abstractmethod</code>","text":"<pre><code>delete_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Remove multiple tokens from the list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.JSONList","title":"JSONList","text":"<pre><code>JSONList(\n    type: Literal[\"white\", \"black\"],\n    prefix: str = \"jwt_list\",\n    json_path: str = \"whitelist.json\",\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWTList</code></p> <p>JSON file-based JWT black/white list.</p> <p>Not recommended for blacklists - no TTL support, user must manage token lifetime.</p> <p>Dependency required: <code>pip install jamlib[json]</code></p> <p>Attributes:</p> Name Type Description <code>_db</code> <code>TinyDB</code> <p>TinyDB instance.</p> <code>_prefix</code> <code>str</code> <p>Key prefix.</p> <p>Methods:</p> Name Description <code>add</code> <p>add single token to list</p> <code>add_many</code> <p>add multiple tokens to list</p> <code>check</code> <p>check if token exists in list</p> <code>check_many</code> <p>check multiple tokens in list</p> <code>delete</code> <p>remove token from list</p> <code>delete_many</code> <p>remove multiple tokens from list</p> <p>Parameters:</p> Name Type Description Default <code>type</code> <code>Literal['white', 'black']</code> <p>Type of list.</p> required <code>prefix</code> <code>str</code> <p>Key prefix (used for logging).</p> <code>'jwt_list'</code> <code>json_path</code> <code>str</code> <p>Path to JSON file.</p> <code>'whitelist.json'</code> <code>logger</code> <code>BaseLogger | None</code> <p>Logger instance.</p> <code>None</code>"},{"location":"api/jam.jose.lists/#jam.jose.lists.JSONList.add","title":"add","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Add a single token to the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.JSONList.add_many","title":"add_many","text":"<pre><code>add_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Add multiple tokens to the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.JSONList.check","title":"check","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Check if a token is present in the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if token exists in list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.JSONList.check_many","title":"check_many","text":"<pre><code>check_many(tokens: list[str]) -&gt; dict[str, bool]\n</code></pre> <p>Check multiple tokens in the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>dict[str, bool]: Mapping of token to presence.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.JSONList.delete","title":"delete","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Remove a token from the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.JSONList.delete_many","title":"delete_many","text":"<pre><code>delete_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Remove multiple tokens from the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.MemoryList","title":"MemoryList","text":"<pre><code>MemoryList(\n    type: Literal[\"white\", \"black\"],\n    prefix: str = \"jwt_list\",\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWTList</code></p> <p>In-memory JWT black/white list.</p> <p>Suitable for development, testing, or when external storage is not needed. No TTL support - tokens persist until explicitly removed.</p> <p>Attributes:</p> Name Type Description <code>_storage</code> <code>dict</code> <p>Internal storage for tokens.</p> <p>Methods:</p> Name Description <code>add</code> <p>add single token to list</p> <code>add_many</code> <p>add multiple tokens to list</p> <code>check</code> <p>check if token exists in list</p> <code>check_many</code> <p>check multiple tokens in list</p> <code>delete</code> <p>remove token from list</p> <code>delete_many</code> <p>remove multiple tokens from list</p> <p>Parameters:</p> Name Type Description Default <code>type</code> <code>Literal['white', 'black']</code> <p>Type of list.</p> required <code>prefix</code> <code>str</code> <p>Key prefix for storage.</p> <code>'jwt_list'</code> <code>logger</code> <code>BaseLogger | None</code> <p>Logger instance.</p> <code>None</code>"},{"location":"api/jam.jose.lists/#jam.jose.lists.MemoryList.add","title":"add","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Add a single token to the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.MemoryList.add_many","title":"add_many","text":"<pre><code>add_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Add multiple tokens to the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.MemoryList.check","title":"check","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Check if a token is present in the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if token exists in list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.MemoryList.check_many","title":"check_many","text":"<pre><code>check_many(tokens: list[str]) -&gt; dict[str, bool]\n</code></pre> <p>Check multiple tokens in the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>dict[str, bool]: Mapping of token to presence.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.MemoryList.delete","title":"delete","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Remove a token from the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.MemoryList.delete_many","title":"delete_many","text":"<pre><code>delete_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Remove multiple tokens from the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.RedisList","title":"RedisList","text":"<pre><code>RedisList(\n    type: Literal[\"white\", \"black\"],\n    prefix: str = \"jwt_list\",\n    redis_uri: str | Redis | None = None,\n    redis: Redis | None = None,\n    ttl: int | None = None,\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWTList</code></p> <p>Redis-based JWT black/white list.</p> <p>Most optimal for production use with TTL support.</p> <p>Dependency required: <code>pip install jamlib[redis]</code></p> <p>Attributes:</p> Name Type Description <code>_redis</code> <code>Redis</code> <p>Redis instance.</p> <code>_prefix</code> <code>str</code> <p>Key prefix.</p> <p>Methods:</p> Name Description <code>add</code> <p>add single token to list</p> <code>add_many</code> <p>add multiple tokens to list</p> <code>check</code> <p>check if token exists in list</p> <code>check_many</code> <p>check multiple tokens in list</p> <code>delete</code> <p>remove token from list</p> <code>delete_many</code> <p>remove multiple tokens from list</p> <p>Parameters:</p> Name Type Description Default <code>type</code> <code>Literal['white', 'black']</code> <p>Type of list.</p> required <code>prefix</code> <code>str</code> <p>Key prefix for Redis keys.</p> <code>'jwt_list'</code> <code>redis_uri</code> <code>str | Redis</code> <p>Redis connection URI or Redis instance.</p> <code>None</code> <code>redis</code> <code>Redis | None</code> <p>Redis instance (alias for redis_uri).</p> <code>None</code> <code>ttl</code> <code>int | None</code> <p>Token TTL in seconds.</p> <code>None</code> <code>logger</code> <code>BaseLogger | None</code> <p>Logger instance.</p> <code>None</code>"},{"location":"api/jam.jose.lists/#jam.jose.lists.RedisList.add","title":"add","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Add a single token to the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.RedisList.add_many","title":"add_many","text":"<pre><code>add_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Add multiple tokens to the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.RedisList.check","title":"check","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Check if a token is present in the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if token exists in list.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.RedisList.check_many","title":"check_many","text":"<pre><code>check_many(tokens: list[str]) -&gt; dict[str, bool]\n</code></pre> <p>Check multiple tokens in the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>dict[str, bool]: Mapping of token to presence.</p>"},{"location":"api/jam.jose.lists/#jam.jose.lists.RedisList.delete","title":"delete","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Remove a token from the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists/#jam.jose.lists.RedisList.delete_many","title":"delete_many","text":"<pre><code>delete_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Remove multiple tokens from the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists.memory/","title":"jam.jose.lists.memory","text":""},{"location":"api/jam.jose.lists.memory/#jam.jose.lists.memory","title":"memory","text":"<p>Classes:</p> Name Description <code>MemoryList</code> <p>In-memory JWT black/white list.</p>"},{"location":"api/jam.jose.lists.memory/#jam.jose.lists.memory.MemoryList","title":"MemoryList","text":"<pre><code>MemoryList(\n    type: Literal[\"white\", \"black\"],\n    prefix: str = \"jwt_list\",\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWTList</code></p> <p>In-memory JWT black/white list.</p> <p>Suitable for development, testing, or when external storage is not needed. No TTL support - tokens persist until explicitly removed.</p> <p>Attributes:</p> Name Type Description <code>_storage</code> <code>dict</code> <p>Internal storage for tokens.</p> <p>Methods:</p> Name Description <code>add</code> <p>add single token to list</p> <code>add_many</code> <p>add multiple tokens to list</p> <code>check</code> <p>check if token exists in list</p> <code>check_many</code> <p>check multiple tokens in list</p> <code>delete</code> <p>remove token from list</p> <code>delete_many</code> <p>remove multiple tokens from list</p> <p>Parameters:</p> Name Type Description Default <code>type</code> <code>Literal['white', 'black']</code> <p>Type of list.</p> required <code>prefix</code> <code>str</code> <p>Key prefix for storage.</p> <code>'jwt_list'</code> <code>logger</code> <code>BaseLogger | None</code> <p>Logger instance.</p> <code>None</code>"},{"location":"api/jam.jose.lists.memory/#jam.jose.lists.memory.MemoryList.add","title":"add","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Add a single token to the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists.memory/#jam.jose.lists.memory.MemoryList.add_many","title":"add_many","text":"<pre><code>add_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Add multiple tokens to the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists.memory/#jam.jose.lists.memory.MemoryList.check","title":"check","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Check if a token is present in the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if token exists in list.</p>"},{"location":"api/jam.jose.lists.memory/#jam.jose.lists.memory.MemoryList.check_many","title":"check_many","text":"<pre><code>check_many(tokens: list[str]) -&gt; dict[str, bool]\n</code></pre> <p>Check multiple tokens in the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>dict[str, bool]: Mapping of token to presence.</p>"},{"location":"api/jam.jose.lists.memory/#jam.jose.lists.memory.MemoryList.delete","title":"delete","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Remove a token from the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists.memory/#jam.jose.lists.memory.MemoryList.delete_many","title":"delete_many","text":"<pre><code>delete_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Remove multiple tokens from the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists.redis/","title":"jam.jose.lists.redis","text":""},{"location":"api/jam.jose.lists.redis/#jam.jose.lists.redis","title":"redis","text":"<p>Classes:</p> Name Description <code>RedisList</code> <p>Redis-based JWT black/white list.</p>"},{"location":"api/jam.jose.lists.redis/#jam.jose.lists.redis.RedisList","title":"RedisList","text":"<pre><code>RedisList(\n    type: Literal[\"white\", \"black\"],\n    prefix: str = \"jwt_list\",\n    redis_uri: str | Redis | None = None,\n    redis: Redis | None = None,\n    ttl: int | None = None,\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWTList</code></p> <p>Redis-based JWT black/white list.</p> <p>Most optimal for production use with TTL support.</p> <p>Dependency required: <code>pip install jamlib[redis]</code></p> <p>Attributes:</p> Name Type Description <code>_redis</code> <code>Redis</code> <p>Redis instance.</p> <code>_prefix</code> <code>str</code> <p>Key prefix.</p> <p>Methods:</p> Name Description <code>add</code> <p>add single token to list</p> <code>add_many</code> <p>add multiple tokens to list</p> <code>check</code> <p>check if token exists in list</p> <code>check_many</code> <p>check multiple tokens in list</p> <code>delete</code> <p>remove token from list</p> <code>delete_many</code> <p>remove multiple tokens from list</p> <p>Parameters:</p> Name Type Description Default <code>type</code> <code>Literal['white', 'black']</code> <p>Type of list.</p> required <code>prefix</code> <code>str</code> <p>Key prefix for Redis keys.</p> <code>'jwt_list'</code> <code>redis_uri</code> <code>str | Redis</code> <p>Redis connection URI or Redis instance.</p> <code>None</code> <code>redis</code> <code>Redis | None</code> <p>Redis instance (alias for redis_uri).</p> <code>None</code> <code>ttl</code> <code>int | None</code> <p>Token TTL in seconds.</p> <code>None</code> <code>logger</code> <code>BaseLogger | None</code> <p>Logger instance.</p> <code>None</code>"},{"location":"api/jam.jose.lists.redis/#jam.jose.lists.redis.RedisList.add","title":"add","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Add a single token to the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists.redis/#jam.jose.lists.redis.RedisList.add_many","title":"add_many","text":"<pre><code>add_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Add multiple tokens to the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose.lists.redis/#jam.jose.lists.redis.RedisList.check","title":"check","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Check if a token is present in the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if token exists in list.</p>"},{"location":"api/jam.jose.lists.redis/#jam.jose.lists.redis.RedisList.check_many","title":"check_many","text":"<pre><code>check_many(tokens: list[str]) -&gt; dict[str, bool]\n</code></pre> <p>Check multiple tokens in the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>dict[str, bool]: Mapping of token to presence.</p>"},{"location":"api/jam.jose.lists.redis/#jam.jose.lists.redis.RedisList.delete","title":"delete","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Remove a token from the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required"},{"location":"api/jam.jose.lists.redis/#jam.jose.lists.redis.RedisList.delete_many","title":"delete_many","text":"<pre><code>delete_many(tokens: list[str]) -&gt; None\n</code></pre> <p>Remove multiple tokens from the list.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>list[str]</code> <p>List of JWT tokens.</p> required"},{"location":"api/jam.jose/","title":"jam.jose","text":""},{"location":"api/jam.jose/#jam.jose","title":"jose","text":"<p>JOSE tools.</p> <p>Modules:</p> Name Description <code>jwe</code> <code>jwk</code> <code>jws</code> <code>jwt</code> <code>lists</code> <p>Module for managing JWT black and white lists.</p> <code>utils</code> <p>Classes:</p> Name Description <code>JWE</code> <p>JWE (JSON Web Encryption) implementation - RFC 7516.</p> <code>JWK</code> <p>JSON Web Key - RFC 7517.</p> <code>JWKEC</code> <p>Elliptic Curve Key - RFC 7517 Section 6.2.</p> <code>JWKOct</code> <p>Symmetric (Octet Sequence) Key - RFC 7517 Section 6.4.</p> <code>JWKRSA</code> <p>RSA Key - RFC 7517 Section 6.3.</p> <code>JWKSet</code> <p>JWK Set - RFC 7517 Section 5.</p> <code>JWS</code> <p>JWS (JSON Web Signature) implementation - RFC 7515.</p> <code>JWT</code> <p>JWT (JSON Web Token) implementation - RFC 7519.</p> <p>Functions:</p> Name Description <code>create_jwe_instance</code> <p>Create JWE instance.</p> <code>create_jws_instance</code> <p>Create JWS instance.</p> <code>create_jwt_instance</code> <p>Create JWT instance.</p>"},{"location":"api/jam.jose/#jam.jose.JWE","title":"JWE","text":"<pre><code>JWE(\n    alg: str,\n    enc: str,\n    key: KeyLike | JWK,\n    password: bytes | None = None,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger = logger,\n)\n</code></pre> <p>               Bases: <code>BaseJWE</code></p> <p>JWE (JSON Web Encryption) implementation - RFC 7516.</p> <p>Provides encryption and decryption of data using JWK keys.</p> Example <pre><code>&gt;&gt;&gt; jwk = JWK.from_dict({\"kty\": \"oct\", \"k\": \"your-secret-key\"})\n&gt;&gt;&gt; jwe = JWE(alg=\"A128KW\", enc=\"A128CBC-HS256\", key=jwk)\n&gt;&gt;&gt; token = jwe.encrypt(\"secret data\")\n&gt;&gt;&gt; plaintext = jwe.decrypt(token)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Key management algorithm (e.g., \"A128KW\", \"RSA-OAEP\").</p> required <code>enc</code> <code>str</code> <p>Content encryption algorithm (e.g., \"A128CBC-HS256\", \"A256GCM\").</p> required <code>key</code> <code>KeyLike | JWK</code> <p>Key for encryption/decryption. Can be KeyLike or JWK.</p> required <code>password</code> <code>bytes | None</code> <p>Password for encrypted private keys.</p> <code>None</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>Encoder for serializing/deserializing JSON data.</p> <code>JsonEncoder</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance.</p> <code>logger</code> <p>Methods:</p> Name Description <code>decrypt</code> <p>Decrypt JWE token.</p> <code>encrypt</code> <p>Encrypt plaintext.</p>"},{"location":"api/jam.jose/#jam.jose.JWE.decrypt","title":"decrypt","text":"<pre><code>decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE compact serialization string.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Decrypted plaintext bytes.</p> <p>Raises:</p> Type Description <code>JamJWEDecryptionError</code> <p>If decryption fails.</p>"},{"location":"api/jam.jose/#jam.jose.JWE.encrypt","title":"encrypt","text":"<pre><code>encrypt(\n    plaintext: bytes | str | dict[str, Any],\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt plaintext.</p> <p>Parameters:</p> Name Type Description Default <code>plaintext</code> <code>bytes | str | dict[str, Any]</code> <p>Data to encrypt. If str, will be encoded to UTF-8. If dict, will be serialized using self._serializer.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>Additional JWE header.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>JWE compact serialization string.</p> <p>Raises:</p> Type Description <code>JamJWEEncryptionError</code> <p>If encryption fails.</p>"},{"location":"api/jam.jose/#jam.jose.JWK","title":"JWK","text":"<pre><code>JWK(data: dict[str, Any])\n</code></pre> <p>               Bases: <code>BaseJWK</code></p> <p>JSON Web Key - RFC 7517.</p> <p>Provides JWK validation and signing capabilities.</p> Example <pre><code>&gt;&gt;&gt; jwk = JWK.from_dict({\"kty\": \"oct\", \"k\": \"your-secret-key\"})\n&gt;&gt;&gt; jwk.sign(b\"data\", \"HS256\")\n&gt;&gt;&gt; jwk.verify(token)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Validated JWK dict.</p> required <p>Methods:</p> Name Description <code>from_dict</code> <p>Create JWK from dict.</p> <code>sign</code> <p>Sign data using JWK.</p> <code>to_dict</code> <p>Convert JWK to dict.</p> <code>validate</code> <p>Validate and normalize JWK dict.</p> <code>verify</code> <p>Verify JWS token and return payload.</p> <p>Attributes:</p> Name Type Description <code>alg</code> <code>str | None</code> <p>Get algorithm from JWK if set.</p> <code>kid</code> <code>str | None</code> <p>Get key ID if set.</p> <code>kty</code> <code>str</code> <p>Get key type.</p>"},{"location":"api/jam.jose/#jam.jose.JWK.alg","title":"alg  <code>property</code>","text":"<pre><code>alg: str | None\n</code></pre> <p>Get algorithm from JWK if set.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>Algorithm or None.</p>"},{"location":"api/jam.jose/#jam.jose.JWK.kid","title":"kid  <code>property</code>","text":"<pre><code>kid: str | None\n</code></pre> <p>Get key ID if set.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>Key ID or None.</p>"},{"location":"api/jam.jose/#jam.jose.JWK.kty","title":"kty  <code>property</code>","text":"<pre><code>kty: str\n</code></pre> <p>Get key type.</p> <p>Returns:</p> Type Description <code>str</code> <p>Key type (RSA, EC, oct).</p>"},{"location":"api/jam.jose/#jam.jose.JWK.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; JWK\n</code></pre> <p>Create JWK from dict.</p> <p>Alias for JWK.validate().</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>JWK dict.</p> required <p>Returns:</p> Type Description <code>JWK</code> <p>JWK instance.</p>"},{"location":"api/jam.jose/#jam.jose.JWK.sign","title":"sign","text":"<pre><code>sign(data: bytes, alg: str | None = None) -&gt; str\n</code></pre> <p>Sign data using JWK.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign.</p> required <code>alg</code> <code>str | None</code> <p>Algorithm to use. If None, uses default for kty.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>JWS compact serialization string.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If algorithm is not supported or key is invalid.</p>"},{"location":"api/jam.jose/#jam.jose.JWK.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert JWK to dict.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>JWK dict.</p>"},{"location":"api/jam.jose/#jam.jose.JWK.validate","title":"validate  <code>staticmethod</code>","text":"<pre><code>validate(data: dict[str, Any]) -&gt; JWK\n</code></pre> <p>Validate and normalize JWK dict.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>JWK dict to validate.</p> required <p>Returns:</p> Type Description <code>JWK</code> <p>JWK instance.</p> <p>Raises:</p> Type Description <code>JamJWKValidationError</code> <p>If JWK is invalid.</p>"},{"location":"api/jam.jose/#jam.jose.JWK.verify","title":"verify","text":"<pre><code>verify(\n    token: str, alg: str | None = None\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token and return payload.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS compact serialization token.</p> required <code>alg</code> <code>str | None</code> <p>Algorithm to use. If None, uses default for kty.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with 'header' and 'payload' keys.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If signature is invalid.</p>"},{"location":"api/jam.jose/#jam.jose.JWKEC","title":"JWKEC","text":"<p>               Bases: <code>JWKCommon</code></p> <p>Elliptic Curve Key - RFC 7517 Section 6.2.</p> <p>Represents an elliptic curve public or private key.</p> Example <p>ec_key: JWKEC = { ...     \"kty\": \"EC\", ...     \"crv\": \"P-256\", ...     \"x\": \"f83OJ3D2xF1Bg8v...\", ...     \"y\": \"x_FEzRu9m36HLN_t...\", ... }</p> <p>Attributes:</p> Name Type Description <code>crv</code> <code>Literal['P-256', 'P-384', 'P-521']</code> <p>Elliptic curve name. The curve on which the key is based.</p> <code>d</code> <code>str</code> <p>EC private key value. The private key value. Present only in private keys.</p> <code>x</code> <code>str</code> <p>EC x coordinate. The x coordinate of the elliptic curve point.</p> <code>y</code> <code>str</code> <p>EC y coordinate. The y coordinate of the elliptic curve point.</p>"},{"location":"api/jam.jose/#jam.jose.JWKEC.crv","title":"crv  <code>instance-attribute</code>","text":"<pre><code>crv: Literal['P-256', 'P-384', 'P-521']\n</code></pre> <p>Elliptic curve name. The curve on which the key is based.</p>"},{"location":"api/jam.jose/#jam.jose.JWKEC.d","title":"d  <code>instance-attribute</code>","text":"<pre><code>d: str\n</code></pre> <p>EC private key value. The private key value. Present only in private keys.</p>"},{"location":"api/jam.jose/#jam.jose.JWKEC.x","title":"x  <code>instance-attribute</code>","text":"<pre><code>x: str\n</code></pre> <p>EC x coordinate. The x coordinate of the elliptic curve point.</p>"},{"location":"api/jam.jose/#jam.jose.JWKEC.y","title":"y  <code>instance-attribute</code>","text":"<pre><code>y: str\n</code></pre> <p>EC y coordinate. The y coordinate of the elliptic curve point.</p>"},{"location":"api/jam.jose/#jam.jose.JWKOct","title":"JWKOct","text":"<p>               Bases: <code>JWKCommon</code></p> <p>Symmetric (Octet Sequence) Key - RFC 7517 Section 6.4.</p> <p>Represents a symmetric (secret) key.</p> Example <p>oct_key: JWKOct = { ...     \"kty\": \"oct\", ...     \"k\": \"AyM32w-xOvmxxkBq...\", ... }</p> <p>Attributes:</p> Name Type Description <code>k</code> <code>str</code> <p>Key value. The base64url-encoded symmetric key value.</p>"},{"location":"api/jam.jose/#jam.jose.JWKOct.k","title":"k  <code>instance-attribute</code>","text":"<pre><code>k: str\n</code></pre> <p>Key value. The base64url-encoded symmetric key value.</p>"},{"location":"api/jam.jose/#jam.jose.JWKRSA","title":"JWKRSA","text":"<p>               Bases: <code>JWKCommon</code></p> <p>RSA Key - RFC 7517 Section 6.3.</p> <p>Represents an RSA public or private key.</p> Example <p>rsa_key: JWKRSA = { ...     \"kty\": \"RSA\", ...     \"n\": \"0vx7agoebGcQSuu...\", ...     \"e\": \"AQAB\", ... }</p> <p>Attributes:</p> Name Type Description <code>d</code> <code>str</code> <p>RSA private exponent d. Present only in private keys.</p> <code>dp</code> <code>str</code> <p>First factor exponent. d mod (p-1). Present only in private keys.</p> <code>dq</code> <code>str</code> <p>Second factor exponent. d mod (q-1). Present only in private keys.</p> <code>e</code> <code>str</code> <p>RSA exponent e. The exponent value for the RSA public key.</p> <code>n</code> <code>str</code> <p>RSA modulus n. The modulus value for the RSA public key.</p> <code>p</code> <code>str</code> <p>First prime p. First prime factor of n. Present only in private keys.</p> <code>q</code> <code>str</code> <p>Second prime q. Second prime factor of n. Present only in private keys.</p> <code>qi</code> <code>str</code> <p>First CRT coefficient. q^(-1) mod p. Present only in private keys.</p>"},{"location":"api/jam.jose/#jam.jose.JWKRSA.d","title":"d  <code>instance-attribute</code>","text":"<pre><code>d: str\n</code></pre> <p>RSA private exponent d. Present only in private keys.</p>"},{"location":"api/jam.jose/#jam.jose.JWKRSA.dp","title":"dp  <code>instance-attribute</code>","text":"<pre><code>dp: str\n</code></pre> <p>First factor exponent. d mod (p-1). Present only in private keys.</p>"},{"location":"api/jam.jose/#jam.jose.JWKRSA.dq","title":"dq  <code>instance-attribute</code>","text":"<pre><code>dq: str\n</code></pre> <p>Second factor exponent. d mod (q-1). Present only in private keys.</p>"},{"location":"api/jam.jose/#jam.jose.JWKRSA.e","title":"e  <code>instance-attribute</code>","text":"<pre><code>e: str\n</code></pre> <p>RSA exponent e. The exponent value for the RSA public key.</p>"},{"location":"api/jam.jose/#jam.jose.JWKRSA.n","title":"n  <code>instance-attribute</code>","text":"<pre><code>n: str\n</code></pre> <p>RSA modulus n. The modulus value for the RSA public key.</p>"},{"location":"api/jam.jose/#jam.jose.JWKRSA.p","title":"p  <code>instance-attribute</code>","text":"<pre><code>p: str\n</code></pre> <p>First prime p. First prime factor of n. Present only in private keys.</p>"},{"location":"api/jam.jose/#jam.jose.JWKRSA.q","title":"q  <code>instance-attribute</code>","text":"<pre><code>q: str\n</code></pre> <p>Second prime q. Second prime factor of n. Present only in private keys.</p>"},{"location":"api/jam.jose/#jam.jose.JWKRSA.qi","title":"qi  <code>instance-attribute</code>","text":"<pre><code>qi: str\n</code></pre> <p>First CRT coefficient. q^(-1) mod p. Present only in private keys.</p>"},{"location":"api/jam.jose/#jam.jose.JWKSet","title":"JWKSet","text":"<pre><code>JWKSet(keys: list[dict[str, Any]] | None = None)\n</code></pre> <p>               Bases: <code>BaseJWKSet</code></p> <p>JWK Set - RFC 7517 Section 5.</p> <p>Represents a set of JWKs. Used to organize and filter collections of keys.</p> Example <pre><code>&gt;&gt;&gt; jwkset = JWKSet(keys=[rsa_key, ec_key])\n&gt;&gt;&gt; jwkset.get_by_kid(\"my-key-id\")\n&gt;&gt;&gt; jwkset.filter(kty=\"RSA\")\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>keys</code> <code>list[dict[str, Any]] | None</code> <p>List of JWK dicts.</p> <code>None</code> <p>Methods:</p> Name Description <code>filter</code> <p>Filter JWKs by criteria.</p> <code>from_dict</code> <p>Create JWKSet from dict.</p> <code>get_by_kid</code> <p>Get JWK by key ID (kid).</p> <code>get_by_kty</code> <p>Get all JWKs by key type.</p> <code>to_dict</code> <p>Convert JWKSet to dict.</p>"},{"location":"api/jam.jose/#jam.jose.JWKSet.filter","title":"filter","text":"<pre><code>filter(**criteria: Any) -&gt; list[dict[str, Any]]\n</code></pre> <p>Filter JWKs by criteria.</p> <p>Parameters:</p> Name Type Description Default <code>**criteria</code> <code>Any</code> <p>Filter criteria (kty, use, alg, key_ops, kid).</p> <code>{}</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>List of matching JWK dicts.</p>"},{"location":"api/jam.jose/#jam.jose.JWKSet.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; JWKSet\n</code></pre> <p>Create JWKSet from dict.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Dict with \"keys\" key.</p> required <p>Returns:</p> Type Description <code>JWKSet</code> <p>JWKSet instance.</p> <p>Raises:</p> Type Description <code>JamJWKValidationError</code> <p>If data is invalid.</p>"},{"location":"api/jam.jose/#jam.jose.JWKSet.get_by_kid","title":"get_by_kid","text":"<pre><code>get_by_kid(kid: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get JWK by key ID (kid).</p> <p>Parameters:</p> Name Type Description Default <code>kid</code> <code>str</code> <p>Key ID to search for.</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>JWK dict if found, None otherwise.</p>"},{"location":"api/jam.jose/#jam.jose.JWKSet.get_by_kty","title":"get_by_kty","text":"<pre><code>get_by_kty(kty: str) -&gt; list[dict[str, Any]]\n</code></pre> <p>Get all JWKs by key type.</p> <p>Parameters:</p> Name Type Description Default <code>kty</code> <code>str</code> <p>Key type (RSA, EC, oct).</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>List of matching JWK dicts.</p>"},{"location":"api/jam.jose/#jam.jose.JWKSet.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert JWKSet to dict.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with \"keys\" key containing list of JWKs.</p>"},{"location":"api/jam.jose/#jam.jose.JWS","title":"JWS","text":"<pre><code>JWS(\n    alg: str,\n    key: KeyLike | JWK,\n    password: bytes | None = None,\n    logger: BaseLogger = logger,\n)\n</code></pre> <p>               Bases: <code>BaseJWS</code></p> <p>JWS (JSON Web Signature) implementation - RFC 7515.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm name</p> required <code>key</code> <code>KeyLike | JWK</code> <p>Key to use for signing/verifying</p> required <code>password</code> <code>bytes | None</code> <p>Password for encrypted private keys</p> <code>None</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <p>Methods:</p> Name Description <code>deserialize_compact</code> <p>Parse JWS Compact Serialization.</p> <code>serialize_compact</code> <p>Create JWS Compact Serialization.</p> <code>sign</code> <p>Sign data and return JWS compact serialization.</p> <code>verify</code> <p>Verify and parse JWS token.</p>"},{"location":"api/jam.jose/#jam.jose.JWS.deserialize_compact","title":"deserialize_compact","text":"<pre><code>deserialize_compact(\n    s: str, validate: bool = True\n) -&gt; dict[str, Any]\n</code></pre> <p>Parse JWS Compact Serialization.</p> <p>Parameters:</p> Name Type Description Default <code>s</code> <code>str</code> <p>JWS in compact serialization format.</p> required <code>validate</code> <code>bool</code> <p>Whether to validate signature. Defaults to True.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Parsed JWS with keys: - header: Protected header dict - payload: Decoded payload bytes - signature: Raw signature bytes</p> <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If validation fails or format is invalid.</p>"},{"location":"api/jam.jose/#jam.jose.JWS.serialize_compact","title":"serialize_compact","text":"<pre><code>serialize_compact(\n    protected: dict[str, Any],\n    payload: bytes | str | dict[str, Any],\n) -&gt; str\n</code></pre> <p>Create JWS Compact Serialization.</p> <p>Parameters:</p> Name Type Description Default <code>protected</code> <code>dict[str, Any]</code> <p>Protected header (must include 'alg').</p> required <code>payload</code> <code>bytes | str</code> <p>Payload to sign. If dict, will be JSON encoded.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS in compact serialization format:  BASE64URL(protected).BASE64URL(payload).BASE64URL(signature)</p>"},{"location":"api/jam.jose/#jam.jose.JWS.sign","title":"sign","text":"<pre><code>sign(\n    header: dict[str, Any],\n    data: bytes | str | dict[str, Any],\n) -&gt; str\n</code></pre> <p>Sign data and return JWS compact serialization.</p> <p>Parameters:</p> Name Type Description Default <code>header</code> <code>dict[str, Any]</code> <p>Protected header (alg will be added automatically).</p> required <code>data</code> <code>bytes | str | dict[str, Any]</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <p>Returns:</p> Type Description <code>str</code> <p>JWS compact serialization string.</p>"},{"location":"api/jam.jose/#jam.jose.JWS.verify","title":"verify","text":"<pre><code>verify(token: str, validate: bool = True) -&gt; dict[str, Any]\n</code></pre> <p>Verify and parse JWS token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS compact serialization string.</p> required <code>validate</code> <code>bool</code> <p>Whether to validate signature.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with 'header', 'payload', 'signature' keys.</p>"},{"location":"api/jam.jose/#jam.jose.JWT","title":"JWT","text":"<pre><code>JWT(\n    alg: str | None = None,\n    enc: str | None = None,\n    secret_key: str | bytes | KeyLike | JWK | None = None,\n    password: str | bytes | None = None,\n    list: dict[str, Any] | None = None,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger = logger,\n    jws: JWS | None = None,\n    jwe: JWE | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWT</code></p> <p>JWT (JSON Web Token) implementation - RFC 7519.</p> <p>Supports JWS (signed), JWE (encrypted), and JWS+JWE (sign then encrypt) tokens.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str | None</code> <p>JWT algorithm name for signing (JWS). Used if jws is not provided.</p> <code>None</code> <code>enc</code> <code>str | None</code> <p>JWE content encryption algorithm. If provided, creates encrypted JWT.</p> <code>None</code> <code>secret_key</code> <code>str | bytes | KeyLike | JWK | None</code> <p>Key for signing/encryption.</p> <code>None</code> <code>password</code> <code>str | bytes | None</code> <p>Password for encrypted private keys.</p> <code>None</code> <code>list</code> <code>dict[str, Any] | None</code> <p>List config for token storage.</p> <code>None</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>JSON encoder/decoder.</p> <code>JsonEncoder</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance.</p> <code>logger</code> <code>jws</code> <code>JWS | None</code> <p>Pre-built JWS instance. If provided, alg is ignored.</p> <code>None</code> <code>jwe</code> <code>JWE | None</code> <p>Pre-built JWE instance. If provided, enc and secret_key are ignored.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If neither alg/enc provided and no jws/jwe provided.</p> <code>ValueError</code> <p>If both alg and jws are provided.</p> <code>ValueError</code> <p>If both enc and jwe are provided.</p> <code>JamJWTUnsupportedAlgorithm</code> <p>If algorithm is not supported.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode the JWT and return the header and payload.</p> <code>decrypt</code> <p>Decrypt JWE or JWS+JWE token.</p> <code>encode</code> <p>Encode the JWT with the given expire, header, and payload (JWS).</p> <code>encrypt</code> <p>Encrypt payload using JWE.</p> <p>Attributes:</p> Name Type Description <code>jti</code> <code>str</code> <p>The JWT ID.</p>"},{"location":"api/jam.jose/#jam.jose.JWT.jti","title":"jti  <code>property</code>","text":"<pre><code>jti: str\n</code></pre> <p>The JWT ID.</p>"},{"location":"api/jam.jose/#jam.jose.JWT.decode","title":"decode","text":"<pre><code>decode(\n    token: str, validate_claims: bool = True\n) -&gt; dict[str, Any]\n</code></pre> <p>Decode the JWT and return the header and payload.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token.</p> required <code>validate_claims</code> <code>bool</code> <p>Whether to validate exp/nbf claims. Defaults to True.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict with 'header' and 'payload' keys (both dicts).</p> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If alg is not provided.</p> <code>JamJWSVerificationError</code> <p>If token has invalid type.</p> <code>JamJWTExpired</code> <p>If token is expired.</p> <code>JamJWTNotYetValid</code> <p>If token is not yet valid.</p>"},{"location":"api/jam.jose/#jam.jose.JWT.decrypt","title":"decrypt","text":"<pre><code>decrypt(token: str) -&gt; dict[str, Any] | bytes\n</code></pre> <p>Decrypt JWE or JWS+JWE token.</p> <p>If token is JWS+JWE (sign then encrypt): 1. Decrypt JWE to get JWS compact serialization 2. Verify JWS to get original payload</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Encrypted JWT token.</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | bytes</code> <p>dict[str, Any]: Decrypted payload.</p> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If enc is not provided.</p>"},{"location":"api/jam.jose/#jam.jose.JWT.encode","title":"encode","text":"<pre><code>encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    header: dict[str, Any] | None = None,\n    payload: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload (JWS).</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID. If None, auto-generated.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT (JWS compact serialization).</p> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If alg is not provided.</p>"},{"location":"api/jam.jose/#jam.jose.JWT.encrypt","title":"encrypt","text":"<pre><code>encrypt(\n    plaintext: bytes | str | dict[str, Any],\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt payload using JWE.</p> <p>If both jws and jwe are configured, creates JWS+JWE (sign then encrypt): 1. Create JWS compact serialization (sign) 2. Use JWS result as plaintext for JWE (encrypt)</p> <p>Parameters:</p> Name Type Description Default <code>plaintext</code> <code>bytes | str | dict[str, Any]</code> <p>Data to encrypt. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>Additional JWE header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Encrypted JWT (JWE or JWS+JWE).</p> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If jwe is not configured.</p>"},{"location":"api/jam.jose/#jam.jose.create_jwe_instance","title":"create_jwe_instance","text":"<pre><code>create_jwe_instance(\n    alg: str,\n    enc: str,\n    key: Any = None,\n    password: bytes | None = None,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger = logger,\n    **kwargs: Any,\n) -&gt; JWE\n</code></pre> <p>Create JWE instance.</p>"},{"location":"api/jam.jose/#jam.jose.create_jws_instance","title":"create_jws_instance","text":"<pre><code>create_jws_instance(\n    alg: str,\n    key: Any = None,\n    password: bytes | None = None,\n    logger: BaseLogger = logger,\n    **kwargs: Any,\n) -&gt; JWS\n</code></pre> <p>Create JWS instance.</p>"},{"location":"api/jam.jose/#jam.jose.create_jwt_instance","title":"create_jwt_instance","text":"<pre><code>create_jwt_instance(\n    alg: str,\n    secret: Any = None,\n    secret_key: Any = None,\n    password: str | bytes | None = None,\n    list: dict[str, Any] | None = None,\n    logger: BaseLogger = logger,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    **kwargs: Any,\n) -&gt; JWT\n</code></pre> <p>Create JWT instance.</p>"},{"location":"api/jam.jose.utils/","title":"jam.jose.utils","text":""},{"location":"api/jam.jose.utils/#jam.jose.utils","title":"utils","text":"<p>Functions:</p> Name Description <code>__base64url_decode__</code> <p>Decodes a URL-safe Base64 encoded string back to bytes.</p> <code>__base64url_encode__</code> <p>Encodes data using URL-safe Base64 encoding.</p>"},{"location":"api/jam.jose.utils/#jam.jose.utils.__base64url_decode__","title":"__base64url_decode__","text":"<pre><code>__base64url_decode__(data: str) -&gt; bytes\n</code></pre> <p>Decodes a URL-safe Base64 encoded string back to bytes.</p> <p>Automatically adds the necessary padding characters ('=') before decoding.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>str</code> <p>The Base64url encoded string to decode.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>The decoded byte data.</p>"},{"location":"api/jam.jose.utils/#jam.jose.utils.__base64url_encode__","title":"__base64url_encode__","text":"<pre><code>__base64url_encode__(data: bytes) -&gt; str\n</code></pre> <p>Encodes data using URL-safe Base64 encoding.</p> <p>Removes padding characters ('=') typically added in standard Base64 encoding.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>The data to encode.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A URL-safe Base64 encoded string without padding.</p>"},{"location":"api/jam.jwt.__algorithms__/","title":"jam.jwt.algorithms","text":""},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__","title":"__algorithms__","text":"<p>Classes:</p> Name Description <code>BaseAlgorithm</code> <p>Base class for JWT signing algorithms.</p> <code>ESAlgorithm</code> <p>ECDSA algorithms (ES256, ES384, ES512).</p> <code>HSAlgorithm</code> <p>HMAC-based algorithms (HS256, HS384, HS512).</p> <code>PSAlgorithm</code> <p>RSA PSS algorithms (PS256, PS384, PS512).</p> <code>RSAlgorithm</code> <p>RSA PKCS1v15 algorithms (RS256, RS384, RS512).</p> <p>Functions:</p> Name Description <code>create_algorithm</code> <p>Create algorithm instance based on algorithm name.</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.BaseAlgorithm","title":"BaseAlgorithm","text":"<pre><code>BaseAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base class for JWT signing algorithms.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm name</p> required <code>secret</code> <code>KeyLike</code> <p>Secret key</p> required <code>password</code> <code>bytes | None</code> <p>Password for private key</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> required <p>Methods:</p> Name Description <code>sign</code> <p>Sign data.</p> <code>verify</code> <p>Verify signature.</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.BaseAlgorithm.sign","title":"sign  <code>abstractmethod</code>","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.BaseAlgorithm.verify","title":"verify  <code>abstractmethod</code>","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.ESAlgorithm","title":"ESAlgorithm","text":"<pre><code>ESAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseAlgorithm</code></p> <p>ECDSA algorithms (ES256, ES384, ES512).</p> <p>Methods:</p> Name Description <code>sign</code> <p>Sign data using ECDSA.</p> <code>verify</code> <p>Verify ECDSA signature.</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.ESAlgorithm.sign","title":"sign","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data using ECDSA.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.ESAlgorithm.verify","title":"verify","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify ECDSA signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.HSAlgorithm","title":"HSAlgorithm","text":"<pre><code>HSAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseAlgorithm</code></p> <p>HMAC-based algorithms (HS256, HS384, HS512).</p> <p>Methods:</p> Name Description <code>sign</code> <p>Sign data using HMAC.</p> <code>verify</code> <p>Verify HMAC signature.</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.HSAlgorithm.sign","title":"sign","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data using HMAC.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.HSAlgorithm.verify","title":"verify","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify HMAC signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.PSAlgorithm","title":"PSAlgorithm","text":"<pre><code>PSAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseAlgorithm</code></p> <p>RSA PSS algorithms (PS256, PS384, PS512).</p> <p>Methods:</p> Name Description <code>sign</code> <p>Sign data using RSA PSS.</p> <code>verify</code> <p>Verify RSA PSS signature.</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.PSAlgorithm.sign","title":"sign","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data using RSA PSS.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.PSAlgorithm.verify","title":"verify","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify RSA PSS signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.RSAlgorithm","title":"RSAlgorithm","text":"<pre><code>RSAlgorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n)\n</code></pre> <p>               Bases: <code>BaseAlgorithm</code></p> <p>RSA PKCS1v15 algorithms (RS256, RS384, RS512).</p> <p>Methods:</p> Name Description <code>sign</code> <p>Sign data using RSA PKCS1v15.</p> <code>verify</code> <p>Verify RSA PKCS1v15 signature.</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.RSAlgorithm.sign","title":"sign","text":"<pre><code>sign(data: bytes) -&gt; str\n</code></pre> <p>Sign data using RSA PKCS1v15.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Data to sign</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Base64url encoded signature</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.RSAlgorithm.verify","title":"verify","text":"<pre><code>verify(sig: bytes, data: bytes, key: KeyLike) -&gt; None\n</code></pre> <p>Verify RSA PKCS1v15 signature.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>bytes</code> <p>Signature to verify</p> required <code>data</code> <code>bytes</code> <p>Data that was signed</p> required <code>key</code> <code>KeyLike</code> <p>Key for verification</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If signature is invalid</p>"},{"location":"api/jam.jwt.__algorithms__/#jam.jwt.__algorithms__.create_algorithm","title":"create_algorithm","text":"<pre><code>create_algorithm(\n    alg: str,\n    secret: KeyLike,\n    password: bytes | None,\n    logger: BaseLogger,\n) -&gt; BaseAlgorithm\n</code></pre> <p>Create algorithm instance based on algorithm name.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm name</p> required <code>secret</code> <code>KeyLike</code> <p>Secret key</p> required <code>password</code> <code>bytes | None</code> <p>Password for private key</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> required <p>Returns:</p> Name Type Description <code>BaseAlgorithm</code> <code>BaseAlgorithm</code> <p>Algorithm instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If algorithm is not supported</p>"},{"location":"api/jam.jwt.__base__/","title":"jam.jwt.base","text":""},{"location":"api/jam.jwt.__base__/#jam.jwt.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseJWT</code> <p>Base JWT module.</p>"},{"location":"api/jam.jwt.__base__/#jam.jwt.__base__.BaseJWT","title":"BaseJWT","text":"<p>               Bases: <code>ABC</code></p> <p>Base JWT module.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode token.</p> <code>encode</code> <p>Encode token.</p>"},{"location":"api/jam.jwt.__base__/#jam.jwt.__base__.BaseJWT.decode","title":"decode  <code>abstractmethod</code>","text":"<pre><code>decode(\n    token: str, public_key: Any | None = None\n) -&gt; dict[str, Any]\n</code></pre> <p>Decode token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT</p> required <code>public_key</code> <code>Any | None</code> <p>Decode with public key if needed</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Payload</p>"},{"location":"api/jam.jwt.__base__/#jam.jwt.__base__.BaseJWT.encode","title":"encode  <code>abstractmethod</code>","text":"<pre><code>encode(payload: dict[str, Any]) -&gt; str\n</code></pre> <p>Encode token.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>JWT Payload</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWT</p>"},{"location":"api/jam.jwt.__types__/","title":"jam.jwt.types","text":""},{"location":"api/jam.jwt.__types__/#jam.jwt.__types__","title":"__types__","text":""},{"location":"api/jam.jwt.lists.__base__/","title":"jam.jwt.lists.base","text":""},{"location":"api/jam.jwt.lists.__base__/#jam.jwt.lists.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseJWTList</code> <p>Abstract class for lists manipulation.</p>"},{"location":"api/jam.jwt.lists.__base__/#jam.jwt.lists.__base__.BaseJWTList","title":"BaseJWTList","text":"<pre><code>BaseJWTList(list_type: Literal['white', 'black'])\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract class for lists manipulation.</p> <p>Methods:</p> Name Description <code>add</code> <p>Method for adding token to list.</p> <code>check</code> <p>Method for checking if a token is present in the list.</p> <code>delete</code> <p>Method for removing a token from a list.</p>"},{"location":"api/jam.jwt.lists.__base__/#jam.jwt.lists.__base__.BaseJWTList.add","title":"add  <code>abstractmethod</code>","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Method for adding token to list.</p>"},{"location":"api/jam.jwt.lists.__base__/#jam.jwt.lists.__base__.BaseJWTList.check","title":"check  <code>abstractmethod</code>","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Method for checking if a token is present in the list.</p>"},{"location":"api/jam.jwt.lists.__base__/#jam.jwt.lists.__base__.BaseJWTList.delete","title":"delete  <code>abstractmethod</code>","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Method for removing a token from a list.</p>"},{"location":"api/jam.jwt.lists.json/","title":"jam.jwt.lists.json","text":""},{"location":"api/jam.jwt.lists.json/#jam.jwt.lists.json","title":"json","text":"<p>Classes:</p> Name Description <code>JSONList</code> <p>Black/White list in JSON format, not recommended for blacklists  because it is not convenient to control token lifetime.</p>"},{"location":"api/jam.jwt.lists.json/#jam.jwt.lists.json.JSONList","title":"JSONList","text":"<pre><code>JSONList(\n    type: Literal[\"white\", \"black\"],\n    json_path: str = \"whitelist.json\",\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWTList</code></p> <p>Black/White list in JSON format, not recommended for blacklists  because it is not convenient to control token lifetime.</p> <p>Dependency required: <code>pip install jamlib[json]</code></p> <p>Attributes:</p> Name Type Description <code>__list__</code> <code>TinyDB</code> <p>TinyDB instance</p> <p>Methods:</p> Name Description <code>add</code> <p>adding token to list</p> <code>check</code> <p>check token in list</p> <code>delete</code> <p>removing token from list</p> <p>Parameters:</p> Name Type Description Default <code>type</code> <code>Literal['white', 'black']</code> <p>Type of list</p> required <code>json_path</code> <code>str</code> <p>Path to .json file</p> <code>'whitelist.json'</code> <code>logger</code> <code>Optional[BaseLogger]</code> <p>Logger instance. Defaults to None.</p> <code>None</code>"},{"location":"api/jam.jwt.lists.json/#jam.jwt.lists.json.JSONList.add","title":"add","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Method for adding token to list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Your JWT token</p> required <p>Returns:</p> Type Description <code>None</code> <p>(None)</p>"},{"location":"api/jam.jwt.lists.json/#jam.jwt.lists.json.JSONList.check","title":"check","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Method for checking if a token is present in list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Your jwt token</p> required <p>Returns:</p> Type Description <code>bool</code> <p>(bool)</p>"},{"location":"api/jam.jwt.lists.json/#jam.jwt.lists.json.JSONList.delete","title":"delete","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Method for removing token from list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Your jwt token</p> required <p>Returns:</p> Type Description <code>None</code> <p>(None)</p>"},{"location":"api/jam.jwt.lists/","title":"jam.jwt.lists","text":""},{"location":"api/jam.jwt.lists/#jam.jwt.lists","title":"lists","text":"<p>Module for managing white and black lists.</p> <p>Modules:</p> Name Description <code>json</code> <code>redis</code> <p>Classes:</p> Name Description <code>BaseJWTList</code> <p>Abstract class for lists manipulation.</p>"},{"location":"api/jam.jwt.lists/#jam.jwt.lists.BaseJWTList","title":"BaseJWTList","text":"<pre><code>BaseJWTList(list_type: Literal['white', 'black'])\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract class for lists manipulation.</p> <p>Methods:</p> Name Description <code>add</code> <p>Method for adding token to list.</p> <code>check</code> <p>Method for checking if a token is present in the list.</p> <code>delete</code> <p>Method for removing a token from a list.</p>"},{"location":"api/jam.jwt.lists/#jam.jwt.lists.BaseJWTList.add","title":"add  <code>abstractmethod</code>","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Method for adding token to list.</p>"},{"location":"api/jam.jwt.lists/#jam.jwt.lists.BaseJWTList.check","title":"check  <code>abstractmethod</code>","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Method for checking if a token is present in the list.</p>"},{"location":"api/jam.jwt.lists/#jam.jwt.lists.BaseJWTList.delete","title":"delete  <code>abstractmethod</code>","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Method for removing a token from a list.</p>"},{"location":"api/jam.jwt.lists.redis/","title":"jam.jwt.lists.redis","text":""},{"location":"api/jam.jwt.lists.redis/#jam.jwt.lists.redis","title":"redis","text":"<p>Classes:</p> Name Description <code>RedisList</code> <p>Black/White lists in Redis, most optimal format.</p>"},{"location":"api/jam.jwt.lists.redis/#jam.jwt.lists.redis.RedisList","title":"RedisList","text":"<pre><code>RedisList(\n    type: Literal[\"white\", \"black\"],\n    redis_uri: str | Redis,\n    in_list_life_time: int | None = None,\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseJWTList</code></p> <p>Black/White lists in Redis, most optimal format.</p> <p>Dependency required: <code>pip install jamlib[redis]</code></p> <p>Attributes:</p> Name Type Description <code>__list__</code> <code>Redis</code> <p>Redis instance</p> <code>exp</code> <code>int | None</code> <p>Token lifetime</p> <p>Parameters:</p> Name Type Description Default <code>type</code> <code>Literal['white', 'black']</code> <p>Type og list</p> required <code>redis_uri</code> <code>str</code> <p>Uri to redis connect</p> required <code>in_list_life_time</code> <code>int | None</code> <p>The lifetime of a token in the list</p> <code>None</code> <code>logger</code> <code>Optional[BaseLogger]</code> <p>Logger instance. Defaults to None.</p> <code>None</code> <p>Methods:</p> Name Description <code>add</code> <p>Method for adding token to list.</p> <code>check</code> <p>Method for checking if a token is present in the list.</p> <code>delete</code> <p>Method for removing a token from a list.</p>"},{"location":"api/jam.jwt.lists.redis/#jam.jwt.lists.redis.RedisList.add","title":"add","text":"<pre><code>add(token: str) -&gt; None\n</code></pre> <p>Method for adding token to list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Your JWT token</p> required <p>Returns:</p> Type Description <code>None</code> <p>(None)</p>"},{"location":"api/jam.jwt.lists.redis/#jam.jwt.lists.redis.RedisList.check","title":"check","text":"<pre><code>check(token: str) -&gt; bool\n</code></pre> <p>Method for checking if a token is present in the list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Your JWT token</p> required <p>Returns:</p> Type Description <code>bool</code> <p>(bool)</p>"},{"location":"api/jam.jwt.lists.redis/#jam.jwt.lists.redis.RedisList.delete","title":"delete","text":"<pre><code>delete(token: str) -&gt; None\n</code></pre> <p>Method for removing a token from a list.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Your JWT token</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p>"},{"location":"api/jam.jwt/","title":"jam.jwt","text":""},{"location":"api/jam.jwt/#jam.jwt","title":"jwt","text":"<p>Modules:</p> Name Description <code>lists</code> <p>Module for managing white and black lists.</p> <code>module</code> <code>utils</code> <p>Classes:</p> Name Description <code>BaseJWT</code> <p>Base JWT module.</p> <code>JWT</code> <p>JWT factory.</p> <p>Functions:</p> Name Description <code>create_instance</code> <p>Create JWT instance.</p>"},{"location":"api/jam.jwt/#jam.jwt.BaseJWT","title":"BaseJWT","text":"<p>               Bases: <code>ABC</code></p> <p>Base JWT module.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode token.</p> <code>encode</code> <p>Encode token.</p>"},{"location":"api/jam.jwt/#jam.jwt.BaseJWT.decode","title":"decode  <code>abstractmethod</code>","text":"<pre><code>decode(\n    token: str, public_key: Any | None = None\n) -&gt; dict[str, Any]\n</code></pre> <p>Decode token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT</p> required <code>public_key</code> <code>Any | None</code> <p>Decode with public key if needed</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Payload</p>"},{"location":"api/jam.jwt/#jam.jwt.BaseJWT.encode","title":"encode  <code>abstractmethod</code>","text":"<pre><code>encode(payload: dict[str, Any]) -&gt; str\n</code></pre> <p>Encode token.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>JWT Payload</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWT</p>"},{"location":"api/jam.jwt/#jam.jwt.JWT","title":"JWT","text":"<pre><code>JWT(\n    alg: str,\n    secret: KeyLike = getenv(\"JAM_JWT_SECRET_KEY\"),\n    password: str | bytes | None = None,\n    list: dict[str, Any] | None = None,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger = logger,\n)\n</code></pre> <p>               Bases: <code>BaseJWT</code></p> <p>JWT factory.</p> <p>This class provides JWT encoding and decoding functionality with support for multiple signing algorithms. It is designed to be easily extensible through inheritance and algorithm customization.</p> Example <p>jwt = JWT(alg=\"HS256\", secret=\"my-secret-key\") token = jwt.encode({\"sub\": \"user123\"}) payload = jwt.decode(token)</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>JWT algorithm name</p> required <code>secret</code> <code>KeyLike</code> <p>Secret key for signing/verification</p> <code>getenv('JAM_JWT_SECRET_KEY')</code> <code>password</code> <code>str | bytes | None</code> <p>Password for encrypted private keys</p> <code>None</code> <code>list</code> <code>dict[str, Any] | None</code> <p>List config</p> <code>None</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>JSON encoder/decoder</p> <code>JsonEncoder</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <p>Raises:</p> Type Description <code>JamJWTUnsupportedAlgorithm</code> <p>If algorithm is not supported or secret is invalid</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode and verify token.</p> <code>encode</code> <p>Encode token.</p>"},{"location":"api/jam.jwt/#jam.jwt.JWT.decode","title":"decode","text":"<pre><code>decode(\n    token: str, public_key: KeyLike | None = None\n) -&gt; dict[str, Any]\n</code></pre> <p>Decode and verify token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token to decode</p> required <code>public_key</code> <code>KeyLike | None</code> <p>Optional public key for verification. If not provided, uses the secret from initialization.</p> <code>None</code> <p>Raises:</p> Type Description <code>JamJWTValidationError</code> <p>If token is invalid, malformed, or verification fails</p> <code>JamJWTUnsupportedAlgorithm</code> <p>If the token's algorithm is not supported</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload</p>"},{"location":"api/jam.jwt/#jam.jwt.JWT.encode","title":"encode","text":"<pre><code>encode(payload: dict[str, Any]) -&gt; str\n</code></pre> <p>Encode token.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>JWT payload</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Encoded JWT token</p> <p>Raises:</p> Type Description <code>JamJWTValidationError</code> <p>If encoding fails</p>"},{"location":"api/jam.jwt/#jam.jwt.create_instance","title":"create_instance","text":"<pre><code>create_instance(\n    alg: str,\n    secret: KeyLike | None = None,\n    secret_key: KeyLike | None = None,\n    password: str | bytes | None = None,\n    list: dict[str, Any] | None = None,\n    logger: BaseLogger = logger,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    **kwargs: Any,\n) -&gt; JWT\n</code></pre> <p>Create JWT instance.</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>Algorithm (HS256, RS256, etc.)</p> required <code>secret</code> <code>KeyLike | None</code> <p>Secret key (or use secret_key for backward compatibility)</p> <code>None</code> <code>secret_key</code> <code>KeyLike | None</code> <p>Alias for secret (deprecated, use 'secret')</p> <code>None</code> <code>password</code> <code>str | bytes | None</code> <p>Password for encrypted keys</p> <code>None</code> <code>list</code> <code>dict[str, Any] | None</code> <p>List config</p> <code>None</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>JSON encoder/decoder</p> <code>JsonEncoder</code> <code>**kwargs</code> <code>Any</code> <p>Additional params (e.g., custom_module)</p> <code>{}</code> <p>Returns:</p> Type Description <code>JWT</code> <p>JWT instance or custom module</p>"},{"location":"api/jam.jwt.module/","title":"jam.jwt.module","text":""},{"location":"api/jam.jwt.module/#jam.jwt.module","title":"module","text":"<p>Classes:</p> Name Description <code>JWT</code> <p>JWT factory.</p>"},{"location":"api/jam.jwt.module/#jam.jwt.module.JWT","title":"JWT","text":"<pre><code>JWT(\n    alg: str,\n    secret: KeyLike = getenv(\"JAM_JWT_SECRET_KEY\"),\n    password: str | bytes | None = None,\n    list: dict[str, Any] | None = None,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger = logger,\n)\n</code></pre> <p>               Bases: <code>BaseJWT</code></p> <p>JWT factory.</p> <p>This class provides JWT encoding and decoding functionality with support for multiple signing algorithms. It is designed to be easily extensible through inheritance and algorithm customization.</p> Example <p>jwt = JWT(alg=\"HS256\", secret=\"my-secret-key\") token = jwt.encode({\"sub\": \"user123\"}) payload = jwt.decode(token)</p> <p>Parameters:</p> Name Type Description Default <code>alg</code> <code>str</code> <p>JWT algorithm name</p> required <code>secret</code> <code>KeyLike</code> <p>Secret key for signing/verification</p> <code>getenv('JAM_JWT_SECRET_KEY')</code> <code>password</code> <code>str | bytes | None</code> <p>Password for encrypted private keys</p> <code>None</code> <code>list</code> <code>dict[str, Any] | None</code> <p>List config</p> <code>None</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>JSON encoder/decoder</p> <code>JsonEncoder</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <p>Raises:</p> Type Description <code>JamJWTUnsupportedAlgorithm</code> <p>If algorithm is not supported or secret is invalid</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode and verify token.</p> <code>encode</code> <p>Encode token.</p>"},{"location":"api/jam.jwt.module/#jam.jwt.module.JWT.decode","title":"decode","text":"<pre><code>decode(\n    token: str, public_key: KeyLike | None = None\n) -&gt; dict[str, Any]\n</code></pre> <p>Decode and verify token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token to decode</p> required <code>public_key</code> <code>KeyLike | None</code> <p>Optional public key for verification. If not provided, uses the secret from initialization.</p> <code>None</code> <p>Raises:</p> Type Description <code>JamJWTValidationError</code> <p>If token is invalid, malformed, or verification fails</p> <code>JamJWTUnsupportedAlgorithm</code> <p>If the token's algorithm is not supported</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload</p>"},{"location":"api/jam.jwt.module/#jam.jwt.module.JWT.encode","title":"encode","text":"<pre><code>encode(payload: dict[str, Any]) -&gt; str\n</code></pre> <p>Encode token.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>JWT payload</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Encoded JWT token</p> <p>Raises:</p> Type Description <code>JamJWTValidationError</code> <p>If encoding fails</p>"},{"location":"api/jam.jwt.utils/","title":"jam.jwt.utils","text":""},{"location":"api/jam.jwt.utils/#jam.jwt.utils","title":"utils","text":"<p>Functions:</p> Name Description <code>base64url_decode</code> <p>Decodes a URL-safe Base64 encoded string back to bytes.</p> <code>base64url_encode</code> <p>Encodes data using URL-safe Base64 encoding.</p>"},{"location":"api/jam.jwt.utils/#jam.jwt.utils.base64url_decode","title":"base64url_decode","text":"<pre><code>base64url_decode(data: str) -&gt; bytes\n</code></pre> <p>Decodes a URL-safe Base64 encoded string back to bytes.</p> <p>Automatically adds the necessary padding characters ('=') before decoding.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>str</code> <p>The Base64url encoded string to decode.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>The decoded byte data.</p>"},{"location":"api/jam.jwt.utils/#jam.jwt.utils.base64url_encode","title":"base64url_encode","text":"<pre><code>base64url_encode(data: bytes) -&gt; str\n</code></pre> <p>Encodes data using URL-safe Base64 encoding.</p> <p>Removes padding characters ('=') typically added in standard Base64 encoding.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>The data to encode.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A URL-safe Base64 encoded string without padding.</p>"},{"location":"api/jam.logger/","title":"jam.logger","text":""},{"location":"api/jam.logger/#jam.logger","title":"logger","text":"<p>Classes:</p> Name Description <code>BaseLogger</code> <p>Interface for logging.</p> <code>JamLogger</code> <p>Default jam logger, use stdlib logging.</p>"},{"location":"api/jam.logger/#jam.logger.BaseLogger","title":"BaseLogger","text":"<pre><code>BaseLogger(\n    log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ]\n    | str = \"INFO\",\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Interface for logging.</p> <p>Methods:</p> Name Description <code>debug</code> <p>Log a debug message.</p> <code>error</code> <p>Log an error message.</p> <code>info</code> <p>Log an informational message.</p> <code>warning</code> <p>Log a warning message.</p>"},{"location":"api/jam.logger/#jam.logger.BaseLogger.debug","title":"debug  <code>abstractmethod</code>","text":"<pre><code>debug(message: str, *args: Any) -&gt; None\n</code></pre> <p>Log a debug message.</p>"},{"location":"api/jam.logger/#jam.logger.BaseLogger.error","title":"error  <code>abstractmethod</code>","text":"<pre><code>error(message: str, exc_info: bool = False) -&gt; None\n</code></pre> <p>Log an error message.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Error message</p> required <code>exc_info</code> <code>bool</code> <p>Whether to include exception info</p> <code>False</code>"},{"location":"api/jam.logger/#jam.logger.BaseLogger.info","title":"info  <code>abstractmethod</code>","text":"<pre><code>info(message: str) -&gt; None\n</code></pre> <p>Log an informational message.</p>"},{"location":"api/jam.logger/#jam.logger.BaseLogger.warning","title":"warning  <code>abstractmethod</code>","text":"<pre><code>warning(message: str, exc_info: bool = False) -&gt; None\n</code></pre> <p>Log a warning message.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Warning message</p> required <code>exc_info</code> <code>bool</code> <p>Whether to include exception info</p> <code>False</code>"},{"location":"api/jam.logger/#jam.logger.JamLogger","title":"JamLogger","text":"<pre><code>JamLogger(\n    log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ]\n    | str = \"INFO\",\n)\n</code></pre> <p>               Bases: <code>BaseLogger</code></p> <p>Default jam logger, use stdlib logging.</p> <p>Methods:</p> Name Description <code>__repr__</code> <p>Return a string representation of the logger.</p> <code>__str__</code> <p>Return a string representation of the logger.</p> <code>debug</code> <p>Log a debug message.</p> <code>error</code> <p>Log an error message.</p> <code>info</code> <p>Log an informational message.</p> <code>warning</code> <p>Log a warning message.</p>"},{"location":"api/jam.logger/#jam.logger.JamLogger.__repr__","title":"__repr__","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Return a string representation of the logger.</p>"},{"location":"api/jam.logger/#jam.logger.JamLogger.__str__","title":"__str__","text":"<pre><code>__str__() -&gt; str\n</code></pre> <p>Return a string representation of the logger.</p>"},{"location":"api/jam.logger/#jam.logger.JamLogger.debug","title":"debug","text":"<pre><code>debug(message: str, *args: Any) -&gt; None\n</code></pre> <p>Log a debug message.</p>"},{"location":"api/jam.logger/#jam.logger.JamLogger.error","title":"error","text":"<pre><code>error(message: str, exc_info: bool = False) -&gt; None\n</code></pre> <p>Log an error message.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Error message</p> required <code>exc_info</code> <code>bool</code> <p>Whether to include exception info</p> <code>False</code>"},{"location":"api/jam.logger/#jam.logger.JamLogger.info","title":"info","text":"<pre><code>info(message: str) -&gt; None\n</code></pre> <p>Log an informational message.</p>"},{"location":"api/jam.logger/#jam.logger.JamLogger.warning","title":"warning","text":"<pre><code>warning(message: str, exc_info: bool = False) -&gt; None\n</code></pre> <p>Log a warning message.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Warning message</p> required <code>exc_info</code> <code>bool</code> <p>Whether to include exception info</p> <code>False</code>"},{"location":"api/jam/","title":"jam","text":""},{"location":"api/jam/#jam","title":"jam","text":"<p>JAM - Universal auth* library.</p> <p>Source code: https://github.com/mkrdnk/jam Documentation: https://jam.makridenko.ru</p> <p>Modules:</p> Name Description <code>aio</code> <p>Async Jam package.</p> <code>cli</code> <p>CLI tool for Jam.</p> <code>encoders</code> <code>exceptions</code> <p>All Jam exceptions</p> <code>ext</code> <p>Extensions and integrations.</p> <code>instance</code> <code>jose</code> <p>JOSE tools.</p> <code>jwt</code> <code>logger</code> <code>oauth2</code> <p>OAuth2 module.</p> <code>otp</code> <p>*OTP auth module</p> <code>paseto</code> <p>PASETO auth* tokens.</p> <code>plugins</code> <p>Jam plugin system.</p> <code>saml</code> <p>Security Assertion Markup Language (SAML 2.0).</p> <code>sessions</code> <p>Module for making server auth sessions.</p> <code>tests</code> <p>Test clients for simplified unit and integration testing.</p> <code>utils</code> <p>Various utilities that help with authorization.</p> <p>Classes:</p> Name Description <code>BaseJam</code> <p>Base jam instance.</p> <code>Jam</code> <p>Main instance.</p>"},{"location":"api/jam/#jam.BaseJam","title":"BaseJam","text":"<pre><code>BaseJam(\n    config: str | dict[str, Any] = \"pyproject.toml\",\n    pointer: str = \"jam\",\n    *,\n    logger: type[BaseLogger] = JamLogger,\n    log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ] = \"INFO\",\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    plugins: list[type[BasePlugin]] = [],\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base jam instance.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Union[str, dict[str, Any]]</code> <p>Configuration</p> <code>'pyproject.toml'</code> <code>pointer</code> <code>str</code> <p>Pointer</p> <code>'jam'</code> <code>logger</code> <code>BaseLogger</code> <p>Logger</p> <code>JamLogger</code> <code>log_level</code> <code>Literal['DEBUG', 'INFO', 'WARNING', 'ERROR', 'CRITICAL']</code> <p>Log level</p> <code>'INFO'</code> <code>serializer</code> <code>Union[BaseEncoder, type[BaseBrowser]]</code> <p>Serializer</p> <code>JsonEncoder</code> <code>plugins</code> <code>list[type[BasePlugin]]</code> <p>List of plugins</p> <code>[]</code> <p>Returns:</p> Type Description <code>None</code> <p>None</p> <p>Methods:</p> Name Description <code>emit</code> <p>Emit event.</p> <code>jwe_decrypt</code> <p>Decrypt JWE token.</p> <code>jwe_encrypt</code> <p>Encrypt data using JWE.</p> <code>jws_sign</code> <p>Sign data using JWS.</p> <code>jws_verify</code> <p>Verify JWS token.</p> <code>jwt_decode</code> <p>Verify and decode JWT token.</p> <code>jwt_encode</code> <p>Encode the JWT with the given expire, header, and payload.</p> <code>oauth2_client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>oauth2_fetch_token</code> <p>Exchange authorization code for access token.</p> <code>oauth2_get_authorized_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>oauth2_refresh_token</code> <p>Use refresh token to obtain a new access token.</p> <code>otp_code</code> <p>Generates an OTP.</p> <code>otp_uri</code> <p>Generates an otpauth:// URI for Google Authenticator.</p> <code>otp_verify_code</code> <p>Checks the OTP code, taking into account the acceptable window.</p> <code>paseto_create</code> <p>Create new PASETO.</p> <code>paseto_decode</code> <p>Decode PASETO and return payload and footer.</p> <code>paseto_make_payload</code> <p>Generate payload for PASETO.</p> <code>session_clear</code> <p>Delete all sessions by key.</p> <code>session_create</code> <p>Create new session.</p> <code>session_delete</code> <p>Delete session.</p> <code>session_get</code> <p>Get data from session.</p> <code>session_rework</code> <p>Rework session.</p> <code>session_update</code> <p>Update session data.</p>"},{"location":"api/jam/#jam.BaseJam.__build_instance","title":"__build_instance","text":"<pre><code>__build_instance(config: dict[str, Any]) -&gt; None\n</code></pre> <p>Build instance.</p> <p>Load modules from configuration and initialize them. Supports both flat modules (name -&gt; path) and nested modules (name -&gt; {subname -&gt; path}).</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any]</code> <p>Configuration</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p>"},{"location":"api/jam/#jam.BaseJam.__build_main_config","title":"__build_main_config","text":"<pre><code>__build_main_config(\n    config: dict[str, Any],\n    default_logger: type[BaseLogger],\n    default_log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ],\n    default_serializer: BaseEncoder | type[BaseEncoder],\n) -&gt; dict[str, Any]\n</code></pre> <p>Build main params from config like logger, loglevel, etc.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict[str, Any]</code> <p>Configuration dictionary</p> required <code>default_logger</code> <code>type[BaseLogger]</code> <p>Default logger class</p> required <code>default_log_level</code> <code>Literal</code> <p>Default log level</p> required <code>default_serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>Default serializer</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Dictionary with logger, log_level, and serializer</p>"},{"location":"api/jam/#jam.BaseJam.__otp","title":"__otp","text":"<pre><code>__otp(\n    type: Literal[\"totp\", \"hotp\"] | None = None,\n) -&gt; type[BaseOTP] | None\n</code></pre> <p>Get OTP type.</p>"},{"location":"api/jam/#jam.BaseJam.__setup_plugins","title":"__setup_plugins","text":"<pre><code>__setup_plugins(plugins: list[type[BasePlugin]]) -&gt; None\n</code></pre> <p>Setup plugins.</p>"},{"location":"api/jam/#jam.BaseJam.emit","title":"emit","text":"<pre><code>emit(event: str, **kwargs) -&gt; Any\n</code></pre> <p>Emit event.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>str</code> <p>Event name,</p> required <code>**kwargs</code> <p>Event data</p> <code>{}</code>"},{"location":"api/jam/#jam.BaseJam.jwe_decrypt","title":"jwe_decrypt  <code>abstractmethod</code>","text":"<pre><code>jwe_decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE token.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>Decrypted data.</p> <p>Raises:</p> Type Description <code>JamJWEDecryptionError</code> <p>If decryption fails.</p>"},{"location":"api/jam/#jam.BaseJam.jwe_encrypt","title":"jwe_encrypt  <code>abstractmethod</code>","text":"<pre><code>jwe_encrypt(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt data using JWE.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to encrypt. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWE header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWE token.</p>"},{"location":"api/jam/#jam.BaseJam.jws_sign","title":"jws_sign  <code>abstractmethod</code>","text":"<pre><code>jws_sign(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Sign data using JWS.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWS header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS token.</p>"},{"location":"api/jam/#jam.BaseJam.jws_verify","title":"jws_verify  <code>abstractmethod</code>","text":"<pre><code>jws_verify(token: str) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS token.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload.</p> <p>Raises:</p> Type Description <code>JamJWSVerificationError</code> <p>If verification fails.</p>"},{"location":"api/jam/#jam.BaseJam.jwt_decode","title":"jwt_decode  <code>abstractmethod</code>","text":"<pre><code>jwt_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n    check_nbf: bool = False,\n    include_headers: bool = False,\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify and decode JWT token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token</p> required <code>check_exp</code> <code>bool</code> <p>Check expire</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check white/black list. Docs: https://jam.makridenko.ru/jwt/lists/what/</p> <code>True</code> <code>check_nbf</code> <code>bool</code> <p>Check not-before time</p> <code>False</code> <code>include_headers</code> <code>bool</code> <p>Include headers in the decoded payload</p> <code>False</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload</p>"},{"location":"api/jam/#jam.BaseJam.jwt_encode","title":"jwt_encode  <code>abstractmethod</code>","text":"<pre><code>jwt_encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    *,\n    payload: dict[str, Any] | None = None,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID. If none use the JTI fabric function.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT.</p>"},{"location":"api/jam/#jam.BaseJam.oauth2_client_credentials_flow","title":"oauth2_client_credentials_flow  <code>abstractmethod</code>","text":"<pre><code>oauth2_client_credentials_flow(\n    provider: str,\n    scope: list[str] | None = None,\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam/#jam.BaseJam.oauth2_fetch_token","title":"oauth2_fetch_token  <code>abstractmethod</code>","text":"<pre><code>oauth2_fetch_token(\n    provider: str,\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam/#jam.BaseJam.oauth2_get_authorized_url","title":"oauth2_get_authorized_url  <code>abstractmethod</code>","text":"<pre><code>oauth2_get_authorized_url(\n    provider: str, scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra ath params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam/#jam.BaseJam.oauth2_refresh_token","title":"oauth2_refresh_token  <code>abstractmethod</code>","text":"<pre><code>oauth2_refresh_token(\n    provider: str,\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam/#jam.BaseJam.otp_code","title":"otp_code  <code>abstractmethod</code>","text":"<pre><code>otp_code(\n    secret: str | bytes, factor: int | None = None\n) -&gt; str\n</code></pre> <p>Generates an OTP.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>OTP code (fixed-length string).</p>"},{"location":"api/jam/#jam.BaseJam.otp_uri","title":"otp_uri  <code>abstractmethod</code>","text":"<pre><code>otp_uri(\n    secret: str,\n    name: str,\n    issuer: str,\n    counter: int | None = None,\n) -&gt; str\n</code></pre> <p>Generates an otpauth:// URI for Google Authenticator.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str</code> <p>User secret key.</p> required <code>name</code> <code>str</code> <p>Account name (e.g., email).</p> required <code>issuer</code> <code>str</code> <p>Service name (e.g., \"GitHub\").</p> required <code>counter</code> <code>int | None</code> <p>Counter (for HOTP). Default is None.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A string of the form \"otpauth://...\"</p>"},{"location":"api/jam/#jam.BaseJam.otp_verify_code","title":"otp_verify_code  <code>abstractmethod</code>","text":"<pre><code>otp_verify_code(\n    secret: str | bytes,\n    code: str,\n    factor: int | None = None,\n    look_ahead: int | None = 1,\n) -&gt; bool\n</code></pre> <p>Checks the OTP code, taking into account the acceptable window.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>code</code> <code>str</code> <p>The code entered.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <code>look_ahead</code> <code>int</code> <p>Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is 1.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the code matches, otherwise False.</p>"},{"location":"api/jam/#jam.BaseJam.paseto_create","title":"paseto_create  <code>abstractmethod</code>","text":"<pre><code>paseto_create(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None,\n) -&gt; str\n</code></pre> <p>Create new PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload</p> required <code>footer</code> <code>dict[str, Any] | str | None</code> <p>Payload if needed</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>PASETO</p>"},{"location":"api/jam/#jam.BaseJam.paseto_decode","title":"paseto_decode  <code>abstractmethod</code>","text":"<pre><code>paseto_decode(\n    token: str,\n) -&gt; dict[str, dict[str, Any] | str | None]\n</code></pre> <p>Decode PASETO and return payload and footer.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>PASETO</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, dict[str, Any] | str | None]</code> <p>{\"payload\": PAYLOAD, \"footer\": FOOTER}</p>"},{"location":"api/jam/#jam.BaseJam.paseto_make_payload","title":"paseto_make_payload  <code>abstractmethod</code>","text":"<pre><code>paseto_make_payload(\n    exp: int | None = None, **data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Generate payload for PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Custom expire if needed</p> <code>None</code> <code>data</code> <code>dict[str, Any]</code> <p>Data in payload</p> <code>{}</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: New payload</p>"},{"location":"api/jam/#jam.BaseJam.session_clear","title":"session_clear  <code>abstractmethod</code>","text":"<pre><code>session_clear(session_key: str) -&gt; None\n</code></pre> <p>Delete all sessions by key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key of session</p> required"},{"location":"api/jam/#jam.BaseJam.session_create","title":"session_create  <code>abstractmethod</code>","text":"<pre><code>session_create(\n    session_key: str, data: dict[str, Any]\n) -&gt; str\n</code></pre> <p>Create new session.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key for session</p> required <code>data</code> <code>dict[str, Any]</code> <p>Session data</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session ID</p>"},{"location":"api/jam/#jam.BaseJam.session_delete","title":"session_delete  <code>abstractmethod</code>","text":"<pre><code>session_delete(session_id: str) -&gt; None\n</code></pre> <p>Delete session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required"},{"location":"api/jam/#jam.BaseJam.session_get","title":"session_get  <code>abstractmethod</code>","text":"<pre><code>session_get(session_id: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get data from session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: Session data if exist</p>"},{"location":"api/jam/#jam.BaseJam.session_rework","title":"session_rework  <code>abstractmethod</code>","text":"<pre><code>session_rework(old_session_id: str) -&gt; str\n</code></pre> <p>Rework session.</p> <p>Parameters:</p> Name Type Description Default <code>old_session_id</code> <code>str</code> <p>Old session id</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session id</p>"},{"location":"api/jam/#jam.BaseJam.session_update","title":"session_update  <code>abstractmethod</code>","text":"<pre><code>session_update(\n    session_id: str, data: dict[str, Any]\n) -&gt; None\n</code></pre> <p>Update session data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <code>data</code> <code>dict[str, Any]</code> <p>New data</p> required"},{"location":"api/jam/#jam.Jam","title":"Jam","text":"<pre><code>Jam(\n    config: str | dict[str, Any] = \"pyproject.toml\",\n    pointer: str = \"jam\",\n    *,\n    logger: type[BaseLogger] = JamLogger,\n    log_level: Literal[\n        \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n    ] = \"INFO\",\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    plugins: list[type[BasePlugin]] = [],\n)\n</code></pre> <p>               Bases: <code>BaseJam</code></p> <p>Main instance.</p> <p>Methods:</p> Name Description <code>jwe_decrypt</code> <p>Decrypt JWE token.</p> <code>jwe_encrypt</code> <p>Encrypt data using JWE.</p> <code>jws_sign</code> <p>Sign data using JWS.</p> <code>jws_verify</code> <p>Verify JWS token.</p> <code>jwt_create</code> <p>Create JWT token.</p> <code>jwt_decode</code> <p>Verify and decode JWT token.</p> <code>jwt_encode</code> <p>Encode the JWT with the given expire, header, and payload.</p> <code>jwt_make_payload</code> <p>Make JWT-specific payload.</p> <code>oauth2_client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>oauth2_fetch_token</code> <p>Exchange authorization code for access token.</p> <code>oauth2_get_authorized_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>oauth2_refresh_token</code> <p>Use refresh token to obtain a new access token.</p> <code>otp_code</code> <p>Generates an OTP.</p> <code>otp_uri</code> <p>Generates an otpauth:// URI for Google Authenticator.</p> <code>otp_verify_code</code> <p>Checks the OTP code, taking into account the acceptable window.</p> <code>paseto_create</code> <p>Create new PASETO.</p> <code>paseto_decode</code> <p>Decode PASETO.</p> <code>paseto_make_payload</code> <p>Generate payload for PASETO.</p> <code>session_clear</code> <p>Delete all sessions by key.</p> <code>session_create</code> <p>Create new session.</p> <code>session_delete</code> <p>Delete session.</p> <code>session_get</code> <p>Get data from session.</p> <code>session_rework</code> <p>Rework session.</p> <code>session_update</code> <p>Update session data.</p>"},{"location":"api/jam/#jam.Jam.jwe_decrypt","title":"jwe_decrypt","text":"<pre><code>jwe_decrypt(token: str) -&gt; bytes\n</code></pre> <p>Decrypt JWE token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWE token.</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>Decrypted data.</p>"},{"location":"api/jam/#jam.Jam.jwe_encrypt","title":"jwe_encrypt","text":"<pre><code>jwe_encrypt(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encrypt data using JWE.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to encrypt. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWE header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWE token.</p>"},{"location":"api/jam/#jam.Jam.jws_sign","title":"jws_sign","text":"<pre><code>jws_sign(\n    data: dict[str, Any] | str,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Sign data using JWS.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any] | str</code> <p>Data to sign. If dict, will be JSON encoded.</p> required <code>header</code> <code>dict[str, Any] | None</code> <p>JWS header.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>JWS token.</p>"},{"location":"api/jam/#jam.Jam.jws_verify","title":"jws_verify","text":"<pre><code>jws_verify(token: str) -&gt; dict[str, Any]\n</code></pre> <p>Verify JWS token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWS token.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload.</p>"},{"location":"api/jam/#jam.Jam.jwt_create","title":"jwt_create","text":"<pre><code>jwt_create(payload: dict[str, Any]) -&gt; str\n</code></pre> <p>Create JWT token.</p> <p>Deprecated</p> <pre><code>Use Jam.jwt_encode\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Data payload</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New token</p>"},{"location":"api/jam/#jam.Jam.jwt_decode","title":"jwt_decode","text":"<pre><code>jwt_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n    check_nbf: bool = False,\n    include_headers: bool = False,\n) -&gt; dict[str, Any]\n</code></pre> <p>Verify and decode JWT token.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>JWT token</p> required <code>check_exp</code> <code>bool</code> <p>Check expire</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check white/black list. Docs: https://jam.makridenko.ru/jwt/lists/what/</p> <code>True</code> <code>check_nbf</code> <code>bool</code> <p>Check not-before time</p> <code>False</code> <code>include_headers</code> <code>bool</code> <p>Include headers in the decoded payload</p> <code>False</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Decoded payload</p> <p>Raises:</p> Type Description <code>JamJWTExpired</code> <p>If token is expired</p> <code>JamJWTNotYetValid</code> <p>If token is not yet valid (nbf claim)</p> <code>JamConfigurationError</code> <p>If JWT list is not connected</p> <code>JamJWTNotInWhiteList</code> <p>If token is not in white list</p> <code>JamJWTInBlackList</code> <p>If token is in black list</p>"},{"location":"api/jam/#jam.Jam.jwt_encode","title":"jwt_encode","text":"<pre><code>jwt_encode(\n    iss: str | None = None,\n    sub: str | None = None,\n    aud: str | None = None,\n    exp: int | None = None,\n    nbf: int | None = None,\n    jti: str | None = None,\n    *,\n    payload: dict[str, Any] | None = None,\n    header: dict[str, Any] | None = None,\n) -&gt; str\n</code></pre> <p>Encode the JWT with the given expire, header, and payload.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>The expiration time in seconds.</p> <code>None</code> <code>nbf</code> <code>int | None</code> <p>The not-before time in seconds.</p> <code>None</code> <code>iss</code> <code>str | None</code> <p>The issuer.</p> <code>None</code> <code>sub</code> <code>str | None</code> <p>The subject.</p> <code>None</code> <code>aud</code> <code>str | None</code> <p>The audience.</p> <code>None</code> <code>jti</code> <code>str | None</code> <p>The JWT ID. If none use the JTI fabric function.</p> <code>None</code> <code>header</code> <code>dict[str, Any] | None</code> <p>The header to include in the JWT.</p> <code>None</code> <code>payload</code> <code>dict[str, Any] | None</code> <p>The payload to include in the JWT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encoded JWT.</p>"},{"location":"api/jam/#jam.Jam.jwt_make_payload","title":"jwt_make_payload","text":"<pre><code>jwt_make_payload(\n    exp: int | None, data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Make JWT-specific payload.</p> <p>Deprecated</p> <pre><code>This method is deprecated; the JWT payload is generated automatically in accordance with the specification.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Token expire</p> required <code>data</code> <code>dict[str, Any]</code> <p>Data to payload</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Payload</p>"},{"location":"api/jam/#jam.Jam.oauth2_client_credentials_flow","title":"oauth2_client_credentials_flow","text":"<pre><code>oauth2_client_credentials_flow(\n    provider: str,\n    scope: list[str] | None = None,\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>OAuth2 provider</p> required <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Raises:</p> Type Description <code>JamOAuth2EmptyRaw</code> <p>If response is empty</p> <code>JamOAuth2Error</code> <p>HTTP error</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam/#jam.Jam.oauth2_fetch_token","title":"oauth2_fetch_token","text":"<pre><code>oauth2_fetch_token(\n    provider: str,\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam/#jam.Jam.oauth2_get_authorized_url","title":"oauth2_get_authorized_url","text":"<pre><code>oauth2_get_authorized_url(\n    provider: str, scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra ath params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam/#jam.Jam.oauth2_refresh_token","title":"oauth2_refresh_token","text":"<pre><code>oauth2_refresh_token(\n    provider: str,\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name</p> required <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam/#jam.Jam.otp_code","title":"otp_code","text":"<pre><code>otp_code(\n    secret: str | bytes, factor: int | None = None\n) -&gt; str\n</code></pre> <p>Generates an OTP.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>OTP code (fixed-length string).</p>"},{"location":"api/jam/#jam.Jam.otp_uri","title":"otp_uri","text":"<pre><code>otp_uri(\n    secret: str,\n    name: str,\n    issuer: str,\n    counter: int | None = None,\n) -&gt; str\n</code></pre> <p>Generates an otpauth:// URI for Google Authenticator.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str</code> <p>User secret key.</p> required <code>name</code> <code>str</code> <p>Account name (e.g., email).</p> required <code>issuer</code> <code>str</code> <p>Service name (e.g., \"GitHub\").</p> required <code>counter</code> <code>int | None</code> <p>Counter (for HOTP). Default is None.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A string of the form \"otpauth://...\"</p>"},{"location":"api/jam/#jam.Jam.otp_verify_code","title":"otp_verify_code","text":"<pre><code>otp_verify_code(\n    secret: str | bytes,\n    code: str,\n    factor: int | None = None,\n    look_ahead: int | None = 1,\n) -&gt; bool\n</code></pre> <p>Checks the OTP code, taking into account the acceptable window.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>str | bytes</code> <p>User secret key.</p> required <code>code</code> <code>str</code> <p>The code entered.</p> required <code>factor</code> <code>int | None</code> <p>Unixtime for TOTP(if none, use now time) / Counter for HOTP.</p> <code>None</code> <code>look_ahead</code> <code>int</code> <p>Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is 1.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the code matches, otherwise False.</p>"},{"location":"api/jam/#jam.Jam.paseto_create","title":"paseto_create","text":"<pre><code>paseto_create(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None,\n) -&gt; str\n</code></pre> <p>Create new PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload</p> required <code>footer</code> <code>dict | str | None</code> <p>Footer</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New token</p>"},{"location":"api/jam/#jam.Jam.paseto_decode","title":"paseto_decode","text":"<pre><code>paseto_decode(\n    token: str,\n    check_exp: bool = True,\n    check_list: bool = True,\n) -&gt; dict[str, dict[str, Any] | str | None]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Token</p> required <code>check_exp</code> <code>bool</code> <p>Check exp in payload</p> <code>True</code> <code>check_list</code> <code>bool</code> <p>Check token in list</p> <code>True</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, dict[str, Any] | str | None]</code> <p>{'payload' PAYLOAD, 'footer': FOOTER}</p>"},{"location":"api/jam/#jam.Jam.paseto_make_payload","title":"paseto_make_payload","text":"<pre><code>paseto_make_payload(\n    exp: int | None = None, **data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Generate payload for PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>exp</code> <code>int | None</code> <p>Token expire</p> <code>None</code> <code>data</code> <code>dict[str, Any]</code> <p>Custom data</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Payload</p>"},{"location":"api/jam/#jam.Jam.session_clear","title":"session_clear","text":"<pre><code>session_clear(session_key: str) -&gt; None\n</code></pre> <p>Delete all sessions by key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key of session</p> required"},{"location":"api/jam/#jam.Jam.session_create","title":"session_create","text":"<pre><code>session_create(\n    session_key: str, data: dict[str, Any]\n) -&gt; str\n</code></pre> <p>Create new session.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>Key for session</p> required <code>data</code> <code>dict[str, Any]</code> <p>Session data</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session ID</p>"},{"location":"api/jam/#jam.Jam.session_delete","title":"session_delete","text":"<pre><code>session_delete(session_id: str) -&gt; None\n</code></pre> <p>Delete session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required"},{"location":"api/jam/#jam.Jam.session_get","title":"session_get","text":"<pre><code>session_get(session_id: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Get data from session.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict[str, Any] | None: Session data if exist</p>"},{"location":"api/jam/#jam.Jam.session_rework","title":"session_rework","text":"<pre><code>session_rework(old_session_id: str) -&gt; str\n</code></pre> <p>Rework session.</p> <p>Parameters:</p> Name Type Description Default <code>old_session_id</code> <code>str</code> <p>Old session id</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If session with given ID does not exist.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>New session id</p>"},{"location":"api/jam/#jam.Jam.session_update","title":"session_update","text":"<pre><code>session_update(\n    session_id: str, data: dict[str, Any]\n) -&gt; None\n</code></pre> <p>Update session data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Session ID</p> required <code>data</code> <code>dict[str, Any]</code> <p>New data</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If session with given ID does not exist.</p>"},{"location":"api/jam.oauth2.__base__/","title":"jam.oauth2.base","text":""},{"location":"api/jam.oauth2.__base__/#jam.oauth2.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseOAuth2Client</code> <p>Base OAuth2 client instance.</p>"},{"location":"api/jam.oauth2.__base__/#jam.oauth2.__base__.BaseOAuth2Client","title":"BaseOAuth2Client","text":"<pre><code>BaseOAuth2Client(\n    client_id: str,\n    client_secret: str,\n    auth_url: str,\n    token_url: str,\n    redirect_url: str,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base OAuth2 client instance.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>ID or your client</p> required <code>client_secret</code> <code>str</code> <p>Secret key for your application</p> required <code>auth_url</code> <code>str</code> <p>App auth url</p> required <code>token_url</code> <code>str</code> <p>App token url</p> required <code>redirect_url</code> <code>str</code> <p>Your app url</p> required <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder. Defaults to JsonEncoder.</p> <code>JsonEncoder</code> <p>Methods:</p> Name Description <code>client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>fetch_token</code> <p>Exchange code for access token.</p> <code>get_authorization_url</code> <p>Get OAuth2 url.</p> <code>refresh_token</code> <p>Update access token.</p>"},{"location":"api/jam.oauth2.__base__/#jam.oauth2.__base__.BaseOAuth2Client.client_credentials_flow","title":"client_credentials_flow  <code>abstractmethod</code>","text":"<pre><code>client_credentials_flow(\n    scope: list[str] | None = None,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>Optional[list[str]]</code> <p>Auth scope</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.oauth2.__base__/#jam.oauth2.__base__.BaseOAuth2Client.fetch_token","title":"fetch_token  <code>abstractmethod</code>","text":"<pre><code>fetch_token(\n    code: str, grant_type: str = \"authorization_code\"\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Auth code</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'authorization_code'</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token response</p>"},{"location":"api/jam.oauth2.__base__/#jam.oauth2.__base__.BaseOAuth2Client.get_authorization_url","title":"get_authorization_url  <code>abstractmethod</code>","text":"<pre><code>get_authorization_url(scope: list[str]) -&gt; str\n</code></pre> <p>Get OAuth2 url.</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>URL for auth</p>"},{"location":"api/jam.oauth2.__base__/#jam.oauth2.__base__.BaseOAuth2Client.refresh_token","title":"refresh_token  <code>abstractmethod</code>","text":"<pre><code>refresh_token(\n    refresh_token: str, grant_type: str = \"refresh_token\"\n) -&gt; dict[str, Any]\n</code></pre> <p>Update access token.</p> <p>Parameters:</p> Name Type Description Default <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>New token response</p>"},{"location":"api/jam.oauth2.__base__/#jam.oauth2.__base__.__BaseOAuth2Server","title":"__BaseOAuth2Server","text":"<pre><code>__BaseOAuth2Server(\n    app_url: str,\n    code_factory: Callable[[], str] = lambda: token_urlsafe(\n        8\n    ),\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base OAuth2 server instance.</p> <p>Parameters:</p> Name Type Description Default <code>app_url</code> <code>str</code> <p>URL of your app</p> required <code>code_factory</code> <code>Callable[[], str]</code> <p>Factory for code generation</p> <code>lambda: token_urlsafe(8)</code>"},{"location":"api/jam.oauth2.builtin.github/","title":"jam.oauth2.builtin.github","text":""},{"location":"api/jam.oauth2.builtin.github/#jam.oauth2.builtin.github","title":"github","text":"<p>Classes:</p> Name Description <code>GitHubOAuth2Client</code> <p>Rady to use GitHub OAuth2 provider.</p>"},{"location":"api/jam.oauth2.builtin.github/#jam.oauth2.builtin.github.GitHubOAuth2Client","title":"GitHubOAuth2Client","text":"<pre><code>GitHubOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Rady to use GitHub OAuth2 provider.</p> <p>See: https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>ID of your app</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>URL for your app</p> required"},{"location":"api/jam.oauth2.builtin.gitlab/","title":"jam.oauth2.builtin.gitlab","text":""},{"location":"api/jam.oauth2.builtin.gitlab/#jam.oauth2.builtin.gitlab","title":"gitlab","text":"<p>Classes:</p> Name Description <code>GitLabOAuth2Client</code> <p>Ready to use GitLab OAuth2 provider.</p>"},{"location":"api/jam.oauth2.builtin.gitlab/#jam.oauth2.builtin.gitlab.GitLabOAuth2Client","title":"GitLabOAuth2Client","text":"<pre><code>GitLabOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Ready to use GitLab OAuth2 provider.</p> <p>See: https://docs.gitlab.com/api/oauth2/</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>Your app url</p> required"},{"location":"api/jam.oauth2.builtin.google/","title":"jam.oauth2.builtin.google","text":""},{"location":"api/jam.oauth2.builtin.google/#jam.oauth2.builtin.google","title":"google","text":"<p>Classes:</p> Name Description <code>GoogleOAuth2Client</code> <p>Ready to use Google OAuth2 provider.</p>"},{"location":"api/jam.oauth2.builtin.google/#jam.oauth2.builtin.google.GoogleOAuth2Client","title":"GoogleOAuth2Client","text":"<pre><code>GoogleOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Ready to use Google OAuth2 provider.</p> <p>See: https://developers.google.com/identity/protocols/oauth2</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>URL for your app</p> required"},{"location":"api/jam.oauth2.builtin/","title":"jam.oauth2.builtin","text":""},{"location":"api/jam.oauth2.builtin/#jam.oauth2.builtin","title":"builtin","text":"<p>Built-in OAuth2 providers.</p> <p>Modules:</p> Name Description <code>github</code> <code>gitlab</code> <code>google</code> <code>yandex</code>"},{"location":"api/jam.oauth2.builtin.yandex/","title":"jam.oauth2.builtin.yandex","text":""},{"location":"api/jam.oauth2.builtin.yandex/#jam.oauth2.builtin.yandex","title":"yandex","text":"<p>Classes:</p> Name Description <code>YandexOAuth2Client</code> <p>Ready to use yandex oauth2 client.</p>"},{"location":"api/jam.oauth2.builtin.yandex/#jam.oauth2.builtin.yandex.YandexOAuth2Client","title":"YandexOAuth2Client","text":"<pre><code>YandexOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Ready to use yandex oauth2 client.</p> <p>See: https://yandex.ru/dev/id/doc</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>Your app URL</p> required"},{"location":"api/jam.oauth2.client/","title":"jam.oauth2.client","text":""},{"location":"api/jam.oauth2.client/#jam.oauth2.client","title":"client","text":"<p>Classes:</p> Name Description <code>OAuth2Client</code> <p>Universal OAuth2 client implementation.</p>"},{"location":"api/jam.oauth2.client/#jam.oauth2.client.OAuth2Client","title":"OAuth2Client","text":"<pre><code>OAuth2Client(\n    client_id: str,\n    client_secret: str,\n    auth_url: str,\n    token_url: str,\n    redirect_url: str,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n)\n</code></pre> <p>               Bases: <code>BaseOAuth2Client</code></p> <p>Universal OAuth2 client implementation.</p> <p>Methods:</p> Name Description <code>client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>fetch_token</code> <p>Exchange authorization code for access token.</p> <code>get_authorization_url</code> <p>Generate full OAuth2 authorization URL.</p> <code>refresh_token</code> <p>Use refresh token to obtain a new access token.</p>"},{"location":"api/jam.oauth2.client/#jam.oauth2.client.OAuth2Client.__http","title":"__http","text":"<pre><code>__http(url: str)\n</code></pre> <p>Create HTTPS connection context manager.</p>"},{"location":"api/jam.oauth2.client/#jam.oauth2.client.OAuth2Client.__post_form","title":"__post_form","text":"<pre><code>__post_form(\n    url: str, params: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Send POST form and parse JSON response.</p>"},{"location":"api/jam.oauth2.client/#jam.oauth2.client.OAuth2Client.client_credentials_flow","title":"client_credentials_flow","text":"<pre><code>client_credentials_flow(\n    scope: list[str] | None = None, **extra_params: Any\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str] | None</code> <p>Auth scope</p> <code>None</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Raises:</p> Type Description <code>JamOAuth2EmptyRaw</code> <p>If response is empty</p> <code>JamOAuth2Error</code> <p>HTTP error</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.oauth2.client/#jam.oauth2.client.OAuth2Client.fetch_token","title":"fetch_token","text":"<pre><code>fetch_token(\n    code: str,\n    grant_type: str = \"authorization_code\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange authorization code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>OAuth2 code</p> required <code>grant_type</code> <code>str</code> <p>Type of oauth2 grant</p> <code>'authorization_code'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token</p>"},{"location":"api/jam.oauth2.client/#jam.oauth2.client.OAuth2Client.get_authorization_url","title":"get_authorization_url","text":"<pre><code>get_authorization_url(\n    scope: list[str], **extra_params: Any\n) -&gt; str\n</code></pre> <p>Generate full OAuth2 authorization URL.</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <code>extra_params</code> <code>Any</code> <p>Extra ath params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Authorization url</p>"},{"location":"api/jam.oauth2.client/#jam.oauth2.client.OAuth2Client.refresh_token","title":"refresh_token","text":"<pre><code>refresh_token(\n    refresh_token: str,\n    grant_type: str = \"refresh_token\",\n    **extra_params: Any,\n) -&gt; dict[str, Any]\n</code></pre> <p>Use refresh token to obtain a new access token.</p> <p>Parameters:</p> Name Type Description Default <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <code>extra_params</code> <code>Any</code> <p>Extra auth params if needed</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Refresh token</p>"},{"location":"api/jam.oauth2/","title":"jam.oauth2","text":""},{"location":"api/jam.oauth2/#jam.oauth2","title":"oauth2","text":"<p>OAuth2 module.</p> <p>Modules:</p> Name Description <code>builtin</code> <p>Built-in OAuth2 providers.</p> <code>client</code> <p>Classes:</p> Name Description <code>BaseOAuth2Client</code> <p>Base OAuth2 client instance.</p> <code>GitHubOAuth2Client</code> <p>Rady to use GitHub OAuth2 provider.</p> <code>GitLabOAuth2Client</code> <p>Ready to use GitLab OAuth2 provider.</p> <code>GoogleOAuth2Client</code> <p>Ready to use Google OAuth2 provider.</p> <code>YandexOAuth2Client</code> <p>Ready to use yandex oauth2 client.</p> <p>Functions:</p> Name Description <code>create_instance</code> <p>Create OAuth2 clients for configured providers.</p>"},{"location":"api/jam.oauth2/#jam.oauth2.BaseOAuth2Client","title":"BaseOAuth2Client","text":"<pre><code>BaseOAuth2Client(\n    client_id: str,\n    client_secret: str,\n    auth_url: str,\n    token_url: str,\n    redirect_url: str,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base OAuth2 client instance.</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>ID or your client</p> required <code>client_secret</code> <code>str</code> <p>Secret key for your application</p> required <code>auth_url</code> <code>str</code> <p>App auth url</p> required <code>token_url</code> <code>str</code> <p>App token url</p> required <code>redirect_url</code> <code>str</code> <p>Your app url</p> required <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder. Defaults to JsonEncoder.</p> <code>JsonEncoder</code> <p>Methods:</p> Name Description <code>client_credentials_flow</code> <p>Obtain access token using client credentials flow (no user interaction).</p> <code>fetch_token</code> <p>Exchange code for access token.</p> <code>get_authorization_url</code> <p>Get OAuth2 url.</p> <code>refresh_token</code> <p>Update access token.</p>"},{"location":"api/jam.oauth2/#jam.oauth2.BaseOAuth2Client.client_credentials_flow","title":"client_credentials_flow  <code>abstractmethod</code>","text":"<pre><code>client_credentials_flow(\n    scope: list[str] | None = None,\n) -&gt; dict[str, Any]\n</code></pre> <p>Obtain access token using client credentials flow (no user interaction).</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>Optional[list[str]]</code> <p>Auth scope</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>JSON with access token</p>"},{"location":"api/jam.oauth2/#jam.oauth2.BaseOAuth2Client.fetch_token","title":"fetch_token  <code>abstractmethod</code>","text":"<pre><code>fetch_token(\n    code: str, grant_type: str = \"authorization_code\"\n) -&gt; dict[str, Any]\n</code></pre> <p>Exchange code for access token.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Auth code</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'authorization_code'</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>OAuth2 token response</p>"},{"location":"api/jam.oauth2/#jam.oauth2.BaseOAuth2Client.get_authorization_url","title":"get_authorization_url  <code>abstractmethod</code>","text":"<pre><code>get_authorization_url(scope: list[str]) -&gt; str\n</code></pre> <p>Get OAuth2 url.</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>list[str]</code> <p>Auth scope</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>URL for auth</p>"},{"location":"api/jam.oauth2/#jam.oauth2.BaseOAuth2Client.refresh_token","title":"refresh_token  <code>abstractmethod</code>","text":"<pre><code>refresh_token(\n    refresh_token: str, grant_type: str = \"refresh_token\"\n) -&gt; dict[str, Any]\n</code></pre> <p>Update access token.</p> <p>Parameters:</p> Name Type Description Default <code>refresh_token</code> <code>str</code> <p>Refresh token</p> required <code>grant_type</code> <code>str</code> <p>Grant type</p> <code>'refresh_token'</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>New token response</p>"},{"location":"api/jam.oauth2/#jam.oauth2.GitHubOAuth2Client","title":"GitHubOAuth2Client","text":"<pre><code>GitHubOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Rady to use GitHub OAuth2 provider.</p> <p>See: https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>ID of your app</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>URL for your app</p> required"},{"location":"api/jam.oauth2/#jam.oauth2.GitLabOAuth2Client","title":"GitLabOAuth2Client","text":"<pre><code>GitLabOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Ready to use GitLab OAuth2 provider.</p> <p>See: https://docs.gitlab.com/api/oauth2/</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>Your app url</p> required"},{"location":"api/jam.oauth2/#jam.oauth2.GoogleOAuth2Client","title":"GoogleOAuth2Client","text":"<pre><code>GoogleOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Ready to use Google OAuth2 provider.</p> <p>See: https://developers.google.com/identity/protocols/oauth2</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>URL for your app</p> required"},{"location":"api/jam.oauth2/#jam.oauth2.YandexOAuth2Client","title":"YandexOAuth2Client","text":"<pre><code>YandexOAuth2Client(\n    client_id: str, client_secret: str, redirect_url: str\n)\n</code></pre> <p>               Bases: <code>OAuth2Client</code></p> <p>Ready to use yandex oauth2 client.</p> <p>See: https://yandex.ru/dev/id/doc</p> <p>Parameters:</p> Name Type Description Default <code>client_id</code> <code>str</code> <p>Client ID</p> required <code>client_secret</code> <code>str</code> <p>Secret key</p> required <code>redirect_url</code> <code>str</code> <p>Your app URL</p> required"},{"location":"api/jam.oauth2/#jam.oauth2.create_instance","title":"create_instance","text":"<pre><code>create_instance(\n    providers: dict[str, dict],\n    logger: BaseLogger = logger,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    **kwargs: Any,\n) -&gt; dict[str, BaseOAuth2Client]\n</code></pre> <p>Create OAuth2 clients for configured providers.</p> <p>Parameters:</p> Name Type Description Default <code>providers</code> <code>dict[str, dict]</code> <p>{provider_name: {client_id, client_secret, redirect_uri, ...}}</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>JSON encoder/decoder</p> <code>JsonEncoder</code> <code>**kwargs</code> <code>Any</code> <p>Additional params</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, BaseOAuth2Client]</code> <p>{provider_name: OAuth2Client instance}</p>"},{"location":"api/jam.otp.__base__/","title":"jam.otp.base","text":""},{"location":"api/jam.otp.__base__/#jam.otp.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseOTP</code> <p>Base *OTP.</p> <code>OTPConfig</code> <p>Config for Jam instance.</p>"},{"location":"api/jam.otp.__base__/#jam.otp.__base__.BaseOTP","title":"BaseOTP","text":"<pre><code>BaseOTP(\n    secret: bytes | str,\n    digits: int = 6,\n    digest: Literal[\"sha1\", \"sha256\", \"sha512\"] = \"sha1\",\n)\n</code></pre> <p>Base *OTP.</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>bytes | str</code> <p>Secret key(str or Byte32).</p> required <code>digits</code> <code>int</code> <p>Number of digits in the code. Default is 6.</p> <code>6</code> <code>digest</code> <code>str</code> <p>Hash algorithm (sha1, sha256, sha512). Default is <code>sha1</code>.</p> <code>'sha1'</code> <p>Methods:</p> Name Description <code>at</code> <p>Generate OTP code.</p> <code>provisioning_uri</code> <p>Generates an otpauth:// URI for Google Authenticator.</p> <code>verify</code> <p>Verify OTP code.</p>"},{"location":"api/jam.otp.__base__/#jam.otp.__base__.BaseOTP.at","title":"at","text":"<pre><code>at(factor: int | None = None) -&gt; str\n</code></pre> <p>Generate OTP code.</p> <p>Parameters:</p> Name Type Description Default <code>factor</code> <code>int | None</code> <p>TOTP uses current time if None, HOTP uses counter.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>OTP code</p>"},{"location":"api/jam.otp.__base__/#jam.otp.__base__.BaseOTP.provisioning_uri","title":"provisioning_uri","text":"<pre><code>provisioning_uri(\n    name: str,\n    issuer: str,\n    type_: str = \"totp\",\n    counter: int | None = None,\n) -&gt; str\n</code></pre> <p>Generates an otpauth:// URI for Google Authenticator.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Account name (e.g., email).</p> required <code>issuer</code> <code>str</code> <p>Service name (e.g., \"GitHub\").</p> required <code>type_</code> <code>str</code> <p>OTP type (\u201ctotp\u201d or 'hotp'). Default is \"totp\".</p> <code>'totp'</code> <code>counter</code> <code>int | None</code> <p>Counter (for HOTP). Default is None.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A string of the form \"otpauth://...\"</p>"},{"location":"api/jam.otp.__base__/#jam.otp.__base__.BaseOTP.verify","title":"verify","text":"<pre><code>verify(\n    code: str,\n    factor: int | None = None,\n    look_ahead: int = 1,\n) -&gt; bool\n</code></pre> <p>Verify OTP code.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Code to verify</p> required <code>factor</code> <code>int | None</code> <p>TOTP uses current time if None, HOTP uses counter.</p> <code>None</code> <code>look_ahead</code> <code>int</code> <p>How many intervals to check ahead</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if valid</p>"},{"location":"api/jam.otp.__base__/#jam.otp.__base__.OTPConfig","title":"OTPConfig  <code>dataclass</code>","text":"<pre><code>OTPConfig(\n    type: str,\n    digits: int,\n    digest: Literal[\"sha1\", \"sha256\", \"sha512\"],\n    interval: int = 30,\n)\n</code></pre> <p>Config for Jam instance.</p>"},{"location":"api/jam.otp.hotp/","title":"jam.otp.hotp","text":""},{"location":"api/jam.otp.hotp/#jam.otp.hotp","title":"hotp","text":"<p>Classes:</p> Name Description <code>HOTP</code> <p>HOTP instance.</p>"},{"location":"api/jam.otp.hotp/#jam.otp.hotp.HOTP","title":"HOTP","text":"<pre><code>HOTP(\n    secret: bytes | str,\n    digits: int = 6,\n    digest: Literal[\"sha1\", \"sha256\", \"sha512\"] = \"sha1\",\n)\n</code></pre> <p>               Bases: <code>BaseOTP</code></p> <p>HOTP instance.</p> <p>Methods:</p> Name Description <code>at</code> <p>Generates a HOTP code for the specified counter.</p> <code>verify</code> <p>Verify HOTP-code.</p>"},{"location":"api/jam.otp.hotp/#jam.otp.hotp.HOTP.at","title":"at","text":"<pre><code>at(factor: int) -&gt; str\n</code></pre> <p>Generates a HOTP code for the specified counter.</p> <p>Parameters:</p> Name Type Description Default <code>factor</code> <code>int</code> <p>Counter (increases after each use).</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>HOTP code (fixed-length string).</p>"},{"location":"api/jam.otp.hotp/#jam.otp.hotp.HOTP.verify","title":"verify","text":"<pre><code>verify(code: str, factor: int, look_ahead: int = 1) -&gt; bool\n</code></pre> <p>Verify HOTP-code.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>Code.</p> required <code>factor</code> <code>int</code> <p>Now counter.</p> required <code>look_ahead</code> <code>int</code> <p>Allowable forward offset (to compensate for desynchronization). Default is 1..</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the code matches, otherwise False.</p>"},{"location":"api/jam.otp/","title":"jam.otp","text":""},{"location":"api/jam.otp/#jam.otp","title":"otp","text":"<p>*OTP auth module</p> <p>Modules:</p> Name Description <code>__base__</code> <code>hotp</code> <code>totp</code> <p>Functions:</p> Name Description <code>create_instance</code> <p>Create OTP class (not instance, since secret is provided per-call).</p>"},{"location":"api/jam.otp/#jam.otp.create_instance","title":"create_instance","text":"<pre><code>create_instance(\n    type: Literal[\"hotp\", \"totp\"], **kwargs: Any\n) -&gt; type[BaseOTP]\n</code></pre> <p>Create OTP class (not instance, since secret is provided per-call).</p> <p>Parameters:</p> Name Type Description Default <code>type</code> <code>str</code> <p>\"hotp\" | \"totp\"</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> required <code>**kwargs</code> <code>Any</code> <p>digits, digest, interval (for TOTP), custom_module</p> <code>{}</code> <p>Returns:</p> Type Description <code>type[BaseOTP]</code> <p>HOTP or TOTP class</p>"},{"location":"api/jam.otp.totp/","title":"jam.otp.totp","text":""},{"location":"api/jam.otp.totp/#jam.otp.totp","title":"totp","text":"<p>Classes:</p> Name Description <code>TOTP</code> <p>TOTP (Time-based One-Time Password, RFC6238).</p>"},{"location":"api/jam.otp.totp/#jam.otp.totp.TOTP","title":"TOTP","text":"<pre><code>TOTP(\n    secret: bytes | str,\n    digits: int = 6,\n    digest: Literal[\"sha1\", \"sha256\", \"sha512\"] = \"sha1\",\n    interval: int = 30,\n)\n</code></pre> <p>               Bases: <code>BaseOTP</code></p> <p>TOTP (Time-based One-Time Password, RFC6238).</p> <p>Parameters:</p> Name Type Description Default <code>secret</code> <code>bytes | str</code> <p>Secret key.</p> required <code>digits</code> <code>int</code> <p>Number of digits in the code. Default is 6.</p> <code>6</code> <code>digest</code> <code>str</code> <p>Hashing algorithm. Default is \"sha1\".</p> <code>'sha1'</code> <code>interval</code> <code>int</code> <p>Time interval in seconds. Default is 30.</p> <code>30</code> <p>Methods:</p> Name Description <code>at</code> <p>Generates a TOTP code for a specified time.</p> <code>verify</code> <p>Checks the TOTP code, taking into account the acceptable window.</p> <p>Attributes:</p> Name Type Description <code>now</code> <code>str</code> <p>Returns the current TOTP code.</p>"},{"location":"api/jam.otp.totp/#jam.otp.totp.TOTP.now","title":"now  <code>property</code>","text":"<pre><code>now: str\n</code></pre> <p>Returns the current TOTP code.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>TOTP code for the current time.</p>"},{"location":"api/jam.otp.totp/#jam.otp.totp.TOTP.at","title":"at","text":"<pre><code>at(factor: int | None = None) -&gt; str\n</code></pre> <p>Generates a TOTP code for a specified time.</p> <p>Parameters:</p> Name Type Description Default <code>factor</code> <code>int | None</code> <p>Time in UNIX seconds. If None, the current time is used. Default is None.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>TOTP code (fixed-length string).</p>"},{"location":"api/jam.otp.totp/#jam.otp.totp.TOTP.verify","title":"verify","text":"<pre><code>verify(\n    code: str,\n    factor: int | None = None,\n    look_ahead: int = 1,\n) -&gt; bool\n</code></pre> <p>Checks the TOTP code, taking into account the acceptable window.</p> <p>Parameters:</p> Name Type Description Default <code>code</code> <code>str</code> <p>The code entered.</p> required <code>factor</code> <code>int | None</code> <p>Time in UNIX seconds. If None, the current time. Default is None.</p> <code>None</code> <code>look_ahead</code> <code>int</code> <p>Acceptable deviation in intervals (\u00b1window). Default is 1.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the code matches, otherwise False.</p>"},{"location":"api/jam.paseto.__base__/","title":"jam.paseto.base","text":""},{"location":"api/jam.paseto.__base__/#jam.paseto.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BasePASETO</code> <p>Base PASETO instance.</p>"},{"location":"api/jam.paseto.__base__/#jam.paseto.__base__.BasePASETO","title":"BasePASETO","text":"<pre><code>BasePASETO()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base PASETO instance.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode PASETO.</p> <code>encode</code> <p>Generate token from key instance.</p> <code>key</code> <p>Create a PASETO instance with the given key.</p> <code>load_rsa_key</code> <p>Load rsa key from string | bytes.</p> <p>Attributes:</p> Name Type Description <code>purpose</code> <code>Literal['local', 'public'] | None</code> <p>Return PASETO purpose.</p>"},{"location":"api/jam.paseto.__base__/#jam.paseto.__base__.BasePASETO.purpose","title":"purpose  <code>property</code>","text":"<pre><code>purpose: Literal['local', 'public'] | None\n</code></pre> <p>Return PASETO purpose.</p>"},{"location":"api/jam.paseto.__base__/#jam.paseto.__base__.BasePASETO.decode","title":"decode  <code>abstractmethod</code>","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[dict[str, Any], dict[str, Any] | None]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Token</p> required <code>serializer</code> <code>BaseEncoder</code> <p>JSON Encoder</p> <code>JsonEncoder</code> <p>Returns:</p> Type Description <code>tuple[dict[str, Any], dict[str, Any] | None]</code> <p>tuple[dict[str, Any], Optional[dict[str, Any]]]: Payload, footer</p>"},{"location":"api/jam.paseto.__base__/#jam.paseto.__base__.BasePASETO.encode","title":"encode  <code>abstractmethod</code>","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Generate token from key instance.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload for token</p> required <code>footer</code> <code>dict[str, Any] | str | None</code> <p>Token footer</p> <code>None</code> <code>serializer</code> <code>BaseEncoder</code> <p>JSON Encoder</p> <code>JsonEncoder</code>"},{"location":"api/jam.paseto.__base__/#jam.paseto.__base__.BasePASETO.key","title":"key  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str | bytes,\n) -&gt; PASETO\n</code></pre> <p>Create a PASETO instance with the given key.</p> <p>Parameters:</p> Name Type Description Default <code>purpose</code> <code>Literal['local', 'public']</code> <p>'local' (symmetric encryption) or 'public' (asymmetric signing)</p> required <code>secret_key</code> <code>str | bytes</code> <p>raw bytes or PEM text depending on purpose</p> required <p>Returns:</p> Name Type Description <code>PASETO</code> <code>PASETO</code> <p>configured PASETO instance for encoding/decoding tokens.</p>"},{"location":"api/jam.paseto.__base__/#jam.paseto.__base__.BasePASETO.load_rsa_key","title":"load_rsa_key  <code>staticmethod</code>","text":"<pre><code>load_rsa_key(\n    key: RSAKeyLike | None, *, private: bool = True\n) -&gt; Any\n</code></pre> <p>Load rsa key from string | bytes.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>RSAKeyLike | None</code> <p>RSA Key</p> required <code>private</code> <code>bool</code> <p>Private or public</p> <code>True</code> <p>Raises:</p> Type Description <code>JamPASETOInvalidRSAKey</code> <p>Invalid RSA key format.</p>"},{"location":"api/jam.paseto/","title":"jam.paseto","text":""},{"location":"api/jam.paseto/#jam.paseto","title":"paseto","text":"<p>PASETO auth* tokens.</p> <p>Modules:</p> Name Description <code>utils</code> <code>v1</code> <code>v2</code> <code>v3</code> <code>v4</code> <p>Classes:</p> Name Description <code>BasePASETO</code> <p>Base PASETO instance.</p> <code>PASETOv1</code> <p>Paseto v1 factory.</p> <code>PASETOv2</code> <p>PASETO v2 factory.</p> <code>PASETOv3</code> <p>PASETO v3 factory.</p> <code>PASETOv4</code> <p>PASETO v4 factory.</p> <p>Functions:</p> Name Description <code>create_instance</code> <p>Create PASETO instance.</p>"},{"location":"api/jam.paseto/#jam.paseto.BasePASETO","title":"BasePASETO","text":"<pre><code>BasePASETO()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base PASETO instance.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode PASETO.</p> <code>encode</code> <p>Generate token from key instance.</p> <code>key</code> <p>Create a PASETO instance with the given key.</p> <code>load_rsa_key</code> <p>Load rsa key from string | bytes.</p> <p>Attributes:</p> Name Type Description <code>purpose</code> <code>Literal['local', 'public'] | None</code> <p>Return PASETO purpose.</p>"},{"location":"api/jam.paseto/#jam.paseto.BasePASETO.purpose","title":"purpose  <code>property</code>","text":"<pre><code>purpose: Literal['local', 'public'] | None\n</code></pre> <p>Return PASETO purpose.</p>"},{"location":"api/jam.paseto/#jam.paseto.BasePASETO.decode","title":"decode  <code>abstractmethod</code>","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[dict[str, Any], dict[str, Any] | None]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>Token</p> required <code>serializer</code> <code>BaseEncoder</code> <p>JSON Encoder</p> <code>JsonEncoder</code> <p>Returns:</p> Type Description <code>tuple[dict[str, Any], dict[str, Any] | None]</code> <p>tuple[dict[str, Any], Optional[dict[str, Any]]]: Payload, footer</p>"},{"location":"api/jam.paseto/#jam.paseto.BasePASETO.encode","title":"encode  <code>abstractmethod</code>","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Generate token from key instance.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Payload for token</p> required <code>footer</code> <code>dict[str, Any] | str | None</code> <p>Token footer</p> <code>None</code> <code>serializer</code> <code>BaseEncoder</code> <p>JSON Encoder</p> <code>JsonEncoder</code>"},{"location":"api/jam.paseto/#jam.paseto.BasePASETO.key","title":"key  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str | bytes,\n) -&gt; PASETO\n</code></pre> <p>Create a PASETO instance with the given key.</p> <p>Parameters:</p> Name Type Description Default <code>purpose</code> <code>Literal['local', 'public']</code> <p>'local' (symmetric encryption) or 'public' (asymmetric signing)</p> required <code>secret_key</code> <code>str | bytes</code> <p>raw bytes or PEM text depending on purpose</p> required <p>Returns:</p> Name Type Description <code>PASETO</code> <code>PASETO</code> <p>configured PASETO instance for encoding/decoding tokens.</p>"},{"location":"api/jam.paseto/#jam.paseto.BasePASETO.load_rsa_key","title":"load_rsa_key  <code>staticmethod</code>","text":"<pre><code>load_rsa_key(\n    key: RSAKeyLike | None, *, private: bool = True\n) -&gt; Any\n</code></pre> <p>Load rsa key from string | bytes.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>RSAKeyLike | None</code> <p>RSA Key</p> required <code>private</code> <code>bool</code> <p>Private or public</p> <code>True</code> <p>Raises:</p> Type Description <code>JamPASETOInvalidRSAKey</code> <p>Invalid RSA key format.</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv1","title":"PASETOv1","text":"<pre><code>PASETOv1()\n</code></pre> <p>               Bases: <code>BasePASETO</code></p> <p>Paseto v1 factory.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode PASETO.</p> <code>encode</code> <p>Encode PASETO.</p> <code>key</code> <p>Return PASETO instance.</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv1.decode","title":"decode","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[dict[str, Any], dict[str, Any] | None]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>PASETO</p> required <code>serializer</code> <code>BaseEncoder</code> <p>Json serializer</p> <code>JsonEncoder</code>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv1.encode","title":"encode","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Encode PASETO.</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv1.key","title":"key  <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str\n    | bytes\n    | None\n    | RSAPrivateKey\n    | RSAPublicKey,\n) -&gt; PASETO\n</code></pre> <p>Return PASETO instance.</p> <p>Parameters:</p> Name Type Description Default <code>purpose</code> <code>Literal['local', 'public']</code> <p>Paseto purpose</p> required <code>secret_key</code> <code>str | bytes</code> <p>PEM or secret key</p> required <p>Raises:</p> Type Description <code>JamPASETOInvalidRSAKey</code> <p>If the key is invalid.</p> <p>Returns:</p> Name Type Description <code>PASETO</code> <code>PASETO</code> <p>Paseto instance</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv2","title":"PASETOv2","text":"<pre><code>PASETOv2()\n</code></pre> <p>               Bases: <code>BasePASETO</code></p> <p>PASETO v2 factory.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode.</p> <code>encode</code> <p>Encode.</p> <code>key</code> <p>Create PASETOv2 instance from provided key.</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv2.decode","title":"decode","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[dict[str, Any], dict[str, Any] | None]\n</code></pre> <p>Decode.</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv2.encode","title":"encode","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Encode.</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv2.key","title":"key  <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str\n    | bytes\n    | Ed25519PrivateKey\n    | Ed25519PublicKey,\n) -&gt; PASETO\n</code></pre> <p>Create PASETOv2 instance from provided key.</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv3","title":"PASETOv3","text":"<pre><code>PASETOv3()\n</code></pre> <p>               Bases: <code>BasePASETO</code></p> <p>PASETO v3 factory.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode PASETO.</p> <code>encode</code> <p>Encode PASETO.</p> <code>key</code> <p>Create PASETOv3 instance.</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv3.decode","title":"decode","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[\n    dict[str, Any], dict[str, Any] | str | bytes | None\n]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>PASETO</p> required <code>serializer</code> <code>type[BaseEncoder] | BaseEncoder]</code> <p>JSON serializer</p> <code>JsonEncoder</code> <p>Returns:</p> Type Description <code>tuple[dict[str, Any], dict[str, Any] | str | bytes | None]</code> <p>Payload and dict</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv3.encode","title":"encode","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | bytes | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Encode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Token payload</p> required <code>footer</code> <code>dict[str, Any] | None</code> <p>Footer if needed</p> <code>None</code> <code>serializer</code> <code>type[BaseEncoder] | BaseEncoder</code> <p>JSON Serializer</p> <code>JsonEncoder</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Token</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv3.key","title":"key  <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str | bytes,\n    public_key: str | bytes | None = None,\n) -&gt; PASETO\n</code></pre> <p>Create PASETOv3 instance.</p> <p>Parameters:</p> Name Type Description Default <code>purpose</code> <code>str</code> <p>\"local\" or \"public\"</p> required <code>secret_key</code> <code>str | bytes</code> <p>Private PEM</p> required <code>public_key</code> <code>str | bytes | None</code> <p>Public PEM</p> <code>None</code>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv4","title":"PASETOv4","text":"<pre><code>PASETOv4()\n</code></pre> <p>               Bases: <code>BasePASETO</code></p> <p>PASETO v4 factory.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode PASETO.</p> <code>encode</code> <p>Encode PASETO.</p> <code>key</code> <p>Create PASETO Factory.</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv4.decode","title":"decode","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[\n    dict[str, Any], dict[str, Any] | str | bytes | None\n]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>PASETO</p> required <code>serializer</code> <code>type[BaseEncoder] | BaseEncoder]</code> <p>JSON Serializer</p> <code>JsonEncoder</code> <p>Raises:</p> Type Description <code>JamPASETOInvalidTokenFormat</code> <p>If the token format is invalid.</p> <code>JamPASETOInvalidED25519Key</code> <p>If the key is not a valid ED25519 key.</p> <code>JamPASETOKeyVerificationError</code> <p>If the token signature is invalid.</p> <code>JamPASETOInvalidPurpose</code> <p>If the purpose is not \"local\" or \"public\".</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv4.encode","title":"encode","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | bytes | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Encode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>PASETO Payload</p> required <code>footer</code> <code>dict[str, Any] | str | None</code> <p>Footer if needed</p> <code>None</code> <code>serializer</code> <code>type[BaseEncoder] | BaseEncoder</code> <p>JSON Serializer</p> <code>JsonEncoder</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>PASETO</p> <p>Raises:</p> Type Description <code>JamPASETOInvalidED25519Key</code> <p>If the key is not a valid ED25519 key.</p> <code>JamPASETOInvalidPurpose</code> <p>If the purpose is not \"local\" or \"public\".</p>"},{"location":"api/jam.paseto/#jam.paseto.PASETOv4.key","title":"key  <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str | bytes | Ed25519PrivateKey,\n) -&gt; PASETO\n</code></pre> <p>Create PASETO Factory.</p> <p>Parameters:</p> Name Type Description Default <code>purpose</code> <code>Literal['local', 'public']</code> <p>PASETO type</p> required <code>secret_key</code> <code>str | bytes | Ed25519PrivateKey</code> <p>Secret or ED Private key</p> required <p>Raises:</p> Type Description <code>JamPASETOInvalidED25519Key</code> <p>If the key is not a valid ED25519 key.</p> <code>JamPASETOInvalidPurpose</code> <p>If the purpose is not \"local\" or \"public\".</p>"},{"location":"api/jam.paseto/#jam.paseto.create_instance","title":"create_instance","text":"<pre><code>create_instance(\n    version: Literal[\"v1\", \"v2\", \"v3\", \"v4\"],\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str | bytes | Any,\n    logger: BaseLogger = logger,\n    **kwargs: Any,\n) -&gt; PASETO\n</code></pre> <p>Create PASETO instance.</p> <p>Parameters:</p> Name Type Description Default <code>version</code> <code>str</code> <p>\"v1\" | \"v2\" | \"v3\" | \"v4\"</p> required <code>purpose</code> <code>str</code> <p>\"local\" | \"public\"</p> required <code>secret_key</code> <code>str | bytes | Any</code> <p>Secret or asymmetric key (can be file path)</p> required <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <code>**kwargs</code> <code>Any</code> <p>Additional params (e.g., custom_module)</p> <code>{}</code> <p>Returns:</p> Type Description <code>PASETO</code> <p>PASETO instance</p>"},{"location":"api/jam.paseto.utils/","title":"jam.paseto.utils","text":""},{"location":"api/jam.paseto.utils/#jam.paseto.utils","title":"utils","text":"<p>Functions:</p> Name Description <code>__b64url_nopad__</code> <p>Return B64 nopad.</p> <code>__gen_hash__</code> <p>Generate hash.</p> <code>__pae__</code> <p>Pre-Authentication Encoding (PAE) as per PASETO spec.</p> <code>base64url_decode</code> <p>Base64 URL-safe decoding with padding.</p> <code>base64url_encode</code> <p>Base64 URL-safe encoding without padding.</p> <code>payload_maker</code> <p>Generate PASETO payload.</p>"},{"location":"api/jam.paseto.utils/#jam.paseto.utils.__b64url_nopad__","title":"__b64url_nopad__","text":"<pre><code>__b64url_nopad__(b: bytes) -&gt; str\n</code></pre> <p>Return B64 nopad.</p>"},{"location":"api/jam.paseto.utils/#jam.paseto.utils.__gen_hash__","title":"__gen_hash__","text":"<pre><code>__gen_hash__(\n    key: bytes, msg: bytes, hash_size: int = 0\n) -&gt; bytes\n</code></pre> <p>Generate hash.</p>"},{"location":"api/jam.paseto.utils/#jam.paseto.utils.__pae__","title":"__pae__","text":"<pre><code>__pae__(pieces: list[bytes]) -&gt; bytes\n</code></pre> <p>Pre-Authentication Encoding (PAE) as per PASETO spec.</p>"},{"location":"api/jam.paseto.utils/#jam.paseto.utils.base64url_decode","title":"base64url_decode","text":"<pre><code>base64url_decode(v: str | bytes) -&gt; bytes\n</code></pre> <p>Base64 URL-safe decoding with padding.</p>"},{"location":"api/jam.paseto.utils/#jam.paseto.utils.base64url_encode","title":"base64url_encode","text":"<pre><code>base64url_encode(data: bytes | str) -&gt; bytes\n</code></pre> <p>Base64 URL-safe encoding without padding.</p>"},{"location":"api/jam.paseto.utils/#jam.paseto.utils.payload_maker","title":"payload_maker","text":"<pre><code>payload_maker(\n    expire: int | None, data: dict[str, Any]\n) -&gt; dict[str, Any]\n</code></pre> <p>Generate PASETO payload.</p> <pre><code>{\n    'iat': 1761326685.45693,\n    'exp': 1761328485.45693,\n    'pit': '52aeaf12-0825-4bc1-aa45-5ded41df2463',\n    # custom data\n    'user': 1,\n    'role': 'admin'\n}\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>expire</code> <code>int | None</code> <p>Token lifetime</p> required <code>data</code> <code>dict[str, Any]</code> <p>Custom data</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, Any]</code> <p>Payload</p>"},{"location":"api/jam.paseto.v1/","title":"jam.paseto.v1","text":""},{"location":"api/jam.paseto.v1/#jam.paseto.v1","title":"v1","text":"<p>Classes:</p> Name Description <code>PASETOv1</code> <p>Paseto v1 factory.</p>"},{"location":"api/jam.paseto.v1/#jam.paseto.v1.PASETOv1","title":"PASETOv1","text":"<pre><code>PASETOv1()\n</code></pre> <p>               Bases: <code>BasePASETO</code></p> <p>Paseto v1 factory.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode PASETO.</p> <code>encode</code> <p>Encode PASETO.</p> <code>key</code> <p>Return PASETO instance.</p>"},{"location":"api/jam.paseto.v1/#jam.paseto.v1.PASETOv1.decode","title":"decode","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[dict[str, Any], dict[str, Any] | None]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>PASETO</p> required <code>serializer</code> <code>BaseEncoder</code> <p>Json serializer</p> <code>JsonEncoder</code>"},{"location":"api/jam.paseto.v1/#jam.paseto.v1.PASETOv1.encode","title":"encode","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Encode PASETO.</p>"},{"location":"api/jam.paseto.v1/#jam.paseto.v1.PASETOv1.key","title":"key  <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str\n    | bytes\n    | None\n    | RSAPrivateKey\n    | RSAPublicKey,\n) -&gt; PASETO\n</code></pre> <p>Return PASETO instance.</p> <p>Parameters:</p> Name Type Description Default <code>purpose</code> <code>Literal['local', 'public']</code> <p>Paseto purpose</p> required <code>secret_key</code> <code>str | bytes</code> <p>PEM or secret key</p> required <p>Raises:</p> Type Description <code>JamPASETOInvalidRSAKey</code> <p>If the key is invalid.</p> <p>Returns:</p> Name Type Description <code>PASETO</code> <code>PASETO</code> <p>Paseto instance</p>"},{"location":"api/jam.paseto.v2/","title":"jam.paseto.v2","text":""},{"location":"api/jam.paseto.v2/#jam.paseto.v2","title":"v2","text":"<p>Classes:</p> Name Description <code>PASETOv2</code> <p>PASETO v2 factory.</p>"},{"location":"api/jam.paseto.v2/#jam.paseto.v2.PASETOv2","title":"PASETOv2","text":"<pre><code>PASETOv2()\n</code></pre> <p>               Bases: <code>BasePASETO</code></p> <p>PASETO v2 factory.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode.</p> <code>encode</code> <p>Encode.</p> <code>key</code> <p>Create PASETOv2 instance from provided key.</p>"},{"location":"api/jam.paseto.v2/#jam.paseto.v2.PASETOv2.decode","title":"decode","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[dict[str, Any], dict[str, Any] | None]\n</code></pre> <p>Decode.</p>"},{"location":"api/jam.paseto.v2/#jam.paseto.v2.PASETOv2.encode","title":"encode","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Encode.</p>"},{"location":"api/jam.paseto.v2/#jam.paseto.v2.PASETOv2.key","title":"key  <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str\n    | bytes\n    | Ed25519PrivateKey\n    | Ed25519PublicKey,\n) -&gt; PASETO\n</code></pre> <p>Create PASETOv2 instance from provided key.</p>"},{"location":"api/jam.paseto.v3/","title":"jam.paseto.v3","text":""},{"location":"api/jam.paseto.v3/#jam.paseto.v3","title":"v3","text":"<p>Classes:</p> Name Description <code>PASETOv3</code> <p>PASETO v3 factory.</p>"},{"location":"api/jam.paseto.v3/#jam.paseto.v3.PASETOv3","title":"PASETOv3","text":"<pre><code>PASETOv3()\n</code></pre> <p>               Bases: <code>BasePASETO</code></p> <p>PASETO v3 factory.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode PASETO.</p> <code>encode</code> <p>Encode PASETO.</p> <code>key</code> <p>Create PASETOv3 instance.</p>"},{"location":"api/jam.paseto.v3/#jam.paseto.v3.PASETOv3.decode","title":"decode","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[\n    dict[str, Any], dict[str, Any] | str | bytes | None\n]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>PASETO</p> required <code>serializer</code> <code>type[BaseEncoder] | BaseEncoder]</code> <p>JSON serializer</p> <code>JsonEncoder</code> <p>Returns:</p> Type Description <code>tuple[dict[str, Any], dict[str, Any] | str | bytes | None]</code> <p>Payload and dict</p>"},{"location":"api/jam.paseto.v3/#jam.paseto.v3.PASETOv3.encode","title":"encode","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | bytes | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Encode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>Token payload</p> required <code>footer</code> <code>dict[str, Any] | None</code> <p>Footer if needed</p> <code>None</code> <code>serializer</code> <code>type[BaseEncoder] | BaseEncoder</code> <p>JSON Serializer</p> <code>JsonEncoder</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Token</p>"},{"location":"api/jam.paseto.v3/#jam.paseto.v3.PASETOv3.key","title":"key  <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str | bytes,\n    public_key: str | bytes | None = None,\n) -&gt; PASETO\n</code></pre> <p>Create PASETOv3 instance.</p> <p>Parameters:</p> Name Type Description Default <code>purpose</code> <code>str</code> <p>\"local\" or \"public\"</p> required <code>secret_key</code> <code>str | bytes</code> <p>Private PEM</p> required <code>public_key</code> <code>str | bytes | None</code> <p>Public PEM</p> <code>None</code>"},{"location":"api/jam.paseto.v4/","title":"jam.paseto.v4","text":""},{"location":"api/jam.paseto.v4/#jam.paseto.v4","title":"v4","text":"<p>Classes:</p> Name Description <code>PASETOv4</code> <p>PASETO v4 factory.</p>"},{"location":"api/jam.paseto.v4/#jam.paseto.v4.PASETOv4","title":"PASETOv4","text":"<pre><code>PASETOv4()\n</code></pre> <p>               Bases: <code>BasePASETO</code></p> <p>PASETO v4 factory.</p> <p>Methods:</p> Name Description <code>decode</code> <p>Decode PASETO.</p> <code>encode</code> <p>Encode PASETO.</p> <code>key</code> <p>Create PASETO Factory.</p>"},{"location":"api/jam.paseto.v4/#jam.paseto.v4.PASETOv4.decode","title":"decode","text":"<pre><code>decode(\n    token: str,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; tuple[\n    dict[str, Any], dict[str, Any] | str | bytes | None\n]\n</code></pre> <p>Decode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str</code> <p>PASETO</p> required <code>serializer</code> <code>type[BaseEncoder] | BaseEncoder]</code> <p>JSON Serializer</p> <code>JsonEncoder</code> <p>Raises:</p> Type Description <code>JamPASETOInvalidTokenFormat</code> <p>If the token format is invalid.</p> <code>JamPASETOInvalidED25519Key</code> <p>If the key is not a valid ED25519 key.</p> <code>JamPASETOKeyVerificationError</code> <p>If the token signature is invalid.</p> <code>JamPASETOInvalidPurpose</code> <p>If the purpose is not \"local\" or \"public\".</p>"},{"location":"api/jam.paseto.v4/#jam.paseto.v4.PASETOv4.encode","title":"encode","text":"<pre><code>encode(\n    payload: dict[str, Any],\n    footer: dict[str, Any] | str | bytes | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n) -&gt; str\n</code></pre> <p>Encode PASETO.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>dict[str, Any]</code> <p>PASETO Payload</p> required <code>footer</code> <code>dict[str, Any] | str | None</code> <p>Footer if needed</p> <code>None</code> <code>serializer</code> <code>type[BaseEncoder] | BaseEncoder</code> <p>JSON Serializer</p> <code>JsonEncoder</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>PASETO</p> <p>Raises:</p> Type Description <code>JamPASETOInvalidED25519Key</code> <p>If the key is not a valid ED25519 key.</p> <code>JamPASETOInvalidPurpose</code> <p>If the purpose is not \"local\" or \"public\".</p>"},{"location":"api/jam.paseto.v4/#jam.paseto.v4.PASETOv4.key","title":"key  <code>classmethod</code>","text":"<pre><code>key(\n    purpose: Literal[\"local\", \"public\"],\n    secret_key: str | bytes | Ed25519PrivateKey,\n) -&gt; PASETO\n</code></pre> <p>Create PASETO Factory.</p> <p>Parameters:</p> Name Type Description Default <code>purpose</code> <code>Literal['local', 'public']</code> <p>PASETO type</p> required <code>secret_key</code> <code>str | bytes | Ed25519PrivateKey</code> <p>Secret or ED Private key</p> required <p>Raises:</p> Type Description <code>JamPASETOInvalidED25519Key</code> <p>If the key is not a valid ED25519 key.</p> <code>JamPASETOInvalidPurpose</code> <p>If the purpose is not \"local\" or \"public\".</p>"},{"location":"api/jam.plugins.__base__/","title":"jam.plugins.base","text":""},{"location":"api/jam.plugins.__base__/#jam.plugins.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BasePlugin</code> <p>Base plugin for Jam.</p>"},{"location":"api/jam.plugins.__base__/#jam.plugins.__base__.BasePlugin","title":"BasePlugin","text":"<pre><code>BasePlugin(jam, config: dict | None = None)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base plugin for Jam.</p> <p>Warning</p> <pre><code>Plugins are experimental and may not be stable.\n\nFor usage, JAM_ENABLE_PLUGINS must be set to 1.\n</code></pre> Example <pre><code>from jam import Jam\nfrom jam.plugins import BasePlugin\n\n\nclass MyPlugin(BasePlugin)\n    name = \"MyPlugin\"\n\n    def on_before_jwt_create(self, **kwargs):\n        print(\"JWT CREATED\")\n\njam = Jam(\n    config=\"config.toml\",\n    plugins=[MyPlugin]\n)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>jam</code> <code>BaseJam</code> <p>The Jam instance.</p> required <code>config</code> <code>dict | None</code> <p>Plugin configuration.</p> <code>None</code> <p>Methods:</p> Name Description <code>emit</code> <p>Emit event.</p> <code>setup</code> <p>Called when plugin is initialized.</p>"},{"location":"api/jam.plugins.__base__/#jam.plugins.__base__.BasePlugin.emit","title":"emit","text":"<pre><code>emit(event: str, **kwargs) -&gt; None\n</code></pre> <p>Emit event.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>str</code> <p>Event name</p> required <code>**kwargs</code> <p>Event data</p> <code>{}</code>"},{"location":"api/jam.plugins.__base__/#jam.plugins.__base__.BasePlugin.setup","title":"setup","text":"<pre><code>setup() -&gt; None\n</code></pre> <p>Called when plugin is initialized.</p>"},{"location":"api/jam.plugins/","title":"jam.plugins","text":""},{"location":"api/jam.plugins/#jam.plugins","title":"plugins","text":"<p>Jam plugin system.</p> <p>Classes:</p> Name Description <code>BasePlugin</code> <p>Base plugin for Jam.</p>"},{"location":"api/jam.plugins/#jam.plugins.BasePlugin","title":"BasePlugin","text":"<pre><code>BasePlugin(jam, config: dict | None = None)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Base plugin for Jam.</p> <p>Warning</p> <pre><code>Plugins are experimental and may not be stable.\n\nFor usage, JAM_ENABLE_PLUGINS must be set to 1.\n</code></pre> Example <pre><code>from jam import Jam\nfrom jam.plugins import BasePlugin\n\n\nclass MyPlugin(BasePlugin)\n    name = \"MyPlugin\"\n\n    def on_before_jwt_create(self, **kwargs):\n        print(\"JWT CREATED\")\n\njam = Jam(\n    config=\"config.toml\",\n    plugins=[MyPlugin]\n)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>jam</code> <code>BaseJam</code> <p>The Jam instance.</p> required <code>config</code> <code>dict | None</code> <p>Plugin configuration.</p> <code>None</code> <p>Methods:</p> Name Description <code>emit</code> <p>Emit event.</p> <code>setup</code> <p>Called when plugin is initialized.</p>"},{"location":"api/jam.plugins/#jam.plugins.BasePlugin.emit","title":"emit","text":"<pre><code>emit(event: str, **kwargs) -&gt; None\n</code></pre> <p>Emit event.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>str</code> <p>Event name</p> required <code>**kwargs</code> <p>Event data</p> <code>{}</code>"},{"location":"api/jam.plugins/#jam.plugins.BasePlugin.setup","title":"setup","text":"<pre><code>setup() -&gt; None\n</code></pre> <p>Called when plugin is initialized.</p>"},{"location":"api/jam.saml.__base__/","title":"jam.saml.base","text":""},{"location":"api/jam.saml.__base__/#jam.saml.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseSAML</code> <p>SAML 2.0 module.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML","title":"BaseSAML","text":"<p>               Bases: <code>ABC</code></p> <p>SAML 2.0 module.</p> <p>Supports both Service Provider (SP) and Identity Provider (IdP) roles.</p> <p>Methods:</p> Name Description <code>build_artifact</code> <p>Create a SAML 2.0 artifact (Base64 string).</p> <code>build_artifact_resolve</code> <p>Build a SAML ArtifactResolve.</p> <code>build_artifact_response</code> <p>Build a SAML ArtifactResponse wrapping the original message.</p> <code>build_attribute_query</code> <p>Build a SAML AttributeQuery.</p> <code>build_attribute_query_response</code> <p>Build a SAML Response for an AttributeQuery.</p> <code>build_logout_request</code> <p>Build a SAML LogoutRequest.</p> <code>build_logout_response</code> <p>Build a SAML LogoutResponse.</p> <code>build_manage_name_id_request</code> <p>Build a SAML ManageNameIDRequest.</p> <code>build_manage_name_id_response</code> <p>Build a SAML ManageNameIDResponse.</p> <code>build_response</code> <p>Build (and optionally encrypt) a SAML Response containing an Assertion.</p> <code>generate_metadata</code> <p>Generate SAML 2.0 metadata XML.</p> <code>parse_artifact_resolve</code> <p>Parse an incoming SAML ArtifactResolve.</p> <code>parse_artifact_response</code> <p>Parse a SAML ArtifactResponse.</p> <code>parse_attribute_query</code> <p>Parse an incoming SAML AttributeQuery.</p> <code>parse_attribute_query_response</code> <p>Parse a SAML Response from an AttributeQuery.</p> <code>parse_authn_request</code> <p>Parse an incoming AuthnRequest from an SP.</p> <code>parse_logout_request</code> <p>Parse a SAML LogoutRequest.</p> <code>parse_logout_response</code> <p>Parse a SAML LogoutResponse.</p> <code>parse_manage_name_id_request</code> <p>Parse a SAML ManageNameIDRequest.</p> <code>parse_manage_name_id_response</code> <p>Parse a SAML ManageNameIDResponse.</p> <code>parse_metadata</code> <p>Parse SAML 2.0 metadata XML.</p> <code>parse_response</code> <p>Parse and validate a SAML Response from an IdP.</p> <code>prepare_authn_request</code> <p>Build an AuthnRequest for the given IdP SSO URL.</p> <code>resolve_artifact</code> <p>Resolve a SAML artifact via SOAP to the IdP's resolve endpoint.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_artifact","title":"build_artifact  <code>abstractmethod</code>","text":"<pre><code>build_artifact(\n    source_message_id: str, *, issuer: str\n) -&gt; str\n</code></pre> <p>Create a SAML 2.0 artifact (Base64 string).</p> <p>Parameters:</p> Name Type Description Default <code>source_message_id</code> <code>str</code> <p>The ID of the message the artifact references.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the issuer.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Base64-encoded artifact string.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_artifact_resolve","title":"build_artifact_resolve  <code>abstractmethod</code>","text":"<pre><code>build_artifact_resolve(\n    artifact: str,\n    *,\n    issuer: str,\n    destination: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML ArtifactResolve.</p> <p>Parameters:</p> Name Type Description Default <code>artifact</code> <code>str</code> <p>The artifact to resolve.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester (SP).</p> required <code>destination</code> <code>str</code> <p>IdP artifact resolution service URL.</p> required <code>**kwargs</code> <code>Any</code> <p>binding (\"post\" or \"redirect\" or \"soap\").</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed ArtifactResolve XML / SOAP envelope.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_artifact_response","title":"build_artifact_response  <code>abstractmethod</code>","text":"<pre><code>build_artifact_response(\n    in_response_to: str,\n    original_message_xml: str,\n    *,\n    issuer: str,\n    destination: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML ArtifactResponse wrapping the original message.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ArtifactResolve ID being responded to.</p> required <code>original_message_xml</code> <code>str</code> <p>The original XML message to embed.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the responder (IdP).</p> required <code>destination</code> <code>str</code> <p>SP endpoint URL.</p> required <code>**kwargs</code> <code>Any</code> <p>binding, status_code.</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed ArtifactResponse XML / SOAP envelope.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_attribute_query","title":"build_attribute_query  <code>abstractmethod</code>","text":"<pre><code>build_attribute_query(\n    subject: str,\n    *,\n    issuer: str,\n    destination: str,\n    attribute_names: list[str] | None = None,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>subject</code> <code>str</code> <p>The subject to query attributes for.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester (SP).</p> required <code>destination</code> <code>str</code> <p>IdP attribute query endpoint URL.</p> required <code>attribute_names</code> <code>list[str] | None</code> <p>Specific attributes to request (None = all).</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, etc.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_attribute_query_response","title":"build_attribute_query_response  <code>abstractmethod</code>","text":"<pre><code>build_attribute_query_response(\n    in_response_to: str,\n    subject: str,\n    attributes: dict[str, Any],\n    *,\n    issuer: str,\n    audience: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML Response for an AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>AttributeQuery ID being responded to.</p> required <code>subject</code> <code>str</code> <p>The subject.</p> required <code>attributes</code> <code>dict[str, Any]</code> <p>User attributes dict.</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>audience</code> <code>str</code> <p>SP entity ID.</p> required <code>**kwargs</code> <code>Any</code> <p>destination, name_id_format.</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed SAML Response XML string.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_logout_request","title":"build_logout_request  <code>abstractmethod</code>","text":"<pre><code>build_logout_request(\n    name_id: str,\n    *,\n    issuer: str,\n    destination: str,\n    session_index: str | None = None,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML LogoutRequest.</p> <p>Parameters:</p> Name Type Description Default <code>name_id</code> <code>str</code> <p>The user identifier being logged out.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the sender (IdP or SP).</p> required <code>destination</code> <code>str</code> <p>SLO endpoint URL of the recipient.</p> required <code>session_index</code> <code>str | None</code> <p>Session identifier (optional).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Extra params (binding, relay_state, etc.).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed LogoutRequest XML (POST \u2192 Base64, Redirect \u2192 URL).</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_logout_response","title":"build_logout_response  <code>abstractmethod</code>","text":"<pre><code>build_logout_response(\n    in_response_to: str,\n    *,\n    issuer: str,\n    destination: str,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML LogoutResponse.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ID of the LogoutRequest being responded to.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the sender.</p> required <code>destination</code> <code>str</code> <p>SLO endpoint URL of the recipient.</p> required <code>status_code</code> <code>str</code> <p>SAML status code (default Success).</p> <code>'urn:oasis:names:tc:SAML:2.0:status:Success'</code> <code>**kwargs</code> <code>Any</code> <p>Extra params (binding, relay_state, etc.).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed LogoutResponse XML (POST \u2192 Base64, Redirect \u2192 URL).</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_manage_name_id_request","title":"build_manage_name_id_request  <code>abstractmethod</code>","text":"<pre><code>build_manage_name_id_request(\n    name_id: str,\n    *,\n    issuer: str,\n    destination: str,\n    new_id: str | None = None,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML ManageNameIDRequest.</p> <p>Parameters:</p> Name Type Description Default <code>name_id</code> <code>str</code> <p>Current NameID.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester.</p> required <code>destination</code> <code>str</code> <p>Entity endpoint URL.</p> required <code>new_id</code> <code>str | None</code> <p>New identifier (None = terminate).</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, name_id_format.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_manage_name_id_response","title":"build_manage_name_id_response  <code>abstractmethod</code>","text":"<pre><code>build_manage_name_id_response(\n    in_response_to: str,\n    *,\n    issuer: str,\n    destination: str,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML ManageNameIDResponse.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ManageNameIDRequest ID.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the responder.</p> required <code>destination</code> <code>str</code> <p>Endpoint URL of the requester.</p> required <code>status_code</code> <code>str</code> <p>SAML status code (default Success).</p> <code>'urn:oasis:names:tc:SAML:2.0:status:Success'</code> <code>**kwargs</code> <code>Any</code> <p>binding, relay_state.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.build_response","title":"build_response  <code>abstractmethod</code>","text":"<pre><code>build_response(\n    subject: str,\n    attributes: dict[str, Any],\n    *,\n    issuer: str,\n    audience: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build (and optionally encrypt) a SAML Response containing an Assertion.</p> <p>Parameters:</p> Name Type Description Default <code>subject</code> <code>str</code> <p>The authenticated user identifier.</p> required <code>attributes</code> <code>dict[str, Any]</code> <p>User attributes (email, roles, etc.).</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>audience</code> <code>str</code> <p>SP entity ID (the intended audience).</p> required <code>**kwargs</code> <code>Any</code> <p>in_response_to, name_id_format, session_index,       encrypt (bool, default False).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed (and optionally encrypted) SAML Response XML string.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.generate_metadata","title":"generate_metadata  <code>abstractmethod</code>","text":"<pre><code>generate_metadata(\n    *,\n    entity_id: str,\n    sso_url: str | None = None,\n    acs_url: str | None = None,\n    role: str | None = None,\n) -&gt; str\n</code></pre> <p>Generate SAML 2.0 metadata XML.</p> <p>Parameters:</p> Name Type Description Default <code>entity_id</code> <code>str</code> <p>Entity ID of the IdP or SP.</p> required <code>sso_url</code> <code>str | None</code> <p>SSO URL (required for IdP metadata).</p> <code>None</code> <code>acs_url</code> <code>str | None</code> <p>ACS URL (required for SP metadata).</p> <code>None</code> <code>role</code> <code>str | None</code> <p><code>\"idp\"</code> or <code>\"sp\"</code>. Auto-detected if omitted.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>SAML metadata XML string.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_artifact_resolve","title":"parse_artifact_resolve  <code>abstractmethod</code>","text":"<pre><code>parse_artifact_resolve(\n    saml_request: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLArtifactResolve\n</code></pre> <p>Parse an incoming SAML ArtifactResolve.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw ArtifactResolve data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default), <code>\"redirect\"</code>, or <code>\"soap\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLArtifactResolve</code> <p>Parsed SAMLArtifactResolve.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_artifact_response","title":"parse_artifact_response  <code>abstractmethod</code>","text":"<pre><code>parse_artifact_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLArtifactResponse\n</code></pre> <p>Parse a SAML ArtifactResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw ArtifactResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default), <code>\"redirect\"</code>, or <code>\"soap\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLArtifactResponse</code> <p>Parsed SAMLArtifactResponse.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_attribute_query","title":"parse_attribute_query  <code>abstractmethod</code>","text":"<pre><code>parse_attribute_query(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLAttributeQuery\n</code></pre> <p>Parse an incoming SAML AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw AttributeQuery data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLAttributeQuery</code> <p>Parsed SAMLAttributeQuery.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_attribute_query_response","title":"parse_attribute_query_response  <code>abstractmethod</code>","text":"<pre><code>parse_attribute_query_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLResponse\n</code></pre> <p>Parse a SAML Response from an AttributeQuery.</p> <p>Delegates to parse_response.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw SAMLResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>audience, issuer, etc.</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLResponse</code> <p>Parsed SAMLResponse.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_authn_request","title":"parse_authn_request  <code>abstractmethod</code>","text":"<pre><code>parse_authn_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLRequest\n</code></pre> <p>Parse an incoming AuthnRequest from an SP.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw SAMLRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>Expected issuer (for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLRequest</code> <p>Parsed SAMLRequest data.</p> <p>Raises:</p> Type Description <code>JamSAMLInvalidIssuer</code> <p>Issuer doesn't match expected.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_logout_request","title":"parse_logout_request  <code>abstractmethod</code>","text":"<pre><code>parse_logout_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLLogoutRequest\n</code></pre> <p>Parse a SAML LogoutRequest.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw LogoutRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLLogoutRequest</code> <p>Parsed SAMLLogoutRequest.</p> <p>Raises:</p> Type Description <code>JamSAMLInvalidIssuer</code> <p>Issuer doesn't match expected.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_logout_response","title":"parse_logout_response  <code>abstractmethod</code>","text":"<pre><code>parse_logout_response(\n    saml_response: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLLogoutResponse\n</code></pre> <p>Parse a SAML LogoutResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw LogoutResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLLogoutResponse</code> <p>Parsed SAMLLogoutResponse.</p> <p>Raises:</p> Type Description <code>JamSAMLInvalidIssuer</code> <p>Issuer doesn't match expected.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_manage_name_id_request","title":"parse_manage_name_id_request  <code>abstractmethod</code>","text":"<pre><code>parse_manage_name_id_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLManageNameIDRequest\n</code></pre> <p>Parse a SAML ManageNameIDRequest.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw ManageNameIDRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLManageNameIDRequest</code> <p>Parsed SAMLManageNameIDRequest.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_manage_name_id_response","title":"parse_manage_name_id_response  <code>abstractmethod</code>","text":"<pre><code>parse_manage_name_id_response(\n    saml_response: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLManageNameIDResponse\n</code></pre> <p>Parse a SAML ManageNameIDResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw ManageNameIDResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLManageNameIDResponse</code> <p>Parsed SAMLManageNameIDResponse.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_metadata","title":"parse_metadata  <code>abstractmethod</code>","text":"<pre><code>parse_metadata(metadata_xml: str) -&gt; Any\n</code></pre> <p>Parse SAML 2.0 metadata XML.</p> <p>Parameters:</p> Name Type Description Default <code>metadata_xml</code> <code>str</code> <p>Raw metadata XML string.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>SAMLMetadata with parsed fields.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.parse_response","title":"parse_response  <code>abstractmethod</code>","text":"<pre><code>parse_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLResponse\n</code></pre> <p>Parse and validate a SAML Response from an IdP.</p> <p>Automatically decrypts <code>&lt;saml:EncryptedAssertion&gt;</code> if the instance has a <code>private_key</code> configured.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw SAMLResponse data (Base64 string for POST,            query-string for Redirect).</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>Expected audience, expected issuer, etc.</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLResponse</code> <p>Parsed SAMLResponse with assertion data.</p> <p>Raises:</p> Type Description <code>JamSAMLExpired</code> <p>Assertion is expired.</p> <code>JamSAMLNotYetValid</code> <p>Assertion is not yet valid.</p> <code>JamSAMLInvalidAudience</code> <p>Audience doesn't match.</p> <code>JamSAMLInvalidIssuer</code> <p>Issuer doesn't match.</p> <code>JamSAMLValidationError</code> <p>Signature verification failed.</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.prepare_authn_request","title":"prepare_authn_request  <code>abstractmethod</code>","text":"<pre><code>prepare_authn_request(\n    idp_sso_url: str,\n    *,\n    acs_url: str,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build an AuthnRequest for the given IdP SSO URL.</p> <p>Parameters:</p> Name Type Description Default <code>idp_sso_url</code> <code>str</code> <p>IdP single sign-on endpoint URL.</p> required <code>acs_url</code> <code>str</code> <p>SP assertion consumer service URL.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>Extra params (relay_state, force_authn, etc.).</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>Redirect</code> <code>str</code> <p>IdP URL with signed AuthnRequest.</p> <code>POST</code> <code>str</code> <p>Base64-encoded SAMLRequest as a string (embed in form).</p>"},{"location":"api/jam.saml.__base__/#jam.saml.__base__.BaseSAML.resolve_artifact","title":"resolve_artifact  <code>abstractmethod</code>","text":"<pre><code>resolve_artifact(\n    artifact: str,\n    *,\n    issuer: str,\n    resolve_url: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Resolve a SAML artifact via SOAP to the IdP's resolve endpoint.</p> <p>Parameters:</p> Name Type Description Default <code>artifact</code> <code>str</code> <p>The artifact to resolve.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester (SP).</p> required <code>resolve_url</code> <code>str</code> <p>IdP artifact resolution service URL.</p> required <code>**kwargs</code> <code>Any</code> <p>Extra params (timeout, headers, expected_issuer, etc.).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>The original SAML message XML extracted from the ArtifactResponse.</p>"},{"location":"api/jam.saml.binding/","title":"jam.saml.binding","text":""},{"location":"api/jam.saml.binding/#jam.saml.binding","title":"binding","text":"<p>Functions:</p> Name Description <code>build_redirect_signature</code> <p>Sign a query string for HTTP-Redirect binding with RSA-SHA256.</p> <code>build_redirect_url</code> <p>Build a signed redirect URL for HTTP-Redirect binding.</p> <code>decode_post</code> <p>Decode Base64-encoded SAML data from HTTP-POST binding.</p> <code>decode_redirect</code> <p>Decode Deflate + Base64 SAML data from HTTP-Redirect binding.</p> <code>encode_post</code> <p>Base64-encode SAML XML for HTTP-POST binding.</p> <code>encode_redirect</code> <p>Deflate + Base64-encode SAML XML for HTTP-Redirect binding.</p> <code>parse_redirect_request</code> <p>Parse a SAML redirect binding query string.</p> <code>verify_redirect_signature</code> <p>Verify an HTTP-Redirect binding signature.</p>"},{"location":"api/jam.saml.binding/#jam.saml.binding.build_redirect_signature","title":"build_redirect_signature","text":"<pre><code>build_redirect_signature(\n    signed_query: str, key: RSAPrivateKey\n) -&gt; str\n</code></pre> <p>Sign a query string for HTTP-Redirect binding with RSA-SHA256.</p> <p>Parameters:</p> Name Type Description Default <code>signed_query</code> <code>str</code> <p>Raw query string (SAMLRequest=SigAlg=...).</p> required <code>key</code> <code>RSAPrivateKey</code> <p>RSA private key.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Base64-encoded signature.</p>"},{"location":"api/jam.saml.binding/#jam.saml.binding.build_redirect_url","title":"build_redirect_url","text":"<pre><code>build_redirect_url(\n    base_url: str,\n    saml_params: dict[str, str],\n    signing_key: RSAPrivateKey | None = None,\n) -&gt; str\n</code></pre> <p>Build a signed redirect URL for HTTP-Redirect binding.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>IdP SSO endpoint URL.</p> required <code>saml_params</code> <code>dict[str, str]</code> <p>Query params (SAMLRequest, RelayState, etc.).</p> required <code>signing_key</code> <code>RSAPrivateKey | None</code> <p>RSA private key for signing (optional).</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Full redirect URL with query string.</p>"},{"location":"api/jam.saml.binding/#jam.saml.binding.decode_post","title":"decode_post","text":"<pre><code>decode_post(b64_str: str) -&gt; str\n</code></pre> <p>Decode Base64-encoded SAML data from HTTP-POST binding.</p>"},{"location":"api/jam.saml.binding/#jam.saml.binding.decode_redirect","title":"decode_redirect","text":"<pre><code>decode_redirect(encoded: str) -&gt; str\n</code></pre> <p>Decode Deflate + Base64 SAML data from HTTP-Redirect binding.</p>"},{"location":"api/jam.saml.binding/#jam.saml.binding.encode_post","title":"encode_post","text":"<pre><code>encode_post(xml_str: str) -&gt; str\n</code></pre> <p>Base64-encode SAML XML for HTTP-POST binding.</p>"},{"location":"api/jam.saml.binding/#jam.saml.binding.encode_redirect","title":"encode_redirect","text":"<pre><code>encode_redirect(xml_str: str) -&gt; str\n</code></pre> <p>Deflate + Base64-encode SAML XML for HTTP-Redirect binding.</p>"},{"location":"api/jam.saml.binding/#jam.saml.binding.parse_redirect_request","title":"parse_redirect_request","text":"<pre><code>parse_redirect_request(\n    query_string: str,\n    verify_key: RSAPublicKey | None = None,\n) -&gt; tuple[str, str]\n</code></pre> <p>Parse a SAML redirect binding query string.</p> <p>Parameters:</p> Name Type Description Default <code>query_string</code> <code>str</code> <p>The raw query string from the redirect URL.</p> required <code>verify_key</code> <code>RSAPublicKey | None</code> <p>RSA public key to verify signature (optional).</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>Tuple of (decoded SAML XML string, RelayState).</p> <p>Raises:</p> Type Description <code>JamSAMLValidationError</code> <p>If signature verification fails.</p>"},{"location":"api/jam.saml.binding/#jam.saml.binding.verify_redirect_signature","title":"verify_redirect_signature","text":"<pre><code>verify_redirect_signature(\n    signed_query: str, signature_b64: str, key: RSAPublicKey\n) -&gt; bool\n</code></pre> <p>Verify an HTTP-Redirect binding signature.</p> <p>Parameters:</p> Name Type Description Default <code>signed_query</code> <code>str</code> <p>Raw query string that was signed.</p> required <code>signature_b64</code> <code>str</code> <p>Base64-encoded signature.</p> required <code>key</code> <code>RSAPublicKey</code> <p>RSA public key.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if signature is valid.</p> <p>Raises:</p> Type Description <code>JamSAMLValidationError</code> <p>If signature is invalid.</p>"},{"location":"api/jam.saml.encryption/","title":"jam.saml.encryption","text":""},{"location":"api/jam.saml.encryption/#jam.saml.encryption","title":"encryption","text":"<p>Functions:</p> Name Description <code>decrypt_aes_key</code> <p>Decrypt an RSA-OAEP encrypted AES key.</p> <code>decrypt_assertion</code> <p>Decrypt a <code>&lt;saml:EncryptedAssertion&gt;</code> element.</p> <code>encrypt_aes_key</code> <p>Encrypt an AES key with RSA-OAEP.</p> <code>encrypt_assertion</code> <p>Encrypt a SAML Assertion element into an EncryptedAssertion.</p> <code>load_encryption_key</code> <p>Load an RSA public key from PEM for encryption.</p>"},{"location":"api/jam.saml.encryption/#jam.saml.encryption.decrypt_aes_key","title":"decrypt_aes_key","text":"<pre><code>decrypt_aes_key(\n    encrypted_key_b64: str, private_key: RSAPrivateKey\n) -&gt; bytes\n</code></pre> <p>Decrypt an RSA-OAEP encrypted AES key.</p> <p>Parameters:</p> Name Type Description Default <code>encrypted_key_b64</code> <code>str</code> <p>Base64-encoded encrypted AES key.</p> required <code>private_key</code> <code>RSAPrivateKey</code> <p>RSA private key for decryption.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Decrypted 256-bit AES key bytes.</p>"},{"location":"api/jam.saml.encryption/#jam.saml.encryption.decrypt_assertion","title":"decrypt_assertion","text":"<pre><code>decrypt_assertion(\n    enc_assertion_elem: Element, private_key: RSAPrivateKey\n) -&gt; Element\n</code></pre> <p>Decrypt a <code>&lt;saml:EncryptedAssertion&gt;</code> element.</p> <p>Extracts the RSA-OAEP wrapped AES key, decrypts it, then decrypts the assertion XML payload.</p> <p>Parameters:</p> Name Type Description Default <code>enc_assertion_elem</code> <code>Element</code> <p>The <code>&lt;saml:EncryptedAssertion&gt;</code> element.</p> required <code>private_key</code> <code>RSAPrivateKey</code> <p>Recipient's RSA private key.</p> required <p>Returns:</p> Type Description <code>Element</code> <p>Decrypted <code>&lt;saml:Assertion&gt;</code> element.</p> <p>Raises:</p> Type Description <code>JamSAMLValidationError</code> <p>If decryption fails or structure is invalid.</p>"},{"location":"api/jam.saml.encryption/#jam.saml.encryption.encrypt_aes_key","title":"encrypt_aes_key","text":"<pre><code>encrypt_aes_key(\n    aes_key: bytes, public_key: RSAPublicKey\n) -&gt; str\n</code></pre> <p>Encrypt an AES key with RSA-OAEP.</p> <p>Parameters:</p> Name Type Description Default <code>aes_key</code> <code>bytes</code> <p>256-bit AES key bytes.</p> required <code>public_key</code> <code>RSAPublicKey</code> <p>RSA public key for encryption.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Base64-encoded encrypted AES key.</p>"},{"location":"api/jam.saml.encryption/#jam.saml.encryption.encrypt_assertion","title":"encrypt_assertion","text":"<pre><code>encrypt_assertion(\n    assertion_elem: Element, public_key: RSAPublicKey\n) -&gt; Element\n</code></pre> <p>Encrypt a SAML Assertion element into an EncryptedAssertion.</p> <p>Generates a random AES-256-GCM key, encrypts the canonicalized assertion XML with it, then wraps the AES key with RSA-OAEP.</p> <p>Parameters:</p> Name Type Description Default <code>assertion_elem</code> <code>Element</code> <p>The <code>&lt;saml:Assertion&gt;</code> element to encrypt.</p> required <code>public_key</code> <code>RSAPublicKey</code> <p>Recipient's RSA public key.</p> required <p>Returns:</p> Type Description <code>Element</code> <p><code>&lt;saml:EncryptedAssertion&gt;</code> element.</p>"},{"location":"api/jam.saml.encryption/#jam.saml.encryption.load_encryption_key","title":"load_encryption_key","text":"<pre><code>load_encryption_key(pem: str) -&gt; RSAPublicKey\n</code></pre> <p>Load an RSA public key from PEM for encryption.</p> <p>Parameters:</p> Name Type Description Default <code>pem</code> <code>str</code> <p>PEM-encoded RSA public key or certificate.</p> required <p>Returns:</p> Type Description <code>RSAPublicKey</code> <p>RSA public key.</p>"},{"location":"api/jam.saml/","title":"jam.saml","text":""},{"location":"api/jam.saml/#jam.saml","title":"saml","text":"<p>Security Assertion Markup Language (SAML 2.0).</p> <p>Supports both Service Provider and Identity Provider roles. Zero external dependencies \u2014 built on stdlib xml + cryptography.</p> <p>Modules:</p> Name Description <code>binding</code> <code>encryption</code> <code>metadata</code> <code>saml</code> <code>signature</code> <code>types</code> <code>xml</code> <p>Classes:</p> Name Description <code>BaseSAML</code> <p>SAML 2.0 module.</p> <code>SAML</code> <p>Concrete SAML 2.0 implementation.</p> <code>SAMLArtifactResolve</code> <p>Parsed SAML ArtifactResolve.</p> <code>SAMLArtifactResponse</code> <p>Parsed SAML ArtifactResponse.</p> <code>SAMLAssertion</code> <p>Parsed SAML assertion data.</p> <code>SAMLAttributeQuery</code> <p>Parsed SAML AttributeQuery.</p> <code>SAMLAuthnStatement</code> <p>SAML authentication statement.</p> <code>SAMLConditions</code> <p>SAML assertion conditions (time constraints + audience).</p> <code>SAMLLogoutRequest</code> <p>Parsed SAML LogoutRequest.</p> <code>SAMLLogoutResponse</code> <p>Parsed SAML LogoutResponse.</p> <code>SAMLManageNameIDRequest</code> <p>Parsed SAML ManageNameIDRequest.</p> <code>SAMLManageNameIDResponse</code> <p>Parsed SAML ManageNameIDResponse.</p> <code>SAMLMetadata</code> <p>Parsed SAML metadata.</p> <code>SAMLRequest</code> <p>Parsed SAML AuthnRequest.</p> <code>SAMLResponse</code> <p>Parsed SAML protocol Response.</p> <code>SAMLSubject</code> <p>SAML assertion subject (NameID + confirmation).</p> <p>Functions:</p> Name Description <code>create_instance</code> <p>Create a SAML instance.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML","title":"BaseSAML","text":"<p>               Bases: <code>ABC</code></p> <p>SAML 2.0 module.</p> <p>Supports both Service Provider (SP) and Identity Provider (IdP) roles.</p> <p>Methods:</p> Name Description <code>build_artifact</code> <p>Create a SAML 2.0 artifact (Base64 string).</p> <code>build_artifact_resolve</code> <p>Build a SAML ArtifactResolve.</p> <code>build_artifact_response</code> <p>Build a SAML ArtifactResponse wrapping the original message.</p> <code>build_attribute_query</code> <p>Build a SAML AttributeQuery.</p> <code>build_attribute_query_response</code> <p>Build a SAML Response for an AttributeQuery.</p> <code>build_logout_request</code> <p>Build a SAML LogoutRequest.</p> <code>build_logout_response</code> <p>Build a SAML LogoutResponse.</p> <code>build_manage_name_id_request</code> <p>Build a SAML ManageNameIDRequest.</p> <code>build_manage_name_id_response</code> <p>Build a SAML ManageNameIDResponse.</p> <code>build_response</code> <p>Build (and optionally encrypt) a SAML Response containing an Assertion.</p> <code>generate_metadata</code> <p>Generate SAML 2.0 metadata XML.</p> <code>parse_artifact_resolve</code> <p>Parse an incoming SAML ArtifactResolve.</p> <code>parse_artifact_response</code> <p>Parse a SAML ArtifactResponse.</p> <code>parse_attribute_query</code> <p>Parse an incoming SAML AttributeQuery.</p> <code>parse_attribute_query_response</code> <p>Parse a SAML Response from an AttributeQuery.</p> <code>parse_authn_request</code> <p>Parse an incoming AuthnRequest from an SP.</p> <code>parse_logout_request</code> <p>Parse a SAML LogoutRequest.</p> <code>parse_logout_response</code> <p>Parse a SAML LogoutResponse.</p> <code>parse_manage_name_id_request</code> <p>Parse a SAML ManageNameIDRequest.</p> <code>parse_manage_name_id_response</code> <p>Parse a SAML ManageNameIDResponse.</p> <code>parse_metadata</code> <p>Parse SAML 2.0 metadata XML.</p> <code>parse_response</code> <p>Parse and validate a SAML Response from an IdP.</p> <code>prepare_authn_request</code> <p>Build an AuthnRequest for the given IdP SSO URL.</p> <code>resolve_artifact</code> <p>Resolve a SAML artifact via SOAP to the IdP's resolve endpoint.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_artifact","title":"build_artifact  <code>abstractmethod</code>","text":"<pre><code>build_artifact(\n    source_message_id: str, *, issuer: str\n) -&gt; str\n</code></pre> <p>Create a SAML 2.0 artifact (Base64 string).</p> <p>Parameters:</p> Name Type Description Default <code>source_message_id</code> <code>str</code> <p>The ID of the message the artifact references.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the issuer.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Base64-encoded artifact string.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_artifact_resolve","title":"build_artifact_resolve  <code>abstractmethod</code>","text":"<pre><code>build_artifact_resolve(\n    artifact: str,\n    *,\n    issuer: str,\n    destination: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML ArtifactResolve.</p> <p>Parameters:</p> Name Type Description Default <code>artifact</code> <code>str</code> <p>The artifact to resolve.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester (SP).</p> required <code>destination</code> <code>str</code> <p>IdP artifact resolution service URL.</p> required <code>**kwargs</code> <code>Any</code> <p>binding (\"post\" or \"redirect\" or \"soap\").</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed ArtifactResolve XML / SOAP envelope.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_artifact_response","title":"build_artifact_response  <code>abstractmethod</code>","text":"<pre><code>build_artifact_response(\n    in_response_to: str,\n    original_message_xml: str,\n    *,\n    issuer: str,\n    destination: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML ArtifactResponse wrapping the original message.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ArtifactResolve ID being responded to.</p> required <code>original_message_xml</code> <code>str</code> <p>The original XML message to embed.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the responder (IdP).</p> required <code>destination</code> <code>str</code> <p>SP endpoint URL.</p> required <code>**kwargs</code> <code>Any</code> <p>binding, status_code.</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed ArtifactResponse XML / SOAP envelope.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_attribute_query","title":"build_attribute_query  <code>abstractmethod</code>","text":"<pre><code>build_attribute_query(\n    subject: str,\n    *,\n    issuer: str,\n    destination: str,\n    attribute_names: list[str] | None = None,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>subject</code> <code>str</code> <p>The subject to query attributes for.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester (SP).</p> required <code>destination</code> <code>str</code> <p>IdP attribute query endpoint URL.</p> required <code>attribute_names</code> <code>list[str] | None</code> <p>Specific attributes to request (None = all).</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, etc.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_attribute_query_response","title":"build_attribute_query_response  <code>abstractmethod</code>","text":"<pre><code>build_attribute_query_response(\n    in_response_to: str,\n    subject: str,\n    attributes: dict[str, Any],\n    *,\n    issuer: str,\n    audience: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML Response for an AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>AttributeQuery ID being responded to.</p> required <code>subject</code> <code>str</code> <p>The subject.</p> required <code>attributes</code> <code>dict[str, Any]</code> <p>User attributes dict.</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>audience</code> <code>str</code> <p>SP entity ID.</p> required <code>**kwargs</code> <code>Any</code> <p>destination, name_id_format.</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed SAML Response XML string.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_logout_request","title":"build_logout_request  <code>abstractmethod</code>","text":"<pre><code>build_logout_request(\n    name_id: str,\n    *,\n    issuer: str,\n    destination: str,\n    session_index: str | None = None,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML LogoutRequest.</p> <p>Parameters:</p> Name Type Description Default <code>name_id</code> <code>str</code> <p>The user identifier being logged out.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the sender (IdP or SP).</p> required <code>destination</code> <code>str</code> <p>SLO endpoint URL of the recipient.</p> required <code>session_index</code> <code>str | None</code> <p>Session identifier (optional).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Extra params (binding, relay_state, etc.).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed LogoutRequest XML (POST \u2192 Base64, Redirect \u2192 URL).</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_logout_response","title":"build_logout_response  <code>abstractmethod</code>","text":"<pre><code>build_logout_response(\n    in_response_to: str,\n    *,\n    issuer: str,\n    destination: str,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML LogoutResponse.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ID of the LogoutRequest being responded to.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the sender.</p> required <code>destination</code> <code>str</code> <p>SLO endpoint URL of the recipient.</p> required <code>status_code</code> <code>str</code> <p>SAML status code (default Success).</p> <code>'urn:oasis:names:tc:SAML:2.0:status:Success'</code> <code>**kwargs</code> <code>Any</code> <p>Extra params (binding, relay_state, etc.).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed LogoutResponse XML (POST \u2192 Base64, Redirect \u2192 URL).</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_manage_name_id_request","title":"build_manage_name_id_request  <code>abstractmethod</code>","text":"<pre><code>build_manage_name_id_request(\n    name_id: str,\n    *,\n    issuer: str,\n    destination: str,\n    new_id: str | None = None,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML ManageNameIDRequest.</p> <p>Parameters:</p> Name Type Description Default <code>name_id</code> <code>str</code> <p>Current NameID.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester.</p> required <code>destination</code> <code>str</code> <p>Entity endpoint URL.</p> required <code>new_id</code> <code>str | None</code> <p>New identifier (None = terminate).</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, name_id_format.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_manage_name_id_response","title":"build_manage_name_id_response  <code>abstractmethod</code>","text":"<pre><code>build_manage_name_id_response(\n    in_response_to: str,\n    *,\n    issuer: str,\n    destination: str,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML ManageNameIDResponse.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ManageNameIDRequest ID.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the responder.</p> required <code>destination</code> <code>str</code> <p>Endpoint URL of the requester.</p> required <code>status_code</code> <code>str</code> <p>SAML status code (default Success).</p> <code>'urn:oasis:names:tc:SAML:2.0:status:Success'</code> <code>**kwargs</code> <code>Any</code> <p>binding, relay_state.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.build_response","title":"build_response  <code>abstractmethod</code>","text":"<pre><code>build_response(\n    subject: str,\n    attributes: dict[str, Any],\n    *,\n    issuer: str,\n    audience: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build (and optionally encrypt) a SAML Response containing an Assertion.</p> <p>Parameters:</p> Name Type Description Default <code>subject</code> <code>str</code> <p>The authenticated user identifier.</p> required <code>attributes</code> <code>dict[str, Any]</code> <p>User attributes (email, roles, etc.).</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>audience</code> <code>str</code> <p>SP entity ID (the intended audience).</p> required <code>**kwargs</code> <code>Any</code> <p>in_response_to, name_id_format, session_index,       encrypt (bool, default False).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed (and optionally encrypted) SAML Response XML string.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.generate_metadata","title":"generate_metadata  <code>abstractmethod</code>","text":"<pre><code>generate_metadata(\n    *,\n    entity_id: str,\n    sso_url: str | None = None,\n    acs_url: str | None = None,\n    role: str | None = None,\n) -&gt; str\n</code></pre> <p>Generate SAML 2.0 metadata XML.</p> <p>Parameters:</p> Name Type Description Default <code>entity_id</code> <code>str</code> <p>Entity ID of the IdP or SP.</p> required <code>sso_url</code> <code>str | None</code> <p>SSO URL (required for IdP metadata).</p> <code>None</code> <code>acs_url</code> <code>str | None</code> <p>ACS URL (required for SP metadata).</p> <code>None</code> <code>role</code> <code>str | None</code> <p><code>\"idp\"</code> or <code>\"sp\"</code>. Auto-detected if omitted.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>SAML metadata XML string.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_artifact_resolve","title":"parse_artifact_resolve  <code>abstractmethod</code>","text":"<pre><code>parse_artifact_resolve(\n    saml_request: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLArtifactResolve\n</code></pre> <p>Parse an incoming SAML ArtifactResolve.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw ArtifactResolve data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default), <code>\"redirect\"</code>, or <code>\"soap\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLArtifactResolve</code> <p>Parsed SAMLArtifactResolve.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_artifact_response","title":"parse_artifact_response  <code>abstractmethod</code>","text":"<pre><code>parse_artifact_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLArtifactResponse\n</code></pre> <p>Parse a SAML ArtifactResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw ArtifactResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default), <code>\"redirect\"</code>, or <code>\"soap\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLArtifactResponse</code> <p>Parsed SAMLArtifactResponse.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_attribute_query","title":"parse_attribute_query  <code>abstractmethod</code>","text":"<pre><code>parse_attribute_query(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLAttributeQuery\n</code></pre> <p>Parse an incoming SAML AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw AttributeQuery data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLAttributeQuery</code> <p>Parsed SAMLAttributeQuery.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_attribute_query_response","title":"parse_attribute_query_response  <code>abstractmethod</code>","text":"<pre><code>parse_attribute_query_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLResponse\n</code></pre> <p>Parse a SAML Response from an AttributeQuery.</p> <p>Delegates to parse_response.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw SAMLResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>audience, issuer, etc.</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLResponse</code> <p>Parsed SAMLResponse.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_authn_request","title":"parse_authn_request  <code>abstractmethod</code>","text":"<pre><code>parse_authn_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLRequest\n</code></pre> <p>Parse an incoming AuthnRequest from an SP.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw SAMLRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>Expected issuer (for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLRequest</code> <p>Parsed SAMLRequest data.</p> <p>Raises:</p> Type Description <code>JamSAMLInvalidIssuer</code> <p>Issuer doesn't match expected.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_logout_request","title":"parse_logout_request  <code>abstractmethod</code>","text":"<pre><code>parse_logout_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLLogoutRequest\n</code></pre> <p>Parse a SAML LogoutRequest.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw LogoutRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLLogoutRequest</code> <p>Parsed SAMLLogoutRequest.</p> <p>Raises:</p> Type Description <code>JamSAMLInvalidIssuer</code> <p>Issuer doesn't match expected.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_logout_response","title":"parse_logout_response  <code>abstractmethod</code>","text":"<pre><code>parse_logout_response(\n    saml_response: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLLogoutResponse\n</code></pre> <p>Parse a SAML LogoutResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw LogoutResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLLogoutResponse</code> <p>Parsed SAMLLogoutResponse.</p> <p>Raises:</p> Type Description <code>JamSAMLInvalidIssuer</code> <p>Issuer doesn't match expected.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_manage_name_id_request","title":"parse_manage_name_id_request  <code>abstractmethod</code>","text":"<pre><code>parse_manage_name_id_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLManageNameIDRequest\n</code></pre> <p>Parse a SAML ManageNameIDRequest.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw ManageNameIDRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLManageNameIDRequest</code> <p>Parsed SAMLManageNameIDRequest.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_manage_name_id_response","title":"parse_manage_name_id_response  <code>abstractmethod</code>","text":"<pre><code>parse_manage_name_id_response(\n    saml_response: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLManageNameIDResponse\n</code></pre> <p>Parse a SAML ManageNameIDResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw ManageNameIDResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLManageNameIDResponse</code> <p>Parsed SAMLManageNameIDResponse.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_metadata","title":"parse_metadata  <code>abstractmethod</code>","text":"<pre><code>parse_metadata(metadata_xml: str) -&gt; Any\n</code></pre> <p>Parse SAML 2.0 metadata XML.</p> <p>Parameters:</p> Name Type Description Default <code>metadata_xml</code> <code>str</code> <p>Raw metadata XML string.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>SAMLMetadata with parsed fields.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.parse_response","title":"parse_response  <code>abstractmethod</code>","text":"<pre><code>parse_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLResponse\n</code></pre> <p>Parse and validate a SAML Response from an IdP.</p> <p>Automatically decrypts <code>&lt;saml:EncryptedAssertion&gt;</code> if the instance has a <code>private_key</code> configured.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw SAMLResponse data (Base64 string for POST,            query-string for Redirect).</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>Expected audience, expected issuer, etc.</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLResponse</code> <p>Parsed SAMLResponse with assertion data.</p> <p>Raises:</p> Type Description <code>JamSAMLExpired</code> <p>Assertion is expired.</p> <code>JamSAMLNotYetValid</code> <p>Assertion is not yet valid.</p> <code>JamSAMLInvalidAudience</code> <p>Audience doesn't match.</p> <code>JamSAMLInvalidIssuer</code> <p>Issuer doesn't match.</p> <code>JamSAMLValidationError</code> <p>Signature verification failed.</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.prepare_authn_request","title":"prepare_authn_request  <code>abstractmethod</code>","text":"<pre><code>prepare_authn_request(\n    idp_sso_url: str,\n    *,\n    acs_url: str,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build an AuthnRequest for the given IdP SSO URL.</p> <p>Parameters:</p> Name Type Description Default <code>idp_sso_url</code> <code>str</code> <p>IdP single sign-on endpoint URL.</p> required <code>acs_url</code> <code>str</code> <p>SP assertion consumer service URL.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>Extra params (relay_state, force_authn, etc.).</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>Redirect</code> <code>str</code> <p>IdP URL with signed AuthnRequest.</p> <code>POST</code> <code>str</code> <p>Base64-encoded SAMLRequest as a string (embed in form).</p>"},{"location":"api/jam.saml/#jam.saml.BaseSAML.resolve_artifact","title":"resolve_artifact  <code>abstractmethod</code>","text":"<pre><code>resolve_artifact(\n    artifact: str,\n    *,\n    issuer: str,\n    resolve_url: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Resolve a SAML artifact via SOAP to the IdP's resolve endpoint.</p> <p>Parameters:</p> Name Type Description Default <code>artifact</code> <code>str</code> <p>The artifact to resolve.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester (SP).</p> required <code>resolve_url</code> <code>str</code> <p>IdP artifact resolution service URL.</p> required <code>**kwargs</code> <code>Any</code> <p>Extra params (timeout, headers, expected_issuer, etc.).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>The original SAML message XML extracted from the ArtifactResponse.</p>"},{"location":"api/jam.saml/#jam.saml.SAML","title":"SAML","text":"<pre><code>SAML(\n    *,\n    role: str = \"sp\",\n    private_key: str | None = None,\n    public_key: str | None = None,\n    certificate: str | None = None,\n    entity_id: str | None = None,\n    acs_url: str | None = None,\n    sso_url: str | None = None,\n    idp_public_key: str | None = None,\n    sp_public_key: str | None = None,\n    encryption_key: str | None = None,\n    default_exp: int = 300,\n    allowed_clock_skew: int = 120,\n    want_assertions_signed: bool = True,\n    id_store: dict | None = None,\n    replay_ttl: int = 300,\n)\n</code></pre> <p>               Bases: <code>BaseSAML</code></p> <p>Concrete SAML 2.0 implementation.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p><code>\"sp\"</code> or <code>\"idp\"</code>.</p> <code>'sp'</code> <code>private_key</code> <code>str | None</code> <p>PEM string or path to private key file.</p> <code>None</code> <code>public_key</code> <code>str | None</code> <p>PEM string or path to public key / certificate.</p> <code>None</code> <code>certificate</code> <code>str | None</code> <p>PEM cert string (included in metadata/KeyInfo).</p> <code>None</code> <code>entity_id</code> <code>str | None</code> <p>Entity ID of this party.</p> <code>None</code> <code>acs_url</code> <code>str | None</code> <p>ACS URL (<code>sp</code> role).</p> <code>None</code> <code>sso_url</code> <code>str | None</code> <p>SSO URL (<code>idp</code> role).</p> <code>None</code> <code>idp_public_key</code> <code>str | None</code> <p>IdP public key for signature verification (<code>sp</code>).</p> <code>None</code> <code>sp_public_key</code> <code>str | None</code> <p>SP public key for signature verification (<code>idp</code>).</p> <code>None</code> <code>encryption_key</code> <code>str | None</code> <p>SP public key PEM for assertion encryption (<code>idp</code>).</p> <code>None</code> <code>default_exp</code> <code>int</code> <p>Default assertion lifetime in seconds.</p> <code>300</code> <code>allowed_clock_skew</code> <code>int</code> <p>Clock skew tolerance in seconds (default 120).</p> <code>120</code> <code>want_assertions_signed</code> <code>bool</code> <p>Require signed assertions (<code>sp</code>, default True).</p> <code>True</code> <code>id_store</code> <code>dict | None</code> <p>Dict for replay attack protection. Auto-created if None.</p> <code>None</code> <code>replay_ttl</code> <code>int</code> <p>Seconds before a consumed ID is eligible for cleanup.</p> <code>300</code> <p>Methods:</p> Name Description <code>build_artifact</code> <p>Create a SAML 2.0 artifact from a message ID.</p> <code>build_artifact_resolve</code> <p>Build a signed SAML ArtifactResolve.</p> <code>build_artifact_response</code> <p>Build a signed SAML ArtifactResponse wrapping the original message.</p> <code>build_attribute_query</code> <p>Build a signed SAML AttributeQuery.</p> <code>build_attribute_query_response</code> <p>Build a SAML Response for an AttributeQuery (no AuthnStatement).</p> <code>build_logout_request</code> <p>Build a SAML LogoutRequest.</p> <code>build_logout_response</code> <p>Build a SAML LogoutResponse.</p> <code>build_manage_name_id_request</code> <p>Build a signed SAML ManageNameIDRequest.</p> <code>build_manage_name_id_response</code> <p>Build a signed SAML ManageNameIDResponse.</p> <code>build_response</code> <p>Build and sign a SAML Response with an Assertion.</p> <code>generate_metadata</code> <p>Generate SAML metadata XML.</p> <code>parse_artifact_resolve</code> <p>Parse an incoming SAML ArtifactResolve.</p> <code>parse_artifact_response</code> <p>Parse a SAML ArtifactResponse.</p> <code>parse_attribute_query</code> <p>Parse an incoming SAML AttributeQuery.</p> <code>parse_attribute_query_response</code> <p>Parse a SAML Response from an AttributeQuery.</p> <code>parse_authn_request</code> <p>Parse an incoming AuthnRequest from an SP.</p> <code>parse_logout_request</code> <p>Parse a SAML LogoutRequest.</p> <code>parse_logout_response</code> <p>Parse a SAML LogoutResponse.</p> <code>parse_manage_name_id_request</code> <p>Parse a SAML ManageNameIDRequest.</p> <code>parse_manage_name_id_response</code> <p>Parse a SAML ManageNameIDResponse.</p> <code>parse_metadata</code> <p>Parse SAML metadata XML into a SAMLMetadata object.</p> <code>parse_response</code> <p>Parse and validate a SAML Response from an IdP.</p> <code>prepare_authn_request</code> <p>Build AuthnRequest and return IdP redirect URL or POST form data.</p> <code>resolve_artifact</code> <p>Resolve a SAML artifact via SOAP/HTTP to the IdP resolve endpoint.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_artifact","title":"build_artifact","text":"<pre><code>build_artifact(\n    source_message_id: str, *, issuer: str\n) -&gt; str\n</code></pre> <p>Create a SAML 2.0 artifact from a message ID.</p> 2 bytes type code (0x0001) + 2 bytes endpoint index <ul> <li>40 random bytes \u2192 Base64.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>source_message_id</code> <code>str</code> <p>The ID of the referenced message (unused                in artifact format, stored for reference).</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the issuer (stored for reference).</p> required <p>Returns:</p> Type Description <code>str</code> <p>Base64-encoded artifact string.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_artifact_resolve","title":"build_artifact_resolve","text":"<pre><code>build_artifact_resolve(\n    artifact: str,\n    *,\n    issuer: str,\n    destination: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML ArtifactResolve.</p> <p>Parameters:</p> Name Type Description Default <code>artifact</code> <code>str</code> <p>The artifact to resolve.</p> required <code>issuer</code> <code>str</code> <p>SP entity ID.</p> required <code>destination</code> <code>str</code> <p>IdP artifact resolution service URL.</p> required <code>**kwargs</code> <code>Any</code> <p>binding (\"post\", \"redirect\", or \"soap\").</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p> <code>SOAP</code> <code>str</code> <p>Raw XML.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_artifact_response","title":"build_artifact_response","text":"<pre><code>build_artifact_response(\n    in_response_to: str,\n    original_message_xml: str,\n    *,\n    issuer: str,\n    destination: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML ArtifactResponse wrapping the original message.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ArtifactResolve ID.</p> required <code>original_message_xml</code> <code>str</code> <p>The original SAML message XML to embed.</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>destination</code> <code>str</code> <p>SP endpoint URL.</p> required <code>**kwargs</code> <code>Any</code> <p>binding, status_code.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p> <code>SOAP</code> <code>str</code> <p>Raw XML.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_attribute_query","title":"build_attribute_query","text":"<pre><code>build_attribute_query(\n    subject: str,\n    *,\n    issuer: str,\n    destination: str,\n    attribute_names: list[str] | None = None,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>subject</code> <code>str</code> <p>Subject to query attributes for.</p> required <code>issuer</code> <code>str</code> <p>SP entity ID.</p> required <code>destination</code> <code>str</code> <p>IdP attribute query endpoint URL.</p> required <code>attribute_names</code> <code>list[str] | None</code> <p>Specific attributes to request (None = all).</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, name_id_format.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_attribute_query_response","title":"build_attribute_query_response","text":"<pre><code>build_attribute_query_response(\n    in_response_to: str,\n    subject: str,\n    attributes: dict[str, Any],\n    *,\n    issuer: str,\n    audience: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML Response for an AttributeQuery (no AuthnStatement).</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>AttributeQuery ID.</p> required <code>subject</code> <code>str</code> <p>The subject.</p> required <code>attributes</code> <code>dict[str, Any]</code> <p>User attributes dict.</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>audience</code> <code>str</code> <p>SP entity ID.</p> required <code>**kwargs</code> <code>Any</code> <p>destination, name_id_format.</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed SAML Response XML string.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_logout_request","title":"build_logout_request","text":"<pre><code>build_logout_request(\n    name_id: str,\n    *,\n    issuer: str,\n    destination: str,\n    session_index: str | None = None,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML LogoutRequest.</p> <p>Parameters:</p> Name Type Description Default <code>name_id</code> <code>str</code> <p>User identifier to log out.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the sender.</p> required <code>destination</code> <code>str</code> <p>SLO endpoint of the recipient.</p> required <code>session_index</code> <code>str | None</code> <p>Session index (optional).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>binding, relay_state, name_id_format.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_logout_response","title":"build_logout_response","text":"<pre><code>build_logout_response(\n    in_response_to: str,\n    *,\n    issuer: str,\n    destination: str,\n    status_code: str = STATUS_SUCCESS,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML LogoutResponse.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>LogoutRequest ID to respond to.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the sender.</p> required <code>destination</code> <code>str</code> <p>SLO endpoint of the recipient.</p> required <code>status_code</code> <code>str</code> <p>SAML status code.</p> <code>STATUS_SUCCESS</code> <code>**kwargs</code> <code>Any</code> <p>binding, relay_state.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_manage_name_id_request","title":"build_manage_name_id_request","text":"<pre><code>build_manage_name_id_request(\n    name_id: str,\n    *,\n    issuer: str,\n    destination: str,\n    new_id: str | None = None,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML ManageNameIDRequest.</p> <p>Parameters:</p> Name Type Description Default <code>name_id</code> <code>str</code> <p>Current NameID.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester.</p> required <code>destination</code> <code>str</code> <p>Recipient endpoint URL.</p> required <code>new_id</code> <code>str | None</code> <p>New identifier (None = terminate).</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, name_id_format.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_manage_name_id_response","title":"build_manage_name_id_response","text":"<pre><code>build_manage_name_id_response(\n    in_response_to: str,\n    *,\n    issuer: str,\n    destination: str,\n    status_code: str = STATUS_SUCCESS,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML ManageNameIDResponse.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ManageNameIDRequest ID.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the responder.</p> required <code>destination</code> <code>str</code> <p>Endpoint URL of the requester.</p> required <code>status_code</code> <code>str</code> <p>SAML status code (default Success).</p> <code>STATUS_SUCCESS</code> <code>**kwargs</code> <code>Any</code> <p>binding, relay_state.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.build_response","title":"build_response","text":"<pre><code>build_response(\n    subject: str,\n    attributes: dict[str, Any],\n    *,\n    issuer: str,\n    audience: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build and sign a SAML Response with an Assertion.</p> <p>Parameters:</p> Name Type Description Default <code>subject</code> <code>str</code> <p>Authenticated user identifier.</p> required <code>attributes</code> <code>dict[str, Any]</code> <p>User attributes dict.</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>audience</code> <code>str</code> <p>SP entity ID.</p> required <code>**kwargs</code> <code>Any</code> <p>in_response_to, name_id_format, session_index,       destination, encrypt (bool, default False).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed (and optionally encrypted) SAML Response XML string.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.generate_metadata","title":"generate_metadata","text":"<pre><code>generate_metadata(\n    *,\n    entity_id: str | None = None,\n    sso_url: str | None = None,\n    acs_url: str | None = None,\n    role: str | None = None,\n) -&gt; str\n</code></pre> <p>Generate SAML metadata XML.</p> <p>Parameters:</p> Name Type Description Default <code>entity_id</code> <code>str | None</code> <p>Entity ID (defaults to instance entity_id).</p> <code>None</code> <code>sso_url</code> <code>str | None</code> <p>SSO URL (IdP metadata).</p> <code>None</code> <code>acs_url</code> <code>str | None</code> <p>ACS URL (SP metadata).</p> <code>None</code> <code>role</code> <code>str | None</code> <p><code>\"idp\"</code> or <code>\"sp\"</code> (defaults to instance role).</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Metadata XML string.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_artifact_resolve","title":"parse_artifact_resolve","text":"<pre><code>parse_artifact_resolve(\n    saml_request: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLArtifactResolve\n</code></pre> <p>Parse an incoming SAML ArtifactResolve.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw ArtifactResolve data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default), <code>\"redirect\"</code>, or <code>\"soap\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLArtifactResolve</code> <p>Parsed SAMLArtifactResolve.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_artifact_response","title":"parse_artifact_response","text":"<pre><code>parse_artifact_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLArtifactResponse\n</code></pre> <p>Parse a SAML ArtifactResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw ArtifactResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default), <code>\"redirect\"</code>, or <code>\"soap\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLArtifactResponse</code> <p>Parsed SAMLArtifactResponse.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_attribute_query","title":"parse_attribute_query","text":"<pre><code>parse_attribute_query(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLAttributeQuery\n</code></pre> <p>Parse an incoming SAML AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw AttributeQuery data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLAttributeQuery</code> <p>Parsed SAMLAttributeQuery.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_attribute_query_response","title":"parse_attribute_query_response","text":"<pre><code>parse_attribute_query_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLResponse\n</code></pre> <p>Parse a SAML Response from an AttributeQuery.</p> <p>Delegates to parse_response.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw SAMLResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>audience, issuer, etc.</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLResponse</code> <p>Parsed SAMLResponse.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_authn_request","title":"parse_authn_request","text":"<pre><code>parse_authn_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLRequest\n</code></pre> <p>Parse an incoming AuthnRequest from an SP.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw SAMLRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLRequest</code> <p>Parsed SAMLRequest.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_logout_request","title":"parse_logout_request","text":"<pre><code>parse_logout_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLLogoutRequest\n</code></pre> <p>Parse a SAML LogoutRequest.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw LogoutRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer, public_key (override verification key).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLLogoutRequest</code> <p>Parsed SAMLLogoutRequest.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_logout_response","title":"parse_logout_response","text":"<pre><code>parse_logout_response(\n    saml_response: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLLogoutResponse\n</code></pre> <p>Parse a SAML LogoutResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw LogoutResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer, public_key (override verification key).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLLogoutResponse</code> <p>Parsed SAMLLogoutResponse.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_manage_name_id_request","title":"parse_manage_name_id_request","text":"<pre><code>parse_manage_name_id_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLManageNameIDRequest\n</code></pre> <p>Parse a SAML ManageNameIDRequest.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw ManageNameIDRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLManageNameIDRequest</code> <p>Parsed SAMLManageNameIDRequest.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_manage_name_id_response","title":"parse_manage_name_id_response","text":"<pre><code>parse_manage_name_id_response(\n    saml_response: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLManageNameIDResponse\n</code></pre> <p>Parse a SAML ManageNameIDResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw ManageNameIDResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLManageNameIDResponse</code> <p>Parsed SAMLManageNameIDResponse.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_metadata","title":"parse_metadata","text":"<pre><code>parse_metadata(metadata_xml: str) -&gt; SAMLMetadata\n</code></pre> <p>Parse SAML metadata XML into a SAMLMetadata object.</p> <p>Parameters:</p> Name Type Description Default <code>metadata_xml</code> <code>str</code> <p>Raw metadata XML string.</p> required <p>Returns:</p> Type Description <code>SAMLMetadata</code> <p>SAMLMetadata with entity_id, endpoints, cert, etc.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.parse_response","title":"parse_response","text":"<pre><code>parse_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLResponse\n</code></pre> <p>Parse and validate a SAML Response from an IdP.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw SAMLResponse (Base64 POST or query-string).</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>audience, issuer, verify_signature.</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLResponse</code> <p>SAMLResponse with parsed assertion data.</p>"},{"location":"api/jam.saml/#jam.saml.SAML.prepare_authn_request","title":"prepare_authn_request","text":"<pre><code>prepare_authn_request(\n    idp_sso_url: str,\n    *,\n    acs_url: str | None = None,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build AuthnRequest and return IdP redirect URL or POST form data.</p> <p>Parameters:</p> Name Type Description Default <code>idp_sso_url</code> <code>str</code> <p>IdP SSO endpoint URL.</p> required <code>acs_url</code> <code>str | None</code> <p>SP ACS URL.</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, issuer, etc.</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Redirect URL (<code>binding=\"redirect\"</code>) or Base64 form data (<code>\"post\"</code>).</p>"},{"location":"api/jam.saml/#jam.saml.SAML.resolve_artifact","title":"resolve_artifact","text":"<pre><code>resolve_artifact(\n    artifact: str,\n    *,\n    issuer: str,\n    resolve_url: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Resolve a SAML artifact via SOAP/HTTP to the IdP resolve endpoint.</p> <p>Parameters:</p> Name Type Description Default <code>artifact</code> <code>str</code> <p>The artifact to resolve.</p> required <code>issuer</code> <code>str</code> <p>SP entity ID.</p> required <code>resolve_url</code> <code>str</code> <p>IdP artifact resolution service URL.</p> required <code>**kwargs</code> <code>Any</code> <p>timeout (int, default 10), expected_issuer (IdP       entity ID to validate the ArtifactResponse issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>The original SAML message XML from the ArtifactResponse.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLArtifactResolve","title":"SAMLArtifactResolve  <code>dataclass</code>","text":"<pre><code>SAMLArtifactResolve(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    artifact: str,\n)\n</code></pre> <p>Parsed SAML ArtifactResolve.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLArtifactResponse","title":"SAMLArtifactResponse  <code>dataclass</code>","text":"<pre><code>SAMLArtifactResponse(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    in_response_to: str | None = None,\n    original_message: str | None = None,\n)\n</code></pre> <p>Parsed SAML ArtifactResponse.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLAssertion","title":"SAMLAssertion  <code>dataclass</code>","text":"<pre><code>SAMLAssertion(\n    id: str,\n    issuer: str,\n    issue_instant: datetime,\n    subject: SAMLSubject | None = None,\n    conditions: SAMLConditions | None = None,\n    attributes: dict[str, Any] = dict(),\n    authn_statement: SAMLAuthnStatement | None = None,\n)\n</code></pre> <p>Parsed SAML assertion data.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLAttributeQuery","title":"SAMLAttributeQuery  <code>dataclass</code>","text":"<pre><code>SAMLAttributeQuery(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    subject: str,\n    destination: str | None = None,\n    attribute_names: list[str] | None = None,\n)\n</code></pre> <p>Parsed SAML AttributeQuery.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLAuthnStatement","title":"SAMLAuthnStatement  <code>dataclass</code>","text":"<pre><code>SAMLAuthnStatement(\n    authn_instant: datetime,\n    session_index: str | None = None,\n)\n</code></pre> <p>SAML authentication statement.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLConditions","title":"SAMLConditions  <code>dataclass</code>","text":"<pre><code>SAMLConditions(\n    not_before: datetime | None = None,\n    not_on_or_after: datetime | None = None,\n    audience_restriction: list[str] = list(),\n)\n</code></pre> <p>SAML assertion conditions (time constraints + audience).</p>"},{"location":"api/jam.saml/#jam.saml.SAMLLogoutRequest","title":"SAMLLogoutRequest  <code>dataclass</code>","text":"<pre><code>SAMLLogoutRequest(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    name_id: str,\n    session_index: str | None = None,\n    destination: str | None = None,\n)\n</code></pre> <p>Parsed SAML LogoutRequest.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLLogoutResponse","title":"SAMLLogoutResponse  <code>dataclass</code>","text":"<pre><code>SAMLLogoutResponse(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    destination: str | None = None,\n    in_response_to: str | None = None,\n)\n</code></pre> <p>Parsed SAML LogoutResponse.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLManageNameIDRequest","title":"SAMLManageNameIDRequest  <code>dataclass</code>","text":"<pre><code>SAMLManageNameIDRequest(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    name_id: str,\n    new_id: str | None = None,\n    destination: str | None = None,\n)\n</code></pre> <p>Parsed SAML ManageNameIDRequest.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLManageNameIDResponse","title":"SAMLManageNameIDResponse  <code>dataclass</code>","text":"<pre><code>SAMLManageNameIDResponse(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    destination: str | None = None,\n    in_response_to: str | None = None,\n)\n</code></pre> <p>Parsed SAML ManageNameIDResponse.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLMetadata","title":"SAMLMetadata  <code>dataclass</code>","text":"<pre><code>SAMLMetadata(\n    entity_id: str,\n    role: str,\n    sso_url: str | None = None,\n    acs_url: str | None = None,\n    certificate: str | None = None,\n    want_authn_requests_signed: bool = False,\n    authn_requests_signed: bool = False,\n)\n</code></pre> <p>Parsed SAML metadata.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLRequest","title":"SAMLRequest  <code>dataclass</code>","text":"<pre><code>SAMLRequest(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    destination: str | None = None,\n    acs_url: str | None = None,\n    binding: str | None = None,\n)\n</code></pre> <p>Parsed SAML AuthnRequest.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLResponse","title":"SAMLResponse  <code>dataclass</code>","text":"<pre><code>SAMLResponse(\n    id: str,\n    issuer: str,\n    issue_instant: datetime,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    destination: str | None = None,\n    in_response_to: str | None = None,\n    assertion: SAMLAssertion | None = None,\n)\n</code></pre> <p>Parsed SAML protocol Response.</p>"},{"location":"api/jam.saml/#jam.saml.SAMLSubject","title":"SAMLSubject  <code>dataclass</code>","text":"<pre><code>SAMLSubject(\n    name_id: str,\n    format: str = \"urn:oasis:names:tc:SAML:1.1:nameid-format:unspecified\",\n    subject_confirmation_method: str = \"urn:oasis:names:tc:SAML:2.0:cm:bearer\",\n    subject_confirmation_data: dict[str, Any] | None = None,\n)\n</code></pre> <p>SAML assertion subject (NameID + confirmation).</p>"},{"location":"api/jam.saml/#jam.saml.create_instance","title":"create_instance","text":"<pre><code>create_instance(\n    role: str = \"sp\",\n    private_key: str | None = None,\n    public_key: str | None = None,\n    certificate: str | None = None,\n    entity_id: str | None = None,\n    acs_url: str | None = None,\n    sso_url: str | None = None,\n    idp_public_key: str | None = None,\n    sp_public_key: str | None = None,\n    encryption_key: str | None = None,\n    default_exp: int = 300,\n    allowed_clock_skew: int = 120,\n    want_assertions_signed: bool = True,\n    replay_ttl: int = 300,\n    **kwargs: Any,\n) -&gt; SAML\n</code></pre> <p>Create a SAML instance.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p><code>\"sp\"</code> (Service Provider) or <code>\"idp\"</code> (Identity Provider).</p> <code>'sp'</code> <code>private_key</code> <code>str | None</code> <p>PEM string or path to private key file.</p> <code>None</code> <code>public_key</code> <code>str | None</code> <p>PEM string or path to public key / certificate file.</p> <code>None</code> <code>certificate</code> <code>str | None</code> <p>PEM string or path to certificate file (included in metadata/signature).</p> <code>None</code> <code>entity_id</code> <code>str | None</code> <p>Entity ID of this SAML party.</p> <code>None</code> <code>acs_url</code> <code>str | None</code> <p>Assertion Consumer Service URL (for SP role).</p> <code>None</code> <code>sso_url</code> <code>str | None</code> <p>Single Sign-On URL (for IdP role).</p> <code>None</code> <code>idp_public_key</code> <code>str | None</code> <p>IdP public key / certificate PEM (for SP \u2014 signature verification).</p> <code>None</code> <code>sp_public_key</code> <code>str | None</code> <p>SP public key / certificate PEM (for IdP \u2014 SLO verification).</p> <code>None</code> <code>encryption_key</code> <code>str | None</code> <p>SP public key PEM for assertion encryption (IdP).</p> <code>None</code> <code>default_exp</code> <code>int</code> <p>Default assertion lifetime in seconds (default 300).</p> <code>300</code> <code>allowed_clock_skew</code> <code>int</code> <p>Clock skew tolerance in seconds (default 120).</p> <code>120</code> <code>want_assertions_signed</code> <code>bool</code> <p>Require signed assertions (SP, default True).</p> <code>True</code> <code>replay_ttl</code> <code>int</code> <p>Seconds before a consumed ID is eligible for cleanup (default 300).</p> <code>300</code> <code>**kwargs</code> <code>Any</code> <p>Additional parameters passed to the SAML constructor.</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAML</code> <p>SAML instance.</p>"},{"location":"api/jam.saml.metadata/","title":"jam.saml.metadata","text":""},{"location":"api/jam.saml.metadata/#jam.saml.metadata","title":"metadata","text":"<p>Functions:</p> Name Description <code>generate_idp_metadata</code> <p>Generate SAML IdP metadata XML.</p> <code>generate_sp_metadata</code> <p>Generate SAML SP metadata XML.</p> <code>parse_metadata</code> <p>Parse SAML 2.0 metadata XML.</p>"},{"location":"api/jam.saml.metadata/#jam.saml.metadata.generate_idp_metadata","title":"generate_idp_metadata","text":"<pre><code>generate_idp_metadata(\n    entity_id: str,\n    sso_url: str,\n    certificate: str | None = None,\n    want_authn_requests_signed: bool = False,\n) -&gt; str\n</code></pre> <p>Generate SAML IdP metadata XML.</p> <p>Parameters:</p> Name Type Description Default <code>entity_id</code> <code>str</code> <p>IdP entity ID.</p> required <code>sso_url</code> <code>str</code> <p>Single Sign-On endpoint URL.</p> required <code>certificate</code> <code>str | None</code> <p>PEM certificate string (included in KeyDescriptor).</p> <code>None</code> <code>want_authn_requests_signed</code> <code>bool</code> <p>Whether IdP requires signed AuthnRequests.</p> <code>False</code> <p>Returns:</p> Type Description <code>str</code> <p>SAML metadata XML string.</p>"},{"location":"api/jam.saml.metadata/#jam.saml.metadata.generate_sp_metadata","title":"generate_sp_metadata","text":"<pre><code>generate_sp_metadata(\n    entity_id: str,\n    acs_url: str,\n    certificate: str | None = None,\n    authn_requests_signed: bool = False,\n) -&gt; str\n</code></pre> <p>Generate SAML SP metadata XML.</p> <p>Parameters:</p> Name Type Description Default <code>entity_id</code> <code>str</code> <p>SP entity ID.</p> required <code>acs_url</code> <code>str</code> <p>Assertion Consumer Service URL.</p> required <code>certificate</code> <code>str | None</code> <p>PEM certificate string (included in KeyDescriptor).</p> <code>None</code> <code>authn_requests_signed</code> <code>bool</code> <p>Whether SP signs AuthnRequests.</p> <code>False</code> <p>Returns:</p> Type Description <code>str</code> <p>SAML metadata XML string.</p>"},{"location":"api/jam.saml.metadata/#jam.saml.metadata.parse_metadata","title":"parse_metadata","text":"<pre><code>parse_metadata(metadata_xml: str) -&gt; SAMLMetadata\n</code></pre> <p>Parse SAML 2.0 metadata XML.</p> <p>Parameters:</p> Name Type Description Default <code>metadata_xml</code> <code>str</code> <p>Raw metadata XML string.</p> required <p>Returns:</p> Type Description <code>SAMLMetadata</code> <p>SAMLMetadata with entity_id, role, endpoints, and certificate.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If neither IdP nor SP descriptor is found.</p>"},{"location":"api/jam.saml.saml/","title":"jam.saml.saml","text":""},{"location":"api/jam.saml.saml/#jam.saml.saml","title":"saml","text":"<p>Classes:</p> Name Description <code>SAML</code> <p>Concrete SAML 2.0 implementation.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML","title":"SAML","text":"<pre><code>SAML(\n    *,\n    role: str = \"sp\",\n    private_key: str | None = None,\n    public_key: str | None = None,\n    certificate: str | None = None,\n    entity_id: str | None = None,\n    acs_url: str | None = None,\n    sso_url: str | None = None,\n    idp_public_key: str | None = None,\n    sp_public_key: str | None = None,\n    encryption_key: str | None = None,\n    default_exp: int = 300,\n    allowed_clock_skew: int = 120,\n    want_assertions_signed: bool = True,\n    id_store: dict | None = None,\n    replay_ttl: int = 300,\n)\n</code></pre> <p>               Bases: <code>BaseSAML</code></p> <p>Concrete SAML 2.0 implementation.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p><code>\"sp\"</code> or <code>\"idp\"</code>.</p> <code>'sp'</code> <code>private_key</code> <code>str | None</code> <p>PEM string or path to private key file.</p> <code>None</code> <code>public_key</code> <code>str | None</code> <p>PEM string or path to public key / certificate.</p> <code>None</code> <code>certificate</code> <code>str | None</code> <p>PEM cert string (included in metadata/KeyInfo).</p> <code>None</code> <code>entity_id</code> <code>str | None</code> <p>Entity ID of this party.</p> <code>None</code> <code>acs_url</code> <code>str | None</code> <p>ACS URL (<code>sp</code> role).</p> <code>None</code> <code>sso_url</code> <code>str | None</code> <p>SSO URL (<code>idp</code> role).</p> <code>None</code> <code>idp_public_key</code> <code>str | None</code> <p>IdP public key for signature verification (<code>sp</code>).</p> <code>None</code> <code>sp_public_key</code> <code>str | None</code> <p>SP public key for signature verification (<code>idp</code>).</p> <code>None</code> <code>encryption_key</code> <code>str | None</code> <p>SP public key PEM for assertion encryption (<code>idp</code>).</p> <code>None</code> <code>default_exp</code> <code>int</code> <p>Default assertion lifetime in seconds.</p> <code>300</code> <code>allowed_clock_skew</code> <code>int</code> <p>Clock skew tolerance in seconds (default 120).</p> <code>120</code> <code>want_assertions_signed</code> <code>bool</code> <p>Require signed assertions (<code>sp</code>, default True).</p> <code>True</code> <code>id_store</code> <code>dict | None</code> <p>Dict for replay attack protection. Auto-created if None.</p> <code>None</code> <code>replay_ttl</code> <code>int</code> <p>Seconds before a consumed ID is eligible for cleanup.</p> <code>300</code> <p>Methods:</p> Name Description <code>build_artifact</code> <p>Create a SAML 2.0 artifact from a message ID.</p> <code>build_artifact_resolve</code> <p>Build a signed SAML ArtifactResolve.</p> <code>build_artifact_response</code> <p>Build a signed SAML ArtifactResponse wrapping the original message.</p> <code>build_attribute_query</code> <p>Build a signed SAML AttributeQuery.</p> <code>build_attribute_query_response</code> <p>Build a SAML Response for an AttributeQuery (no AuthnStatement).</p> <code>build_logout_request</code> <p>Build a SAML LogoutRequest.</p> <code>build_logout_response</code> <p>Build a SAML LogoutResponse.</p> <code>build_manage_name_id_request</code> <p>Build a signed SAML ManageNameIDRequest.</p> <code>build_manage_name_id_response</code> <p>Build a signed SAML ManageNameIDResponse.</p> <code>build_response</code> <p>Build and sign a SAML Response with an Assertion.</p> <code>generate_metadata</code> <p>Generate SAML metadata XML.</p> <code>parse_artifact_resolve</code> <p>Parse an incoming SAML ArtifactResolve.</p> <code>parse_artifact_response</code> <p>Parse a SAML ArtifactResponse.</p> <code>parse_attribute_query</code> <p>Parse an incoming SAML AttributeQuery.</p> <code>parse_attribute_query_response</code> <p>Parse a SAML Response from an AttributeQuery.</p> <code>parse_authn_request</code> <p>Parse an incoming AuthnRequest from an SP.</p> <code>parse_logout_request</code> <p>Parse a SAML LogoutRequest.</p> <code>parse_logout_response</code> <p>Parse a SAML LogoutResponse.</p> <code>parse_manage_name_id_request</code> <p>Parse a SAML ManageNameIDRequest.</p> <code>parse_manage_name_id_response</code> <p>Parse a SAML ManageNameIDResponse.</p> <code>parse_metadata</code> <p>Parse SAML metadata XML into a SAMLMetadata object.</p> <code>parse_response</code> <p>Parse and validate a SAML Response from an IdP.</p> <code>prepare_authn_request</code> <p>Build AuthnRequest and return IdP redirect URL or POST form data.</p> <code>resolve_artifact</code> <p>Resolve a SAML artifact via SOAP/HTTP to the IdP resolve endpoint.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_artifact","title":"build_artifact","text":"<pre><code>build_artifact(\n    source_message_id: str, *, issuer: str\n) -&gt; str\n</code></pre> <p>Create a SAML 2.0 artifact from a message ID.</p> 2 bytes type code (0x0001) + 2 bytes endpoint index <ul> <li>40 random bytes \u2192 Base64.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>source_message_id</code> <code>str</code> <p>The ID of the referenced message (unused                in artifact format, stored for reference).</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the issuer (stored for reference).</p> required <p>Returns:</p> Type Description <code>str</code> <p>Base64-encoded artifact string.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_artifact_resolve","title":"build_artifact_resolve","text":"<pre><code>build_artifact_resolve(\n    artifact: str,\n    *,\n    issuer: str,\n    destination: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML ArtifactResolve.</p> <p>Parameters:</p> Name Type Description Default <code>artifact</code> <code>str</code> <p>The artifact to resolve.</p> required <code>issuer</code> <code>str</code> <p>SP entity ID.</p> required <code>destination</code> <code>str</code> <p>IdP artifact resolution service URL.</p> required <code>**kwargs</code> <code>Any</code> <p>binding (\"post\", \"redirect\", or \"soap\").</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p> <code>SOAP</code> <code>str</code> <p>Raw XML.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_artifact_response","title":"build_artifact_response","text":"<pre><code>build_artifact_response(\n    in_response_to: str,\n    original_message_xml: str,\n    *,\n    issuer: str,\n    destination: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML ArtifactResponse wrapping the original message.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ArtifactResolve ID.</p> required <code>original_message_xml</code> <code>str</code> <p>The original SAML message XML to embed.</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>destination</code> <code>str</code> <p>SP endpoint URL.</p> required <code>**kwargs</code> <code>Any</code> <p>binding, status_code.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p> <code>SOAP</code> <code>str</code> <p>Raw XML.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_attribute_query","title":"build_attribute_query","text":"<pre><code>build_attribute_query(\n    subject: str,\n    *,\n    issuer: str,\n    destination: str,\n    attribute_names: list[str] | None = None,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>subject</code> <code>str</code> <p>Subject to query attributes for.</p> required <code>issuer</code> <code>str</code> <p>SP entity ID.</p> required <code>destination</code> <code>str</code> <p>IdP attribute query endpoint URL.</p> required <code>attribute_names</code> <code>list[str] | None</code> <p>Specific attributes to request (None = all).</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, name_id_format.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_attribute_query_response","title":"build_attribute_query_response","text":"<pre><code>build_attribute_query_response(\n    in_response_to: str,\n    subject: str,\n    attributes: dict[str, Any],\n    *,\n    issuer: str,\n    audience: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML Response for an AttributeQuery (no AuthnStatement).</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>AttributeQuery ID.</p> required <code>subject</code> <code>str</code> <p>The subject.</p> required <code>attributes</code> <code>dict[str, Any]</code> <p>User attributes dict.</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>audience</code> <code>str</code> <p>SP entity ID.</p> required <code>**kwargs</code> <code>Any</code> <p>destination, name_id_format.</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed SAML Response XML string.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_logout_request","title":"build_logout_request","text":"<pre><code>build_logout_request(\n    name_id: str,\n    *,\n    issuer: str,\n    destination: str,\n    session_index: str | None = None,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML LogoutRequest.</p> <p>Parameters:</p> Name Type Description Default <code>name_id</code> <code>str</code> <p>User identifier to log out.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the sender.</p> required <code>destination</code> <code>str</code> <p>SLO endpoint of the recipient.</p> required <code>session_index</code> <code>str | None</code> <p>Session index (optional).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>binding, relay_state, name_id_format.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_logout_response","title":"build_logout_response","text":"<pre><code>build_logout_response(\n    in_response_to: str,\n    *,\n    issuer: str,\n    destination: str,\n    status_code: str = STATUS_SUCCESS,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a SAML LogoutResponse.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>LogoutRequest ID to respond to.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the sender.</p> required <code>destination</code> <code>str</code> <p>SLO endpoint of the recipient.</p> required <code>status_code</code> <code>str</code> <p>SAML status code.</p> <code>STATUS_SUCCESS</code> <code>**kwargs</code> <code>Any</code> <p>binding, relay_state.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_manage_name_id_request","title":"build_manage_name_id_request","text":"<pre><code>build_manage_name_id_request(\n    name_id: str,\n    *,\n    issuer: str,\n    destination: str,\n    new_id: str | None = None,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML ManageNameIDRequest.</p> <p>Parameters:</p> Name Type Description Default <code>name_id</code> <code>str</code> <p>Current NameID.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the requester.</p> required <code>destination</code> <code>str</code> <p>Recipient endpoint URL.</p> required <code>new_id</code> <code>str | None</code> <p>New identifier (None = terminate).</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default) or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, name_id_format.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_manage_name_id_response","title":"build_manage_name_id_response","text":"<pre><code>build_manage_name_id_response(\n    in_response_to: str,\n    *,\n    issuer: str,\n    destination: str,\n    status_code: str = STATUS_SUCCESS,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build a signed SAML ManageNameIDResponse.</p> <p>Parameters:</p> Name Type Description Default <code>in_response_to</code> <code>str</code> <p>ManageNameIDRequest ID.</p> required <code>issuer</code> <code>str</code> <p>Entity ID of the responder.</p> required <code>destination</code> <code>str</code> <p>Endpoint URL of the requester.</p> required <code>status_code</code> <code>str</code> <p>SAML status code (default Success).</p> <code>STATUS_SUCCESS</code> <code>**kwargs</code> <code>Any</code> <p>binding, relay_state.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>POST</code> <code>str</code> <p>Base64-encoded signed XML.</p> <code>Redirect</code> <code>str</code> <p>Signed redirect URL.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.build_response","title":"build_response","text":"<pre><code>build_response(\n    subject: str,\n    attributes: dict[str, Any],\n    *,\n    issuer: str,\n    audience: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build and sign a SAML Response with an Assertion.</p> <p>Parameters:</p> Name Type Description Default <code>subject</code> <code>str</code> <p>Authenticated user identifier.</p> required <code>attributes</code> <code>dict[str, Any]</code> <p>User attributes dict.</p> required <code>issuer</code> <code>str</code> <p>IdP entity ID.</p> required <code>audience</code> <code>str</code> <p>SP entity ID.</p> required <code>**kwargs</code> <code>Any</code> <p>in_response_to, name_id_format, session_index,       destination, encrypt (bool, default False).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Signed (and optionally encrypted) SAML Response XML string.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.generate_metadata","title":"generate_metadata","text":"<pre><code>generate_metadata(\n    *,\n    entity_id: str | None = None,\n    sso_url: str | None = None,\n    acs_url: str | None = None,\n    role: str | None = None,\n) -&gt; str\n</code></pre> <p>Generate SAML metadata XML.</p> <p>Parameters:</p> Name Type Description Default <code>entity_id</code> <code>str | None</code> <p>Entity ID (defaults to instance entity_id).</p> <code>None</code> <code>sso_url</code> <code>str | None</code> <p>SSO URL (IdP metadata).</p> <code>None</code> <code>acs_url</code> <code>str | None</code> <p>ACS URL (SP metadata).</p> <code>None</code> <code>role</code> <code>str | None</code> <p><code>\"idp\"</code> or <code>\"sp\"</code> (defaults to instance role).</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Metadata XML string.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_artifact_resolve","title":"parse_artifact_resolve","text":"<pre><code>parse_artifact_resolve(\n    saml_request: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLArtifactResolve\n</code></pre> <p>Parse an incoming SAML ArtifactResolve.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw ArtifactResolve data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default), <code>\"redirect\"</code>, or <code>\"soap\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLArtifactResolve</code> <p>Parsed SAMLArtifactResolve.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_artifact_response","title":"parse_artifact_response","text":"<pre><code>parse_artifact_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLArtifactResponse\n</code></pre> <p>Parse a SAML ArtifactResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw ArtifactResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> (default), <code>\"redirect\"</code>, or <code>\"soap\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLArtifactResponse</code> <p>Parsed SAMLArtifactResponse.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_attribute_query","title":"parse_attribute_query","text":"<pre><code>parse_attribute_query(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLAttributeQuery\n</code></pre> <p>Parse an incoming SAML AttributeQuery.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw AttributeQuery data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLAttributeQuery</code> <p>Parsed SAMLAttributeQuery.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_attribute_query_response","title":"parse_attribute_query_response","text":"<pre><code>parse_attribute_query_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLResponse\n</code></pre> <p>Parse a SAML Response from an AttributeQuery.</p> <p>Delegates to parse_response.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw SAMLResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>audience, issuer, etc.</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLResponse</code> <p>Parsed SAMLResponse.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_authn_request","title":"parse_authn_request","text":"<pre><code>parse_authn_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLRequest\n</code></pre> <p>Parse an incoming AuthnRequest from an SP.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw SAMLRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer for validation).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLRequest</code> <p>Parsed SAMLRequest.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_logout_request","title":"parse_logout_request","text":"<pre><code>parse_logout_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLLogoutRequest\n</code></pre> <p>Parse a SAML LogoutRequest.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw LogoutRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer, public_key (override verification key).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLLogoutRequest</code> <p>Parsed SAMLLogoutRequest.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_logout_response","title":"parse_logout_response","text":"<pre><code>parse_logout_response(\n    saml_response: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLLogoutResponse\n</code></pre> <p>Parse a SAML LogoutResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw LogoutResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer, public_key (override verification key).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLLogoutResponse</code> <p>Parsed SAMLLogoutResponse.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_manage_name_id_request","title":"parse_manage_name_id_request","text":"<pre><code>parse_manage_name_id_request(\n    saml_request: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLManageNameIDRequest\n</code></pre> <p>Parse a SAML ManageNameIDRequest.</p> <p>Parameters:</p> Name Type Description Default <code>saml_request</code> <code>str</code> <p>Raw ManageNameIDRequest data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLManageNameIDRequest</code> <p>Parsed SAMLManageNameIDRequest.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_manage_name_id_response","title":"parse_manage_name_id_response","text":"<pre><code>parse_manage_name_id_response(\n    saml_response: str,\n    *,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; SAMLManageNameIDResponse\n</code></pre> <p>Parse a SAML ManageNameIDResponse.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw ManageNameIDResponse data.</p> required <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> (default) or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>issuer (expected issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLManageNameIDResponse</code> <p>Parsed SAMLManageNameIDResponse.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_metadata","title":"parse_metadata","text":"<pre><code>parse_metadata(metadata_xml: str) -&gt; SAMLMetadata\n</code></pre> <p>Parse SAML metadata XML into a SAMLMetadata object.</p> <p>Parameters:</p> Name Type Description Default <code>metadata_xml</code> <code>str</code> <p>Raw metadata XML string.</p> required <p>Returns:</p> Type Description <code>SAMLMetadata</code> <p>SAMLMetadata with entity_id, endpoints, cert, etc.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.parse_response","title":"parse_response","text":"<pre><code>parse_response(\n    saml_response: str,\n    *,\n    binding: str = \"post\",\n    **kwargs: Any,\n) -&gt; SAMLResponse\n</code></pre> <p>Parse and validate a SAML Response from an IdP.</p> <p>Parameters:</p> Name Type Description Default <code>saml_response</code> <code>str</code> <p>Raw SAMLResponse (Base64 POST or query-string).</p> required <code>binding</code> <code>str</code> <p><code>\"post\"</code> or <code>\"redirect\"</code>.</p> <code>'post'</code> <code>**kwargs</code> <code>Any</code> <p>audience, issuer, verify_signature.</p> <code>{}</code> <p>Returns:</p> Type Description <code>SAMLResponse</code> <p>SAMLResponse with parsed assertion data.</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.prepare_authn_request","title":"prepare_authn_request","text":"<pre><code>prepare_authn_request(\n    idp_sso_url: str,\n    *,\n    acs_url: str | None = None,\n    binding: str = \"redirect\",\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Build AuthnRequest and return IdP redirect URL or POST form data.</p> <p>Parameters:</p> Name Type Description Default <code>idp_sso_url</code> <code>str</code> <p>IdP SSO endpoint URL.</p> required <code>acs_url</code> <code>str | None</code> <p>SP ACS URL.</p> <code>None</code> <code>binding</code> <code>str</code> <p><code>\"redirect\"</code> or <code>\"post\"</code>.</p> <code>'redirect'</code> <code>**kwargs</code> <code>Any</code> <p>relay_state, issuer, etc.</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>Redirect URL (<code>binding=\"redirect\"</code>) or Base64 form data (<code>\"post\"</code>).</p>"},{"location":"api/jam.saml.saml/#jam.saml.saml.SAML.resolve_artifact","title":"resolve_artifact","text":"<pre><code>resolve_artifact(\n    artifact: str,\n    *,\n    issuer: str,\n    resolve_url: str,\n    **kwargs: Any,\n) -&gt; str\n</code></pre> <p>Resolve a SAML artifact via SOAP/HTTP to the IdP resolve endpoint.</p> <p>Parameters:</p> Name Type Description Default <code>artifact</code> <code>str</code> <p>The artifact to resolve.</p> required <code>issuer</code> <code>str</code> <p>SP entity ID.</p> required <code>resolve_url</code> <code>str</code> <p>IdP artifact resolution service URL.</p> required <code>**kwargs</code> <code>Any</code> <p>timeout (int, default 10), expected_issuer (IdP       entity ID to validate the ArtifactResponse issuer).</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>The original SAML message XML from the ArtifactResponse.</p>"},{"location":"api/jam.saml.signature/","title":"jam.saml.signature","text":""},{"location":"api/jam.saml.signature/#jam.saml.signature","title":"signature","text":"<p>Functions:</p> Name Description <code>extract_public_key_from_keyinfo</code> <p>Extract the RSA public key from an XML Signature's KeyInfo/X509Data.</p> <code>load_private_key</code> <p>Load an RSA private key from a PEM string.</p> <code>load_public_key</code> <p>Load an RSA public key from a PEM string or X.509 certificate.</p> <code>sign_assertion</code> <p>Sign a SAML Assertion with an enveloped XML signature.</p> <code>verify_assertion_signature</code> <p>Verify the enveloped XML signature on a SAML Assertion.</p>"},{"location":"api/jam.saml.signature/#jam.saml.signature.extract_public_key_from_keyinfo","title":"extract_public_key_from_keyinfo","text":"<pre><code>extract_public_key_from_keyinfo(\n    sig: Element,\n) -&gt; RSAPublicKey\n</code></pre> <p>Extract the RSA public key from an XML Signature's KeyInfo/X509Data.</p> <p>Parameters:</p> Name Type Description Default <code>sig</code> <code>Element</code> <p>The Signature Element containing KeyInfo.</p> required <p>Returns:</p> Type Description <code>RSAPublicKey</code> <p>RSAPublicKey.</p> <p>Raises:</p> Type Description <code>JamSAMLValidationError</code> <p>If KeyInfo is missing or no certificate found.</p>"},{"location":"api/jam.saml.signature/#jam.saml.signature.load_private_key","title":"load_private_key","text":"<pre><code>load_private_key(pem_str: str) -&gt; RSAPrivateKey\n</code></pre> <p>Load an RSA private key from a PEM string.</p> <p>Parameters:</p> Name Type Description Default <code>pem_str</code> <code>str</code> <p>PEM-encoded private key.</p> required <p>Returns:</p> Type Description <code>RSAPrivateKey</code> <p>RSAPrivateKey.</p> <p>Raises:</p> Type Description <code>JamSAMLValidationError</code> <p>If key is not RSA.</p>"},{"location":"api/jam.saml.signature/#jam.saml.signature.load_public_key","title":"load_public_key","text":"<pre><code>load_public_key(pem_str: str) -&gt; RSAPublicKey\n</code></pre> <p>Load an RSA public key from a PEM string or X.509 certificate.</p> <p>Parameters:</p> Name Type Description Default <code>pem_str</code> <code>str</code> <p>PEM-encoded public key or certificate.</p> required <p>Returns:</p> Type Description <code>RSAPublicKey</code> <p>RSAPublicKey.</p> <p>Raises:</p> Type Description <code>JamSAMLValidationError</code> <p>If key is not RSA.</p>"},{"location":"api/jam.saml.signature/#jam.saml.signature.sign_assertion","title":"sign_assertion","text":"<pre><code>sign_assertion(\n    assertion: Element,\n    key: RSAPrivateKey,\n    cert_pem: str | None = None,\n) -&gt; Element\n</code></pre> <p>Sign a SAML Assertion with an enveloped XML signature.</p> <p>The signature is added as a child of the Assertion element. Uses RSA-SHA256 and Exclusive XML Canonicalization.</p> <p>Parameters:</p> Name Type Description Default <code>assertion</code> <code>Element</code> <p>The Assertion Element to sign (must have an ID attribute).</p> required <code>key</code> <code>RSAPrivateKey</code> <p>RSA private key for signing.</p> required <code>cert_pem</code> <code>str | None</code> <p>Optional PEM certificate for X509Data in KeyInfo.</p> <code>None</code> <p>Returns:</p> Type Description <code>Element</code> <p>The assertion Element with Signature child appended.</p> <p>Raises:</p> Type Description <code>JamSAMLValidationError</code> <p>If assertion has no ID.</p>"},{"location":"api/jam.saml.signature/#jam.saml.signature.verify_assertion_signature","title":"verify_assertion_signature","text":"<pre><code>verify_assertion_signature(\n    assertion: Element, key: RSAPublicKey | None = None\n) -&gt; bool\n</code></pre> <p>Verify the enveloped XML signature on a SAML Assertion.</p> <p>Verifies both the digest (content integrity) and the RSA signature.</p> <p>Parameters:</p> Name Type Description Default <code>assertion</code> <code>Element</code> <p>The Assertion Element containing a Signature child.</p> required <code>key</code> <code>RSAPublicKey | None</code> <p>RSA public key. If None, extracted from KeyInfo in the signature.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if signature is valid.</p> <p>Raises:</p> Type Description <code>JamSAMLValidationError</code> <p>If signature is missing, malformed, or invalid.</p>"},{"location":"api/jam.saml.types/","title":"jam.saml.types","text":""},{"location":"api/jam.saml.types/#jam.saml.types","title":"types","text":"<p>Classes:</p> Name Description <code>SAMLArtifactResolve</code> <p>Parsed SAML ArtifactResolve.</p> <code>SAMLArtifactResponse</code> <p>Parsed SAML ArtifactResponse.</p> <code>SAMLAssertion</code> <p>Parsed SAML assertion data.</p> <code>SAMLAttributeQuery</code> <p>Parsed SAML AttributeQuery.</p> <code>SAMLAuthnStatement</code> <p>SAML authentication statement.</p> <code>SAMLConditions</code> <p>SAML assertion conditions (time constraints + audience).</p> <code>SAMLLogoutRequest</code> <p>Parsed SAML LogoutRequest.</p> <code>SAMLLogoutResponse</code> <p>Parsed SAML LogoutResponse.</p> <code>SAMLManageNameIDRequest</code> <p>Parsed SAML ManageNameIDRequest.</p> <code>SAMLManageNameIDResponse</code> <p>Parsed SAML ManageNameIDResponse.</p> <code>SAMLMetadata</code> <p>Parsed SAML metadata.</p> <code>SAMLRequest</code> <p>Parsed SAML AuthnRequest.</p> <code>SAMLResponse</code> <p>Parsed SAML protocol Response.</p> <code>SAMLSubject</code> <p>SAML assertion subject (NameID + confirmation).</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLArtifactResolve","title":"SAMLArtifactResolve  <code>dataclass</code>","text":"<pre><code>SAMLArtifactResolve(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    artifact: str,\n)\n</code></pre> <p>Parsed SAML ArtifactResolve.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLArtifactResponse","title":"SAMLArtifactResponse  <code>dataclass</code>","text":"<pre><code>SAMLArtifactResponse(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    in_response_to: str | None = None,\n    original_message: str | None = None,\n)\n</code></pre> <p>Parsed SAML ArtifactResponse.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLAssertion","title":"SAMLAssertion  <code>dataclass</code>","text":"<pre><code>SAMLAssertion(\n    id: str,\n    issuer: str,\n    issue_instant: datetime,\n    subject: SAMLSubject | None = None,\n    conditions: SAMLConditions | None = None,\n    attributes: dict[str, Any] = dict(),\n    authn_statement: SAMLAuthnStatement | None = None,\n)\n</code></pre> <p>Parsed SAML assertion data.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLAttributeQuery","title":"SAMLAttributeQuery  <code>dataclass</code>","text":"<pre><code>SAMLAttributeQuery(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    subject: str,\n    destination: str | None = None,\n    attribute_names: list[str] | None = None,\n)\n</code></pre> <p>Parsed SAML AttributeQuery.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLAuthnStatement","title":"SAMLAuthnStatement  <code>dataclass</code>","text":"<pre><code>SAMLAuthnStatement(\n    authn_instant: datetime,\n    session_index: str | None = None,\n)\n</code></pre> <p>SAML authentication statement.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLConditions","title":"SAMLConditions  <code>dataclass</code>","text":"<pre><code>SAMLConditions(\n    not_before: datetime | None = None,\n    not_on_or_after: datetime | None = None,\n    audience_restriction: list[str] = list(),\n)\n</code></pre> <p>SAML assertion conditions (time constraints + audience).</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLLogoutRequest","title":"SAMLLogoutRequest  <code>dataclass</code>","text":"<pre><code>SAMLLogoutRequest(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    name_id: str,\n    session_index: str | None = None,\n    destination: str | None = None,\n)\n</code></pre> <p>Parsed SAML LogoutRequest.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLLogoutResponse","title":"SAMLLogoutResponse  <code>dataclass</code>","text":"<pre><code>SAMLLogoutResponse(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    destination: str | None = None,\n    in_response_to: str | None = None,\n)\n</code></pre> <p>Parsed SAML LogoutResponse.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLManageNameIDRequest","title":"SAMLManageNameIDRequest  <code>dataclass</code>","text":"<pre><code>SAMLManageNameIDRequest(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    name_id: str,\n    new_id: str | None = None,\n    destination: str | None = None,\n)\n</code></pre> <p>Parsed SAML ManageNameIDRequest.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLManageNameIDResponse","title":"SAMLManageNameIDResponse  <code>dataclass</code>","text":"<pre><code>SAMLManageNameIDResponse(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    destination: str | None = None,\n    in_response_to: str | None = None,\n)\n</code></pre> <p>Parsed SAML ManageNameIDResponse.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLMetadata","title":"SAMLMetadata  <code>dataclass</code>","text":"<pre><code>SAMLMetadata(\n    entity_id: str,\n    role: str,\n    sso_url: str | None = None,\n    acs_url: str | None = None,\n    certificate: str | None = None,\n    want_authn_requests_signed: bool = False,\n    authn_requests_signed: bool = False,\n)\n</code></pre> <p>Parsed SAML metadata.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLRequest","title":"SAMLRequest  <code>dataclass</code>","text":"<pre><code>SAMLRequest(\n    id: str,\n    issuer: str | None,\n    issue_instant: datetime,\n    destination: str | None = None,\n    acs_url: str | None = None,\n    binding: str | None = None,\n)\n</code></pre> <p>Parsed SAML AuthnRequest.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLResponse","title":"SAMLResponse  <code>dataclass</code>","text":"<pre><code>SAMLResponse(\n    id: str,\n    issuer: str,\n    issue_instant: datetime,\n    status_code: str = \"urn:oasis:names:tc:SAML:2.0:status:Success\",\n    destination: str | None = None,\n    in_response_to: str | None = None,\n    assertion: SAMLAssertion | None = None,\n)\n</code></pre> <p>Parsed SAML protocol Response.</p>"},{"location":"api/jam.saml.types/#jam.saml.types.SAMLSubject","title":"SAMLSubject  <code>dataclass</code>","text":"<pre><code>SAMLSubject(\n    name_id: str,\n    format: str = \"urn:oasis:names:tc:SAML:1.1:nameid-format:unspecified\",\n    subject_confirmation_method: str = \"urn:oasis:names:tc:SAML:2.0:cm:bearer\",\n    subject_confirmation_data: dict[str, Any] | None = None,\n)\n</code></pre> <p>SAML assertion subject (NameID + confirmation).</p>"},{"location":"api/jam.saml.xml/","title":"jam.saml.xml","text":""},{"location":"api/jam.saml.xml/#jam.saml.xml","title":"xml","text":"<p>Functions:</p> Name Description <code>canonicalize_xml</code> <p>Inclusive XML canonicalization (C14N) of an Element.</p> <code>fmt_instant</code> <p>Format a datetime as SAML-style UTC string (<code>2024-01-15T12:00:00Z</code>).</p> <code>make_element</code> <p>Create an Element with the given namespace-qualified tag.</p> <code>make_id</code> <p>Generate a unique SAML identifier (<code>_&lt;hex&gt;</code>).</p> <code>parse_instant</code> <p>Parse a SAML-style UTC string into a datetime.</p> <code>register_namespaces</code> <p>Register SAML and DS namespace prefixes with ElementTree.</p> <code>safe_fromstring</code> <p>Parse XML string with XXE and entity expansion protection.</p>"},{"location":"api/jam.saml.xml/#jam.saml.xml.canonicalize_xml","title":"canonicalize_xml","text":"<pre><code>canonicalize_xml(elem: Element) -&gt; bytes\n</code></pre> <p>Inclusive XML canonicalization (C14N) of an Element.</p> <p>Note: uses standard (non-exclusive) C14N for Python 3.10 compatibility.</p>"},{"location":"api/jam.saml.xml/#jam.saml.xml.fmt_instant","title":"fmt_instant","text":"<pre><code>fmt_instant(dt: datetime | None = None) -&gt; str\n</code></pre> <p>Format a datetime as SAML-style UTC string (<code>2024-01-15T12:00:00Z</code>).</p>"},{"location":"api/jam.saml.xml/#jam.saml.xml.make_element","title":"make_element","text":"<pre><code>make_element(\n    tag: str, ns: str, attrib: dict[str, str] | None = None\n) -&gt; Element\n</code></pre> <p>Create an Element with the given namespace-qualified tag.</p>"},{"location":"api/jam.saml.xml/#jam.saml.xml.make_id","title":"make_id","text":"<pre><code>make_id() -&gt; str\n</code></pre> <p>Generate a unique SAML identifier (<code>_&lt;hex&gt;</code>).</p>"},{"location":"api/jam.saml.xml/#jam.saml.xml.parse_instant","title":"parse_instant","text":"<pre><code>parse_instant(text: str) -&gt; datetime\n</code></pre> <p>Parse a SAML-style UTC string into a datetime.</p>"},{"location":"api/jam.saml.xml/#jam.saml.xml.register_namespaces","title":"register_namespaces","text":"<pre><code>register_namespaces() -&gt; None\n</code></pre> <p>Register SAML and DS namespace prefixes with ElementTree.</p>"},{"location":"api/jam.saml.xml/#jam.saml.xml.safe_fromstring","title":"safe_fromstring","text":"<pre><code>safe_fromstring(xml_str: str) -&gt; Element\n</code></pre> <p>Parse XML string with XXE and entity expansion protection.</p> <p>Rejects XML containing DTD/DOCTYPE declarations (required for XXE). Python 3.8+ already limits entity expansion by default in ElementTree.</p> <p>Parameters:</p> Name Type Description Default <code>xml_str</code> <code>str</code> <p>XML string to parse.</p> required <p>Returns:</p> Type Description <code>Element</code> <p>Root element.</p> <p>Raises:</p> Type Description <code>JamSAMLValidationError</code> <p>If XML is malformed or contains unsafe entities.</p>"},{"location":"api/jam.saml.xml/#jam.saml.xml.sub_element","title":"sub_element","text":"<pre><code>sub_element(\n    parent: Element,\n    tag: str,\n    ns: str,\n    text: str | None = None,\n    attrib: dict[str, str] | None = None,\n) -&gt; Element\n</code></pre> <p>Add a child Element with namespace-qualified tag and optional text.</p>"},{"location":"api/jam.sessions.__base__/","title":"jam.sessions.base","text":""},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__","title":"__base__","text":"<p>Classes:</p> Name Description <code>BaseSessionModule</code> <p>Abstract base class for session management modules.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule","title":"BaseSessionModule","text":"<pre><code>BaseSessionModule(\n    id_factory: Callable[[], str] = lambda: str(uuid4()),\n    is_session_crypt: bool = False,\n    session_aes_secret: bytes | str | None = None,\n    serializer: type[BaseEncoder]\n    | BaseEncoder = JsonEncoder,\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Abstract base class for session management modules.</p> <p>You can create your own module for sessions. For example:</p> <pre><code>from jam.sessions import BaseSessionModule\n\nclass CustomSessionModule(BaseSessionModule):\n    def __init__(self, some_param: str) -&gt; None:\n        super().__init__(\n            is_session_crypt=True,\n            session_aes_secret=b'your-32-byte-base64-encoded-key'\n        )\n\n    # Your initialization code here\n    def create(session_key: str) -&gt; str:\n        ...\n\n    def get(session_id: str) -&gt; dict:\n        ...\n\n    def delete(session_id: str) -&gt; None:\n        ...\n\n    def update(session_id: str, data: dict) -&gt; None:\n        ...\n\n    def rework(session_id: str) -&gt; str:\n        ...\n\n    def clear(session_key: str) -&gt; None:\n        ...\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>id_factory</code> <code>Callable[str]</code> <p>A callable that generates unique IDs. Defaults to a UUID factory.</p> <code>lambda: str(uuid4())</code> <code>is_session_crypt</code> <code>bool</code> <p>If True, session keys will be encoded. Defaults to False.</p> <code>False</code> <code>session_aes_secret</code> <code>Optional[bytes, str]</code> <p>AES secret for encoding session keys.</p> <code>None</code> <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder. Defaults to JsonEncoder.</p> <code>JsonEncoder</code> <code>logger</code> <code>Optional[BaseLogger]</code> <p>Logger instance. Defaults to None.</p> <code>None</code> <p>Raises:</p> Type Description <code>JamSessionEmptyAESKey</code> <p>If 'is_session_crypt' is True and 'session_aes_secret' is not provided.</p> <p>Methods:</p> Name Description <code>__decode_session_data__</code> <p>Decode session data.</p> <code>__decode_session_id__</code> <p>Decode the session using AES decryption.</p> <code>__decode_session_id_if_needed__</code> <p>Decode the session ID if it is encoded.</p> <code>__encode_session_data__</code> <p>Encode session data.</p> <code>__encode_session_id__</code> <p>Encode the session using AES encryption.</p> <code>__encode_session_id_if_needed__</code> <p>Encode the session ID if it is not already encoded.</p> <code>clear</code> <p>Clear all sessions by key.</p> <code>create</code> <p>Create a new session with the given session key and data.</p> <code>delete</code> <p>Delete a session by its key or ID.</p> <code>get</code> <p>Retrieve a session by its key or ID.</p> <code>rework</code> <p>Rework a session and return its new ID.</p> <code>update</code> <p>Update an existing session with new data.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Return the unique ID use id_factory.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre> <p>Return the unique ID use id_factory.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.__decode_session_data__","title":"__decode_session_data__","text":"<pre><code>__decode_session_data__(data: str) -&gt; dict\n</code></pre> <p>Decode session data.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.__decode_session_id__","title":"__decode_session_id__","text":"<pre><code>__decode_session_id__(data: str) -&gt; str\n</code></pre> <p>Decode the session using AES decryption.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.__decode_session_id_if_needed__","title":"__decode_session_id_if_needed__","text":"<pre><code>__decode_session_id_if_needed__(data: str) -&gt; str\n</code></pre> <p>Decode the session ID if it is encoded.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.__encode_session_data__","title":"__encode_session_data__","text":"<pre><code>__encode_session_data__(data: dict) -&gt; str\n</code></pre> <p>Encode session data.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.__encode_session_id__","title":"__encode_session_id__","text":"<pre><code>__encode_session_id__(data: str) -&gt; str\n</code></pre> <p>Encode the session using AES encryption.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.__encode_session_id_if_needed__","title":"__encode_session_id_if_needed__","text":"<pre><code>__encode_session_id_if_needed__(data: str) -&gt; str\n</code></pre> <p>Encode the session ID if it is not already encoded.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.clear","title":"clear  <code>abstractmethod</code>","text":"<pre><code>clear(session_key: str) -&gt; None\n</code></pre> <p>Clear all sessions by key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The key for the sessions to clear.</p> required"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.create","title":"create  <code>abstractmethod</code>","text":"<pre><code>create(session_key: str, data: dict[str, Any]) -&gt; str\n</code></pre> <p>Create a new session with the given session key and data.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The key for the session.</p> required <code>data</code> <code>dict</code> <p>The data to be stored in the session.</p> required"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.delete","title":"delete  <code>abstractmethod</code>","text":"<pre><code>delete(session_id: str) -&gt; None\n</code></pre> <p>Delete a session by its key or ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session.</p> required"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.get","title":"get  <code>abstractmethod</code>","text":"<pre><code>get(session_id: str) -&gt; dict[str, Any] | None\n</code></pre> <p>Retrieve a session by its key or ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session.</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>dict | None: The session data if found, otherwise None.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.rework","title":"rework  <code>abstractmethod</code>","text":"<pre><code>rework(session_id: str) -&gt; str\n</code></pre> <p>Rework a session and return its new ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to rework.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If the session with the given ID does not exist.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The new session ID.</p>"},{"location":"api/jam.sessions.__base__/#jam.sessions.__base__.BaseSessionModule.update","title":"update  <code>abstractmethod</code>","text":"<pre><code>update(session_id: str, data: dict[str, Any]) -&gt; None\n</code></pre> <p>Update an existing session with new data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to update.</p> required <code>data</code> <code>dict</code> <p>The new data to be stored in the session.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If the session with the given ID does not exist.</p>"},{"location":"api/jam.sessions.json/","title":"jam.sessions.json","text":""},{"location":"api/jam.sessions.json/#jam.sessions.json","title":"json","text":"<p>Classes:</p> Name Description <code>JSONSessions</code> <p>Session management module for JSON storage.</p>"},{"location":"api/jam.sessions.json/#jam.sessions.json.JSONSessions","title":"JSONSessions","text":"<pre><code>JSONSessions(\n    json_path: str = \"sessions.json\",\n    is_session_crypt: bool = False,\n    session_aes_secret: bytes | str | None = getenv(\n        \"JAM_SESSION_AES_SECRET\", None\n    ),\n    id_factory: Callable[[], str] = lambda: str(uuid4()),\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseSessionModule</code></p> <p>Session management module for JSON storage.</p> <p>Parameters:</p> Name Type Description Default <code>json_path</code> <code>str</code> <p>Path to the JSON file where sessions will be stored.</p> <code>'sessions.json'</code> <code>is_session_crypt</code> <code>bool</code> <p>If True, session keys will be encoded.</p> <code>False</code> <code>session_aes_secret</code> <code>Optional[bytes]</code> <p>AES secret for encoding session keys. Required if <code>is_session_crypt</code> is True.</p> <code>getenv('JAM_SESSION_AES_SECRET', None)</code> <code>id_factory</code> <code>Callable[[], str]</code> <p>A callable that generates unique IDs. Defaults to a UUID factory.</p> <code>lambda: str(uuid4())</code> <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder. Defaults to JsonEncoder.</p> <code>JsonEncoder</code> <code>logger</code> <code>Optional[BaseLogger]</code> <p>Logger instance. Defaults to None.</p> <code>None</code> <p>Raises:</p> Type Description <code>JamSessionEmptyAESKey</code> <p>If 'is_session_crypt' is True and 'session_aes_secret' is not provided.</p> <p>Methods:</p> Name Description <code>clear</code> <p>Clear all sessions for a given session key.</p> <code>create</code> <p>Create a new session.</p> <code>delete</code> <p>Delete a session by its ID.</p> <code>get</code> <p>Retrieve session data by session ID.</p> <code>rework</code> <p>Rework (regenerate) a session ID.</p> <code>update</code> <p>Update session data by its ID.</p>"},{"location":"api/jam.sessions.json/#jam.sessions.json.JSONSessions.clear","title":"clear","text":"<pre><code>clear(session_key: str) -&gt; None\n</code></pre> <p>Clear all sessions for a given session key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The session key to clear.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p>"},{"location":"api/jam.sessions.json/#jam.sessions.json.JSONSessions.create","title":"create","text":"<pre><code>create(session_key: str, data: dict) -&gt; str\n</code></pre> <p>Create a new session.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The session key.</p> required <code>data</code> <code>dict</code> <p>The data to be stored in the session.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The ID of the created session.</p>"},{"location":"api/jam.sessions.json/#jam.sessions.json.JSONSessions.delete","title":"delete","text":"<pre><code>delete(session_id: str) -&gt; None\n</code></pre> <p>Delete a session by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to delete.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p>"},{"location":"api/jam.sessions.json/#jam.sessions.json.JSONSessions.get","title":"get","text":"<pre><code>get(session_id) -&gt; dict | None\n</code></pre> <p>Retrieve session data by session ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to retrieve.</p> required <p>Returns:</p> Type Description <code>dict | None</code> <p>dict | None: The session data if found, otherwise None.</p>"},{"location":"api/jam.sessions.json/#jam.sessions.json.JSONSessions.rework","title":"rework","text":"<pre><code>rework(session_id: str) -&gt; str\n</code></pre> <p>Rework (regenerate) a session ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The current session ID to be reworked.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If session not found</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The new session ID.</p>"},{"location":"api/jam.sessions.json/#jam.sessions.json.JSONSessions.update","title":"update","text":"<pre><code>update(session_id: str, data: dict) -&gt; None\n</code></pre> <p>Update session data by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to update.</p> required <code>data</code> <code>dict</code> <p>The new data to store in the session.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If session not found</p>"},{"location":"api/jam.sessions/","title":"jam.sessions","text":""},{"location":"api/jam.sessions/#jam.sessions","title":"sessions","text":"<p>Module for making server auth sessions.</p> <p>Modules:</p> Name Description <code>__base__</code> <code>json</code> <code>redis</code> <p>Functions:</p> Name Description <code>create_instance</code> <p>Create session module instance.</p>"},{"location":"api/jam.sessions/#jam.sessions.create_instance","title":"create_instance","text":"<pre><code>create_instance(\n    session_type: str | None = None,\n    sessions_type: str | None = None,\n    logger: BaseLogger = logger,\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    **kwargs: Any,\n) -&gt; BaseSessionModule\n</code></pre> <p>Create session module instance.</p> <p>Parameters:</p> Name Type Description Default <code>session_type</code> <code>str | None</code> <p>\"redis\" | \"json\" | \"custom\"</p> <code>None</code> <code>sessions_type</code> <code>str | None</code> <p>Alias for session_type (deprecated, use 'session_type')</p> <code>None</code> <code>logger</code> <code>BaseLogger</code> <p>Logger instance</p> <code>logger</code> <code>serializer</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>JSON encoder/decoder</p> <code>JsonEncoder</code> <code>**kwargs</code> <code>Any</code> <p>Config params specific to session type</p> <code>{}</code> <p>Returns:</p> Type Description <code>BaseSessionModule</code> <p>BaseSessionModule instance</p>"},{"location":"api/jam.sessions.redis/","title":"jam.sessions.redis","text":""},{"location":"api/jam.sessions.redis/#jam.sessions.redis","title":"redis","text":"<p>Classes:</p> Name Description <code>RedisSessions</code> <p>Redis session management module.</p>"},{"location":"api/jam.sessions.redis/#jam.sessions.redis.RedisSessions","title":"RedisSessions","text":"<pre><code>RedisSessions(\n    redis_uri: str | Redis = \"redis://localhost:6379/0\",\n    redis_sessions_key: str = \"sessions\",\n    ttl: int | None = 3600,\n    is_session_crypt: bool = False,\n    session_aes_secret: bytes | str | None = getenv(\n        \"JAM_SESSION_AES_SECRET\", None\n    ),\n    id_factory: Callable[[], str] = lambda: str(uuid4()),\n    serializer: BaseEncoder\n    | type[BaseEncoder] = JsonEncoder,\n    logger: BaseLogger | None = None,\n)\n</code></pre> <p>               Bases: <code>BaseSessionModule</code></p> <p>Redis session management module.</p> <p>Parameters:</p> Name Type Description Default <code>redis_uri</code> <code>str | Redis</code> <p>The URI for the Redis server.</p> <code>'redis://localhost:6379/0'</code> <code>redis_sessions_key</code> <code>str</code> <p>The key under which sessions are stored in Redis.</p> <code>'sessions'</code> <code>ttl</code> <code>Optional[int]</code> <p>Default time-to-live for sessions in seconds. Defaults to 3600 seconds (1 hour).</p> <code>3600</code> <code>is_session_crypt</code> <code>bool</code> <p>If True, session keys will be encoded.</p> <code>False</code> <code>session_aes_secret</code> <code>Optional[bytes, str]</code> <p>AES secret for encoding session keys. Required if <code>is_session_key_crypt</code> is True.</p> <code>getenv('JAM_SESSION_AES_SECRET', None)</code> <code>id_factory</code> <code>Callable[[], str]</code> <p>A callable that generates unique IDs. Defaults to a UUID factory.</p> <code>lambda: str(uuid4())</code> <code>serializer</code> <code>Union[BaseEncoder, type[BaseEncoder]]</code> <p>JSON encoder/decoder. Defaults to JsonEncoder.</p> <code>JsonEncoder</code> <code>logger</code> <code>Optional[BaseLogger]</code> <p>Logger instance. Defaults to None.</p> <code>None</code> <p>Raises:</p> Type Description <code>JamSessionEmptyAESKey</code> <p>If 'is_session_crypt' is True and 'session_aes_secret' is not provided.</p> <p>Methods:</p> Name Description <code>clear</code> <p>Clear all sessions for a given session key.</p> <code>create</code> <p>Create a new session with the given session key and data.</p> <code>delete</code> <p>Delete a session by its ID.</p> <code>get</code> <p>Retrieve a session by its key or ID.</p> <code>rework</code> <p>Rework a session and return its new ID.</p> <code>update</code> <p>Update an existing session with new data.</p>"},{"location":"api/jam.sessions.redis/#jam.sessions.redis.RedisSessions.clear","title":"clear","text":"<pre><code>clear(session_key: str) -&gt; None\n</code></pre> <p>Clear all sessions for a given session key.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The session key to clear.</p> required"},{"location":"api/jam.sessions.redis/#jam.sessions.redis.RedisSessions.create","title":"create","text":"<pre><code>create(session_key: str, data: dict) -&gt; str\n</code></pre> <p>Create a new session with the given session key and data.</p> <p>Parameters:</p> Name Type Description Default <code>session_key</code> <code>str</code> <p>The key for the session.</p> required <code>data</code> <code>dict</code> <p>The data to be stored in the session.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The unique ID of the created session.</p>"},{"location":"api/jam.sessions.redis/#jam.sessions.redis.RedisSessions.delete","title":"delete","text":"<pre><code>delete(session_id: str) -&gt; None\n</code></pre> <p>Delete a session by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The session ID.</p> required"},{"location":"api/jam.sessions.redis/#jam.sessions.redis.RedisSessions.get","title":"get","text":"<pre><code>get(session_id: str) -&gt; dict | None\n</code></pre> <p>Retrieve a session by its key or ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The session key or ID.</p> required <p>Returns:</p> Type Description <code>dict | None</code> <p>dict | None: The session data if found, otherwise None.</p>"},{"location":"api/jam.sessions.redis/#jam.sessions.redis.RedisSessions.rework","title":"rework","text":"<pre><code>rework(session_id: str) -&gt; str\n</code></pre> <p>Rework a session and return its new ID.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to rework.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If the session with the given ID does not exist.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The new session ID.</p>"},{"location":"api/jam.sessions.redis/#jam.sessions.redis.RedisSessions.update","title":"update","text":"<pre><code>update(session_id: str, data: dict) -&gt; None\n</code></pre> <p>Update an existing session with new data.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The ID of the session to update.</p> required <code>data</code> <code>dict</code> <p>The new data to be stored in the session.</p> required <p>Raises:</p> Type Description <code>JamSessionNotFound</code> <p>If the session with the given ID does not exist.</p>"},{"location":"api/jam.utils.aes/","title":"jam.utils.aes","text":""},{"location":"api/jam.utils.aes/#jam.utils.aes","title":"aes","text":"<p>Functions:</p> Name Description <code>generate_aes_key</code> <p>Generate a new AES key.</p>"},{"location":"api/jam.utils.aes/#jam.utils.aes.generate_aes_key","title":"generate_aes_key","text":"<pre><code>generate_aes_key() -&gt; bytes\n</code></pre> <p>Generate a new AES key.</p>"},{"location":"api/jam.utils.await_maybe/","title":"jam.utils.await_maybe","text":""},{"location":"api/jam.utils.await_maybe/#jam.utils.await_maybe","title":"await_maybe","text":"<p>Functions:</p> Name Description <code>await_maybe</code> <p>Source: https://github.com/strawberry-graphql/strawberry/blob/main/strawberry/utils/await_maybe.py.</p>"},{"location":"api/jam.utils.await_maybe/#jam.utils.await_maybe.await_maybe","title":"await_maybe  <code>async</code>","text":"<pre><code>await_maybe(value: AwaitableOrValue[T]) -&gt; T\n</code></pre> <p>Source: https://github.com/strawberry-graphql/strawberry/blob/main/strawberry/utils/await_maybe.py.</p>"},{"location":"api/jam.utils.basic_auth/","title":"jam.utils.basic_auth","text":""},{"location":"api/jam.utils.basic_auth/#jam.utils.basic_auth","title":"basic_auth","text":"<p>Functions:</p> Name Description <code>basic_auth_decode</code> <p>Decodes basic auth token to login and password.</p> <code>basic_auth_encode</code> <p>Encodes the login and password in a basic auth token.</p>"},{"location":"api/jam.utils.basic_auth/#jam.utils.basic_auth.basic_auth_decode","title":"basic_auth_decode","text":"<pre><code>basic_auth_decode(data: str) -&gt; tuple[str, str]\n</code></pre> <p>Decodes basic auth token to login and password.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>str</code> <p>Decoded data</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If incorrect format</p> Example <p>login, password = basic_auth_decode(header) print(login, password) admin qwerty1234</p>"},{"location":"api/jam.utils.basic_auth/#jam.utils.basic_auth.basic_auth_encode","title":"basic_auth_encode","text":"<pre><code>basic_auth_encode(login: str, password: str) -&gt; str\n</code></pre> <p>Encodes the login and password in a basic auth token.</p> <p>Parameters:</p> Name Type Description Default <code>login</code> <code>str</code> <p>Login</p> required <code>password</code> <code>str</code> <p>Password</p> required Example <pre><code>&gt;&gt;&gt; header = basic_auth_encode(\"admin\", \"qwerty1234\")\n&gt;&gt;&gt; print(header)\nYWRtaW46cXdlcnR5MTIzNA==\n</code></pre>"},{"location":"api/jam.utils.config_maker/","title":"jam.utils.config_maker","text":""},{"location":"api/jam.utils.config_maker/#jam.utils.config_maker","title":"config_maker","text":"<p>Functions:</p> Name Description <code>__apply_jwt_config_migration__</code> <p>Backward compatibility: copy jam.jwt -&gt; jam.jose.jwt with deprecation warning.</p> <code>__config_maker__</code> <p>Base config masker.</p> <code>__key_loader__</code> <p>Loads a key from file, if <code>key</code> is a path to a file.</p> <code>__module_loader__</code> <p>Loader custom modules from config.</p>"},{"location":"api/jam.utils.config_maker/#jam.utils.config_maker.__apply_jwt_config_migration__","title":"__apply_jwt_config_migration__","text":"<pre><code>__apply_jwt_config_migration__(config: dict) -&gt; dict\n</code></pre> <p>Backward compatibility: copy jam.jwt -&gt; jam.jose.jwt with deprecation warning.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>dict</code> <p>Parsed configuration dict</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Updated config with migration applied</p>"},{"location":"api/jam.utils.config_maker/#jam.utils.config_maker.__config_maker__","title":"__config_maker__","text":"<pre><code>__config_maker__(\n    config: str | dict[str, Any],\n    pointer: str = GENERIC_POINTER,\n) -&gt; dict[str, Any]\n</code></pre> <p>Base config masker.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>str | dict[str, Any</code> <p>Config dict or file path</p> required <code>pointer</code> <code>str</code> <p>Pointer to config read</p> <code>GENERIC_POINTER</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Parsed config</p>"},{"location":"api/jam.utils.config_maker/#jam.utils.config_maker.__json_config_parser","title":"__json_config_parser","text":"<pre><code>__json_config_parser(\n    path: str,\n    encoder: BaseEncoder | type[BaseEncoder] = JsonEncoder,\n) -&gt; dict[str, Any]\n</code></pre> <p>JSON config parser with env substitution.</p> <p>Supports: - ${VAR} - environment variable (required) - ${VAR:-default} - environment variable with default value - $VAR - short form (required)</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to JSON file</p> required <code>encoder</code> <code>BaseEncoder | type[BaseEncoder]</code> <p>Encoder to use for parsing</p> <code>JsonEncoder</code> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If file not found or invalid JSON or required env var not set</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Parsed config with environment variable substitution</p>"},{"location":"api/jam.utils.config_maker/#jam.utils.config_maker.__key_loader__","title":"__key_loader__","text":"<pre><code>__key_loader__(key: str) -&gt; str\n</code></pre> <p>Loads a key from file, if <code>key</code> is a path to a file.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key to load. If it is a path to a file, the file will be loaded.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Loaded key or original key if not a file path.</p>"},{"location":"api/jam.utils.config_maker/#jam.utils.config_maker.__module_loader__","title":"__module_loader__","text":"<pre><code>__module_loader__(path: str) -&gt; Callable\n</code></pre> <p>Loader custom modules from config.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to module. For example: <code>my_app.classes.SomeClass</code></p> required <p>Raises:</p> Type Description <code>TypeError</code> <p>If path not str</p> <p>Returns:</p> Type Description <code>Callable</code> <p>Callable</p>"},{"location":"api/jam.utils.config_maker/#jam.utils.config_maker.__toml_config_parser","title":"__toml_config_parser","text":"<pre><code>__toml_config_parser(\n    path: str = \"pyproject.toml\",\n    pointer: str = GENERIC_POINTER,\n) -&gt; dict[str, Any]\n</code></pre> <p>Private method for parsing TOML config with env substitution.</p> <p>Supports: - ${VAR} - environment variable (required) - ${VAR:-default} - environment variable with default value - $VAR - short form (required)</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to config.toml</p> <code>'pyproject.toml'</code> <code>pointer</code> <code>str</code> <p>Pointer to config read</p> <code>GENERIC_POINTER</code> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If file not found or invalid TOML file or required env var not set</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dict with config param</p>"},{"location":"api/jam.utils.config_maker/#jam.utils.config_maker.__yaml_config_parser","title":"__yaml_config_parser","text":"<pre><code>__yaml_config_parser(\n    path: str, pointer: str = GENERIC_POINTER\n) -&gt; dict[str, Any]\n</code></pre> <p>Private method for parsing YML config with env substitution.</p> <p>Supports: - ${VAR} - environment variable (required) - ${VAR:-default} - environment variable with default value - $VAR - short form (required)</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to config.yml</p> required <code>pointer</code> <code>str</code> <p>Pointer to config section to read.</p> <code>GENERIC_POINTER</code> <p>Raises:</p> Type Description <code>JamConfigurationError</code> <p>If pyyaml not installed, file not found, or invalid YAML</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Parsed YAML section with environment variable substitution.</p>"},{"location":"api/jam.utils.ed/","title":"jam.utils.ed","text":""},{"location":"api/jam.utils.ed/#jam.utils.ed","title":"ed","text":"<p>Functions:</p> Name Description <code>generate_ecdsa_p384_keypair</code> <p>Generate ECDSA P-384 key pair.</p> <code>generate_ed25519_keypair</code> <p>Generate Ed25519 key.</p>"},{"location":"api/jam.utils.ed/#jam.utils.ed.generate_ecdsa_p384_keypair","title":"generate_ecdsa_p384_keypair","text":"<pre><code>generate_ecdsa_p384_keypair() -&gt; dict[str, str]\n</code></pre> <p>Generate ECDSA P-384 key pair.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: {'private': KEY, 'public': KEY}</p>"},{"location":"api/jam.utils.ed/#jam.utils.ed.generate_ed25519_keypair","title":"generate_ed25519_keypair","text":"<pre><code>generate_ed25519_keypair() -&gt; dict[str, str]\n</code></pre> <p>Generate Ed25519 key.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: {'private': KEY, 'public': KEY}</p>"},{"location":"api/jam.utils/","title":"jam.utils","text":""},{"location":"api/jam.utils/#jam.utils","title":"utils","text":"<p>Various utilities that help with authorization.</p> <p>Modules:</p> Name Description <code>aes</code> <code>await_maybe</code> <code>basic_auth</code> <code>config_maker</code> <code>ed</code> <code>otp_keys</code> <code>rsa</code> <code>salt_hash</code> <p>Utilities for secure password hashing and verification. Uses PBKDF2-HMAC-SHA256 with salt and constant-time comparison.</p> <code>symmetric</code> <code>version_check</code> <code>xchacha20poly1305</code> <code>xor</code> <p>Functions:</p> Name Description <code>basic_auth_decode</code> <p>Decodes basic auth token to login and password.</p> <code>basic_auth_encode</code> <p>Encodes the login and password in a basic auth token.</p> <code>check_password</code> <p>Verifies a password by recalculating the hash and comparing it to the stored hash.</p> <code>deserialize_hash</code> <p>Splits a stored string into salt and hash.</p> <code>generate_aes_key</code> <p>Generate a new AES key.</p> <code>generate_ecdsa_p384_keypair</code> <p>Generate ECDSA P-384 key pair.</p> <code>generate_ed25519_keypair</code> <p>Generate Ed25519 key.</p> <code>generate_otp_key</code> <p>Generate generic OTP secret key.</p> <code>generate_rsa_key_pair</code> <p>RSA key generation utility.</p> <code>generate_symmetric_key</code> <p>Generate a N-byte symmetric key.</p> <code>hash_password</code> <p>Hashes a password with a salt using PBKDF2-HMAC-SHA256.</p> <code>otp_key_from_string</code> <p>Generate OTP-valid key from string.</p> <code>serialize_hash</code> <p>Combines salt and hash into a single string for database storage.</p> <code>xor_my_data</code> <p>Encrypts a string using a secret key with the XOR cipher.</p>"},{"location":"api/jam.utils/#jam.utils.basic_auth_decode","title":"basic_auth_decode","text":"<pre><code>basic_auth_decode(data: str) -&gt; tuple[str, str]\n</code></pre> <p>Decodes basic auth token to login and password.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>str</code> <p>Decoded data</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If incorrect format</p> Example <p>login, password = basic_auth_decode(header) print(login, password) admin qwerty1234</p>"},{"location":"api/jam.utils/#jam.utils.basic_auth_encode","title":"basic_auth_encode","text":"<pre><code>basic_auth_encode(login: str, password: str) -&gt; str\n</code></pre> <p>Encodes the login and password in a basic auth token.</p> <p>Parameters:</p> Name Type Description Default <code>login</code> <code>str</code> <p>Login</p> required <code>password</code> <code>str</code> <p>Password</p> required Example <pre><code>&gt;&gt;&gt; header = basic_auth_encode(\"admin\", \"qwerty1234\")\n&gt;&gt;&gt; print(header)\nYWRtaW46cXdlcnR5MTIzNA==\n</code></pre>"},{"location":"api/jam.utils/#jam.utils.check_password","title":"check_password","text":"<pre><code>check_password(\n    password: str,\n    salt_hex: str,\n    hash_hex: str,\n    iterations: int = 100000,\n) -&gt; bool\n</code></pre> <p>Verifies a password by recalculating the hash and comparing it to the stored hash.</p> <p>Parameters:</p> Name Type Description Default <code>password</code> <code>str</code> <p>Password to verify.</p> required <code>salt_hex</code> <code>str</code> <p>Hex representation of the salt.</p> required <code>hash_hex</code> <code>str</code> <p>Hex representation of the stored hash.</p> required <code>iterations</code> <code>int</code> <p>Number of PBKDF2 iterations, must match the hashing call.</p> <code>100000</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the password is correct, False otherwise.</p> Example <pre><code>&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\")\n&gt;&gt;&gt; check_password(\"my_password\", salt, hash_)\nTrue\n&gt;&gt;&gt; check_password(\"wrong_password\", salt, hash_)\nFalse\n\n# Using custom iterations\n&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\", iterations=150_000)\n&gt;&gt;&gt; check_password(\"my_password\", salt, hash_, iterations=150_000)\nTrue\n</code></pre>"},{"location":"api/jam.utils/#jam.utils.deserialize_hash","title":"deserialize_hash","text":"<pre><code>deserialize_hash(data: str) -&gt; tuple[str, str]\n</code></pre> <p>Splits a stored string into salt and hash.</p> Example <pre><code>&gt;&gt;&gt; salt, hash_ = deserialize_hash(\"abcdef1234$9876543210\")\n&gt;&gt;&gt; isinstance(salt, str)\nTrue\n&gt;&gt;&gt; isinstance(hash_, str)\nTrue\n</code></pre>"},{"location":"api/jam.utils/#jam.utils.generate_aes_key","title":"generate_aes_key","text":"<pre><code>generate_aes_key() -&gt; bytes\n</code></pre> <p>Generate a new AES key.</p>"},{"location":"api/jam.utils/#jam.utils.generate_ecdsa_p384_keypair","title":"generate_ecdsa_p384_keypair","text":"<pre><code>generate_ecdsa_p384_keypair() -&gt; dict[str, str]\n</code></pre> <p>Generate ECDSA P-384 key pair.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: {'private': KEY, 'public': KEY}</p>"},{"location":"api/jam.utils/#jam.utils.generate_ed25519_keypair","title":"generate_ed25519_keypair","text":"<pre><code>generate_ed25519_keypair() -&gt; dict[str, str]\n</code></pre> <p>Generate Ed25519 key.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: {'private': KEY, 'public': KEY}</p>"},{"location":"api/jam.utils/#jam.utils.generate_otp_key","title":"generate_otp_key","text":"<pre><code>generate_otp_key(entropy_bits: int = 128) -&gt; str\n</code></pre> <p>Generate generic OTP secret key.</p> <p>Parameters:</p> Name Type Description Default <code>entropy_bits</code> <code>int</code> <p>Entropy bits to key</p> <code>128</code> <p>Returns:</p> Type Description <code>str</code> <p>str</p>"},{"location":"api/jam.utils/#jam.utils.generate_rsa_key_pair","title":"generate_rsa_key_pair","text":"<pre><code>generate_rsa_key_pair(\n    key_size: int = 2048,\n) -&gt; dict[str, str]\n</code></pre> <p>RSA key generation utility.</p> <p>Parameters:</p> Name Type Description Default <code>key_size</code> <code>int</code> <p>Size of RSA key</p> <code>2048</code> <p>Returns:</p> Type Description <code>dict</code> <p>with public and private keys in format:</p> <pre><code>{\n    \"public\": \"some_key\",\n    \"private\": \"key\"\n}\n</code></pre>"},{"location":"api/jam.utils/#jam.utils.generate_symmetric_key","title":"generate_symmetric_key","text":"<pre><code>generate_symmetric_key(n: int = 32) -&gt; str\n</code></pre> <p>Generate a N-byte symmetric key.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>Bytes for key</p> <code>32</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Key</p>"},{"location":"api/jam.utils/#jam.utils.hash_password","title":"hash_password","text":"<pre><code>hash_password(\n    password: str,\n    salt: bytes | None = None,\n    iterations: int = 100000,\n    salt_size: int = 16,\n) -&gt; tuple[str, str]\n</code></pre> <p>Hashes a password with a salt using PBKDF2-HMAC-SHA256.</p> <p>Parameters:</p> Name Type Description Default <code>password</code> <code>str</code> <p>Password to hash.</p> required <code>salt</code> <code>bytes | None</code> <p>Salt. If None, a random salt is generated.</p> <code>None</code> <code>iterations</code> <code>int</code> <p>Number of PBKDF2 iterations.</p> <code>100000</code> <code>salt_size</code> <code>int</code> <p>Size of the random salt in bytes.</p> <code>16</code> <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>tuple[str, str]: (hex_salt, hex_hash)</p> Example <pre><code>&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\")\n&gt;&gt;&gt; isinstance(salt, str)\nTrue\n&gt;&gt;&gt; isinstance(hash_, str)\nTrue\n\n# Using custom iterations and salt size\n&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\", iterations=150_000, salt_size=24)\n</code></pre>"},{"location":"api/jam.utils/#jam.utils.otp_key_from_string","title":"otp_key_from_string","text":"<pre><code>otp_key_from_string(s: str) -&gt; str\n</code></pre> <p>Generate OTP-valid key from string.</p> <p>Parameters:</p> Name Type Description Default <code>s</code> <code>str</code> <p>String for key</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>str</code> <p>OTP key</p> Example <pre><code>&gt;&gt;&gt; from jam.utils import otp_key_from_string\n&gt;&gt;&gt; user_email: str = \"some.email@mail.com\"\n&gt;&gt;&gt; key = otp_key_from_string(user_email)\n&gt;&gt;&gt; print(key)\n'O54O6YRKTH3IPNEBIUMKMK3FZ35OF6Q5'\n</code></pre>"},{"location":"api/jam.utils/#jam.utils.serialize_hash","title":"serialize_hash","text":"<pre><code>serialize_hash(salt_hex: str, hash_hex: str) -&gt; str\n</code></pre> <p>Combines salt and hash into a single string for database storage.</p> Example <pre><code>&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\")\n&gt;&gt;&gt; serialized = serialize_hash(salt, hash_)\n&gt;&gt;&gt; isinstance(serialized, str)\nTrue\n</code></pre>"},{"location":"api/jam.utils/#jam.utils.xor_my_data","title":"xor_my_data","text":"<pre><code>xor_my_data(data: str, key: str) -&gt; str\n</code></pre> <p>Encrypts a string using a secret key with the XOR cipher.</p> <p>This function performs an XOR operation on each byte of the input data with the corresponding byte of the key. If the key is shorter than the data, it wraps around and continues from the beginning of the key. If you need to decrypt data, use the same function with the same secret key. XOR is a symmetric operation.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>str</code> <p>The plain text string to be encrypted.</p> required <code>key</code> <code>str</code> <p>The secret key used for encryption.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encrypted data represented as a hexadecimal string.</p> Example <pre><code>&gt;&gt;&gt; encrypted = xor_my_data(\"Hello, World!\", \"secretkey\")\n&gt;&gt;&gt; print(encrypted)\n&lt;...encrypted hex string...&gt;\n</code></pre>"},{"location":"api/jam.utils.otp_keys/","title":"jam.utils.otp_keys","text":""},{"location":"api/jam.utils.otp_keys/#jam.utils.otp_keys","title":"otp_keys","text":"<p>Functions:</p> Name Description <code>generate_otp_key</code> <p>Generate generic OTP secret key.</p> <code>otp_key_from_string</code> <p>Generate OTP-valid key from string.</p>"},{"location":"api/jam.utils.otp_keys/#jam.utils.otp_keys.generate_otp_key","title":"generate_otp_key","text":"<pre><code>generate_otp_key(entropy_bits: int = 128) -&gt; str\n</code></pre> <p>Generate generic OTP secret key.</p> <p>Parameters:</p> Name Type Description Default <code>entropy_bits</code> <code>int</code> <p>Entropy bits to key</p> <code>128</code> <p>Returns:</p> Type Description <code>str</code> <p>str</p>"},{"location":"api/jam.utils.otp_keys/#jam.utils.otp_keys.otp_key_from_string","title":"otp_key_from_string","text":"<pre><code>otp_key_from_string(s: str) -&gt; str\n</code></pre> <p>Generate OTP-valid key from string.</p> <p>Parameters:</p> Name Type Description Default <code>s</code> <code>str</code> <p>String for key</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>str</code> <p>OTP key</p> Example <pre><code>&gt;&gt;&gt; from jam.utils import otp_key_from_string\n&gt;&gt;&gt; user_email: str = \"some.email@mail.com\"\n&gt;&gt;&gt; key = otp_key_from_string(user_email)\n&gt;&gt;&gt; print(key)\n'O54O6YRKTH3IPNEBIUMKMK3FZ35OF6Q5'\n</code></pre>"},{"location":"api/jam.utils.rsa/","title":"jam.utils.rsa","text":""},{"location":"api/jam.utils.rsa/#jam.utils.rsa","title":"rsa","text":"<p>Functions:</p> Name Description <code>generate_rsa_key_pair</code> <p>RSA key generation utility.</p>"},{"location":"api/jam.utils.rsa/#jam.utils.rsa.generate_rsa_key_pair","title":"generate_rsa_key_pair","text":"<pre><code>generate_rsa_key_pair(\n    key_size: int = 2048,\n) -&gt; dict[str, str]\n</code></pre> <p>RSA key generation utility.</p> <p>Parameters:</p> Name Type Description Default <code>key_size</code> <code>int</code> <p>Size of RSA key</p> <code>2048</code> <p>Returns:</p> Type Description <code>dict</code> <p>with public and private keys in format:</p> <pre><code>{\n    \"public\": \"some_key\",\n    \"private\": \"key\"\n}\n</code></pre>"},{"location":"api/jam.utils.salt_hash/","title":"jam.utils.salt_hash","text":""},{"location":"api/jam.utils.salt_hash/#jam.utils.salt_hash","title":"salt_hash","text":"<p>Utilities for secure password hashing and verification. Uses PBKDF2-HMAC-SHA256 with salt and constant-time comparison.</p> <p>Functions:</p> Name Description <code>check_password</code> <p>Verifies a password by recalculating the hash and comparing it to the stored hash.</p> <code>deserialize_hash</code> <p>Splits a stored string into salt and hash.</p> <code>hash_password</code> <p>Hashes a password with a salt using PBKDF2-HMAC-SHA256.</p> <code>serialize_hash</code> <p>Combines salt and hash into a single string for database storage.</p>"},{"location":"api/jam.utils.salt_hash/#jam.utils.salt_hash.check_password","title":"check_password","text":"<pre><code>check_password(\n    password: str,\n    salt_hex: str,\n    hash_hex: str,\n    iterations: int = 100000,\n) -&gt; bool\n</code></pre> <p>Verifies a password by recalculating the hash and comparing it to the stored hash.</p> <p>Parameters:</p> Name Type Description Default <code>password</code> <code>str</code> <p>Password to verify.</p> required <code>salt_hex</code> <code>str</code> <p>Hex representation of the salt.</p> required <code>hash_hex</code> <code>str</code> <p>Hex representation of the stored hash.</p> required <code>iterations</code> <code>int</code> <p>Number of PBKDF2 iterations, must match the hashing call.</p> <code>100000</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the password is correct, False otherwise.</p> Example <pre><code>&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\")\n&gt;&gt;&gt; check_password(\"my_password\", salt, hash_)\nTrue\n&gt;&gt;&gt; check_password(\"wrong_password\", salt, hash_)\nFalse\n\n# Using custom iterations\n&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\", iterations=150_000)\n&gt;&gt;&gt; check_password(\"my_password\", salt, hash_, iterations=150_000)\nTrue\n</code></pre>"},{"location":"api/jam.utils.salt_hash/#jam.utils.salt_hash.deserialize_hash","title":"deserialize_hash","text":"<pre><code>deserialize_hash(data: str) -&gt; tuple[str, str]\n</code></pre> <p>Splits a stored string into salt and hash.</p> Example <pre><code>&gt;&gt;&gt; salt, hash_ = deserialize_hash(\"abcdef1234$9876543210\")\n&gt;&gt;&gt; isinstance(salt, str)\nTrue\n&gt;&gt;&gt; isinstance(hash_, str)\nTrue\n</code></pre>"},{"location":"api/jam.utils.salt_hash/#jam.utils.salt_hash.hash_password","title":"hash_password","text":"<pre><code>hash_password(\n    password: str,\n    salt: bytes | None = None,\n    iterations: int = 100000,\n    salt_size: int = 16,\n) -&gt; tuple[str, str]\n</code></pre> <p>Hashes a password with a salt using PBKDF2-HMAC-SHA256.</p> <p>Parameters:</p> Name Type Description Default <code>password</code> <code>str</code> <p>Password to hash.</p> required <code>salt</code> <code>bytes | None</code> <p>Salt. If None, a random salt is generated.</p> <code>None</code> <code>iterations</code> <code>int</code> <p>Number of PBKDF2 iterations.</p> <code>100000</code> <code>salt_size</code> <code>int</code> <p>Size of the random salt in bytes.</p> <code>16</code> <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>tuple[str, str]: (hex_salt, hex_hash)</p> Example <pre><code>&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\")\n&gt;&gt;&gt; isinstance(salt, str)\nTrue\n&gt;&gt;&gt; isinstance(hash_, str)\nTrue\n\n# Using custom iterations and salt size\n&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\", iterations=150_000, salt_size=24)\n</code></pre>"},{"location":"api/jam.utils.salt_hash/#jam.utils.salt_hash.serialize_hash","title":"serialize_hash","text":"<pre><code>serialize_hash(salt_hex: str, hash_hex: str) -&gt; str\n</code></pre> <p>Combines salt and hash into a single string for database storage.</p> Example <pre><code>&gt;&gt;&gt; salt, hash_ = hash_password(\"my_password\")\n&gt;&gt;&gt; serialized = serialize_hash(salt, hash_)\n&gt;&gt;&gt; isinstance(serialized, str)\nTrue\n</code></pre>"},{"location":"api/jam.utils.symmetric/","title":"jam.utils.symmetric","text":""},{"location":"api/jam.utils.symmetric/#jam.utils.symmetric","title":"symmetric","text":"<p>Functions:</p> Name Description <code>generate_symmetric_key</code> <p>Generate a N-byte symmetric key.</p>"},{"location":"api/jam.utils.symmetric/#jam.utils.symmetric.generate_symmetric_key","title":"generate_symmetric_key","text":"<pre><code>generate_symmetric_key(n: int = 32) -&gt; str\n</code></pre> <p>Generate a N-byte symmetric key.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>Bytes for key</p> <code>32</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Key</p>"},{"location":"api/jam.utils.version_check/","title":"jam.utils.version_check","text":""},{"location":"api/jam.utils.version_check/#jam.utils.version_check","title":"version_check","text":"<p>Functions:</p> Name Description <code>__is_compatible__</code> <p>Checks if the income version is compatible with the required version.</p>"},{"location":"api/jam.utils.version_check/#jam.utils.version_check.__is_compatible__","title":"__is_compatible__","text":"<pre><code>__is_compatible__(\n    income_version: str | None, required_version: str\n) -&gt; bool\n</code></pre> <p>Checks if the income version is compatible with the required version.</p>"},{"location":"api/jam.utils.xchacha20poly1305/","title":"jam.utils.xchacha20poly1305","text":""},{"location":"api/jam.utils.xchacha20poly1305/#jam.utils.xchacha20poly1305","title":"xchacha20poly1305","text":"<p>Functions:</p> Name Description <code>xchacha20poly1305_decrypt</code> <p>Decrypt counterpart.</p> <code>xchacha20poly1305_encrypt</code> <p>Emulates XChaCha20-Poly1305 AEAD using cryptography's ChaCha20Poly1305.</p>"},{"location":"api/jam.utils.xchacha20poly1305/#jam.utils.xchacha20poly1305.xchacha20poly1305_decrypt","title":"xchacha20poly1305_decrypt","text":"<pre><code>xchacha20poly1305_decrypt(\n    key: bytes, nonce: bytes, ciphertext: bytes, aad: bytes\n) -&gt; bytes\n</code></pre> <p>Decrypt counterpart.</p>"},{"location":"api/jam.utils.xchacha20poly1305/#jam.utils.xchacha20poly1305.xchacha20poly1305_encrypt","title":"xchacha20poly1305_encrypt","text":"<pre><code>xchacha20poly1305_encrypt(\n    key: bytes, nonce: bytes, plaintext: bytes, aad: bytes\n) -&gt; bytes\n</code></pre> <p>Emulates XChaCha20-Poly1305 AEAD using cryptography's ChaCha20Poly1305.</p>"},{"location":"api/jam.utils.xor/","title":"jam.utils.xor","text":""},{"location":"api/jam.utils.xor/#jam.utils.xor","title":"xor","text":"<p>Functions:</p> Name Description <code>xor_my_data</code> <p>Encrypts a string using a secret key with the XOR cipher.</p>"},{"location":"api/jam.utils.xor/#jam.utils.xor.xor_my_data","title":"xor_my_data","text":"<pre><code>xor_my_data(data: str, key: str) -&gt; str\n</code></pre> <p>Encrypts a string using a secret key with the XOR cipher.</p> <p>This function performs an XOR operation on each byte of the input data with the corresponding byte of the key. If the key is shorter than the data, it wraps around and continues from the beginning of the key. If you need to decrypt data, use the same function with the same secret key. XOR is a symmetric operation.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>str</code> <p>The plain text string to be encrypted.</p> required <code>key</code> <code>str</code> <p>The secret key used for encryption.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The encrypted data represented as a hexadecimal string.</p> Example <pre><code>&gt;&gt;&gt; encrypted = xor_my_data(\"Hello, World!\", \"secretkey\")\n&gt;&gt;&gt; print(encrypted)\n&lt;...encrypted hex string...&gt;\n</code></pre>"},{"location":"breaking_changes/deprecated/","title":"Deprecated methods","text":"<ul> <li><code>jam.Jam.jwt_make_payload</code>: The JWT specification has been introduced, so signing is now done via JWS</li> <li><code>jam.Jam.jwt_create</code>: Use <code>jam.Jam.jwt_encode</code></li> <li><code>jam.jwt.JWT</code>: Use <code>jam.jose.JWT</code></li> </ul>"},{"location":"breaking_changes/jam114_to_200/","title":"Breaking changes 1.1.4 to 2.0.0","text":""},{"location":"breaking_changes/jam114_to_200/#instance","title":"Instance","text":"<p>With the introduction of a more convenient configuration format, it was decided to move <code>auth_type</code> to the config.</p> <p>Old format: <pre><code>from jam import Jam\n\njam = Jam(auth_type=\"jwt\", config=config)\n</code></pre></p> <p>New format: <pre><code>from jam import Jam\n\n'''config.yml\njam:\n  auth_type: jwt\n  alg: HS256\n  secret_key: SECRET_KEY\n  expire: 3600\n'''\n\njam = Jam(config=\"config.yml\")\n</code></pre> More about configurations: jam.makridenko.ru/config</p>"},{"location":"breaking_changes/jam114_to_200/#configuring-jwt-lists","title":"Configuring JWT lists","text":"<p>Previously, lists were configured by passing an instance of the <code>jam.jwt.lists.*</code> class: <pre><code>from jam import Jam\nfrom jam.jwt.lists.json import JSONList\n\nconfig = {\n    \"alg\": \"HS256\",\n    \"secret_key\": \"some_key\",\n    \"expire\": 3600,\n    \"list\": JSONList(type=\"black\", json_path=\"blacklist.json\")\n}\njam = Jam(auth_type=\"jwt\", config=config)\n</code></pre></p> <p>Now, custom settings are passed for standard <code>redis</code> and <code>json</code> lists. Example in <code>dict</code> configuration: <pre><code>from jam import Jam\n\nconfig = {\n    \"alg\": \"HS256\",\n    \"secret_key\": \"some_key\",\n    \"expire\": 3600,\n    \"list\": {\n        \"type\": \"black\",\n        \"backend\": \"redis\",\n        \"redis_uri\": \"redis://0.0.0.0:6379/0\"\n    }\n}\n\njam = Jam(config=config)\n</code></pre></p> <p>For custom lists: <pre><code>from jam import Jam\n\nconfig = {\n    \"alg\": \"HS256\",\n    \"secret_key\": \"some_key\",\n    \"expire\": 3600,\n    \"list\": {\n        \"type\": \"black\",\n        \"backend\": \"custom\",\n        \"custom_module\": \"app.some_module.SomeModule\",\n        \"param1\": \"val1\",\n        \"param2\": 123\n    }\n}\n\njam = Jam(config=config)\n</code></pre></p>"},{"location":"breaking_changes/jam114_to_200/#renaming-abclist","title":"Renaming <code>ABCList</code>","text":"<p>It was decided to name all abstract modules <code>Base&lt;NAME&gt;</code>, so <code>ABCList</code> was renamed to <code>BaseJWTList</code>. More details: <code>jam.jwt.list.__abc_list_repo__</code></p>"},{"location":"breaking_changes/jam114_to_200/#renaming-__abstractinstance","title":"Renaming <code>__AbstractInstance</code>:","text":"<p>It was decided to name all abstract modules <code>Base&lt;NAME&gt;</code>, so <code>__AbstractInstance</code> was renamed to <code>BaseJam</code>. More details: <code>jam.__abc_instances__.BaseJam</code></p>"},{"location":"breaking_changes/jam114_to_200/#removal-of-jamutilsmake_jwt_config","title":"Removal of <code>jam.utils.make_jwt_config</code>","text":"<p>Configuration is now done by writing config files, so a separate function is no longer needed.</p>"},{"location":"breaking_changes/jam114_to_200/#optional-dependencies","title":"Optional dependencies","text":"<p>Renamed: * <code>pip install jamlib[json-lists]</code> -&gt; <code>pip install jamlib[json]</code> * <code>pip install jamlib[redis-lists]</code> -&gt; <code>pip install jamlib[redis]</code></p>"},{"location":"breaking_changes/jam256_to_300/","title":"Breaking changes 2.5.6 to 3.0.0","text":""},{"location":"breaking_changes/jam256_to_300/#delete-deprecated-methods","title":"Delete deprecated methods","text":""},{"location":"breaking_changes/jam256_to_300/#config","title":"Config","text":"<p>The old configuration type is no longer relevant. This helps to implement several types of authorization in a single instance, for example, JWT + OAuth2.</p> <p>Old version: <pre><code>[jam]\nauth_type = \"jwt\"\nsecret_key = \"SECRET\"\n</code></pre> In this version of the config, to implement another type of authorization, it was necessary to create another instance: <pre><code>[another_jam]\nauth_type = \"sessions\"\nsession_type = \"redis\"\nredis_uri = \"redis://0.0.0.0:6379\"\n</code></pre> And: <pre><code>from jam import Jam\n\njwt_jam = Jam(config=\"config.toml\")\nsessions_jam = Jam(config=\"config.toml\", pointer=\"another_jam\")\n</code></pre></p> <p>New version: <pre><code>[jam.jwt]\nalg = \"HS256\"\nsecret_key = \"SECRET\"\n\n[jam.sessions]\nsession_type = \"redis\"\nredis_uri = \"redis://0.0.0.0:6379\"\n</code></pre> And: <pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\n\n#  Now we can use both authorization options in one instance.\njwt_token = jam.jwt_create_token({\"user\": 1})\nsession_id = jam.session_create(\"user\", {\"user\": 1})\n</code></pre></p>"},{"location":"breaking_changes/jam256_to_300/#instance-methods","title":"Instance methods","text":"<p>It was decided to bring all methods to a single naming format: <code>&lt;auth_type&gt;_&lt;action&gt;</code>. As a result, the following methods were renamed:</p> <ul> <li><code>gen_jwt_token</code> -&gt; <code>jwt_create</code></li> <li><code>make_payload</code> -&gt; <code>jwt_make_payload</code></li> <li><code>verify_jwt_token</code> -&gt; <code>jwt_decode</code></li> <li><code>clear_sessions</code> -&gt; <code>session_clear</code></li> <li><code>get_session</code> -&gt; <code>session_get</code></li> <li><code>create_session</code> -&gt; <code>session_create</code></li> <li><code>delete_session</code> -&gt; <code>session_delete</code></li> <li><code>update_session</code> -&gt; <code>session_update</code></li> <li><code>rework_session</code> -&gt; <code>session_rework</code></li> <li><code>get_otp_code</code> -&gt; <code>otp_code</code></li> <li><code>get_otp_uri</code> -&gt; <code>otp_uri</code></li> <li><code>verify_otp_code</code> -&gt; <code>otp_verify_code</code></li> </ul>"},{"location":"breaking_changes/jam256_to_300/#quick-methods","title":"Quick methods","text":"<p>Removed <code>jam.quick</code> as unnecessary.</p>"},{"location":"breaking_changes/jam256_to_300/#module-system-refactoring","title":"Module System Refactoring","text":""},{"location":"breaking_changes/jam256_to_300/#unified-module-initialization","title":"Unified Module Initialization","text":"<p>All modules now use factory functions (<code>create_instance</code>) instead of wrapper classes for consistency and better performance.</p> <p>What changed: - <code>jam.modules.SessionModule</code> \u2192 Removed, use <code>jam.sessions.create_instance</code> - <code>jam.modules.OAuth2Module</code> \u2192 Removed, use <code>jam.oauth2.create_instance</code> - <code>jam.sessions.utils.init_session_instance</code> \u2192 Removed, use <code>jam.sessions.create_instance</code> - <code>jam.paseto.utils.init_paseto_instance</code> \u2192 Removed, use <code>jam.paseto.create_instance</code> - <code>jam.otp.utils.init_otp_instance</code> \u2192 Removed, use <code>jam.otp.create_instance</code></p> <p>Impact on users:</p>"},{"location":"breaking_changes/jam256_to_300/#for-most-users-no-changes-needed","title":"For most users: NO CHANGES NEEDED","text":"<p>The public API (<code>jam.jwt_*()</code>, <code>jam.session_*()</code>, <code>jam.oauth2_*()</code>, etc.) remains unchanged.</p>"},{"location":"breaking_changes/jam256_to_300/#for-users-directly-importing-wrapper-classes","title":"For users directly importing wrapper classes:","text":"<p>Before: <pre><code>from jam.modules import SessionModule, OAuth2Module\n\n# This will no longer work\nsession = SessionModule(sessions_type=\"redis\", redis_uri=\"redis://localhost\")\noauth2 = OAuth2Module(config={\"providers\": {...}})\n</code></pre></p> <p>After: <pre><code>from jam.sessions import create_instance as create_session\nfrom jam.oauth2 import create_instance as create_oauth2\n\n# Use factory functions instead\nsession = create_session(session_type=\"redis\", redis_uri=\"redis://localhost\")\noauth2 = create_oauth2(providers={...})\n</code></pre></p>"},{"location":"breaking_changes/jam256_to_300/#for-users-accessing-internal-structure","title":"For users accessing internal structure:","text":"<p><code>self.oauth2</code> internal structure changed:</p> <p>Before: <pre><code>jam = Jam(config=\"config.yml\")\n# self.oauth2 was OAuth2Module object with methods\njam.oauth2.get_authorization_url(\"github\", [\"read:user\"])\n</code></pre></p> <p>After: <pre><code>jam = Jam(config=\"config.yml\")\n# self.oauth2 is now a dict: {provider_name: client_instance}\njam.oauth2[\"github\"].get_authorization_url([\"read:user\"])\n\n# Public API methods still work the same:\njam.oauth2_get_authorized_url(\"github\", [\"read:user\"])  # \u2190 USE THIS\n</code></pre></p>"},{"location":"breaking_changes/jam256_to_300/#redis-sessions","title":"Redis sessions","text":"<p>Rename parameter <code>default_ttl</code> to <code>ttl</code>.</p>"},{"location":"breaking_changes/jam256_to_300/#framework-integration","title":"Framework integration","text":"<p>A complete overhaul of framework integrations; see the documentation.</p>"},{"location":"framework_integrations/fastapi/","title":"FastAPI","text":"<p>FastAPI is fully compatible with Starlette, so use the Starlette integration:</p> <pre><code>from jam.ext.starlette import JWTBackend, SessionBackend, PASETOBackend\n</code></pre>"},{"location":"framework_integrations/flask/","title":"Flask integration","text":"<p>Integration with Flask is done in the form of extension classes, which are recommended in the official documentation.</p>"},{"location":"framework_integrations/flask/#how-it-works","title":"How it works","text":"<p>Jam extensions for Flask check requests for the presence of a token in cookies or the header (depending on the settings) and store the data in <code>flask.g</code>.</p> <p>Example:</p> <pre><code>from flask import Flask, g, jsonify\n\nfrom jam.ext.flask import JWTExtension # For example\n\n\napp = Flask(__name__)\njwt_ext = JWTExtension(\n    config=\"config.toml\",\n    header_name=\"Authorization\"\n)\njwt_ext.init_app(app)\n\n@app.post(\"/login\")\ndef login():\n\n    # some auth logic\n\n    jwt = g.jwt # &lt;- get JWT instance from flask.g\n    token = jwt.encode({\"username\": username})\n    return jsonify({\"token\": token})\n\n@app.get(\"/profile\")\ndef get_profile():\n    payload = g.payload # &lt;- get token payload from flask.g\n    if payload:\n        return jsonify(payload)\n    else:\n        return jsonify({\"error\": \"Unauthorized\"}), 401\n</code></pre>"},{"location":"framework_integrations/flask/#setup-extension","title":"Setup extension","text":"<p>The extension is installed by passing your app to <code>.init_app</code>:</p> <pre><code>from flask import Flask\nfrom jam.ext.flask import SessionExtension  # for example\n\napp = Flask(__name__)\nsession_extensions = SessionExtension(\n    config=\"config.toml\",\n    cookie_name=\"sessionId\"\n)\nsession_extensions.init_app(app)\n</code></pre>"},{"location":"framework_integrations/flask/#jwt-extension","title":"JWT Extension","text":"<p>Module: <code>jam.ext.flask.JWTExtension</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> <li><code>cookie_name</code>: <code>str | None = None</code> - Cookie to check.</li> <li><code>header_name</code>: <code>str | None = None</code> - Header to check.</li> <li><code>use_list</code>: <code>bool = False</code> - Use token black/white list.</li> <li><code>bearer</code>: <code>bool = True</code> - Set <code>Bearer</code> to header.</li> </ul> <pre><code>from flask import Flask\nfrom jam.ext.flask import JWTExtension\n\napp = Flask(__name__)\njwt_ext = JWTExtension(\n    config=\"config.toml\",\n    header_name=\"Authorization\",\n    bearer=True\n)\njwt_ext.init_app(app)\n</code></pre>"},{"location":"framework_integrations/flask/#paseto-extension","title":"PASETO Extension","text":"<p>Module: <code>jam.ext.flask.PASETOExtension</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> <li><code>cookie_name</code>: <code>str | None = None</code> - Cookie to check.</li> <li><code>header_name</code>: <code>str | None = None</code> - Header to check.</li> <li><code>bearer</code>: <code>bool = True</code> - Set <code>Bearer</code> to header.</li> </ul> <pre><code>from flask import Flask\nfrom jam.ext.flask import PASETOExtension\n\napp = Flask(__name__)\npaseto_ext = PASETOExtension(\n    config=\"config.toml\",\n    header_name=\"Authorization\",\n    bearer=True\n)\npaseto_ext.init_app(app)\n</code></pre>"},{"location":"framework_integrations/flask/#server-side-sessions-extension","title":"Server side sessions extension","text":"<p>Module: <code>jam.ext.flask.SessionExtension</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> <li><code>cookie_name</code>: <code>str | None = None</code> - Cookie to check.</li> <li><code>header_name</code>: <code>str | None = None</code> - Header to check.</li> <li><code>bearer</code>: <code>bool = True</code> - Set <code>Bearer</code> to header.</li> </ul> <pre><code>from flask import Flask\nfrom jam.ext.flask import SessionExtension\n\napp = Flask(__name__)\nsession_ext = SessionExtension(\n    config=\"config.toml\",\n    header_name=\"Authorization\",\n    bearer=True\n)\nsession_ext.init_app(app)\n</code></pre>"},{"location":"framework_integrations/flask/#oauth2-extension","title":"OAuth2 extension","text":"<p>Module: <code>jam.ext.flask.OAuth2Extension</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> </ul> <pre><code>from flask import Flask\nfrom jam.ext.flask import OAuth2Extension\n\napp = Flask(__name__)\noauth_ext = OAuth2Extension(\n    config=\"config.toml\",\n)\noauth_ext.init_app(app)\n</code></pre>"},{"location":"framework_integrations/litestar/","title":"Litestar integration","text":"<p>Integration with Litestar is implemented by plugins.</p>"},{"location":"framework_integrations/litestar/#how-it-works","title":"How it works","text":"<p>Plugins generally do two things:</p> <ul> <li>Create a middleware for a specific authorization type.</li> <li>Add an authorization module to Litestar DI.</li> </ul> <p>Example:</p> <pre><code>from litestar import Litestar\nfrom jam.ext.litestar import JWTPlugin, SimpleUser\n\napp = Litestar(\n    plugins=[JWTPlugin(\n        config=\"config.toml\",\n        cookie_name=\"JWT\",  # for example\n        user=SimpleUser\n    )]\n)\n\n# ---\n## And now we can use JWT from DI\nfrom litestar import post, Response\nfrom jam.jose import JWT\n\n@post(\"/login\")\nasync def login(login: str, password: str, jwt: JWT) -&gt; Response:\n    # check login and password\n    token = jwt.encode(  # use JWT from DI\n        payload={\"username\": login}\n    )\n    response = Response({\"token\": token})\n    response.set_cookie(\"jwt\", token)\n    return response\n\n# Middleware by JamPlugin check cookie \"jwt\"\n# (because it has `cookie_name=\"jwt\"` at config)\nfrom litestar import get, Request\nfrom litestar.exceptions import HTTPException\n\n@get(\"/user\")\nasync def get_user(request: Request) -&gt; dict:\n    if not request.user:\n        raise HTTPException(status_code=403, detail=\"Unauthorized\")\n    return request.user.payload  # For more informations about the user, see Users documentation in Jam and Litestar docs\n</code></pre>"},{"location":"framework_integrations/litestar/#setup-plugins","title":"Setup plugins","text":"<p>All plugins are installed in the same way; you simply pass them to <code>Litestar(plugins=[])</code> with the necessary parameters. For more information, see the Litestar plugin documentation.</p>"},{"location":"framework_integrations/litestar/#plugin-configuration","title":"Plugin configuration","text":"<p>The plugins use the standard Jam configuration, along with some additional settings:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Standard Jam configuration.</li> <li><code>pointer</code>: <code>str</code> - Pointer for config.</li> <li>other plugin specific settings, see for each plugin.</li> </ul> <pre><code>import os\nfrom litestar import Litestar\nfrom jam.ext.litestar import PASETOPlugin  # for example\n\nconfig = {\n    \"paseto\": {\n        \"version\": \"v4\",\n        \"purpose\": \"local\",\n        \"key\": os.getenv(\"PASETO_KEY\")\n    }\n}\n\napp = Litestar(\n    plugins=[\n        PASETOPlugin(\n            config=config,\n            header_name=\"Authorization\",\n            middleware=False\n        ),\n    ],\n)\n</code></pre>"},{"location":"framework_integrations/litestar/#users","title":"Users","text":"<p>In Litestar, user classes included in the <code>request</code> are used for authentication. Jam also uses this concept to facilitate integration.</p>"},{"location":"framework_integrations/litestar/#create-user-model","title":"Create user model","text":"<p>Users are created using the abstract <code>base_user</code> class, in which you must implement the <code>from_payload</code> <code>classmethod</code>, which takes data from a token or session as input and converts it into a class:</p> <pre><code>from jam.ext.litestar import BaseUser\n\n# Let's say we're using a payload\n# like this for authentication:\nexample_payload = {\n    \"id\": 123\n    \"username\": \"some_username\",\n    \"role\": \"role\"\n}\n\n# Create your own user\n@dataclass\nclass MyUser(BaseUser):\n    id: int\n    username: str\n    role: str\n\n    @classmethod\n    def from_payload(cls, payload: dict[str, Any]) -&gt; \"MyUser\":\n        return cls(\n            id=payload[\"id\"],\n            username=payload[\"username\"],\n            role=payload[\"role\"],\n        )\n\napp = Litestar(\n    plugins=[\n        JWTPlugin(\n            config=config,\n            cookie_name=\"jwt\",\n            user=MyUser\n        )\n    ],\n)\n</code></pre> <p>After that, we can use this user directly from <code>request</code>:</p> <pre><code>from litestar import get, Request\n\n@get(\"/profile\")\nasync def get_profile(request: Request) -&gt; MyUser | None:\n    if not request.user:\n        return None\n    else:\n      return request.user\n</code></pre>"},{"location":"framework_integrations/litestar/#simple-user","title":"Simple user","text":"<p>If you don't need complex user logic, you can use the <code>SimpleUser</code> class. It's a class with a single attribute, <code>payload</code>: <code>dict[str, Any]</code>, which holds the entire payload.</p> <p>Example:</p> <pre><code>from litestar import Litestar, get, Request\nfrom jam.ext.litestar import SessionsPlugin, SimpleUser\n\n\n@get(\"/profile\")\nasync def get_profile(request: Request) -&gt; dict | None:\n    if not request.user:\n        return None\n    else:\n        return request.user.payload\n\napp = Litestar(\n    routes=[get_profile],\n    plugins=[\n        SessionsPlugin(\n            config=config,\n            cookie_name=\"jwt\",\n            user=SimpleUser\n        )\n    ],\n)\n</code></pre>"},{"location":"framework_integrations/litestar/#jwt-plugin","title":"JWT Plugin","text":"<p>Module: <code>jam.ext.litestar.JWTPlugin</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> <li><code>cookie_name</code>: <code>str | None = None</code> - Cookie to check, if <code>middleware=True</code>.</li> <li><code>header_name</code>: <code>str | None = None</code> - Header to check, if <code>middleware=True</code>.</li> <li><code>middleware</code>: <code>bool = True</code> - Enable/disable middleware.</li> <li><code>bearer</code>: <code>bool = False</code> - Use bearer prefix for token (e.g. \"Bearer \").</li> <li><code>use_list</code>: <code>bool = False</code> - Use token black/white list.</li> <li><code>user</code>: <code>type[BaseUser] | None = None</code> - User for request.</li> </ul> <p>Tip</p> <p>This plugin added <code>jwt</code>: <code>jam.jwt.JWT</code> to Litestar DI.</p> <pre><code>from litestar import Litestar\nfrom jam.ext.litestar import JWTPlugin, SimpleUser\n\n\napp = Litestar(\n    plugins=[\n        JWTPlugin(\n            config=config,\n            cookie_name=\"jwt\",\n            user=SimpleUser\n        )\n    ],\n)\n</code></pre>"},{"location":"framework_integrations/litestar/#paseto-plugin","title":"PASETO Plugin","text":"<p>Module: <code>jam.ext.litestar.PASETOPlugin</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> <li><code>cookie_name</code>: <code>str | None = None</code> - Cookie to check, if <code>middleware=True</code>.</li> <li><code>header_name</code>: <code>str | None = None</code> - Header to check, if <code>middleware=True</code>.</li> <li><code>bearer</code>: <code>bool = False</code> - Use bearer prefix for token (e.g. \"Bearer\").</li> <li><code>middleware</code>: <code>bool = True</code> - Enable/disable middleware.</li> <li><code>user</code>: <code>type[BaseUser] | None = None</code> - User for request.</li> </ul> <p>Tip</p> <p>This plugin added <code>paseto</code>: <code>jam.paseto.PASETOv*</code> to Litestar DI.</p> <pre><code>from litestar import Litestar\nfrom jam.ext.litestar import PASETOPlugin, SimpleUser\n\n\napp = Litestar(\n    plugins=[\n        PASETOPlugin(\n            config=config,\n            header_name=\"Authorization\",\n            user=SimpleUser\n        )\n    ],\n)\n</code></pre>"},{"location":"framework_integrations/litestar/#server-side-sessions-plugin","title":"Server side sessions plugin","text":"<p>Module: <code>jam.ext.litestar.SessionPlugin</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> <li><code>cookie_name</code>: <code>str | None = None</code> - Cookie to check, if <code>middleware=True</code>.</li> <li><code>header_name</code>: <code>str | None = None</code> - Header to check, if <code>middleware=True</code>.</li> <li><code>bearer</code>: <code>bool = False</code> - Use bearer prefix for token (e.g. \"Bearer\").</li> <li><code>middleware</code>: <code>bool = True</code> - Enable/disable middleware.</li> <li><code>user</code>: <code>type[BaseUser] | None = None</code> - User for request.</li> </ul> <p>Tip</p> <p>This plugin added <code>session</code>: <code>jam.aio.sessions.*</code> to Litestar DI.</p> <pre><code>from litestar import Litestar\nfrom jam.ext.litestar import SessionPlugin, SimpleUser\n\n\napp = Litestar(\n    plugins=[\n        SessionPlugin(\n            config=config,\n            cookie_name=\"session\",\n            user=SimpleUser\n        )\n    ],\n)\n</code></pre>"},{"location":"framework_integrations/litestar/#oauth2-plugin","title":"OAuth2 Plugin","text":"<p>Module: <code>jam.ext.litestar.OAuth2Plugin</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> </ul> <p>Tip</p> <p>This plugin added <code>oauth2</code>: <code>jam.oauth2.*</code> to Litestar DI. And this plugin does not add middleware!</p> <pre><code>from litestar import Litestar\nfrom jam.ext.litestar import OAuth2Plugin\n\n\napp = Litestar(\n    plugins=[\n        OAuth2Plugin(\n            config=config,\n        )\n    ],\n)\n</code></pre>"},{"location":"framework_integrations/starlette/","title":"Starlette integration","text":"<p>Integration with Starlet is implemented using authorization backends for middlewares.</p>"},{"location":"framework_integrations/starlette/#how-it-works","title":"How it works","text":"<p>All you need to do is pass the relevant authorization backend to the middleware, and everything will work. Jam implements middleware that checks the <code>header</code> or <code>cookie</code> (depending on your settings) and packages the data into the <code>request</code>.</p> <p>Example:</p> <pre><code>from starlette.applications import Starlette\nfrom starlette.middleware import Middleware\nfrom starlette.middleware.authentication import AuthenticationMiddleware\nfrom starlette.routing import Route\nfrom starlette.requests import Request\nfrom starlette.responses import PlainTextResponse\nfrom starlette.exceptions import HTTPException\nfrom starlette.status import HTTP_401_UNAUTHORIZED\n\nfrom jam.ext.starlette import JWTBackend  # for example\n\n\nasync def get_profile(request: Request):\n    if not request.user.is_authenticated:\n        raise HTTPException(status_code=HTTP_401_UNAUTHORIZED)\n    return PlainTextResponse(request.user.username)\n\n\napp = Starlette(\n    routes=[Route(\"/profile\", get_profile, methods=[\"GET\"])],\n    middleware=[\n        Middleware(\n            AuthenticationMiddleware,\n            backend=JWTBackend(\n                config=\"config.toml\",\n                cookie_name=\"jwt\"\n            ),\n        )\n    ]\n)\n</code></pre>"},{"location":"framework_integrations/starlette/#setup-backend","title":"Setup backend","text":"<p>The authorization backend accepts a standard Jam configuration as its configuration:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Standard Jam configuration.</li> <li><code>pointer</code>: <code>str</code> - Pointer for config.</li> <li>other backend specific settings, see for each backend.</li> </ul> <pre><code>config = {\n    \"jose\": {\n        \"jwt\": {\n            \"alg\": \"HS256\",\n            \"secret\": \"SECRET\"\n        }\n    }\n}\n\napp = Starlette(\n    middleware=[\n        Middleware(\n            AuthenticationMiddleware,\n            backend=JWTBackend(\n                config=config\n            ),\n        )\n    ]\n)\n</code></pre>"},{"location":"framework_integrations/starlette/#users","title":"Users","text":"<p>In Starlette, user classes included in the <code>request</code> are used for authentication. Jam also uses this concept to facilitate integration.</p>"},{"location":"framework_integrations/starlette/#create-user-model","title":"Create user model","text":"<p>Users are created using the abstract <code>base_user</code> class, in which you must implement the <code>from_payload</code> <code>classmethod</code>, which takes data from a token or session as input and converts it into a class:</p> <p><pre><code>from jam.ext.starlette import BaseUser\n\n# Let's say we're using a payload\n# like this for authentication:\nexample_payload = {\n    \"id\": 123\n    \"username\": \"some_username\",\n    \"role\": \"role\"\n}\n\n# Create your own user\n@dataclass\nclass MyUser(BaseUser):\n    id: int\n    username: str\n    role: str\n\n    @classmethod\n    def from_payload(cls, payload: dict[str, Any]) -&gt; \"MyUser\":\n        return cls(\n            id=payload[\"id\"],\n            username=payload[\"username\"],\n            role=payload[\"role\"],\n        )\n\napp = Starlette(\n    middleware=[\n        Middleware(\n            AuthenticationMiddleware,\n            backend=JWTBackend(\n                config=config,\n                user_class=MyUser,\n            ),\n        )\n    ]\n)\n</code></pre> After that, we can use this user directly from <code>request</code>:</p> <pre><code>async def get_username(request: Request) -&gt; str | None:\n    if request.user.is_authenticated:\n        return request.user.name\n    else:\n        return None\n</code></pre>"},{"location":"framework_integrations/starlette/#simple-user","title":"Simple user","text":"<p>If you don't need complex user logic, you can use the <code>SimpleUser</code> class. It's a class with a single attribute, <code>payload</code>: <code>dict[str, Any]</code>, which holds the entire payload.</p> <p>Example:</p> <pre><code>from jam.ext.starlette import SimpleUser\n\nasync def get_payload(request: Request) -&gt; dict | None:\n    if not request.user.is_authenticated:\n        return None\n    else:\n        return request.user.payload\n\napp = Starlette(\n    middleware=[\n        Middleware(\n            AuthenticationMiddleware,\n            backend=JWTBackend(\n                config=config,\n                user_class=SimpleUser,\n            ),\n        )\n    ]\n)\n</code></pre>"},{"location":"framework_integrations/starlette/#jwt-backend","title":"JWT Backend","text":"<p>Module: <code>jam.ext.starlette.JWTBackend</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> <li><code>cookie_name</code>: <code>str | None = None</code> - Cookie to check.</li> <li><code>header_name</code>: <code>str | None = None</code> - Header to check.</li> <li><code>bearer</code>: <code>bool = False</code> - Use bearer prefix for token (e.g. \"Bearer\").</li> <li><code>use_list</code>: <code>bool = False</code> - Use token black/white list.</li> <li><code>user</code>: <code>type[BaseUser] = SimpleUser</code> - User for request.</li> </ul> <pre><code>from jam.ext.starlette import JWTBackend\n\n\napp = Starlette(\n    middleware=[\n        Middleware(\n            AuthenticationMiddleware,\n            backend=JWTBackend(\n                config=\"config.toml\",\n                cookie_name=\"jwt\"\n            ),\n        )\n    ]\n)\n</code></pre>"},{"location":"framework_integrations/starlette/#paseto-backend","title":"PASETO Backend","text":"<p>Module: <code>jam.ext.starlette.PASETOBackend</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> <li><code>cookie_name</code>: <code>str | None = None</code> - Cookie to check.</li> <li><code>header_name</code>: <code>str | None = None</code> - Header to check.</li> <li><code>bearer</code>: <code>bool = False</code> - Use bearer prefix for token (e.g. \"Bearer\").</li> <li><code>user</code>: <code>type[BaseUser] = SimpleUser</code> - User for request.</li> </ul> <pre><code>from jam.ext.starlette import PASETOBackend\n\n\napp = Starlette(\n    middleware=[\n        Middleware(\n            AuthenticationMiddleware,\n            backend=PASETOBackend(\n                config=\"config.toml\",\n                header_name=\"Authorization\"\n            ),\n        )\n    ]\n)\n</code></pre>"},{"location":"framework_integrations/starlette/#server-side-sessions-backend","title":"Server side sessions backend","text":"<p>Module: <code>jam.ext.starlette.SessionBackend</code></p> <p>Args:</p> <ul> <li><code>config</code>: <code>str | dict[str, Any] | None = None</code> - Jam config.</li> <li><code>pointer</code>: <code>str</code> - Config pointer.</li> <li><code>cookie_name</code>: <code>str | None = None</code> - Cookie to check.</li> <li><code>header_name</code>: <code>str | None = None</code> - Header to check.</li> <li><code>bearer</code>: <code>bool = False</code> - Use bearer prefix for token (e.g. \"Bearer\").</li> <li><code>user</code>: <code>type[BaseUser] = SimpleUser</code> - User for request.</li> </ul> <pre><code>from jam.ext.starlette import SessionBackend\n\n\napp = Starlette(\n    middleware=[\n        Middleware(\n            AuthenticationMiddleware,\n            backend=JWTBackend(\n                config=\"config.toml\",\n                cookie_name=\"sessionId\"\n            ),\n        )\n    ]\n)\n</code></pre>"},{"location":"usage/cli/","title":"Jam CLI","text":"<p>Jam CLI is just a tool for generating different keys. For example, it is suitable for debugging with real keys or convenient deployment.</p>"},{"location":"usage/cli/#installation","title":"Installation","text":"<pre><code>pip install jamlib[cli]\n</code></pre>"},{"location":"usage/cli/#usage","title":"Usage","text":"<pre><code>$ jam [OPTIONS] COMMAND [ARGS]...\n</code></pre>"},{"location":"usage/cli/#options","title":"Options","text":"<ul> <li><code>--version</code>: Show the version and exit.</li> <li><code>--help</code>: Show help message and exit.</li> </ul>"},{"location":"usage/cli/#commands","title":"Commands","text":"<ul> <li><code>keys</code>: Generate cryptographic keys.</li> <li><code>password</code>: Password hashing and verification utilities.</li> </ul>"},{"location":"usage/cli/#keys","title":"Keys","text":"<p>Generate cryptographic keys.</p> <pre><code>$ jam keys COMMAND [ARGS]...\n</code></pre>"},{"location":"usage/cli/#commands_1","title":"Commands","text":"<ul> <li><code>aes</code>: Generate AES key.</li> <li><code>ecdsa</code>: Generate ECDSA P-384 key pair.</li> <li><code>ed25519</code>: Generate Ed25519 key pair.</li> <li><code>rsa</code>: Generate RSA key pair.</li> <li><code>symmetric</code>: Generate symmetric key.</li> </ul>"},{"location":"usage/cli/#password","title":"Password","text":"<p>Password hashing and verification utilities.</p> <pre><code>$ jam password [OPTIONS] COMMAND [ARGS]...\n</code></pre>"},{"location":"usage/cli/#commands_2","title":"Commands","text":"<ul> <li><code>hash</code>: Hash a password.</li> <li><code>verify</code>: Verify a password against a hash.</li> </ul>"},{"location":"usage/custom/","title":"Custom modules","text":"<p>You can easily override the implementation of any module using the <code>Base*</code> class. For example, if we want to override the implementation of the JWT module, we use <code>jam.jwt.BaseJWT</code>:</p> <pre><code>from jam import Jam\nfrom jam.jose import BaseJWT\n\nclass MyJWT(BaseJWT):\n    def __init__(\n        self,\n        secret_key: str,\n    ):\n        self.secret_key = secret_key\n\n    def encode(self, payload: dict) -&gt; str:\n        # your logic\n        return token\n\n    def decode(self, token: str) -&gt; dict:\n        # your logic\n        return payload\n\n\nconfig = {\n    \"jose\": {\n        \"jwt\": {\n            \"secret_key\" = \"your_secret\"\n        }\n    }\n}\n\nJam.MODULES[\"jwt\"] = \"path.to.MyJWT\"\njam = Jam(config=config)\n\ntoken = jam.jwt_encode(payload={\"user_id\": 123})\n</code></pre> <p>You can also replace modules within framework integrations. For example, suppose you want to replace the <code>JWT</code> implementation module in the Starlette integration:</p> <pre><code>from starlette.applications import Starlette\nfrom starlette.middleware import Middleware\nfrom starlette.middleware.authentication import AuthenticationMiddleware\n\nfrom jam.ext.starlette import JWTBackend\nfrom jam.jose import BaseJWT\n\nclass MyJWT(BaseJWT):\n    def __init__(\n        self,\n        secret_key: str,\n    ):\n        self.secret_key = secret_key\n\n    def encode(self, payload: dict) -&gt; str:\n        # your logic\n        return token\n\n    def decode(self, token: str) -&gt; dict:\n        # your logic\n        return payload\n\nJWTBackend.MODULE = MyJWT\n\napp = Starlette(\n    middleware=[\n        Middleware(\n            AuthenticationMiddleware,\n            backend=JWTBackend(config=\"config.toml\")\n        ),\n    ],\n)\n</code></pre>"},{"location":"usage/jwt/","title":"JWT","text":"<p>Deprecated</p> <p><code>jam.jwt</code> is deprecated, use <code>jam.jose</code>.</p>"},{"location":"usage/jwt/#use-in-instance","title":"Use in instance","text":""},{"location":"usage/jwt/#config","title":"Config","text":"<ul> <li><code>alg</code>: <code>str</code> - Algorithm for generating JWT tokens. Supports: <code>HS256</code>, <code>HS384</code>, <code>HS512</code>, <code>RS256</code>, <code>RS384</code>, <code>RS512</code>, <code>ES256</code>, <code>ES384</code>, <code>ES512</code>, <code>PS256</code>, <code>PS384</code>, <code>PS512</code>.</li> <li><code>secret_key</code>: <code>str</code> - Secret key for token signing. By default, Jam reads the environment variable <code>JAM_JWT_SECRET_KEY</code>.</li> <li><code>password</code>: <code>str</code> - Password for encrypted private keys.</li> <li><code>list</code>: <code>dict[str, Any] | None = None</code> - Token black/white list config. See: <code>jam.jwt.list</code>.</li> </ul> <pre><code>[jam.jwt]\nalg = \"HS256\"\nsecret_key = \"YOURSECRETKEY\"\npassword = \"PASSWORD_FOR_PRIVATE_KEY\"\n\n[jam.jwt.list]\ntype = \"black\"\nbackend = \"redis\"\nredis_uri = \"redis://localhost:6379\"\n</code></pre>"},{"location":"usage/jwt/#usage","title":"Usage","text":"<pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\n</code></pre>"},{"location":"usage/jwt/#generate-payload","title":"Generate payload","text":"<p>Method: <code>jam.jwt_make_payload</code></p> <p>Args:</p> <ul> <li><code>exp</code>: <code>int | None</code> - Token life time.</li> <li><code>data</code>: <code>dict[str, Any]</code> - Custom data.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Payload for token.</p> <pre><code>payload = jam.jwt_make_payload(\n    exp=3600,\n    data={\n        \"user\": 1,\n        \"role\": \"admin\"\n    }\n)\nprint(payload)\n&gt;&gt;&gt; {\n        'iat': 1772116588.752527,\n        'exp': 1772120188.752542,\n        'jti': 'b3402bbc-d766-42bc-a763-3efc882dc540',\n        'user': 1,\n        'role': 'admin'\n    }\n</code></pre>"},{"location":"usage/jwt/#create-token","title":"Create token","text":"<p>Method: <code>jam.jwt_create</code></p> <p>Args:</p> <ul> <li><code>payload</code>: <code>dict[str, Any]</code> - Payload for token.</li> </ul> <p>Returns:</p> <p><code>str</code>: Token.</p> <pre><code>token = jam.jwt_create(payload=payload)\nprint(token)\n&gt;&gt;&gt; eyJhbGciOiAi...\n</code></pre>"},{"location":"usage/jwt/#decode-token","title":"Decode token","text":"<p>Method: <code>jam.jwt_decode</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Token to decode.</li> <li><code>check_exp</code>: <code>bool</code> - Check token expiration.</li> <li><code>check_list</code>: <code>bool</code> - Check token black/white list.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Decoded token data.</p> <pre><code>data = jam.jwt_decode(\n    token=token,\n    check_exp=True,\n    check_list=False\n)\nprint(data)\n&gt;&gt;&gt; {\n        'exp': 1772120451.373754,\n        'iat': 1772116851.373746,\n        'jti': '7cd12c49-2987-4011-8578-d3e7405d7673',\n        'role': 'admin',\n        'user': 1\n    }\n</code></pre>"},{"location":"usage/jwt/#list","title":"List","text":"<p>Method: <code>jam.jwt.list</code></p>"},{"location":"usage/jwt/#add-token-to-list","title":"Add token to list","text":"<p>Method: <code>jam.jwt.list.add</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Token to add.</li> </ul> <pre><code>jam.jwt.list.add(\n    token=token\n)\n</code></pre>"},{"location":"usage/jwt/#remove-token-from-list","title":"Remove token from list","text":"<p>Method: <code>jam.jwt.list.delete</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Token to remove.</li> </ul> <pre><code>jam.jwt.list.remove(\n    token=token\n)\n</code></pre>"},{"location":"usage/jwt/#check-token-in-list","title":"Check token in list","text":"<p>Method: <code>jam.jwt.list.check</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Token to check.</li> </ul> <p>Returns:</p> <p><code>bool</code>: <code>True</code> if token is in list, <code>False</code> otherwise.</p> <pre><code>result = jam.jwt.list.check(\n    token=token\n)\nprint(result)\n&gt;&gt;&gt; False\n</code></pre>"},{"location":"usage/jwt/#use-out-of-instance","title":"Use out of instance","text":""},{"location":"usage/jwt/#built","title":"Built","text":"<p>Module: <code>jam.jwt.JWT</code></p> <p>Args:</p> <ul> <li><code>alg</code>: <code>str</code> - Algorithm for token generation.</li> <li><code>secret_key</code>: <code>str | bytes</code> - Secret key for token generation. By default, Jam reads the environment variable <code>JAM_JWT_SECRET_KEY</code>.</li> <li><code>password</code>: <code>str | bytes | None</code> - Password for token generation.</li> <li><code>list</code>: <code>dict[str, Any] | None</code> - List config. See: <code>jam.jwt.list</code>.</li> </ul> <pre><code>from jam.jwt import JWT\n\njwt = JWT(\n    alg=\"HS256\",\n    secret_key=\"SECRET_KEY\",\n    password=None,\n    list={\n        \"type\": \"black\",\n        \"backend\": \"json\",\n        \"json_path\": \"lists.json\"\n    }\n)\n</code></pre>"},{"location":"usage/jwt/#encode-token","title":"Encode token","text":"<p>Method: <code>JWT.encode</code></p> <p>Args:</p> <ul> <li><code>payload</code>: <code>dict[str, Any]</code> - Payload for token.</li> </ul> <p>Returns:</p> <p><code>str</code>: Token.</p> <pre><code>token = jwt.encode(\n    payload={\n        \"id\": 1\n        \"username\": \"lyaguxa\"\n    }\n)\nprint(token)\n&gt;&gt;&gt; eyJhbGciOiAiSFM...\n</code></pre>"},{"location":"usage/jwt/#decode-token_1","title":"Decode token","text":"<p>Method: <code>JWT.decode</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Token to decode.</li> <li><code>public_key</code>: <code>str | bytes | None</code> - Public key for token verification.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Decoded token data.</p> <pre><code>data = jwt.decode(\n    token=token,\n    public_key=None\n)\nprint(data)\n&gt;&gt;&gt; {\n        'id': 1,\n        'username': 'lyaguxa'\n    }\n</code></pre>"},{"location":"usage/jwt/#list_1","title":"List","text":"<p>Method: <code>JWT.list</code></p>"},{"location":"usage/jwt/#add-token-to-list_1","title":"Add token to list","text":"<p>Method: <code>JWT.list.add</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Token to add.</li> </ul> <pre><code>jwt.list.add(token)\n</code></pre>"},{"location":"usage/jwt/#remove-token-from-list_1","title":"Remove token from list","text":"<p>Method: <code>JWT.list.delete</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Token to remove.</li> </ul> <pre><code>jwt.list.delete(token)\n</code></pre>"},{"location":"usage/jwt/#check-token-in-list_1","title":"Check token in list","text":"<p>Method: <code>JWT.list.check</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Token to check.</li> </ul> <p>Returns:</p> <p><code>bool</code>: <code>True</code> if token is in list, <code>False</code> otherwise.</p> <pre><code>result = jwt.list.check(token)\nprint(result)\n&gt;&gt;&gt; False\n</code></pre>"},{"location":"usage/logging/","title":"Logging","text":"<p>By default, Jam uses <code>JamLogger</code>, a class that utilizes standard <code>logging</code>. Therefore, you can use the standard Python method to collect logs:</p> <pre><code>import logging\n\nlogger = logging.getLogger(\"jam\")\n</code></pre> <p>If for some reason you are not satisfied with the standard logger, you can easily replace it in <code>Jam</code> using <code>BaseLogger</code>: <pre><code>from jam import Jam, BaseLogger\n\nclass YourCustomLogger(BaseLogger):\n\n    def __init__(self, log_level: str):\n        ...\n\n    def info(self, message: str):\n        # your logger implementation here\n        pass\n\n    def warning(self, message: str):\n        # your logger implementation here\n        pass\n\n    def error(self, message: str):\n        # your logger implementation here\n        pass\n\n    def critical(self, message: str):\n        # your logger implementation here\n        pass\n\n\njam = Jam(logger=BaseLogger)    \n</code></pre></p>"},{"location":"usage/oauth2/","title":"OAuth2","text":""},{"location":"usage/oauth2/#use-in-instance","title":"Use in instance","text":""},{"location":"usage/oauth2/#configuration","title":"Configuration","text":"<p>Args:</p> <ul> <li><code>providers</code>: <code>hashmap</code></li> <li><code>client_id</code>: <code>str</code> - Client ID in your app.</li> <li><code>client_secret</code>: <code>str</code> - Client secret in your app.</li> <li><code>auth_url</code>: <code>str</code> - Authorization URL.</li> <li><code>token_url</code>: <code>str</code> - Token URL.</li> <li><code>redirect_uri</code>: <code>str</code> - Redirect URI.</li> </ul> <p>Example:</p> <pre><code>[jam.oauth2.providers.linkedin]  # OAuth2 client\nclient_id = \"$LINKEDIN_CLIENT_ID\"\nclient_secret = \"$LINKEDIN_CLIENT_SECRET\"\nauth_url = \"https://www.linkedin.com/oauth/v2/authorization\"\ntoken_url = \"https://www.linkedin.com/oauth/v2/accessToken\"\nredirect_url = \"https://example.com/callback/linkedin\"\n\n[jam.oauth2.providers.github]  # builtin providers\nclient_id = \"$GITHUB_CLIENT_ID\"\nclient_secret = \"$GITHUB_CLIENT_SECRET\"\nredirect_url = \"https://example.com/callback/github\"\n</code></pre>"},{"location":"usage/oauth2/#get-auth-url","title":"Get auth url","text":"<p>Method: <code>jam.oauth2_get_authorized_url</code></p> <p>Args:</p> <ul> <li><code>provider</code>: <code>str</code> - Provider name from config.</li> <li><code>scope</code>: <code>list[str]</code> - Scope for provider.</li> </ul> <p>Returns:</p> <p><code>str</code>: URL for authoriaztion.</p> <pre><code>url = jam.oauth2_get_authorized_url(\n    provider=\"github\",\n    scope=[\"gist\"],\n#    **extra_params\n)\n</code></pre>"},{"location":"usage/oauth2/#fetch-token","title":"Fetch token","text":"<p>Method: <code>jam.oauth2_fetch_token</code></p> <p>Args:</p> <ul> <li><code>provider</code>: <code>str</code> - Provider name from config.</li> <li><code>code</code>: <code>str</code> - OAuth2 code.</li> <li><code>grant_type</code>: <code>str = authorization_code</code> - Type of oauth2 grant.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code> - Tokens.</p> <pre><code>tokens = jam.oauth2_fetch_token(\n    provider=\"github\",\n    code=\"HGvdskHG\",\n    grant_type=\"authorization_code\"\n)\n</code></pre>"},{"location":"usage/oauth2/#refresh-token","title":"Refresh token","text":"<p>Method: <code>jam.oauth2_refresh_token</code></p> <p>Args:</p> <ul> <li><code>provider</code>: <code>str</code> - Provider name from config.</li> <li><code>refresh_token</code>: <code>str</code> - Refresh token.</li> <li><code>grant_type</code>: <code>str = \"refresh_token\"</code> - Grant type.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Refresh token.</p> <pre><code>tokens = jam.oauth2_refresh_token(\n    provider=\"github\",\n    refresh_token=token,\n)\n</code></pre>"},{"location":"usage/oauth2/#client-credentials-flow","title":"Client credentials flow","text":"<p>Method: <code>jam.oauth2_client_credentials_flow</code></p> <p>Args:</p> <ul> <li><code>provider</code>: <code>str</code> - Provider name from config.</li> <li><code>scope</code>: <code>list[str]</code> - Scope for provider.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Data.</p> <pre><code>tokens = jam.oauth2_client_credentials(\n    provider=\"github\",\n    scope=[\"gist\"]\n)\n</code></pre>"},{"location":"usage/oauth2/#use-out-of-instance","title":"Use out of instance","text":""},{"location":"usage/oauth2/#built","title":"Built","text":""},{"location":"usage/oauth2/#custom-provider","title":"Custom provider","text":"<p>Module: <code>jam.oauth2.client.OAuth2Client</code></p> <p>Args:</p> <ul> <li><code>client_id</code>: <code>str</code> - Client ID from oauth2 provider.</li> <li><code>client_secret</code>: <code>str</code> - Client secret from oauth2 provider.</li> <li><code>auth_url</code>: <code>str</code> - URL for authentication.</li> <li><code>token_url</code>: <code>str</code> - URL for token exchange.</li> <li><code>redirect_uri</code>: <code>str</code> - Redirect URI for oauth2 flow.</li> <li><code>serializer</code>: <code>BaseEncoder | type[BaseEncoder] = JsonEncoder</code> - JSON Serializer.</li> </ul> <pre><code>from jam.oauth2 import OAuth2Client\n\n\ncustom_service = OAuth2Client(\n    client_id=\"ID\",\n    client_secret=\"SECRET\",\n    auth_url=\"https://example.com/oauth2/auth\",\n    token_url=\"https://example.com/oauth2/token\",\n    redirect_uri=\"https://example.com/oauth2/callback\",\n)\n</code></pre>"},{"location":"usage/oauth2/#builtin-providers","title":"Builtin providers","text":"<p>Modules:</p> <ul> <li><code>jam.oauth2.GitHubOAuth2Client</code></li> <li><code>jam.oauth2.GitLabOAuth2Client</code></li> <li><code>jam.oauth2.GoogleOAuth2Client</code></li> <li><code>jam.oauth2.YandexOAuth2Client</code></li> </ul> <p>Args:</p> <ul> <li><code>client_id</code>: <code>str</code> - Client ID from oauth2 provider.</li> <li><code>client_secret</code>: <code>str</code> - Client secret from oauth2 provider.</li> <li><code>redirect_uri</code>: <code>str</code> - Redirect URI for oauth2 flow.</li> </ul> <pre><code>from jam.aouth2 import GitHubOAuth2Client\n\ngithub_oauth2 = GitHubOAuth2Client(\n    client_id=\"ID\",\n    client_secret=\"SECRET\",\n    redirect_uri=\"https://example.com/oauth2/callback\",\n)\n</code></pre>"},{"location":"usage/oauth2/#get-auth-url_1","title":"Get auth url","text":"<p>Method: <code>oauth2.get_auth_url</code></p> <p>Args:</p> <ul> <li><code>scope</code>: <code>list[str]</code> - Scope for the oauth2 flow.</li> </ul> <p>Returns:</p> <p><code>str</code> - Authorization URL for the oauth2 flow.</p> <pre><code>url = oauth2.get_auth_url(scope=[\"user\", \"email\", \"avatar\"])\n</code></pre>"},{"location":"usage/oauth2/#fetch-token_1","title":"Fetch token","text":"<p>Method: <code>oauth2.fetch_token</code></p> <p>Args:</p> <ul> <li><code>code</code>: <code>str</code> - Authorization code from oauth2 provider.</li> <li><code>grant_type</code>: <code>str = \"authorization_code\"</code> - Grant type for oauth2 flow.</li> </ul> <p>Returns:</p> <p><code>dict</code> - Token response from oauth2 provider.</p> <pre><code>tokens = oauth2.fetch_token(code=\"KJSKJDSLY7DJSK\")\n</code></pre>"},{"location":"usage/oauth2/#refresh-token_1","title":"Refresh token","text":"<p>Method: <code>oauth2.refresh_token</code></p> <p>Args:</p> <ul> <li><code>refresh_token</code>: <code>str</code> - Refresh token from oauth2 provider.</li> <li><code>grant_type</code>: <code>str = \"refresh_token\"</code> - Grant type for oauth2 flow.</li> </ul> <p>Returns:</p> <p><code>dict</code> - Token response from oauth2 provider.</p> <pre><code>tokens = oauth2.refresh_token(refresh_token=token)\n</code></pre>"},{"location":"usage/oauth2/#client-credentials","title":"Client credentials","text":"<p>Method: <code>oauth2.client_credentials_flow</code></p> <p>Args:</p> <ul> <li><code>scope</code>: <code>list[str]</code> - Scope for the oauth2 flow.</li> </ul> <p>Returns:</p> <p><code>dict</code> - Token response from oauth2 provider.</p> <pre><code>tokens = oauth2.client_credentials_flow(scope=[\"user\", \"email\", \"avatar\"])\n</code></pre>"},{"location":"usage/otp/","title":"OTP","text":""},{"location":"usage/otp/#use-in-instance","title":"Use in instance","text":""},{"location":"usage/otp/#configuration","title":"Configuration","text":"<p>Args:</p> <ul> <li><code>type</code>: <code>str</code> - <code>totp</code> for TOTP, <code>hotp</code> for HOTP.</li> <li><code>digits</code>: <code>int</code> - Number of digits for the OTP code.</li> <li><code>digest</code>: <code>str</code> - Hash algorithm to use for the OTP code.</li> <li><code>interval</code>: <code>int</code> - Interval in seconds for TOTP codes.</li> </ul> <p>Example:</p> <pre><code>[jam.otp]\ntype = \"totp\"\ndigits = 6\ndigest = \"sha256\"\ninterval = 30\n</code></pre>"},{"location":"usage/otp/#usage","title":"Usage","text":""},{"location":"usage/otp/#get-code","title":"Get code","text":"<p>Method: <code>jam.otp_code</code></p> <p>Args:</p> <ul> <li><code>secret</code>: <code>str</code> - Secret key for the OTP code.</li> <li><code>factor</code>: <code>int | None = None</code> - Factor for HOTP codes. Defaults to <code>None</code> for TOTP codes.</li> </ul> <p>Returns:</p> <p><code>str</code> - Code</p> <pre><code>code = jam.otp_code(\n    secret=\"CCCUULFTIG5YMEX3HMNHEDCLFM\"\n)\nprint(code)\n&gt;&gt;&gt; 379982\n</code></pre>"},{"location":"usage/otp/#gen-otp-uri","title":"Gen OTP uri","text":"<p>Method: <code>jam.otp_uri</code></p> <p>Args:</p> <ul> <li><code>secret</code>: <code>str</code> - Secret key for the OTP code.</li> <li><code>name</code>: <code>str</code> - Account name.</li> <li><code>issuer</code>: <code>str</code> - Issuer for the OTP code.</li> <li><code>counter</code>: <code>int | None = None</code> - Counter for HOTP codes. Defaults to <code>None</code> for TOTP codes.</li> </ul> <p>Returns:</p> <p><code>str</code> - URI.</p> <pre><code>uri = jam.otp_uri(\n    secret=\"CCCUULFTIG5YMEX3HMNHEDCLFM\",\n    issuer=\"MyApp\",\n    name=\"user@example.com\"\n)\nprint(uri)\n&gt;&gt;&gt; otpauth://totp/MyApp%3Auser%40example.com?secret=CCCUULFTIG5YMEX3HMNHEDCLFM&amp;issuer=MyApp&amp;algorithm=SHA256&amp;digits=6\n</code></pre>"},{"location":"usage/otp/#verify-code","title":"Verify code","text":"<p>Method: <code>jam.otp_verify_code</code></p> <p>Args:</p> <ul> <li><code>secret</code>: <code>str</code> - Secret key for the OTP code.</li> <li><code>code</code>: <code>str</code> - OTP code to verify.</li> <li><code>factor</code>: <code>int | None = None</code> - Factor for HOTP codes. Defaults to <code>None</code> for TOTP codes.</li> <li><code>look_ahead</code>: <code>int = 1</code> - Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is <code>1</code>.</li> </ul> <p>Returns:</p> <p><code>bool</code> - Whether the code is valid.</p> <pre><code>valid = jam.otp_verify_code(\n    secret=\"CCCUULFTIG5YMEX3HMNHEDCLFM\",\n    code=\"379982\"\n)\nprint(valid)\n&gt;&gt;&gt; True\n</code></pre>"},{"location":"usage/otp/#use-out-of-instance","title":"Use out of instance","text":"<p>Modules:</p> <ul> <li><code>jam.otp.TOTP</code></li> <li><code>jam.otp.HOTP</code></li> </ul>"},{"location":"usage/otp/#totp","title":"TOTP","text":""},{"location":"usage/otp/#built","title":"Built","text":"<p>Module: <code>jam.otp.TOTP</code></p> <p>Args:</p> <ul> <li><code>secret</code>: <code>str | bytes</code> - TOTP Secret.</li> <li><code>digits</code>: <code>int = 6</code> - Number of digits for the OTP code. Defaults to <code>6</code>.</li> <li><code>algorithm</code>: <code>str = \"sha1\"</code> - Algorithm for the OTP code. Defaults to <code>\"sha1\"</code>.</li> <li><code>interval</code>: <code>int = 30</code> - Interval for TOTP codes in seconds. Defaults to <code>30</code>.</li> </ul> <pre><code>from jam.otp import TOTP\n\ntotp = TOTP(\n    secret=\"CCCUULFTIG5YMEX3HMNHEDCLFM\",\n    digits=6,\n    algorithm=\"sha1\",\n    interval=30,\n)\n</code></pre>"},{"location":"usage/otp/#usage_1","title":"Usage","text":""},{"location":"usage/otp/#current-code","title":"Current code","text":"<p>Method: <code>totp.now</code></p> <p>Returns: <code>str</code> - Current OTP code.</p> <pre><code>code = totp.now\nprint(code)\n&gt;&gt;&gt; 867877\n</code></pre>"},{"location":"usage/otp/#code-for-specified-time","title":"Code for specified time","text":"<p>Method: <code>totp.at</code></p> <p>Args:</p> <ul> <li><code>factor</code>: <code>int | None = Non</code> - Time in UNIX seconds.</li> </ul> <p>Returns:</p> <p><code>str</code> - OTP code for the specified time.</p> <pre><code>code = totp.at(factor=None)\nprint(code)\n&gt;&gt;&gt; 867877\n</code></pre>"},{"location":"usage/otp/#verify-code_1","title":"Verify code","text":"<p>Method: <code>totp.verify</code></p> <p>Args:</p> <ul> <li><code>code</code>: <code>str</code> - OTP code to verify.</li> <li><code>factor</code>: <code>int | None = None</code> - Factor for HOTP codes. Defaults to <code>None</code> for TOTP codes.</li> <li><code>look_ahead</code>: <code>int = 1</code> - Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is <code>1</code>.</li> </ul> <p>Returns:</p> <p><code>bool</code> - Whether the code is valid.</p> <pre><code>valid = totp.verify(code=\"379982\")\nprint(valid)\n&gt;&gt;&gt; True\n</code></pre>"},{"location":"usage/otp/#hotp","title":"HOTP","text":""},{"location":"usage/otp/#built_1","title":"Built","text":"<p>Module: <code>jam.otp.HOTP</code></p> <p>Args:</p> <ul> <li><code>secret</code>: <code>str</code> - HOTP Secret.</li> <li><code>digits</code>: <code>int = 6</code> - Number of digits for the OTP code. Defaults to <code>6</code>.</li> <li><code>digest</code>: <code>str = \"sha1\"</code> - Algorithm for the OTP code. Defaults to <code>\"sha1\"</code>.</li> </ul> <pre><code>from jam.otp import HOTP\n\nhotp = HOTP(\n    secret=\"CCCUULFTIG5YMEX3HMNHEDCLFM\",\n    digits=6,\n    digest=\"sha1\",\n)\n</code></pre>"},{"location":"usage/otp/#usage_2","title":"Usage","text":""},{"location":"usage/otp/#get-code_1","title":"Get code","text":"<p>Method <code>hotp.at</code></p> <p>Args:</p> <ul> <li><code>factor</code>: <code>int</code> - Counter value for HOTP codes.</li> </ul> <p>Returns:</p> <p><code>str</code> - Code</p> <pre><code>code = hotp.at(\n    factor=1\n)\nprint(code)\n&gt;&gt;&gt; 989760\n</code></pre>"},{"location":"usage/otp/#verify-code_2","title":"Verify code","text":"<p>Method: <code>hotp.verify</code></p> <p>Args:</p> <ul> <li><code>code</code>: <code>str</code> - OTP code to verify.</li> <li><code>factor</code>: <code>int | None = None</code> - Counter value for HOTP codes. Defaults to <code>None</code> for TOTP codes.</li> <li><code>look_ahead</code>: <code>int = 1</code> - Acceptable deviation in intervals (\u00b1window(totp) / \u00b1look ahead(hotp)). Default is <code>1</code>.</li> </ul> <p>Returns:</p> <p><code>bool</code> - Whether the code is valid.</p> <pre><code>valid = hotp.verify(code=\"379982\", factor=1)\nprint(valid)\n&gt;&gt;&gt; True\n</code></pre>"},{"location":"usage/paseto/","title":"PASETO","text":""},{"location":"usage/paseto/#use-in-instance","title":"Use in instance","text":""},{"location":"usage/paseto/#config","title":"Config","text":"<p>Args:</p> <ul> <li><code>version</code>: <code>str</code> - PASETO version(v1 / v2 / v3 / v4).</li> <li><code>purpose</code>: <code>str</code> - <code>local</code> / <code>public</code>.</li> <li><code>secret_key</code>: <code>str | None</code>: Secret key for PASETO.</li> </ul> <pre><code>[jam.paseto]\nversion = \"v4\"\npurpose = \"local\"\nsecret_key = \"3KVs1nMaWb8jP0_aYMhsRN_hHf9dwV1UdqKk_wUXlnM\"\n</code></pre>"},{"location":"usage/paseto/#usage","title":"Usage","text":"<pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\n</code></pre>"},{"location":"usage/paseto/#generate-payload","title":"Generate payload","text":"<p>Method: <code>jam.paseto_make_payload</code></p> <p>Args:</p> <ul> <li><code>exp</code>: <code>int | None = Non</code>: Token lifetime.</li> <li><code>data</code>: <code>dict[str, Any]</code>: Data to payload.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Payload.</p> <pre><code>payload = jam.paseto_make_payload(\n    exp=3600,\n    data={\n        \"id\": 1,\n        \"role\": \"admin\"\n    }\n)\nprint(token)\n&gt;&gt;&gt; {\n        'iat': 1772129106.102009,\n        'exp': 1772132706.102009,\n        'pit': '0c2c38d2-5dcb-4294-bb2d-0820f6ff787d',\n        'data': {'id': 1, 'role': 'admin'}\n    }\n</code></pre>"},{"location":"usage/paseto/#create-token","title":"Create token","text":"<p>Method: <code>jam.paset_create</code></p> <p>Args:</p> <ul> <li><code>payload</code>: <code>dict[str, Any]</code> - Token payload.</li> <li><code>footer</code>: <code>dict[str, Any] | str | None</code> - Token footer.</li> </ul> <p>Returns:</p> <p><code>str</code>: PASETO.</p> <pre><code>token = jam.paseto_create(\n    payload=payload,\n    footer={\n        \"some\": \"footer\",\n        \"as\": \"dict\"\n    }\n)\nprint(token)\n&gt;&gt;&gt; v4.local.wTgWfsaSTjBcuZSqI7mT...\n</code></pre>"},{"location":"usage/paseto/#decode-token","title":"Decode token","text":"<p>Method: <code>jam.paseto_decode</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - PASETO.</li> <li><code>check_exp</code>: <code>bool = True</code> - Check token expiration.</li> <li><code>check_list</code>: <code>bool = True</code> - Check token list.</li> </ul> <p>Returns:</p> <p><code>dict[str, dict[str, Any] | str | None]</code> - dict like <code>{\"payload\": {&lt;payload_data&gt;}, \"footer\": &lt;footer_data&gt;</code>.</p> <pre><code>data = jam.paseto_decode(\n    token=token,\n    check_exp=True,\n    check_list=False\n)\nprint(data)\n&gt;&gt;&gt; {\n        'payload': {\n            'data': {'id': 1, 'role': 'admin'},\n            'exp': 1772132706.102009,\n            'iat': 1772129106.102009,\n            'pit': '0c2c38d2-5dcb-4294-bb2d-0820f6ff787d'\n            },\n        'footer': {\n            'as': 'dict',\n            'some': 'footer'\n        }\n    }\n</code></pre>"},{"location":"usage/paseto/#use-out-of-instance","title":"Use out of instance","text":"<p>Modules:</p> <ul> <li><code>jam.paseto.PASETOv1</code></li> <li><code>jam.paseto.PASETOv2</code></li> <li><code>jam.paseto.PASETOv3</code></li> <li><code>jam.paseto.PASETOv4</code></li> </ul> <p>For example, we will show how to work with v4.</p>"},{"location":"usage/paseto/#built","title":"Built","text":"<p>Method: <code>PASETOv4.key</code></p> <p>Args:</p> <ul> <li><code>purpose</code>: <code>str</code> - <code>local</code> / <code>public</code>.</li> <li><code>secret_key</code>: <code>str | bytes</code>: Symmetric key for local and Asymmetric key for public.</li> </ul> <p>Returns:</p> <p><code>PASETOv4</code>: Built PASETOv4 instance.</p> <pre><code>from jam.paseto import PASETOv4`\n\npaseto = PASETOv4.key(\n    purpose=\"local\",\n    secret_key=\"3KVs1nMaWb8jP0_aYMhsRN_hHf9dwV1UdqKk_wUXlnM\"\n)\n</code></pre>"},{"location":"usage/paseto/#encode-token","title":"Encode token","text":"<p>Method: <code>paseto.encode</code></p> <p>Args:</p> <ul> <li><code>payload</code>: <code>dict[str, Any]</code> - Token payload.</li> <li><code>footer</code>: <code>dict[str, Any] | str | None = Non</code> - Token footer.</li> <li><code>serializer</code>: <code>type[BaseEncoder] | BaseEncoder = JamEncoder</code> - JSON serializer.</li> </ul> <p>Returns:</p> <p><code>str</code>: PASETO.</p> <pre><code>token = paseto.encode(\n    payload={\"id\": 1, \"role\": \"admin\"},\n    footer=\"some_footer_as_string\"\n)\nprint(token)\n&gt;&gt;&gt; v4.local.Py0Y4CbmylrmFo3F54u7l1gZCfd\n</code></pre>"},{"location":"usage/paseto/#decode-token_1","title":"Decode token","text":"<p>Method: <code>paseto.decode</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - PASETO token.</li> <li><code>serializer</code>: <code>type[BaseEncoder] | BaseEncoder = JamEncoder</code> - JSON serializer.</li> </ul> <p>Returns:</p> <p><code>tuple[dict[str, Any], dict[str, Any] | str, | None]</code> - Decoded payload and footer.</p> <pre><code>payload, footer = paseto.decode(\n    token=token,\n    check_exp=True,\n    check_list=False\n)\nprint(payload)\n&gt;&gt;&gt; {\n        'id': 1,\n        'role': 'admin'\n    }\nprint(footer)\n&gt;&gt;&gt; \"some_footer_as_string\"\n</code></pre>"},{"location":"usage/saml/","title":"SAML","text":"<p>SAML 2.0 (Security Assertion Markup Language) support.</p> <p>The module implements both roles:</p> <ul> <li>Service Provider (SP) - accepts logins from external IdPs.</li> <li>Identity Provider (IdP) - issues SAML assertions.</li> </ul> <p>Warning</p> <p>The SAML module is not implemented in <code>jam.Jam</code> / <code>jam.aio.Jam</code>. The reason is listed here: makridenko.ru</p> <p>Supported features:</p> <ul> <li>HTTP-POST, HTTP-Redirect and HTTP-Artifact bindings.</li> <li>XML-DSig signatures (RSA-SHA256) with embedded certificates.</li> <li>Assertion encryption (AES-256-GCM + RSA-OAEP) via <code>EncryptedAssertion</code>.</li> <li>Single Logout (SLO).</li> <li>Attribute Query.</li> <li>NameID Management.</li> <li>Metadata generation and parsing.</li> <li>Replay protection and clock skew tolerance.</li> </ul> <p>Works with zero extra dependencies (<code>xml.etree</code> + <code>cryptography</code>).</p>"},{"location":"usage/saml/#setup","title":"Setup","text":"<p>Module: <code>jam.saml.SAML</code></p> <p>There is no <code>Jam</code> instance integration yet, so the module is used directly or through the <code>jam.saml.create_instance</code> factory.</p> <p>Args:</p> <ul> <li><code>role</code>: <code>str = \"sp\"</code> - <code>\"sp\"</code> (Service Provider) or <code>\"idp\"</code> (Identity Provider).</li> <li><code>private_key</code>: <code>str | None</code> - PEM string or path to the private key file (used for signing and decryption).</li> <li><code>public_key</code>: <code>str | None</code> - PEM string or path to the public key / certificate (used for verification).</li> <li><code>certificate</code>: <code>str | None</code> - PEM certificate string (included in metadata and signatures).</li> <li><code>entity_id</code>: <code>str | None</code> - Entity ID of this party.</li> <li><code>acs_url</code>: <code>str | None</code> - Assertion Consumer Service URL (SP role).</li> <li><code>sso_url</code>: <code>str | None</code> - Single Sign-On URL (IdP role).</li> <li><code>idp_public_key</code>: <code>str | None</code> - IdP public key for signature verification (SP role).</li> <li><code>sp_public_key</code>: <code>str | None</code> - SP public key for signature verification (IdP role).</li> <li><code>encryption_key</code>: <code>str | None</code> - SP public key for assertion encryption (IdP role).</li> <li><code>default_exp</code>: <code>int = 300</code> - Default assertion lifetime in seconds.</li> <li><code>allowed_clock_skew</code>: <code>int = 120</code> - Clock skew tolerance in seconds.</li> <li><code>want_assertions_signed</code>: <code>bool = True</code> - Require signed assertions (SP role).</li> <li><code>id_store</code>: <code>dict | None</code> - Dict for replay protection. Auto-created if <code>None</code>.</li> <li><code>replay_ttl</code>: <code>int = 300</code> - Seconds before a consumed ID is eligible for cleanup.</li> </ul> <pre><code>from jam.saml import SAML\n\nidp = SAML(\n    role=\"idp\",\n    private_key=\"path/to/idp_private_key.pem\",\n    certificate=\"path/to/idp_cert.pem\",\n    entity_id=\"https://idp.example.com\",\n    sso_url=\"https://idp.example.com/sso\",\n)\n\nsp = SAML(\n    role=\"sp\",\n    private_key=\"path/to/sp_private_key.pem\",\n    entity_id=\"https://sp.example.com\",\n    acs_url=\"https://sp.example.com/acs\",\n    idp_public_key=\"path/to/idp_cert.pem\",\n)\n</code></pre>"},{"location":"usage/saml/#service-provider","title":"Service Provider","text":""},{"location":"usage/saml/#init","title":"Init","text":"<p>Module: <code>jam.saml.SAML</code> with <code>role=\"sp\"</code>.</p> <pre><code>from jam.saml import SAML\n\nsp = SAML(\n    role=\"sp\",\n    entity_id=\"https://sp.example.com\",\n    acs_url=\"https://sp.example.com/acs\",\n    idp_public_key=\"path/to/idp_cert.pem\",\n)\n</code></pre>"},{"location":"usage/saml/#prepare-authnrequest","title":"Prepare AuthnRequest","text":"<p>Method: <code>sp.prepare_authn_request</code></p> <p>Args:</p> <ul> <li><code>idp_sso_url</code>: <code>str</code> - IdP single sign-on endpoint URL.</li> <li><code>acs_url</code>: <code>str | None</code> - SP assertion consumer service URL (defaults to the instance <code>acs_url</code>).</li> <li><code>binding</code>: <code>str = \"redirect\"</code> - <code>\"redirect\"</code> or <code>\"post\"</code>.</li> <li><code>**kwargs</code> - <code>relay_state</code>, <code>issuer</code>, <code>force_authn</code>, etc.</li> </ul> <p>Returns:</p> <p><code>str</code> - Redirect: IdP URL with a signed AuthnRequest. POST: Base64-encoded SAMLRequest for an HTML form.</p> <p>Redirect binding:</p> <pre><code>url = sp.prepare_authn_request(\n    idp_sso_url=\"https://idp.example.com/sso\",\n    acs_url=\"https://sp.example.com/acs\",\n    binding=\"redirect\",\n)\nprint(url)\n&gt;&gt;&gt; https://idp.example.com/sso?SAMLRequest=...&amp;SigAlg=http%3A%2F%2Fwww.w3.org%2F2001%2F04%2Fxmldsig-more%23rsa-sha256&amp;Signature=...\n</code></pre> <p>POST binding (embed the result in an HTML form field named <code>SAMLRequest</code>):</p> <pre><code>saml_request = sp.prepare_authn_request(\n    idp_sso_url=\"https://idp.example.com/sso\",\n    acs_url=\"https://sp.example.com/acs\",\n    binding=\"post\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-response","title":"Parse response","text":"<p>Method: <code>sp.parse_response</code></p> <p>Args:</p> <ul> <li><code>saml_response</code>: <code>str</code> - Raw SAMLResponse data (Base64 for POST, query-string for Redirect).</li> <li><code>binding</code>: <code>str = \"post\"</code> - <code>\"post\"</code> or <code>\"redirect\"</code>.</li> <li><code>**kwargs</code>:</li> <li><code>audience</code>: <code>str</code> - Expected audience (SP entity ID).</li> <li><code>issuer</code>: <code>str</code> - Expected issuer (IdP entity ID).</li> <li><code>acs_url</code>: <code>str</code> - Expected Recipient (ACS URL).</li> <li><code>verify_signature</code>: <code>bool</code> - Override <code>want_assertions_signed</code>.</li> </ul> <p>Returns:</p> <p><code>SAMLResponse</code> - Parsed response with the assertion data.</p> <pre><code>from jam.saml.binding import encode_post\n\nsaml_response = sp.parse_response(\n    encoded_response,\n    binding=\"post\",\n    audience=\"https://sp.example.com\",\n    issuer=\"https://idp.example.com\",\n)\n\nprint(saml_response.status_code)\n&gt;&gt;&gt; urn:oasis:names:tc:SAML:2.0:status:Success\nprint(saml_response.assertion.subject.name_id)\n&gt;&gt;&gt; user@example.com\nprint(saml_response.assertion.attributes)\n&gt;&gt;&gt; {\"email\": \"user@example.com\", \"role\": \"admin\"}\n</code></pre> <p>Encrypted assertions are decrypted automatically when the SP has a <code>private_key</code>:</p> <pre><code>sp = SAML(\n    role=\"sp\",\n    private_key=\"path/to/sp_private_key.pem\",\n    idp_public_key=\"path/to/idp_cert.pem\",\n)\n\nresult = sp.parse_response(encoded_response, binding=\"post\")\n</code></pre>"},{"location":"usage/saml/#identity-provider","title":"Identity Provider","text":""},{"location":"usage/saml/#init_1","title":"Init","text":"<p>Module: <code>jam.saml.SAML</code> with <code>role=\"idp\"</code>.</p> <pre><code>from jam.saml import SAML\n\nidp = SAML(\n    role=\"idp\",\n    private_key=\"path/to/idp_private_key.pem\",\n    certificate=\"path/to/idp_cert.pem\",\n    entity_id=\"https://idp.example.com\",\n    sso_url=\"https://idp.example.com/sso\",\n    sp_public_key=\"path/to/sp_cert.pem\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-authnrequest","title":"Parse AuthnRequest","text":"<p>Method: <code>idp.parse_authn_request</code></p> <p>Args:</p> <ul> <li><code>saml_request</code>: <code>str</code> - Raw SAMLRequest data.</li> <li><code>binding</code>: <code>str = \"redirect\"</code> - <code>\"redirect\"</code> or <code>\"post\"</code>.</li> <li><code>**kwargs</code> - <code>issuer</code> (expected issuer for validation).</li> </ul> <p>Returns:</p> <p><code>SAMLRequest</code> - Parsed request.</p> <pre><code>request = idp.parse_authn_request(\n    query_string,\n    binding=\"redirect\",\n    issuer=\"https://sp.example.com\",\n)\n\nprint(request.id)\n&gt;&gt;&gt; _abc123...\nprint(request.acs_url)\n&gt;&gt;&gt; https://sp.example.com/acs\n</code></pre>"},{"location":"usage/saml/#build-response","title":"Build response","text":"<p>Method: <code>idp.build_response</code></p> <p>Args:</p> <ul> <li><code>subject</code>: <code>str</code> - The authenticated user identifier.</li> <li><code>attributes</code>: <code>dict[str, Any]</code> - User attributes (email, roles, etc.).</li> <li><code>issuer</code>: <code>str</code> - IdP entity ID.</li> <li><code>audience</code>: <code>str</code> - SP entity ID (the intended audience).</li> <li><code>**kwargs</code>:</li> <li><code>in_response_to</code>: <code>str</code> - AuthnRequest ID.</li> <li><code>name_id_format</code>: <code>str</code> - NameID format.</li> <li><code>session_index</code>: <code>str</code> - Session index.</li> <li><code>destination</code>: <code>str</code> - ACS URL (defaults to the instance <code>acs_url</code>).</li> <li><code>encrypt</code>: <code>bool = False</code> - Encrypt the assertion with <code>encryption_key</code>.</li> </ul> <p>Returns:</p> <p><code>str</code> - Signed (and optionally encrypted) SAML Response XML.</p> <pre><code>xml_str = idp.build_response(\n    subject=\"user@example.com\",\n    attributes={\"email\": \"user@example.com\", \"role\": \"admin\"},\n    issuer=\"https://idp.example.com\",\n    audience=\"https://sp.example.com\",\n    in_response_to=request.id,\n)\n</code></pre> <p>Encrypt the assertion (the SP must have the matching <code>private_key</code>):</p> <pre><code>idp = SAML(\n    role=\"idp\",\n    private_key=\"path/to/idp_private_key.pem\",\n    encryption_key=\"path/to/sp_cert.pem\",\n)\n\nxml_str = idp.build_response(\n    subject=\"user@example.com\",\n    attributes={\"email\": \"user@example.com\"},\n    issuer=\"https://idp.example.com\",\n    audience=\"https://sp.example.com\",\n    encrypt=True,\n)\n</code></pre>"},{"location":"usage/saml/#single-logout","title":"Single Logout","text":"<p>SLO is supported in both directions. Both <code>build_*</code> and <code>parse_*</code> methods accept <code>binding=\"post\"</code> or <code>binding=\"redirect\"</code>.</p>"},{"location":"usage/saml/#build-logoutrequest","title":"Build LogoutRequest","text":"<p>Method: <code>saml.build_logout_request</code></p> <p>Args:</p> <ul> <li><code>name_id</code>: <code>str</code> - User identifier to log out.</li> <li><code>issuer</code>: <code>str</code> - Entity ID of the sender.</li> <li><code>destination</code>: <code>str</code> - SLO endpoint of the recipient.</li> <li><code>session_index</code>: <code>str | None</code> - Session index (optional).</li> <li><code>**kwargs</code> - <code>binding</code>, <code>relay_state</code>, <code>name_id_format</code>.</li> </ul> <p>Returns:</p> <p><code>str</code> - POST: Base64-encoded signed XML. Redirect: signed redirect URL.</p> <pre><code>result = sp.build_logout_request(\n    name_id=\"user@example.com\",\n    issuer=\"https://sp.example.com\",\n    destination=\"https://idp.example.com/slo\",\n    session_index=\"_session_abc\",\n    binding=\"post\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-logoutrequest","title":"Parse LogoutRequest","text":"<p>Method: <code>saml.parse_logout_request</code></p> <p>Args:</p> <ul> <li><code>saml_request</code>: <code>str</code> - Raw LogoutRequest data.</li> <li><code>binding</code>: <code>str = \"redirect\"</code> - <code>\"redirect\"</code> or <code>\"post\"</code>.</li> <li><code>**kwargs</code> - <code>issuer</code> (expected issuer).</li> </ul> <p>Returns:</p> <p><code>SAMLLogoutRequest</code> - Parsed request.</p> <pre><code>logout_request = idp.parse_logout_request(\n    encoded,\n    binding=\"post\",\n    issuer=\"https://sp.example.com\",\n)\nprint(logout_request.name_id)\n&gt;&gt;&gt; user@example.com\nprint(logout_request.session_index)\n&gt;&gt;&gt; _session_abc\n</code></pre>"},{"location":"usage/saml/#build-logoutresponse","title":"Build LogoutResponse","text":"<p>Method: <code>saml.build_logout_response</code></p> <p>Args:</p> <ul> <li><code>in_response_to</code>: <code>str</code> - LogoutRequest ID to respond to.</li> <li><code>issuer</code>: <code>str</code> - Entity ID of the sender.</li> <li><code>destination</code>: <code>str</code> - SLO endpoint of the recipient.</li> <li><code>status_code</code>: <code>str = \"urn:oasis:names:tc:SAML:2.0:status:Success\"</code> - SAML status code.</li> <li><code>**kwargs</code> - <code>binding</code>, <code>relay_state</code>.</li> </ul> <p>Returns:</p> <p><code>str</code> - POST: Base64-encoded signed XML. Redirect: signed redirect URL.</p> <pre><code>result = idp.build_logout_response(\n    in_response_to=logout_request.id,\n    issuer=\"https://idp.example.com\",\n    destination=\"https://sp.example.com/slo\",\n    binding=\"post\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-logoutresponse","title":"Parse LogoutResponse","text":"<p>Method: <code>saml.parse_logout_response</code></p> <p>Args:</p> <ul> <li><code>saml_response</code>: <code>str</code> - Raw LogoutResponse data.</li> <li><code>binding</code>: <code>str = \"redirect\"</code> - <code>\"redirect\"</code> or <code>\"post\"</code>.</li> <li><code>**kwargs</code> - <code>issuer</code> (expected issuer).</li> </ul> <p>Returns:</p> <p><code>SAMLLogoutResponse</code> - Parsed response.</p> <pre><code>logout_response = sp.parse_logout_response(\n    encoded,\n    binding=\"post\",\n    issuer=\"https://idp.example.com\",\n)\nprint(logout_response.status_code)\n&gt;&gt;&gt; urn:oasis:names:tc:SAML:2.0:status:Success\n</code></pre>"},{"location":"usage/saml/#attribute-query","title":"Attribute Query","text":"<p>Lets an SP request specific user attributes from the IdP.</p>"},{"location":"usage/saml/#build-attributequery","title":"Build AttributeQuery","text":"<p>Method: <code>sp.build_attribute_query</code></p> <p>Args:</p> <ul> <li><code>subject</code>: <code>str</code> - Subject to query attributes for.</li> <li><code>issuer</code>: <code>str</code> - SP entity ID.</li> <li><code>destination</code>: <code>str</code> - IdP attribute query endpoint URL.</li> <li><code>attribute_names</code>: <code>list[str] | None</code> - Specific attributes to request (<code>None</code> = all).</li> <li><code>binding</code>: <code>str = \"post\"</code> - <code>\"post\"</code> or <code>\"redirect\"</code>.</li> </ul> <p>Returns:</p> <p><code>str</code> - POST: Base64-encoded signed XML. Redirect: signed redirect URL.</p> <pre><code>encoded = sp.build_attribute_query(\n    subject=\"user@example.com\",\n    issuer=\"https://sp.example.com\",\n    destination=\"https://idp.example.com/attr\",\n    attribute_names=[\"email\", \"role\"],\n    binding=\"post\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-attributequery","title":"Parse AttributeQuery","text":"<p>Method: <code>idp.parse_attribute_query</code></p> <p>Args:</p> <ul> <li><code>saml_request</code>: <code>str</code> - Raw AttributeQuery data.</li> <li><code>binding</code>: <code>str = \"redirect\"</code> - <code>\"redirect\"</code> or <code>\"post\"</code>.</li> <li><code>**kwargs</code> - <code>issuer</code> (expected issuer).</li> </ul> <p>Returns:</p> <p><code>SAMLAttributeQuery</code> - Parsed query.</p> <pre><code>query = idp.parse_attribute_query(encoded, binding=\"post\")\nprint(query.subject)\n&gt;&gt;&gt; user@example.com\nprint(query.attribute_names)\n&gt;&gt;&gt; [\"email\", \"role\"]\n</code></pre>"},{"location":"usage/saml/#build-attributequeryresponse","title":"Build AttributeQueryResponse","text":"<p>Method: <code>idp.build_attribute_query_response</code></p> <p>Args:</p> <ul> <li><code>in_response_to</code>: <code>str</code> - AttributeQuery ID.</li> <li><code>subject</code>: <code>str</code> - The subject.</li> <li><code>attributes</code>: <code>dict[str, Any]</code> - User attributes.</li> <li><code>issuer</code>: <code>str</code> - IdP entity ID.</li> <li><code>audience</code>: <code>str</code> - SP entity ID.</li> </ul> <p>Returns:</p> <p><code>str</code> - Signed SAML Response XML.</p> <pre><code>xml_str = idp.build_attribute_query_response(\n    in_response_to=query.id,\n    subject=\"user@example.com\",\n    attributes={\"email\": \"user@example.com\", \"role\": \"admin\"},\n    issuer=\"https://idp.example.com\",\n    audience=\"https://sp.example.com\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-attributequeryresponse","title":"Parse AttributeQueryResponse","text":"<p>Method: <code>sp.parse_attribute_query_response</code></p> <p>Same signature and return type as <code>sp.parse_response</code>.</p> <pre><code>result = sp.parse_attribute_query_response(\n    encoded,\n    binding=\"post\",\n    audience=\"https://sp.example.com\",\n    issuer=\"https://idp.example.com\",\n)\nprint(result.assertion.attributes)\n&gt;&gt;&gt; {\"email\": \"user@example.com\", \"role\": \"admin\"}\n</code></pre>"},{"location":"usage/saml/#artifact-binding","title":"Artifact Binding","text":"<p>The SP receives an artifact instead of the actual message, then resolves it through a direct SOAP back-channel request to the IdP.</p>"},{"location":"usage/saml/#create-an-artifact","title":"Create an artifact","text":"<p>Method: <code>saml.build_artifact</code></p> <p>Args:</p> <ul> <li><code>source_message_id</code>: <code>str</code> - ID of the referenced message.</li> <li><code>issuer</code>: <code>str</code> - Entity ID of the issuer.</li> </ul> <p>Returns:</p> <p><code>str</code> - Base64-encoded artifact.</p> <pre><code>artifact = idp.build_artifact(\n    source_message_id=response_id,\n    issuer=\"https://idp.example.com\",\n)\nprint(artifact)\n&gt;&gt;&gt; AQAAEAAA...\n</code></pre>"},{"location":"usage/saml/#build-artifactresolve","title":"Build ArtifactResolve","text":"<p>Method: <code>sp.build_artifact_resolve</code></p> <p>Args:</p> <ul> <li><code>artifact</code>: <code>str</code> - The artifact to resolve.</li> <li><code>issuer</code>: <code>str</code> - SP entity ID.</li> <li><code>destination</code>: <code>str</code> - IdP artifact resolution service URL.</li> <li><code>**kwargs</code> - <code>binding</code> (<code>\"post\"</code>, <code>\"redirect\"</code>, or <code>\"soap\"</code>).</li> </ul> <p>Returns:</p> <p><code>str</code> - POST: Base64. Redirect: signed URL. SOAP: raw XML.</p> <pre><code>resolve_xml = sp.build_artifact_resolve(\n    artifact=artifact,\n    issuer=\"https://sp.example.com\",\n    destination=\"https://idp.example.com/artifact\",\n    binding=\"soap\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-artifactresolve","title":"Parse ArtifactResolve","text":"<p>Method: <code>idp.parse_artifact_resolve</code></p> <p>Args:</p> <ul> <li><code>saml_request</code>: <code>str</code> - Raw ArtifactResolve data.</li> <li><code>binding</code>: <code>str = \"post\"</code> - <code>\"post\"</code>, <code>\"redirect\"</code>, or <code>\"soap\"</code>.</li> <li><code>**kwargs</code> - <code>issuer</code> (expected issuer).</li> </ul> <p>Returns:</p> <p><code>SAMLArtifactResolve</code> - Parsed resolve request.</p> <pre><code>resolve = idp.parse_artifact_resolve(soap_envelope, binding=\"soap\")\nprint(resolve.artifact)\n&gt;&gt;&gt; AQAAEAAA...\n</code></pre>"},{"location":"usage/saml/#build-artifactresponse","title":"Build ArtifactResponse","text":"<p>Method: <code>idp.build_artifact_response</code></p> <p>Args:</p> <ul> <li><code>in_response_to</code>: <code>str</code> - ArtifactResolve ID.</li> <li><code>original_message_xml</code>: <code>str</code> - The original SAML message XML to embed.</li> <li><code>issuer</code>: <code>str</code> - IdP entity ID.</li> <li><code>destination</code>: <code>str</code> - SP endpoint URL.</li> <li><code>**kwargs</code> - <code>binding</code>, <code>status_code</code>.</li> </ul> <p>Returns:</p> <p><code>str</code> - POST: Base64. Redirect: signed URL. SOAP: raw XML.</p> <pre><code>response_xml = idp.build_artifact_response(\n    in_response_to=resolve.id,\n    original_message_xml=original_response_xml,\n    issuer=\"https://idp.example.com\",\n    destination=\"https://sp.example.com/acs\",\n    binding=\"soap\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-artifactresponse","title":"Parse ArtifactResponse","text":"<p>Method: <code>sp.parse_artifact_response</code></p> <p>Args:</p> <ul> <li><code>saml_response</code>: <code>str</code> - Raw ArtifactResponse data.</li> <li><code>binding</code>: <code>str = \"post\"</code> - <code>\"post\"</code>, <code>\"redirect\"</code>, or <code>\"soap\"</code>.</li> <li><code>**kwargs</code> - <code>issuer</code> (expected issuer).</li> </ul> <p>Returns:</p> <p><code>SAMLArtifactResponse</code> - Parsed response with the embedded <code>original_message</code>.</p> <pre><code>artifact_response = sp.parse_artifact_response(\n    soap_response,\n    binding=\"soap\",\n)\nprint(artifact_response.original_message)\n&gt;&gt;&gt; &lt;samlp:Response ...&gt;\n</code></pre>"},{"location":"usage/saml/#resolve-artifact-over-http","title":"Resolve artifact over HTTP","text":"<p>Method: <code>sp.resolve_artifact</code></p> <p>Sends the ArtifactResolve as a SOAP request to the IdP's artifact resolution service and returns the original message.</p> <p>Args:</p> <ul> <li><code>artifact</code>: <code>str</code> - The artifact to resolve.</li> <li><code>issuer</code>: <code>str</code> - SP entity ID.</li> <li><code>resolve_url</code>: <code>str</code> - IdP artifact resolution service URL.</li> <li><code>**kwargs</code> - <code>timeout</code> (int, default 10), <code>expected_issuer</code> (IdP entity ID for validation).</li> </ul> <p>Returns:</p> <p><code>str</code> - The original SAML message XML from the ArtifactResponse.</p> <pre><code>original_message = sp.resolve_artifact(\n    artifact=artifact,\n    issuer=\"https://sp.example.com\",\n    resolve_url=\"https://idp.example.com/artifact\",\n    timeout=15,\n)\n</code></pre>"},{"location":"usage/saml/#nameid-management","title":"NameID Management","text":"<p>Change or terminate a NameID with the other party.</p>"},{"location":"usage/saml/#build-managenameidrequest","title":"Build ManageNameIDRequest","text":"<p>Method: <code>saml.build_manage_name_id_request</code></p> <p>Args:</p> <ul> <li><code>name_id</code>: <code>str</code> - Current NameID.</li> <li><code>issuer</code>: <code>str</code> - Entity ID of the requester.</li> <li><code>destination</code>: <code>str</code> - Recipient endpoint URL.</li> <li><code>new_id</code>: <code>str | None</code> - New identifier (<code>None</code> = terminate).</li> <li><code>binding</code>: <code>str = \"post\"</code> - <code>\"post\"</code> or <code>\"redirect\"</code>.</li> </ul> <p>Returns:</p> <p><code>str</code> - POST: Base64-encoded signed XML. Redirect: signed redirect URL.</p> <p>Change the identifier:</p> <pre><code>encoded = sp.build_manage_name_id_request(\n    name_id=\"user@example.com\",\n    new_id=\"newuser@example.com\",\n    issuer=\"https://sp.example.com\",\n    destination=\"https://idp.example.com/nameid\",\n    binding=\"post\",\n)\n</code></pre> <p>Terminate the identifier (no <code>new_id</code>):</p> <pre><code>encoded = sp.build_manage_name_id_request(\n    name_id=\"user@example.com\",\n    issuer=\"https://sp.example.com\",\n    destination=\"https://idp.example.com/nameid\",\n    binding=\"post\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-managenameidrequest","title":"Parse ManageNameIDRequest","text":"<p>Method: <code>idp.parse_manage_name_id_request</code></p> <p>Args:</p> <ul> <li><code>saml_request</code>: <code>str</code> - Raw ManageNameIDRequest data.</li> <li><code>binding</code>: <code>str = \"redirect\"</code> - <code>\"redirect\"</code> or <code>\"post\"</code>.</li> <li><code>**kwargs</code> - <code>issuer</code> (expected issuer).</li> </ul> <p>Returns:</p> <p><code>SAMLManageNameIDRequest</code> - Parsed request.</p> <pre><code>request = idp.parse_manage_name_id_request(encoded, binding=\"post\")\nprint(request.name_id)\n&gt;&gt;&gt; user@example.com\nprint(request.new_id)\n&gt;&gt;&gt; newuser@example.com\n</code></pre>"},{"location":"usage/saml/#build-managenameidresponse","title":"Build ManageNameIDResponse","text":"<p>Method: <code>saml.build_manage_name_id_response</code></p> <p>Args:</p> <ul> <li><code>in_response_to</code>: <code>str</code> - ManageNameIDRequest ID.</li> <li><code>issuer</code>: <code>str</code> - Entity ID of the responder.</li> <li><code>destination</code>: <code>str</code> - Endpoint URL of the requester.</li> <li><code>status_code</code>: <code>str</code> - SAML status code (default Success).</li> <li><code>**kwargs</code> - <code>binding</code>, <code>relay_state</code>.</li> </ul> <p>Returns:</p> <p><code>str</code> - POST: Base64-encoded signed XML. Redirect: signed redirect URL.</p> <pre><code>encoded = idp.build_manage_name_id_response(\n    in_response_to=request.id,\n    issuer=\"https://idp.example.com\",\n    destination=\"https://sp.example.com/nameid\",\n    binding=\"post\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-managenameidresponse","title":"Parse ManageNameIDResponse","text":"<p>Method: <code>sp.parse_manage_name_id_response</code></p> <p>Args:</p> <ul> <li><code>saml_response</code>: <code>str</code> - Raw ManageNameIDResponse data.</li> <li><code>binding</code>: <code>str = \"redirect\"</code> - <code>\"redirect\"</code> or <code>\"post\"</code>.</li> <li><code>**kwargs</code> - <code>issuer</code> (expected issuer).</li> </ul> <p>Returns:</p> <p><code>SAMLManageNameIDResponse</code> - Parsed response.</p> <pre><code>response = sp.parse_manage_name_id_response(encoded, binding=\"post\")\nprint(response.status_code)\n&gt;&gt;&gt; urn:oasis:names:tc:SAML:2.0:status:Success\n</code></pre>"},{"location":"usage/saml/#metadata","title":"Metadata","text":""},{"location":"usage/saml/#generate-metadata","title":"Generate metadata","text":"<p>Method: <code>saml.generate_metadata</code></p> <p>Args:</p> <ul> <li><code>entity_id</code>: <code>str | None</code> - Entity ID (defaults to the instance <code>entity_id</code>).</li> <li><code>sso_url</code>: <code>str | None</code> - SSO URL (IdP metadata).</li> <li><code>acs_url</code>: <code>str | None</code> - ACS URL (SP metadata).</li> <li><code>role</code>: <code>str | None</code> - <code>\"idp\"</code> or <code>\"sp\"</code> (defaults to the instance role).</li> </ul> <p>Returns:</p> <p><code>str</code> - SAML metadata XML.</p> <pre><code>idp_metadata = idp.generate_metadata(\n    entity_id=\"https://idp.example.com\",\n    sso_url=\"https://idp.example.com/sso\",\n)\n\nsp_metadata = sp.generate_metadata(\n    entity_id=\"https://sp.example.com\",\n    acs_url=\"https://sp.example.com/acs\",\n)\n</code></pre>"},{"location":"usage/saml/#parse-metadata","title":"Parse metadata","text":"<p>Method: <code>saml.parse_metadata</code></p> <p>Args:</p> <ul> <li><code>metadata_xml</code>: <code>str</code> - Raw metadata XML string.</li> </ul> <p>Returns:</p> <p><code>SAMLMetadata</code> - Parsed metadata.</p> <pre><code>metadata = sp.parse_metadata(idp_metadata)\nprint(metadata.entity_id)\n&gt;&gt;&gt; https://idp.example.com\nprint(metadata.sso_url)\n&gt;&gt;&gt; https://idp.example.com/sso\n</code></pre>"},{"location":"usage/saml/#security","title":"Security","text":""},{"location":"usage/saml/#signature-verification","title":"Signature verification","text":"<p>Assertions and protocol messages are signed with RSA-SHA256 and an embedded certificate. The SP verifies assertions using the <code>idp_public_key</code>; the IdP verifies requests using <code>sp_public_key</code>. When no key is configured, the certificate embedded in the signature <code>KeyInfo</code> is used.</p> <p>Set <code>want_assertions_signed=False</code> on the SP to skip assertion signature verification:</p> <pre><code>sp = SAML(\n    role=\"sp\",\n    want_assertions_signed=False,\n)\n</code></pre>"},{"location":"usage/saml/#clock-skew","title":"Clock skew","text":"<p>Expired or not-yet-valid assertions are rejected. The <code>allowed_clock_skew</code> (default 120 seconds) tolerates small clock differences between the parties.</p>"},{"location":"usage/saml/#replay-protection","title":"Replay protection","text":"<p>Every incoming message ID is checked against the <code>id_store</code> before being consumed, which raises <code>JamSAMLReplayDetected</code> on duplicate IDs. Pass your own dict to share state between instances or processes:</p> <pre><code>sp = SAML(\n    role=\"sp\",\n    id_store=shared_store,\n)\n</code></pre>"},{"location":"usage/saml/#xxe-protection","title":"XXE protection","text":"<p>Incoming XML is parsed with <code>safe_fromstring</code>, which rejects DTD/entity declarations, preventing XXE and entity-expansion attacks.</p>"},{"location":"usage/saml/#exceptions","title":"Exceptions","text":"Exception Description <code>JamSAMLError</code> Base SAML error. <code>JamSAMLExpired</code> The assertion has expired. <code>JamSAMLNotYetValid</code> The assertion is not yet valid. <code>JamSAMLInvalidAudience</code> Audience validation failed. <code>JamSAMLInvalidIssuer</code> Issuer validation failed. <code>JamSAMLInvalidRecipient</code> SubjectConfirmationData Recipient does not match the expected ACS URL. <code>JamSAMLReplayDetected</code> The message ID has already been consumed (replay attack). <code>JamSAMLSOAPError</code> SOAP / artifact resolution failed. <code>JamSAMLValidationError</code> Generic signature or XML validation failure. <code>JamSAMLEmptyPrivateKey</code> Signing requires a <code>private_key</code>. <code>JamSAMLEmptyPublicKey</code> Verification requires a <code>public_key</code>. <code>JamSAMLUnsupportedAlgorithm</code> Unsupported SAML algorithm."},{"location":"usage/serializers/","title":"JSON serialization","text":"<p>Jam makes it easy to replace the JSON serializer in your code; all you need to do is specify a serializer that inherits from <code>jam.BaseEncoder</code> in the configuration.</p> <pre><code>from jam import Jam, BaseEncoder\n\n\nclass SomeEncoder(BaseEncoder)\n\n    @classmethod\n    @abstractmethod\n    def dumps(cls, var: dict[str, Any]) -&gt; bytes:\n        \"\"\"Dump dict.\"\"\"\n        # some logic\n\n    @classmethod\n    @abstractmethod\n    def loads(cls, var: str | bytes) -&gt; dict[str, Any]:\n        \"\"\"Load json.\"\"\"\n        # some logic\n\n\nconfig = {\n    \"serializer\": SomeEncoder,\n    \"paseto\": {\n        \"version\": \"v3\",\n        \"purpose\": \"local\",\n        \"secret_key\": os.getenv(\"PASETO_SECRET_KEY\")\n    }\n}\n\njam = Jam(\n    config=config,\n    # serializer=SomeSerializer  &lt;- Or you can pass it as a parameter to the `jam.Jam` class\n)\n</code></pre> <p>By default, Jam uses the stdlib json module for serialization. Jam also includes a pre-built serializer for <code>msgspec</code>. To use it, you need to install <code>msgspec</code> and pass it to Jam:</p> <pre><code>pip install jamlib msgspec\n</code></pre> <pre><code>from jam import jam\nfrom jam.encoders import MsgspecJsonEncoder\n\njam = Jam(\n    config=\"jamconfig.yaml\",\n    serializer=MsgspecJsonEncoder,\n)\n</code></pre>"},{"location":"usage/sessions/","title":"Server side sessions","text":""},{"location":"usage/sessions/#use-in-instance","title":"Use in instance","text":""},{"location":"usage/sessions/#config","title":"Config","text":"<p>Out of the box, Jam provides two types of sessions: <code>redis</code> and <code>json</code>. To select the session type, you need to specify it in the configuration. Different session types have different configuration parameters.</p> <ul> <li><code>session_type</code>: <code>str</code> - <code>redis</code> / <code>json</code></li> <li><code>is_session_crypt</code>: <code>bool</code>:</li> </ul> <p>Sometimes you need to encrypt the session ID so that it cannot be forged. If you want to encrypt the session ID, set this parameter to True and pass the encryption key in the session_aes_secret parameter.</p> <ul> <li><code>session_aes_secret</code>: <code>str</code> - The encryption key for the session ID. Key must be 32 url-safe base64-encoded bytes. You can use jam.utils.generate_aes_key to generate it. By default, Jam reads the key from the <code>JAM_SESSION_AES_SECRET</code> environment variable.</li> </ul>"},{"location":"usage/sessions/#redis","title":"Redis","text":"<p>In Redis, sessions are stored as HASH, where name is constructed from <code>&lt;session_path&gt;:&lt;session_key&gt;</code>. The session ID is used as the key, and a serialized JSON object with session data is used as the <code>value</code>.</p> <p>Args:</p> <ul> <li><code>redis_uri</code>: <code>str</code> - Redis address.</li> <li><code>default_ttl</code>: <code>int</code> - Session lifetime in seconds.</li> <li><code>session_path</code>: <code>str</code> - Prefix for session keys in Redis. The default is <code>sessions</code>.</li> </ul> <pre><code>[jam.session]\nsession_type = \"redis\"\nredis_uri = \"redis://0.0.0.0:6379/0\"\nttl = 3600\nsession_path = \"sessions\n</code></pre>"},{"location":"usage/sessions/#json","title":"JSON","text":"<p>In JSON, sessions are stored as files in the directory specified in the <code>json_path</code> parameter.</p> <p>Args:</p> <ul> <li><code>json_path</code>: <code>str</code> - Path to file.</li> </ul> <pre><code>[jam.session]\nsession_type = \"json\"\njson_path = \"sessions.json\"\n</code></pre>"},{"location":"usage/sessions/#custom","title":"Custom","text":"<p>You can also implement your session module using the <code>BaseSession</code>. interface and passing it to the config, for example, to store sessions in a database.</p> <p>See: Customization</p>"},{"location":"usage/sessions/#usage","title":"Usage","text":"<pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\n</code></pre>"},{"location":"usage/sessions/#create-session","title":"Create session","text":"<p>Method: <code>jam.session_create</code></p> <p>Args:</p> <ul> <li><code>session_key</code>: <code>str</code> - Key of session. Username for example.</li> <li><code>data</code>: <code>dict[str, Any]</code> - Some data to store.</li> </ul> <p>Returns:</p> <p><code>str</code>: Session ID.</p> <pre><code>session_id = jam.session_create(\n    session_key=\"user1\",\n    data={\n        \"role\": \"admin\"\n    }\n)\nprint(session_id)\n&gt;&gt;&gt; 565df195-b963-4ebb-8978-318998ef191c\n</code></pre>"},{"location":"usage/sessions/#get-session-data","title":"Get session data","text":"<p>Method: <code>jam.session_get</code></p> <p>Args:</p> <ul> <li><code>session_id</code>: <code>str</code> - Session ID.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any] | None</code>: Session data if session exists.</p> <pre><code>data = jam.session_get(\n    session_id=session_id\n)\nprint(data)\n&gt;&gt;&gt; {\n        \"role\": \"admin\"\n    }\n</code></pre>"},{"location":"usage/sessions/#update-session-data","title":"Update session data","text":"<p>Method: <code>jam.session_update</code></p> <p>Args:</p> <ul> <li><code>session_id</code>: <code>str</code> - Session ID.</li> <li><code>data</code>: <code>dict[str, Any]</code>: New data.</li> </ul> <p>Returns: <code>None</code></p> <pre><code>jam.session_update(\n    session_id=session_id,\n    data={\n        \"role\": \"banned\"\n    }\n)\n</code></pre>"},{"location":"usage/sessions/#delete-session","title":"Delete session","text":"<p>Method: <code>jam.session_delete</code></p> <p>Args:</p> <ul> <li><code>session_id</code>: <code>str</code> - Session ID.</li> </ul> <p>Returns: <code>None</code></p> <pre><code>jam.session_delete(\n    session_id=session_id\n)\n</code></pre>"},{"location":"usage/sessions/#clear-all-user-sessions","title":"Clear all user sessions","text":"<p>Method: <code>jam.session_clear</code></p> <p>Args:</p> <ul> <li><code>session_key</code>: <code>str</code> - Session key.</li> </ul> <p>Returns: <code>None</code></p> <pre><code>jam.session_clear(\n    session_key=\"user1\"\n)\n</code></pre>"},{"location":"usage/sessions/#session-rework","title":"Session rework","text":"<p>Method: <code>jam.session_rework</code></p> <p>Args:</p> <ul> <li><code>old_session_id</code>: <code>str</code> - Old session ID.</li> </ul> <p>Returns:</p> <p><code>str</code>: New session ID.</p> <pre><code>new_session_id = jam.session_rework(\n    old_session_id=session_id\n)\n</code></pre>"},{"location":"usage/sessions/#use-out-of-instance","title":"Use out of instance","text":""},{"location":"usage/sessions/#redis_1","title":"Redis","text":"<p>Module: <code>jam.sessions.redis.Redis</code></p> <p>Args:</p> <ul> <li><code>redis_uri</code>: <code>str | Redis</code> - Redis URI or Redis instance.</li> <li><code>redis_sessions_key</code>: <code>str = \"sessions\"</code> - Redis key for sessions.</li> <li><code>ttl</code>: <code>int | None = 3600</code> - Session life time.</li> <li><code>is_session_crypt</code>: <code>bool = False</code> - Encrypt session data.</li> <li><code>session_aes_secret</code>: <code>bytes | str | None = None</code> - AES key for encrypting session data.</li> <li><code>id_factory</code>: <code>Callable[[], str] = lambda: str(uuid4())</code> - Session ID factory.</li> <li><code>serializer</code>: <code>BaseEncoder | type[BaseEncoder] = JsonEncoder</code> - JSON serializer.</li> <li><code>logger</code>: <code>BaseLogger | None = JamLogger</code> - Logger.</li> </ul> <pre><code>session = RedisSessions(\n    redis_uri=\"redis://localhost:6379\",\n    redis_sessions_key=\"sessions\",\n    default_ttl=3600,\n)\n</code></pre>"},{"location":"usage/sessions/#json_1","title":"JSON","text":"<p>Module: <code>jam.sessions.json.JSONSessions</code></p> <p>Args:</p> <ul> <li><code>json_path</code>: <code>str = sessions.json</code> - Path to json file.</li> <li><code>is_session_crypt</code>: <code>bool = False</code> - Encrypt session data.</li> <li><code>session_aes_secret</code>: <code>bytes | str | None = None</code> - AES key for encrypting session data.</li> <li><code>id_factory</code>: <code>Callable[[], str] = lambda: str(uuid4())</code> - Session ID factory.</li> <li><code>serializer</code>: <code>BaseEncoder | type[BaseEncoder] = JsonEncoder</code> - JSON serializer.</li> <li><code>logger</code>: <code>BaseLogger | None = JamLogger</code> - Logger.</li> </ul> <pre><code>session = JSONSessions(\n    json_path=\"sessions.json\",\n)\n</code></pre>"},{"location":"usage/sessions/#create-session_1","title":"Create session","text":"<p>Method: <code>session.create</code></p> <p>Args:</p> <ul> <li><code>session_key</code>: <code>str</code> - Session key.</li> <li><code>session_data</code>: <code>dict</code> - Session data.</li> </ul> <p>Returns:</p> <p><code>str</code>: Session ID</p> <pre><code>session_id = session.create(\n    session_key=\"user1\",\n    data={\n        \"name\": \"John\",\n        \"age\": 30\n    }\n)\nprint(session_id)\n&gt;&gt;&gt; 46782301-9068-46c8-a24c-b13666438026\n</code></pre>"},{"location":"usage/sessions/#get-session-data_1","title":"Get session data","text":"<p>Method: <code>session.get</code></p> <p>Args:</p> <ul> <li><code>session_id</code>: <code>str</code> - Session ID.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any] | None</code>: Session data if session exists.</p> <pre><code>session_data = session.get(session_id)\nprint(session_data)\n&gt;&gt;&gt; {'name': 'John', 'age': 30}\n</code></pre>"},{"location":"usage/sessions/#update-session","title":"Update session","text":"<p>Method: <code>session.update</code></p> <p>Args:</p> <ul> <li><code>session_id</code>: <code>str</code> - Session ID.</li> </ul> <p>Returns: <code>None</code></p> <pre><code>session.update(\n    session_id=session_id,\n    data={\n        \"name\": \"John\",\n        \"age\": 31\n    }\n)\n</code></pre>"},{"location":"usage/sessions/#delete-session_1","title":"Delete session","text":"<p>Method: <code>session.delete</code></p> <p>Args:</p> <ul> <li><code>session_id</code>: <code>str</code> - Session ID.</li> </ul> <p>Returns: <code>None</code></p> <pre><code>session.delete(session_id)\n</code></pre>"},{"location":"usage/sessions/#clear-all-user-sessions_1","title":"Clear all user sessions","text":"<p>Method: <code>session.clear</code></p> <p>Args:</p> <ul> <li><code>session_key</code>: <code>str</code> - Session key.</li> </ul> <p>Returns: <code>None</code></p> <pre><code>session.clear(session_key=\"user1\")\n</code></pre>"},{"location":"usage/sessions/#session-rework_1","title":"Session rework","text":"<p>Method: <code>session.rework</code></p> <p>Args:</p> <ul> <li><code>session_id</code>: <code>str</code> - Session ID.</li> </ul> <p>Returns: </p> <p><code>str</code>: New session ID.</p> <pre><code>new_session_id = session.rework(\n    session_id=session_id,\n)\n</code></pre>"},{"location":"usage/test_client/","title":"Test client","text":"<p>For convenient testing of your services that use Jam, you can easily replace the main Jam instance with a test instance that has the same interface but works according to its own rules (for example, always succeeds).</p> <p>For example, you have a service for generating JWT tokens.</p> <p>Tip</p> <p>For async services, you can use <code>TestAsyncJam</code> instead of <code>TestJam</code>.</p> <pre><code>from jam import Jam\nform jam.exceptions import JamError\n\n\nclass AuthService:\n    def __init__(\n        self,\n        jam: Jam\n    ) -&gt; None:\n        self.jam = jam\n\n    # Generate token\n    def generate_token(self, user: User) -&gt; str:\n        token = jam.jwt_encode(sub=user.email)\n\n    # Validate token and return user or None\n    def validate_token(self, token) -&gt; User | None:\n        try:\n            payload = self.jam.jwt_decode(token)\n        except JamError:\n            return None\n</code></pre> <p>And you need to write tests for it:</p> <pre><code>import pytest\nfrom jam.tests import TestJam\n\nfrom your_app.services import AuthService\n\n\n@pytest.fixture\ndef auth_service() -&gt; AuthService:\n    return AuthService(\n        jam=TestJam()  # Use TestJam instance here\n    )\n\ndef test_auth_user(auth_service):\n    user = User(id=1, username=\"test_user\")\n    token = auth_service.generate_token(user)  # Generate token\n    assert token is not None\n\n    validated_user = auth_service.validate_token(token)  # Validate token\n    assert validated_user is not None\n    assert validated_user.id == user.id\n    assert validated_user.username == user.username\n\n    # if you want to test invalid token\n    from jam.tests.fakers import invalid_token\n    invalid_user = auth_service.validate_token(invalid_token())\n    assert invalid_user is None\n</code></pre>"},{"location":"usage/jose/","title":"JOSE","text":""},{"location":"usage/jose/#overview","title":"Overview","text":"<p>JOSE (JSON Object Signing and Encryption) is a set of standards for secure data transmission:</p> <ul> <li>JWS (RFC 7515) - JSON Web Signature - digital signature</li> <li>JWE (RFC 7516) - JSON Web Encryption - data encryption</li> <li>JWK (RFC 7517) - JSON Web Key - cryptographic key representation</li> <li>RFC 7518 - JSON Web Algorithms - cryptographic algorithms</li> <li>JWT (RFC 7519) - JSON Web Token - compact token format</li> </ul> <p>Jam provides a complete implementation of all JOSE standards with support for:</p> <ul> <li>Symmetric (HMAC) and asymmetric (RSA, ECDSA, RSA-PSS) signing algorithms</li> <li>Multiple key management algorithms (RSA, AES Key Wrap, ECDH-ES, PBES2,   AES-GCM Key Wrap)</li> <li>Various content encryption modes (AES-CBC-HS, AES-GCM)</li> <li>Three token modes: JWS-only (signed), JWE-only (encrypted), JWS+JWE   (sign-then-encrypt)</li> <li>Automatic JWE key management algorithm selection based on key type</li> <li>HKDF key derivation for symmetric sign-then-encrypt scenarios</li> <li>JWT standard claims validation (exp, nbf)</li> <li>Critical header (<code>crit</code>) validation per RFC 7515</li> <li>Token black/white lists with pluggable backends</li> </ul>"},{"location":"usage/jose/#exports","title":"Exports","text":"<p>The <code>jam.jose</code> package exports the following:</p> <p>Classes:</p> <ul> <li><code>JWK</code> - JSON Web Key</li> <li><code>JWKSet</code> - Set of JWK keys</li> <li><code>JWKRSA</code>, <code>JWKEC</code>, <code>JWKOct</code> - TypedDicts for typed key definitions</li> <li><code>JWS</code> - JSON Web Signature</li> <li><code>JWE</code> - JSON Web Encryption</li> <li><code>JWT</code> - JSON Web Token (supports all three token modes)</li> </ul>"},{"location":"usage/jose/#navigation","title":"Navigation","text":"<ul> <li>JWT - high-level token operations</li> <li>JWS - data signing and verification</li> <li>JWE - data encryption and decryption</li> <li>JWK - cryptographic keys management</li> <li>Lists - token black and white lists</li> <li>Algorithms - supported algorithms reference</li> </ul>"},{"location":"usage/jose/algorithms/","title":"Algorithms","text":""},{"location":"usage/jose/algorithms/#overview","title":"Overview","text":"<p>JOSE supports multiple algorithms organized into three categories:</p> <ul> <li>Signing Algorithms (JWS) - for creating and verifying digital signatures</li> <li>Key Management Algorithms (JWE) - for encrypting Content Encryption Keys</li> <li>Content Encryption Algorithms (JWE) - for encrypting payload data</li> </ul>"},{"location":"usage/jose/algorithms/#signing-algorithms-jws","title":"Signing Algorithms (JWS)","text":"<p>Used with <code>JWS</code> class and <code>JWT.encode()</code> / <code>JWT.decode()</code>.</p>"},{"location":"usage/jose/algorithms/#hmac-symmetric","title":"HMAC (Symmetric)","text":"Algorithm Key Size Notes <code>HS256</code> 256-bit HMAC with SHA-256 <code>HS384</code> 384-bit HMAC with SHA-384 <code>HS512</code> 512-bit HMAC with SHA-512 <p>Requires a shared secret key on both sides.</p> <pre><code>from jam.jose import JWS, JWT\n\n# JWS\njws = JWS(alg=\"HS256\", key=\"shared-secret-key-32-bytes!\")\ntoken = jws.sign(header={}, data={\"data\": \"value\"})\n\n# JWT\njwt = JWT(alg=\"HS256\", secret_key=\"shared-secret-key-32-bytes!\")\ntoken = jwt.encode(exp=3600, payload={\"user\": \"admin\"})\n</code></pre>"},{"location":"usage/jose/algorithms/#rsa-asymmetric","title":"RSA (Asymmetric)","text":"Algorithm Signature Type Notes <code>RS256</code> PKCS#1 v1.5 SHA-256 <code>RS384</code> PKCS#1 v1.5 SHA-384 <code>RS512</code> PKCS#1 v1.5 SHA-512 <p>Uses RSA key pair. Sign with private key, verify with public key.</p> <pre><code>from jam.jose import JWS, JWT\n\n# Sign with private key\njws_sign = JWS(alg=\"RS256\", key=rsa_private_key)\ntoken = jws_sign.sign(header={}, data={\"data\": \"value\"})\n\n# Verify with public key\njws_verify = JWS(alg=\"RS256\", key=rsa_public_key)\nresult = jws_verify.verify(token)\n\n# JWT\njwt = JWT(alg=\"RS256\", secret_key=rsa_private_key)\ntoken = jwt.encode(exp=3600, payload={\"user\": \"admin\"})\n</code></pre>"},{"location":"usage/jose/algorithms/#ecdsa-asymmetric","title":"ECDSA (Asymmetric)","text":"Algorithm Curve Notes <code>ES256</code> P-256 256-bit security <code>ES384</code> P-384 384-bit security <code>ES512</code> P-521 521-bit security <p>Shorter signatures compared to RSA.</p> <pre><code>from jam.jose import JWS, JWT\n\njws = JWS(alg=\"ES256\", key=ec_private_key)\ntoken = jws.sign(header={}, data={\"data\": \"value\"})\n\njwt = JWT(alg=\"ES256\", secret_key=ec_private_key)\ntoken = jwt.encode(exp=3600, payload={\"user\": \"admin\"})\n</code></pre>"},{"location":"usage/jose/algorithms/#rsa-pss-asymmetric","title":"RSA-PSS (Asymmetric)","text":"Algorithm Signature Type Notes <code>PS256</code> PSS SHA-256, probabilistic <code>PS384</code> PSS SHA-384, probabilistic <code>PS512</code> PSS SHA-512, probabilistic <p>More modern RSA signature scheme with probabilistic salt.</p> <pre><code>from jam.jose import JWS, JWT\n\njws = JWS(alg=\"PS256\", key=rsa_private_key)\ntoken = jws.sign(header={}, data={\"data\": \"value\"})\n\njwt = JWT(alg=\"PS256\", secret_key=rsa_private_key)\ntoken = jwt.encode(exp=3600, payload={\"user\": \"admin\"})\n</code></pre>"},{"location":"usage/jose/algorithms/#key-management-algorithms-jwe","title":"Key Management Algorithms (JWE)","text":"<p>Used with <code>JWE</code> class for encrypting Content Encryption Keys.</p>"},{"location":"usage/jose/algorithms/#rsa","title":"RSA","text":"Algorithm Type Notes <code>RSA1_5</code> RSA PKCS#1 v1.5 (legacy, not recommended) <code>RSA-OAEP</code> RSA OAEP with SHA-256 (recommended) <code>RSA-OAEP-256</code> RSA OAEP with SHA-256 (same as RSA-OAEP) <p>Encrypt CEK with RSA public key, decrypt with private key.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"RSA-OAEP\",\n    enc=\"A128CBC-HS256\",\n    key=rsa_public_key\n)\ntoken = jwe.encrypt(plaintext={\"data\": \"sensitive\"})\n\njwe_dec = JWE(\n    alg=\"RSA-OAEP\",\n    enc=\"A128CBC-HS256\",\n    key=rsa_private_key\n)\ndata = jwe_dec.decrypt(token)\n</code></pre>"},{"location":"usage/jose/algorithms/#aes-key-wrap","title":"AES Key Wrap","text":"Algorithm Key Size Notes <code>A128KW</code> 128-bit AES Key Wrap <code>A192KW</code> 192-bit AES Key Wrap <code>A256KW</code> 256-bit AES Key Wrap <p>Symmetric key wrapping. Requires shared key.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"A256KW\",\n    enc=\"A256GCM\",\n    key=\"your-256-bit-key-here!!\"  # 32 bytes\n)\ntoken = jwe.encrypt(plaintext=\"secret\")\n</code></pre>"},{"location":"usage/jose/algorithms/#ecdh-es","title":"ECDH-ES","text":"Algorithm Curve Notes <code>ECDH-ES</code> - Ephemeral-static ECDH, CEK = derived key <code>ECDH-ES+A128KW</code> P-256 ECDH with AES key wrap <code>ECDH-ES+A192KW</code> P-384 ECDH with AES key wrap <code>ECDH-ES+A256KW</code> P-521 ECDH with AES key wrap <p>Ephemeral-static ECDH provides perfect forward secrecy.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"ECDH-ES+A256KW\",\n    enc=\"A128CBC-HS256\",\n    key=ec_public_key\n)\ntoken = jwe.encrypt(plaintext={\"data\": \"sensitive\"})\n</code></pre>"},{"location":"usage/jose/algorithms/#aes-gcm-key-wrap","title":"AES-GCM Key Wrap","text":"Algorithm Key Size Notes <code>A128GCMKW</code> 128-bit AES-GCM key wrap <code>A256GCMKW</code> 256-bit AES-GCM key wrap <p>Similar to AES Key Wrap but uses GCM mode.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"A256GCMKW\",\n    enc=\"A256GCM\",\n    key=\"your-256-bit-key-here!!\"\n)\ntoken = jwe.encrypt(plaintext={\"data\": \"sensitive\"})\n</code></pre>"},{"location":"usage/jose/algorithms/#pbes2-password-based","title":"PBES2 (Password-Based)","text":"Algorithm Hash Key Wrap Notes <code>PBES2-HS256+A128KW</code> SHA-256 A128KW 100k iterations <code>PBES2-HS384+A192KW</code> SHA-384 A192KW 100k iterations <code>PBES2-HS512+A256KW</code> SHA-512 A256KW 100k iterations <p>Password-based encryption using PBKDF2 with AES key wrap.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"PBES2-HS512+A256KW\",\n    enc=\"A256CBC-HS512\",\n    password=b\"user_password\"\n)\ntoken = jwe.encrypt(plaintext={\"data\": \"sensitive\"})\n</code></pre>"},{"location":"usage/jose/algorithms/#content-encryption-algorithms-jwe","title":"Content Encryption Algorithms (JWE)","text":"<p>Used with <code>JWE</code> class for encrypting payload data.</p>"},{"location":"usage/jose/algorithms/#aes-cbc-hs","title":"AES-CBC-HS","text":"Algorithm Key Size MAC Size Notes <code>A128CBC-HS256</code> 256-bit 128-bit HMAC-SHA-256 <code>A192CBC-HS384</code> 384-bit 192-bit HMAC-SHA-384 <code>A256CBC-HS512</code> 512-bit 256-bit HMAC-SHA-512 <p>HMAC-based authenticated encryption in CBC mode.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"RSA-OAEP\",\n    enc=\"A128CBC-HS256\",\n    key=rsa_public_key\n)\ntoken = jwe.encrypt(plaintext={\"data\": \"sensitive\"})\n</code></pre>"},{"location":"usage/jose/algorithms/#aes-gcm","title":"AES-GCM","text":"Algorithm Key Size Notes <code>A128GCM</code> 128-bit Authenticated encryption <code>A256GCM</code> 256-bit Authenticated encryption <p>Galois/Counter Mode provides built-in authenticated encryption.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"A256KW\",\n    enc=\"A256GCM\",\n    key=\"your-256-bit-key-here!!\"\n)\ntoken = jwe.encrypt(plaintext={\"data\": \"sensitive\"})\n</code></pre>"},{"location":"usage/jose/algorithms/#algorithm-combinations","title":"Algorithm Combinations","text":"<p>Common recommended combinations:</p>"},{"location":"usage/jose/algorithms/#symmetric-hmac","title":"Symmetric (HMAC)","text":"<pre><code>HS256 + A128CBC-HS256  \u2192 Basic security\nHS384 + A192CBC-HS384  \u2192 Medium security\nHS512 + A256CBC-HS512  \u2192 High security\n</code></pre>"},{"location":"usage/jose/algorithms/#rsa-asymmetric_1","title":"RSA (Asymmetric)","text":"<pre><code>RS256/PS256 + RSA-OAEP + A128CBC-HS256  \u2192 Basic RSA encryption\nRS512/PS512 + RSA-OAEP + A256CBC-HS512  \u2192 High security RSA encryption\n</code></pre>"},{"location":"usage/jose/algorithms/#ecdh-perfect-forward-secrecy","title":"ECDH (Perfect Forward Secrecy)","text":"<pre><code>ES256 + ECDH-ES + A128CBC-HS256   \u2192 P-256 ECDH\nES384 + ECDH-ES+A192KW + A192CBC-HS384  \u2192 P-384 ECDH\nES512 + ECDH-ES+A256KW + A256CBC-HS512  \u2192 P-521 ECDH\n</code></pre>"},{"location":"usage/jose/algorithms/#algorithm-security-notes","title":"Algorithm Security Notes","text":"Algorithm Status Notes <code>RSA1_5</code> \u26a0\ufe0f Legacy PKCS#1 v1.5 has known vulnerabilities, avoid if possible <code>none</code> \u274c Disabled Algorithm \"none\" is not supported for security <code>HS256</code> \u2713 Secure with sufficient key length (256+ bits) <code>RS256</code> \u2713 Secure with sufficient key size (2048+ bits) <code>ES256</code> \u2713 Recommended for new implementations <code>RSA-OAEP</code> \u2713 Recommended, uses SHA-256 <code>A128GCM</code> \u2713 Authenticated encryption <code>PBES2-*</code> \u2713 Secure with strong passwords"},{"location":"usage/jose/jwe/","title":"JWE","text":""},{"location":"usage/jose/jwe/#instance-jamjam","title":"Instance (jam.Jam)","text":""},{"location":"usage/jose/jwe/#encrypt-data","title":"Encrypt data","text":"<p>Method: <code>jam.jwe_encrypt</code></p> <p>Creates JWE Compact Serialization - encrypted data.</p> <p>Args:</p> <ul> <li><code>alg</code>: <code>str</code> - Key management algorithm. Available: <code>RSA-OAEP</code>,   <code>RSA-OAEP-256</code>, <code>RSA1_5</code>, <code>A128KW</code>, <code>A192KW</code>, <code>A256KW</code>, <code>ECDH-ES</code>,   <code>ECDH-ES+A128KW</code>, <code>ECDH-ES+A192KW</code>, <code>ECDH-ES+A256KW</code>, <code>A128GCMKW</code>,   <code>A256GCMKW</code>, <code>PBES2-HS256+A128KW</code>, <code>PBES2-HS384+A192KW</code>,   <code>PBES2-HS512+A256KW</code>.</li> <li><code>enc</code>: <code>str</code> - Content encryption algorithm. Available: <code>A128CBC-HS256</code>, <code>A192CBC-HS384</code>, <code>A256CBC-HS512</code>, <code>A128GCM</code>, <code>A256GCM</code>.</li> <li><code>payload</code>: <code>dict[str, Any] | str | bytes</code> - Data to encrypt.</li> <li><code>header</code>: <code>dict[str, Any] | None = None</code> - Additional header fields.</li> </ul> <p>Returns:</p> <p><code>str</code>: JWE in Compact Serialization format.</p> <pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\n\njwe_token = jam.jwe_encrypt(\n    alg=\"RSA-OAEP\",\n    enc=\"A128CBC-HS256\",\n    payload={\"secret\": \"data\"},\n    header={\"custom\": \"value\"}\n)\nprint(jwe_token)\n&gt;&gt;&gt; eyJhbGciOiJSU0ExLjI1NiIsImVuYyI6IkExMjhHQ1Mtc2hhMjU2In0...\n</code></pre>"},{"location":"usage/jose/jwe/#decrypt-data","title":"Decrypt data","text":"<p>Method: <code>jam.jwe_decrypt</code></p> <p>Decrypts JWE token.</p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - JWE token.</li> <li><code>alg</code>: <code>str</code> - Key management algorithm.</li> <li><code>enc</code>: <code>str</code> - Content encryption algorithm.</li> </ul> <p>Returns:</p> <p><code>bytes | str</code>: Decrypted data.</p> <p>Raises:</p> <ul> <li><code>JamJWEDecryptionError</code> - Decryption failed.</li> </ul> <pre><code>data = jam.jwe_decrypt(token=jwe_token)\nprint(data)\n&gt;&gt;&gt; b'{\"secret\": \"data\"}'\n</code></pre>"},{"location":"usage/jose/jwe/#standalone-module","title":"Standalone (module)","text":""},{"location":"usage/jose/jwe/#create-instance","title":"Create instance","text":"<p>Module: <code>jam.jose.JWE</code></p> <p>Args:</p> <ul> <li><code>alg</code>: <code>str</code> - Key management algorithm.</li> <li><code>enc</code>: <code>str</code> - Content encryption algorithm.</li> <li><code>key</code>: <code>str | bytes | KeyLike | JWK</code> - Key for encryption/decryption.</li> <li><code>password</code>: <code>bytes | None = None</code> - Password for PBES2 algorithms.</li> <li><code>serializer</code>: <code>BaseEncoder | type[BaseEncoder] = JsonEncoder</code> - Serializer.</li> <li><code>logger</code>: <code>BaseLogger</code> - Logger instance.</li> </ul> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"RSA-OAEP\",\n    enc=\"A128CBC-HS256\",\n    key=rsa_public_key\n)\n</code></pre>"},{"location":"usage/jose/jwe/#factory-function","title":"Factory function","text":"<pre><code>from jam.jose import create_jwe_instance\n\njwe = create_jwe_instance(\n    alg=\"A256KW\",\n    enc=\"A256GCM\",\n    key=\"your-256-bit-key-here!!\",\n)\n</code></pre>"},{"location":"usage/jose/jwe/#encrypt-data_1","title":"Encrypt data","text":"<p>Method: <code>jwe.encrypt</code></p> <p>Args:</p> <ul> <li><code>plaintext</code>: <code>dict[str, Any] | str | bytes</code> - Data to encrypt. If dict, will be JSON serialized.</li> <li><code>header</code>: <code>dict[str, Any] | None = None</code> - Additional JWE header fields.</li> </ul> <p>Returns:</p> <p><code>str</code>: JWE in Compact Serialization format.</p> <pre><code>jwe_token = jwe.encrypt(\n    plaintext={\"user_id\": 123, \"email\": \"user@example.com\"},\n    header={\"zip\": \"DEF\"}  # Compression header\n)\n</code></pre>"},{"location":"usage/jose/jwe/#decrypt-data_1","title":"Decrypt data","text":"<p>Method: <code>jwe.decrypt</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - JWE token.</li> </ul> <p>Returns:</p> <p><code>bytes</code>: Decrypted data (raw bytes).</p> <p>Raises:</p> <ul> <li><code>JamJWEDecryptionError</code> - Decryption failed.</li> <li><code>JamJWEInvalidFormatError</code> - Invalid token format.</li> </ul> <pre><code>data = jwe.decrypt(token=jwe_token)\nprint(data)\n&gt;&gt;&gt; b'{\"user_id\": 123, \"email\": \"user@example.com\"}'\n</code></pre>"},{"location":"usage/jose/jwe/#encryption-flow","title":"Encryption flow","text":"<p>JWE Compact Serialization format:</p> <pre><code>BASE64URL(header).BASE64URL(encrypted_key).BASE64URL(iv).BASE64URL(ciphertext).BASE64URL(tag)\n</code></pre>"},{"location":"usage/jose/jwe/#steps","title":"Steps","text":"<ol> <li>Generate CEK - Random Content Encryption Key is generated</li> <li>Key Management - CEK is encrypted using selected algorithm</li> <li>Content Encryption - Payload is encrypted using CEK</li> <li>Serialization - All components are base64url encoded</li> </ol>"},{"location":"usage/jose/jwe/#examples","title":"Examples","text":""},{"location":"usage/jose/jwe/#rsa-oaep-aes-cbc-hs","title":"RSA-OAEP + AES-CBC-HS","text":"<p>Classic asymmetric encryption combination.</p> <pre><code>from jam.jose import JWE\n\n# Encrypt with public key\njwe = JWE(\n    alg=\"RSA-OAEP\",\n    enc=\"A128CBC-HS256\",\n    key=rsa_public_key\n)\ntoken = jwe.encrypt(plaintext={\"data\": \"sensitive\"})\n\n# Decrypt with private key\njwe_dec = JWE(\n    alg=\"RSA-OAEP\",\n    enc=\"A128CBC-HS256\",\n    key=rsa_private_key\n)\ndata = jwe_dec.decrypt(token)\n</code></pre>"},{"location":"usage/jose/jwe/#aes-key-wrap-aes-gcm","title":"AES Key Wrap + AES-GCM","text":"<p>Symmetric encryption with shared key.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"A256KW\",\n    enc=\"A256GCM\",\n    key=\"your-32-byte-secret-key-here!!\"\n)\ntoken = jwe.encrypt(plaintext=\"Secret message\")\n\ndata = jwe.decrypt(token)\n</code></pre>"},{"location":"usage/jose/jwe/#ecdh-es","title":"ECDH-ES","text":"<p>Ephemeral-static ECDH for perfect forward secrecy.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"ECDH-ES\",\n    enc=\"A128CBC-HS256\",\n    key=ec_public_key\n)\ntoken = jwe.encrypt(plaintext={\"session\": \"data\"})\n\n# Decrypt with recipient's private key\njwe_dec = JWE(\n    alg=\"ECDH-ES\",\n    enc=\"A128CBC-HS256\",\n    key=ec_private_key\n)\ndata = jwe_dec.decrypt(token)\n</code></pre>"},{"location":"usage/jose/jwe/#pbes2-password-based","title":"PBES2 (Password-Based)","text":"<p>Password-based encryption.</p> <pre><code>from jam.jose import JWE\n\njwe = JWE(\n    alg=\"PBES2-HS512+A256KW\",\n    enc=\"A256CBC-HS512\",\n    key=None,\n    password=b\"user_password\"\n)\ntoken = jwe.encrypt(plaintext={\"data\": \"password_encrypted\"})\n\n# Decrypt with same password\njwe_dec = JWE(\n    alg=\"PBES2-HS512+A256KW\",\n    enc=\"A256CBC-HS512\",\n    key=None,\n    password=b\"user_password\"\n)\ndata = jwe_dec.decrypt(token)\n</code></pre>"},{"location":"usage/jose/jwk/","title":"JWK","text":""},{"location":"usage/jose/jwk/#typeddicts","title":"TypedDicts","text":""},{"location":"usage/jose/jwk/#jwkcommon","title":"JWKCommon","text":"<p>Common JWK parameters shared across all key types.</p> <pre><code>from jam.jose import JWKCommon\n\nkey: JWKCommon = {\n    \"kty\": \"RSA\",           # Required. Key type: RSA, EC, oct\n    \"use\": \"sig\",           # Public key use: \"sig\" or \"enc\"\n    \"key_ops\": [\"sign\"],    # Intended key operations\n    \"alg\": \"RS256\",         # Intended algorithm\n    \"kid\": \"key-id\",        # Unique key identifier\n    \"x5u\": \"https://...\",   # X.509 URL\n    \"x5c\": \"MIID...\",       # X.509 certificate chain (base64)\n    \"x5t\": \"abc...\",        # X.509 SHA-1 thumbprint\n    \"x5t_S256\": \"xyz...\",   # X.509 SHA-256 thumbprint\n}\n</code></pre>"},{"location":"usage/jose/jwk/#jwkrsa","title":"JWKRSA","text":"<p>RSA key parameters (extends <code>JWKCommon</code>).</p> <pre><code>from jam.jose import JWKRSA\n\n# Public key\nrsa_pub: JWKRSA = {\n    \"kty\": \"RSA\",\n    \"n\": \"0vx7agoebGcQSuu...\",   # Modulus (base64url)\n    \"e\": \"AQAB\",                    # Exponent (base64url)\n}\n\n# Private key (requires all CRT parameters per RFC 7518)\nrsa_priv: JWKRSA = {\n    \"kty\": \"RSA\",\n    \"n\": \"...\", \"e\": \"AQAB\",\n    \"d\": \"...\",    # Private exponent\n    \"p\": \"...\",    # First prime factor\n    \"q\": \"...\",    # Second prime factor\n    \"dp\": \"...\",   # d mod (p-1)\n    \"dq\": \"...\",   # d mod (q-1)\n    \"qi\": \"...\",   # q^(-1) mod p\n}\n</code></pre> <p>RSA private key validation</p> <p>When <code>d</code> is present, all CRT parameters (<code>p</code>, <code>q</code>, <code>dp</code>, <code>dq</code>, <code>qi</code>) are required. Missing any of them raises <code>JamJWKValidationError</code> per RFC 7518 Section 6.3.2.</p>"},{"location":"usage/jose/jwk/#jwkec","title":"JWKEC","text":"<p>Elliptic curve key parameters (extends <code>JWKCommon</code>).</p> <pre><code>from jam.jose import JWKEC\n\nec_key: JWKEC = {\n    \"kty\": \"EC\",\n    \"crv\": \"P-256\",    # Supported: P-256, P-384, P-521\n    \"x\": \"f83OJ3D...\",  # X coordinate (base64url)\n    \"y\": \"x_FEzRu...\",  # Y coordinate (base64url)\n    \"d\": \"...\",         # Private key (optional, base64url)\n}\n</code></pre>"},{"location":"usage/jose/jwk/#jwkoct","title":"JWKOct","text":"<p>Symmetric (octet sequence) key parameters (extends <code>JWKCommon</code>).</p> <pre><code>from jam.jose import JWKOct\n\noct_key: JWKOct = {\n    \"kty\": \"oct\",\n    \"k\": \"c2VjcmV0LWtleS0zMi1ieXRlcy1sb25n\",  # Key value (base64url)\n}\n</code></pre>"},{"location":"usage/jose/jwk/#standalone-module","title":"Standalone (module)","text":""},{"location":"usage/jose/jwk/#jwk-json-web-key","title":"JWK - JSON Web Key","text":"<p>JWK represents a cryptographic key in JSON format.</p>"},{"location":"usage/jose/jwk/#create-from-dict","title":"Create from dict","text":"<p>Method: <code>JWK.from_dict</code></p> <p>Creates JWK from dictionary.</p> <p>Args:</p> <ul> <li><code>data</code>: <code>dict[str, Any]</code> - JWK dict. Required field <code>kty</code>.</li> </ul> <p>Returns:</p> <p><code>JWK</code>: Validated JWK instance.</p> <p>Raises:</p> <ul> <li><code>JamJWKValidationError</code> - If JWK is invalid.</li> </ul> <pre><code>from jam.jose import JWK\n\n# Symmetric key (oct)\noct_key = JWK.from_dict({\n    \"kty\": \"oct\",\n    \"k\": \"GawgguFyGrWKav7AX4VKUg\",  # base64url encoded key\n    \"kid\": \"my-signing-key\"\n})\n\n# RSA key\nrsa_key = JWK.from_dict({\n    \"kty\": \"RSA\",\n    \"n\": \"0vx7agoebGcQSuuPiLJXZptN9nndrQmbXEps2aiAFbW...\",\n    \"e\": \"AQAB\",\n    \"kid\": \"rsa-key-1\"\n})\n\n# EC key\nec_key = JWK.from_dict({\n    \"kty\": \"EC\",\n    \"crv\": \"P-256\",\n    \"x\": \"f83OJ3D2xF1Bg8vub9tLe1gHMzV76e8Tus9uPHvRVEU\",\n    \"y\": \"x_FEzRu9m36HLN_tue659LNpXW6pCyStikYjKIWI5a0\",\n    \"kid\": \"ec-key-1\"\n})\n</code></pre>"},{"location":"usage/jose/jwk/#validate","title":"Validate","text":"<p>Static method: <code>JWK.validate</code></p> <p>Validates and creates JWK from dict.</p> <p>Args:</p> <ul> <li><code>data</code>: <code>dict[str, Any]</code> - JWK dict.</li> </ul> <p>Returns:</p> <p><code>JWK</code>: Validated instance.</p> <pre><code>jwk = JWK.validate({\"kty\": \"oct\", \"k\": \"base64key\"})\n</code></pre>"},{"location":"usage/jose/jwk/#properties","title":"Properties","text":"<p>JWK has the following properties:</p> <ul> <li><code>kty</code>: <code>str</code> - Key Type. Possible values: <code>RSA</code>, <code>EC</code>, <code>oct</code>.</li> <li><code>alg</code>: <code>str | None</code> - Algorithm. Specifies algorithm to use with the key.</li> <li><code>kid</code>: <code>str | None</code> - Key ID. Unique key identifier.</li> </ul> <pre><code>jwk = JWK.from_dict({\"kty\": \"oct\", \"k\": \"key\", \"kid\": \"key1\"})\nprint(jwk.kty)  # \"oct\"\nprint(jwk.alg)  # None\nprint(jwk.kid)  # \"key1\"\n</code></pre> <p>All JWK parameters are accessible via <code>to_dict()</code>:</p> Parameter Type Description <code>kty</code> <code>str</code> Key Type (RSA, EC, oct) <code>use</code> <code>str</code> Public key use (<code>sig</code>, <code>enc</code>) <code>key_ops</code> <code>list[str]</code> Intended key operations <code>alg</code> <code>str</code> Intended algorithm <code>kid</code> <code>str</code> Key ID <code>x5u</code> <code>str</code> X.509 URL <code>x5c</code> <code>str</code> X.509 certificate chain <code>x5t</code> <code>str</code> X.509 SHA-1 thumbprint <code>x5t#S256</code> <code>str</code> X.509 SHA-256 thumbprint"},{"location":"usage/jose/jwk/#sign-data","title":"Sign data","text":"<p>Method: <code>jwk.sign</code></p> <p>Signs data using JWK.</p> <p>Args:</p> <ul> <li><code>data</code>: <code>bytes</code> - Data to sign.</li> <li><code>alg</code>: <code>str | None = None</code> - Signing algorithm. If <code>None</code>, uses <code>alg</code> from JWK or default for kty.</li> </ul> <p>Returns:</p> <p><code>str</code>: JWS in Compact Serialization format.</p> <p>Raises:</p> <ul> <li><code>JamJWSVerificationError</code> - If signing failed.</li> </ul> <pre><code>jwk = JWK.from_dict({\"kty\": \"oct\", \"k\": \"your-secret-key-32-bytes-long\"})\ntoken = jwk.sign(b\"data to sign\", alg=\"HS256\")\n</code></pre>"},{"location":"usage/jose/jwk/#verify-token","title":"Verify token","text":"<p>Method: <code>jwk.verify</code></p> <p>Verifies JWS token using JWK.</p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - JWS token.</li> <li><code>alg</code>: <code>str | None = None</code> - Algorithm. If <code>None</code>, uses from token header.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Parsed data with keys <code>header</code>, <code>payload</code>.</p> <p>Raises:</p> <ul> <li><code>JamJWSVerificationError</code> - If verification failed.</li> </ul> <pre><code>result = jwk.verify(token)\nprint(result[\"payload\"])\n&gt;&gt;&gt; b'data to sign'\n</code></pre>"},{"location":"usage/jose/jwk/#convert-to-dict","title":"Convert to dict","text":"<p>Method: <code>jwk.to_dict</code></p> <p>Converts JWK back to dictionary.</p> <p>Returns:</p> <p><code>dict[str, Any]</code>: JWK dict.</p> <pre><code>jwk_dict = jwk.to_dict()\nprint(jwk_dict)\n&gt;&gt;&gt; {'kty': 'oct', 'k': 'your-secret-key-32-bytes-long', 'kid': 'key1'}\n</code></pre>"},{"location":"usage/jose/jwk/#jwkset-set-of-keys","title":"JWKSet - Set of Keys","text":"<p>JWKSet represents a set of JWK keys.</p>"},{"location":"usage/jose/jwk/#create-from-dict_1","title":"Create from dict","text":"<p>Method: <code>JWKSet.from_dict</code></p> <p>Creates JWKSet from dictionary.</p> <p>Args:</p> <ul> <li><code>data</code>: <code>dict[str, Any]</code> - JWKSet dict with <code>keys</code> key.</li> </ul> <p>Returns:</p> <p><code>JWKSet</code>: Validated instance.</p> <p>Raises:</p> <ul> <li><code>JamJWKValidationError</code> - If data is invalid.</li> </ul> <pre><code>from jam.jose import JWKSet\n\njwks = JWKSet.from_dict({\n    \"keys\": [\n        {\"kty\": \"oct\", \"k\": \"key1\", \"kid\": \"1\"},\n        {\"kty\": \"oct\", \"k\": \"key2\", \"kid\": \"2\"},\n        {\"kty\": \"RSA\", \"n\": \"...\", \"e\": \"AQAB\", \"kid\": \"rsa-key\"}\n    ]\n})\n</code></pre>"},{"location":"usage/jose/jwk/#get-by-kid","title":"Get by kid","text":"<p>Method: <code>jwks.get_by_kid</code></p> <p>Gets JWK by Key ID.</p> <p>Args:</p> <ul> <li><code>kid</code>: <code>str</code> - Key ID.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any] | None</code>: JWK dict or <code>None</code> if not found.</p> <pre><code>key = jwks.get_by_kid(\"1\")\nif key:\n    jwk = JWK.from_dict(key)\n</code></pre>"},{"location":"usage/jose/jwk/#get-by-kty","title":"Get by kty","text":"<p>Method: <code>jwks.get_by_kty</code></p> <p>Gets all JWKs with specified Key Type.</p> <p>Args:</p> <ul> <li><code>kty</code>: <code>str</code> - Key Type (<code>RSA</code>, <code>EC</code>, <code>oct</code>).</li> </ul> <p>Returns:</p> <p><code>list[dict[str, Any]]</code>: List of JWK dicts.</p> <pre><code>symmetric_keys = jwks.get_by_kty(\"oct\")\nrsa_keys = jwks.get_by_kty(\"RSA\")\n</code></pre>"},{"location":"usage/jose/jwk/#filter","title":"Filter","text":"<p>Method: <code>jwks.filter</code></p> <p>Filters JWKs by criteria.</p> <p>Args:</p> <ul> <li><code>**criteria</code>: Filter criteria (<code>kty</code>, <code>use</code>, <code>alg</code>, <code>key_ops</code>, <code>kid</code>).</li> </ul> <p>Returns:</p> <p><code>list[dict[str, Any]]</code>: List of matching JWK dicts.</p> <pre><code># Find keys by multiple criteria\nkeys = jwks.filter(kty=\"RSA\", use=\"sig\")\n</code></pre>"},{"location":"usage/jose/jwk/#convert-to-dict_1","title":"Convert to dict","text":"<p>Method: <code>jwks.to_dict</code></p> <p>Converts JWKSet to dictionary.</p> <p>Returns:</p> <p><code>dict[str, Any]</code>: JWKSet dict.</p> <pre><code>jwks_dict = jwks.to_dict()\n</code></pre>"},{"location":"usage/jose/jwk/#jwk-key-types","title":"JWK Key Types","text":""},{"location":"usage/jose/jwk/#oct-symmetric-key","title":"oct - Symmetric Key","text":"<p>Symmetric key for HMAC algorithms.</p> <pre><code>from jam.jose import JWK\n\n# Create\njwk = JWK.from_dict({\n    \"kty\": \"oct\",\n    \"k\": \"c2VjcmV0LWtleS0zMi1ieXRlcy1sb25n\",  # base64url encoded\n    \"kid\": \"hmac-key\"\n})\n\n# Use for HMAC\ntoken = jwk.sign(b\"data\", alg=\"HS256\")\nresult = jwk.verify(token)\n</code></pre>"},{"location":"usage/jose/jwk/#rsa","title":"RSA","text":"<p>Asymmetric RSA key for RSA and RSA-PSS algorithms.</p> <pre><code>from jam.jose import JWK\n\nrsa_jwk = JWK.from_dict({\n    \"kty\": \"RSA\",\n    \"n\": \"...\",\n    \"e\": \"AQAB\",\n    \"d\": \"...\",  # private key only if needed\n    \"p\": \"...\",\n    \"q\": \"...\",\n    \"dp\": \"...\",\n    \"dq\": \"...\",\n    \"qi\": \"...\",\n    \"kid\": \"rsa-key\"\n})\n\n# Sign\ntoken = rsa_jwk.sign(b\"data\", alg=\"RS256\")\nresult = rsa_jwk.verify(token)\n</code></pre>"},{"location":"usage/jose/jwk/#ec-elliptic-curve","title":"EC - Elliptic Curve","text":"<p>Elliptic curve key for ECDSA algorithms.</p> <pre><code>from jam.jose import JWK\n\nec_jwk = JWK.from_dict({\n    \"kty\": \"EC\",\n    \"crv\": \"P-256\",  # or P-384, P-521\n    \"x\": \"...\",\n    \"y\": \"...\",\n    \"d\": \"...\",  # private key only if needed\n    \"kid\": \"ec-key\"\n})\n\n# Sign\ntoken = ec_jwk.sign(b\"data\", alg=\"ES256\")\nresult = ec_jwk.verify(token)\n</code></pre>"},{"location":"usage/jose/jwk/#key-type-specific-classes","title":"Key type-specific classes","text":"<p>Typed key classes are exported for static type checking:</p> <pre><code>from jam.jose import JWKRSA, JWKEC, JWKOct\n\n# These are TypedDicts for type annotations\ndef process_rsa_key(key: JWKRSA) -&gt; None:\n    ...\n\ndef process_ec_key(key: JWKEC) -&gt; None:\n    ...\n\ndef process_symmetric_key(key: JWKOct) -&gt; None:\n    ...\n</code></pre>"},{"location":"usage/jose/jws/","title":"JWS","text":""},{"location":"usage/jose/jws/#use-in-instance","title":"Use in instance","text":""},{"location":"usage/jose/jws/#config","title":"Config","text":"<ul> <li><code>alg</code>: <code>str</code> - Signing algorithm. Available: <code>HS256</code>, <code>HS384</code>, <code>HS512</code>, <code>RS256</code>, <code>RS384</code>, <code>RS512</code>, <code>ES256</code>, <code>ES384</code>, <code>ES512</code>, <code>PS256</code>, <code>PS384</code>, <code>PS512</code>.</li> <li><code>key</code>: <code>str</code> - Secret key for signing/verify.</li> <li><code>password</code>: <code>str | None</code> - Password for key derivation.</li> </ul> <pre><code>[jam.jose.jws]\nalg = \"\"\nkey = \"$JWS_SECRET_KEY\"\npassword = \"$JWS_PASSWORD\"\n</code></pre>"},{"location":"usage/jose/jws/#sign-data","title":"Sign data","text":"<p>Method: <code>jam.jws_sign</code></p> <p>Creates JWS Compact Serialization - digital signature of data.</p> <p>Args:</p> <ul> <li><code>alg</code>: <code>str</code> - Signing algorithm. Available: <code>HS256</code>, <code>HS384</code>, <code>HS512</code>, <code>RS256</code>, <code>RS384</code>, <code>RS512</code>, <code>ES256</code>, <code>ES384</code>, <code>ES512</code>, <code>PS256</code>, <code>PS384</code>, <code>PS512</code>.</li> <li><code>header</code>: <code>dict[str, Any] | None = None</code> - Additional header fields.</li> <li><code>data</code>: <code>dict[str, Any] | str | bytes</code> - Data to sign.</li> </ul> <p>Returns:</p> <p><code>str</code>: JWS in Compact Serialization format.</p> <pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\n\njws_token = jam.jws_sign(\n    alg=\"RS256\",\n    header={\"custom\": \"header_value\"},\n    data={\"message\": \"Hello, World!\"}\n)\nprint(jws_token)\n&gt;&gt;&gt; eyJhbGciOiJSUzI1NiJ9.eyJtc2QiOiJIZWxsbywgV29ybGQhIn0.ABC123...\n</code></pre>"},{"location":"usage/jose/jws/#verify-token","title":"Verify token","text":"<p>Method: <code>jam.jws_verify</code></p> <p>Verifies JWS token and returns data.</p> <p>Args:</p> <ul> <li><code>alg</code>: <code>str</code> - Algorithm for signature verification.</li> <li><code>token</code>: <code>str</code> - JWS token.</li> <li><code>validate</code>: <code>bool = True</code> - Validate signature.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Decoded data with keys <code>header</code>, <code>payload</code>, <code>signature</code>.</p> <p>Raises:</p> <ul> <li><code>JamJWSVerificationError</code> - Invalid signature.</li> </ul> <pre><code>data = jam.jws_verify(\n    alg=\"RS256\",\n    token=jws_token,\n    validate=True\n)\nprint(data)\n&gt;&gt;&gt; {\n    'header': {'alg': 'RS256'},\n    'payload': b'{\"msg\":\"Hello, World!\"}',\n    'signature': b'...'\n}\n</code></pre>"},{"location":"usage/jose/jws/#standalone-module","title":"Standalone (module)","text":""},{"location":"usage/jose/jws/#create-instance","title":"Create instance","text":"<p>Module: <code>jam.jose.JWS</code></p> <p>Args:</p> <ul> <li><code>alg</code>: <code>str</code> - Signing algorithm.</li> <li><code>key</code>: <code>str | bytes | KeyLike | JWK</code> - Key for signing.</li> <li><code>password</code>: <code>bytes | None = None</code> - Password for encrypted keys.</li> <li><code>logger</code>: <code>BaseLogger</code> - Logger instance.</li> </ul> <pre><code>from jam.jose import JWS\n\njws = JWS(\n    alg=\"ES256\",\n    key=\"-----BEGIN EC PRIVATE KEY-----...\"\n)\n</code></pre>"},{"location":"usage/jose/jws/#factory-function","title":"Factory function","text":"<pre><code>from jam.jose import create_jws_instance\n\njws = create_jws_instance(\n    alg=\"HS256\",\n    key=\"your-secret-key\",\n)\n</code></pre>"},{"location":"usage/jose/jws/#sign-data_1","title":"Sign data","text":"<p>Method: <code>jws.sign</code></p> <p>Args:</p> <ul> <li><code>header</code>: <code>dict[str, Any]</code> - JWS header.</li> <li><code>data</code>: <code>dict[str, Any] | str | bytes</code> - Data to sign.</li> </ul> <p>Returns:</p> <p><code>str</code>: JWS in Compact Serialization format.</p> <pre><code>token = jws.sign(\n    header={\"typ\": \"JWT\"},\n    data={\"user_id\": 123}\n)\nprint(token)\n&gt;&gt;&gt; eyJhbGciOiJFUzI1NiJ9.eyJ1c2VyX2lkIjoxMjN9.AMgVRaO2...\n</code></pre>"},{"location":"usage/jose/jws/#verify-token_1","title":"Verify token","text":"<p>Method: <code>jws.verify</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - JWS token.</li> <li><code>validate</code>: <code>bool = True</code> - Validate signature.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Decoded data with keys <code>header</code>, <code>payload</code>, <code>signature</code>.</p> <p>Raises:</p> <ul> <li><code>JamJWSVerificationError</code> - Invalid signature.</li> </ul> <pre><code>result = jws.verify(token, validate=True)\nprint(result[\"header\"])\n&gt;&gt;&gt; {'alg': 'ES256', 'typ': 'JWT'}\nprint(result[\"payload\"])\n&gt;&gt;&gt; b'{\"user_id\":123}'\n</code></pre>"},{"location":"usage/jose/jws/#serialize-compact","title":"Serialize compact","text":"<p>Method: <code>jws.serialize_compact</code></p> <p>Low-level operation for creating JWS Compact Serialization.</p> <p>Args:</p> <ul> <li><code>protected</code>: <code>dict[str, Any]</code> - Protected header.</li> <li><code>payload</code>: <code>str | bytes</code> - Payload to sign.</li> </ul> <p>Returns:</p> <p><code>str</code>: JWS string.</p> <pre><code>jws_token = jws.serialize_compact(\n    protected={\"alg\": \"HS256\", \"custom\": \"value\"},\n    payload=\"Hello\"\n)\n</code></pre>"},{"location":"usage/jose/jws/#deserialize-compact","title":"Deserialize compact","text":"<p>Method: <code>jws.deserialize_compact</code></p> <p>Low-level operation for parsing JWS Compact Serialization.</p> <p>Args:</p> <ul> <li><code>s</code>: <code>str</code> - JWS string.</li> <li><code>validate</code>: <code>bool = True</code> - Validate signature.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Parsed data.</p> <p>Raises:</p> <ul> <li><code>JamJWSVerificationError</code> - Invalid format or signature.</li> </ul> <pre><code>data = jws.deserialize_compact(jws_token, validate=True)\n</code></pre>"},{"location":"usage/jose/jws/#critical-header-validation","title":"Critical header validation","text":"<p>JWS validates the <code>crit</code> (critical) header per RFC 7515. If a header name is listed in <code>crit</code>, it must be a registered JOSE header name (<code>alg</code>, <code>typ</code>, <code>kid</code>, <code>x5u</code>, <code>x5t</code>, <code>cty</code>, <code>crit</code>). Unknown critical headers cause verification to fail.</p> <pre><code># This will fail - \"unknown\" is not a registered header\njws.deserialize_compact(\n    \"eyJhbGciOiJIUzI1NiIsImNyaXQiOlsidW5rbm93biJ9...\",\n    validate=True,\n)\n# Raises JamJWSVerificationError: unknown_critical_header\n</code></pre>"},{"location":"usage/jose/jws/#key-auto-loading","title":"Key auto-loading","text":"<p>When a string is passed as <code>key</code>, JWS attempts to load it as a file path first. If the file exists, its contents are used as the key. Otherwise, the string is used directly as the key material.</p> <pre><code># Loads key from file if path exists\njws = JWS(alg=\"RS256\", key=\"/path/to/private-key.pem\")\n\n# Uses string directly if not a valid file path\njws = JWS(alg=\"HS256\", key=\"my-secret-string-key\")\n</code></pre>"},{"location":"usage/jose/jws/#examples","title":"Examples","text":""},{"location":"usage/jose/jws/#hmac-hs256384512","title":"HMAC (HS256/384/512)","text":"<p>Symmetric algorithm with shared secret key.</p> <pre><code>from jam.jose import JWS\n\njws = JWS(alg=\"HS256\", key=\"your-secret-key-min-32-chars\")\ntoken = jws.sign(header={}, data={\"user\": \"admin\"})\nresult = jws.verify(token)\n</code></pre>"},{"location":"usage/jose/jws/#rsa-rs256384512","title":"RSA (RS256/384/512)","text":"<p>Asymmetric algorithm with RSA key pair.</p> <pre><code>from jam.jose import JWS\n\n# Sign with private key\njws = JWS(alg=\"RS256\", key=private_key)\ntoken = jws.sign(header={}, data={\"data\": \"value\"})\n\n# Verify with public key\njws_verify = JWS(alg=\"RS256\", key=public_key)\nresult = jws_verify.verify(token)\n</code></pre>"},{"location":"usage/jose/jws/#ecdsa-es256384512","title":"ECDSA (ES256/384/512)","text":"<p>Elliptic curve for shorter signatures.</p> <pre><code>from jam.jose import JWS\n\njws = JWS(alg=\"ES256\", key=ec_private_key)\ntoken = jws.sign(header={}, data={\"message\": \"signed\"})\nresult = jws.verify(token)\n</code></pre>"},{"location":"usage/jose/jws/#rsa-pss-ps256384512","title":"RSA-PSS (PS256/384/512)","text":"<p>RSA algorithm with Probabilistic Signature Scheme.</p> <pre><code>from jam.jose import JWS\n\njws = JWS(alg=\"PS256\", key=rsa_private_key)\ntoken = jws.sign(header={}, data={\"data\": \"value\"})\nresult = jws.verify(token)\n</code></pre>"},{"location":"usage/jose/jwt/","title":"JWT","text":"<p>Tip</p> <p>The <code>jam.jwt</code> module is deprecated, but the documentation is still available at /usage/jwt</p>"},{"location":"usage/jose/jwt/#token-modes","title":"Token modes","text":"<p>JWT supports three operating modes:</p>"},{"location":"usage/jose/jwt/#jws-only-signed-tokens","title":"JWS-only (signed tokens)","text":"<p>Standard signed JWT. Payload is readable by anyone but cannot be tampered with.</p> <pre><code>jwt = JWT(alg=\"RS256\", secret_key=private_key)\ntoken = jwt.encode(sub=\"user\", exp=3600)\ndata = jwt.decode(token)\n</code></pre>"},{"location":"usage/jose/jwt/#jwe-only-encrypted-tokens","title":"JWE-only (encrypted tokens)","text":"<p>Encrypted-only token. Payload is confidential but not integrity-protected by signature.</p> <pre><code>jwt = JWT(enc=\"A256GCM\", secret_key=encryption_key)\ntoken = jwt.encrypt({\"secret\": \"data\"})\ndata = jwt.decrypt(token)\n</code></pre>"},{"location":"usage/jose/jwt/#jwsjwe-sign-then-encrypt-rfc-7519","title":"JWS+JWE (sign-then-encrypt, RFC 7519)","text":"<p>Hybrid mode: first signs the payload, then encrypts the signed token. Follows RFC 7519 nested JWT pattern.</p> <pre><code>jwt = JWT(alg=\"RS256\", enc=\"A256GCM\", secret_key=key)\ntoken = jwt.encrypt({\"data\": \"sensitive\"})  # Signs then encrypts\ndata = jwt.decrypt(token)  # Decrypts then verifies signature\n</code></pre>"},{"location":"usage/jose/jwt/#automatic-jwe-algorithm-selection","title":"Automatic JWE algorithm selection","text":"<p>When using JWS+JWE mode with a symmetric key, JWT automatically selects the JWE key management algorithm based on key type:</p> Key type Auto-selected JWE <code>alg</code> RSA <code>RSA-OAEP</code> EC <code>ECDH-ES</code> Symmetric (&gt;=32 bytes) <code>A256KW</code> Symmetric (&lt;32 bytes) <code>A128KW</code> <p>For symmetric keys, an encryption key is derived from the signing key using HKDF-SHA256 with salt <code>jwe-encryption</code> and info <code>encryption-key</code>.</p>"},{"location":"usage/jose/jwt/#use-in-instance","title":"Use in instance","text":""},{"location":"usage/jose/jwt/#config","title":"Config","text":"<pre><code>[jam.jose.jwt]\nalg = \"RS256\"\nsecret = \"$RSA_PRIVATE_KEY\"\n\n[jam.jose.jwt.list]\ntype = \"black\"\nbackend = \"redis\"\nredis_uri = \"redis://localhost:6379\"\n</code></pre> <p>Args:</p> <ul> <li><code>alg</code>: <code>str</code> - JWT signing algorithm. Supports: <code>HS256</code>, <code>HS384</code>, <code>HS512</code>, <code>RS256</code>, <code>RS384</code>, <code>RS512</code>, <code>ES256</code>, <code>ES384</code>, <code>ES512</code>, <code>PS256</code>, <code>PS384</code>, <code>PS512</code>.</li> <li><code>secret</code>: <code>str</code> - Secret key for token signing. Default reads from <code>JAM_JWT_SECRET_KEY</code> environment variable.</li> <li><code>list</code>: <code>dict[str, Any] | None</code> - Token list config. See: Lists.</li> </ul>"},{"location":"usage/jose/jwt/#usage","title":"Usage","text":"<pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\n</code></pre>"},{"location":"usage/jose/jwt/#encode-token","title":"Encode token","text":"<p>Method: <code>jam.jwt_encode</code></p> <p>Args:</p> <ul> <li><code>iss</code>: <code>str | None = None</code> - Token issuer.</li> <li><code>sub</code>: <code>str | None = None</code> - Token subject.</li> <li><code>aud</code>: <code>str | None = None</code> - Token audience.</li> <li><code>exp</code>: <code>int | None = None</code> - Token lifetime in seconds.</li> <li><code>nbf</code>: <code>int | None = None</code> - Token not-before time in seconds.</li> <li><code>jti</code>: <code>str | None = None</code> - Unique token ID. If <code>None</code>, auto-generated.</li> <li><code>header</code>: <code>dict[str, Any] | None = None</code> - Additional header fields.</li> <li><code>payload</code>: <code>dict[str, Any] | None = None</code> - Custom payload data.</li> </ul> <p>Returns:</p> <p><code>str</code>: Encoded JWT.</p> <pre><code>token = jam.jwt_encode(\n    iss=\"YourService\",\n    sub=\"user@mail.com\",\n    exp=3600,\n    payload={\"role\": \"admin\"}\n)\nprint(token)\n&gt;&gt;&gt; eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ1c2VyQ...\n</code></pre>"},{"location":"usage/jose/jwt/#decode-token","title":"Decode token","text":"<p>Method: <code>jam.jwt_decode</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - JWT token.</li> <li><code>validate_claims</code>: <code>bool = True</code> - Validate exp and nbf claims.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Decoded payload with header.</p> <p>Raises:</p> <ul> <li><code>JamJWTExpired</code> - Token has expired.</li> <li><code>JamJWTNotYetValid</code> - Token is not yet valid (nbf).</li> <li><code>JamJWSVerificationError</code> - Invalid signature.</li> </ul> <pre><code>data = jam.jwt_decode(\n    token=token,\n    validate_claims=True\n)\nprint(data)\n&gt;&gt;&gt; {\n    'header': {'alg': 'RS256', 'typ': 'JWT'},\n    'payload': {\n        'iss': 'YourService',\n        'sub': 'user@mail.com',\n        'exp': 1772132706,\n        'iat': 1772129106,\n        'jti': '0c2c38d2-5dcb-4294-bb2d-0820f6ff787d',\n        'role': 'admin'\n    }\n}\n</code></pre>"},{"location":"usage/jose/jwt/#encrypt-token","title":"Encrypt token","text":"<p>Method: <code>jam.jwt_encrypt</code></p> <p>Creates encrypted JWT (JWS+JWE or JWE only).</p> <p>Args:</p> <ul> <li><code>payload</code>: <code>dict[str, Any] | str</code> - Data to encrypt.</li> <li><code>header</code>: <code>dict[str, Any] | None = None</code> - Additional JWE header fields.</li> </ul> <p>Returns:</p> <p><code>str</code>: Encrypted JWT.</p> <pre><code>encrypted = jam.jwt_encrypt(\n    payload={\"user_id\": 123, \"role\": \"admin\"},\n    header={\"custom\": \"value\"}\n)\nprint(encrypted)\n&gt;&gt;&gt; eyJhbGciOiJSU0ExLjI1NiIsImVuYyI6IkExMjhHQ1Mtc2hhMjU2In0...\n</code></pre>"},{"location":"usage/jose/jwt/#decrypt-token","title":"Decrypt token","text":"<p>Method: <code>jam.jwt_decrypt</code></p> <p>Decrypts encrypted JWT.</p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Encrypted JWT token.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Decrypted payload.</p> <p>Raises:</p> <ul> <li><code>JamJWEDecryptionError</code> - Decryption failed.</li> </ul> <pre><code>data = jam.jwt_decrypt(token=encrypted)\nprint(data)\n&gt;&gt;&gt; {'user_id': 123, 'role': 'admin'}\n</code></pre>"},{"location":"usage/jose/jwt/#use-out-of-instance","title":"Use out of instance","text":""},{"location":"usage/jose/jwt/#built","title":"Built","text":"<p>Module: <code>jam.jose.JWT</code></p> <p>Args:</p> <ul> <li><code>alg</code>: <code>str | None</code> - Signing algorithm (JWS).</li> <li><code>enc</code>: <code>str | None</code> - Content encryption algorithm (JWE). Creates encrypted JWT if specified.</li> <li><code>secret_key</code>: <code>str | bytes | KeyLike | JWK | None</code> - Key for signing/encryption.</li> <li><code>password</code>: <code>str | bytes | None</code> - Password for encrypted private keys.</li> <li><code>list</code>: <code>dict[str, Any] | None</code> - Token list config.</li> <li><code>serializer</code>: <code>BaseEncoder | type[BaseEncoder] = JsonEncoder</code> - JSON encoder/decoder.</li> <li><code>logger</code>: <code>BaseLogger</code> - Logger instance.</li> <li><code>jws</code>: <code>JWS | None</code> - Pre-built JWS instance. If provided, alg is ignored.</li> <li><code>jwe</code>: <code>JWE | None</code> - Pre-built JWE instance. If provided, enc and secret_key are ignored.</li> </ul> <pre><code>import os\nfrom jam.jose import JWT\n\njwt = JWT(\n    alg=\"RS256\",\n    secret_key=os.getenv(\"RSA_PRIVATE_KEY\")\n)\n</code></pre>"},{"location":"usage/jose/jwt/#pre-built-jwsjwe-instances","title":"Pre-built JWS/JWE instances","text":"<p>For full control over the underlying JWS and JWE instances, you can pass pre-built instances to the JWT constructor:</p> <pre><code>from jam.jose import JWS, JWE, JWT\n\n# Custom JWS with specific settings\njws = JWS(alg=\"PS256\", key=private_key)\n\n# Custom JWE with specific settings\njwe = JWE(alg=\"ECDH-ES+A256KW\", enc=\"A256GCM\", key=ec_public_key)\n\n# JWT with pre-built instances\njwt = JWT(jws=jws, jwe=jwe)\n</code></pre> <p>When using pre-built instances:</p> <ul> <li>If <code>jws</code> is provided, <code>alg</code> must be <code>None</code> (otherwise raises   <code>JamConfigurationError</code>)</li> <li>If <code>jwe</code> is provided, <code>enc</code> must be <code>None</code> (otherwise raises   <code>JamConfigurationError</code>)</li> <li>At least one of <code>alg</code>, <code>enc</code>, <code>jws</code>, or <code>jwe</code> must be provided</li> </ul>"},{"location":"usage/jose/jwt/#factory-functions","title":"Factory functions","text":"<pre><code>from jam.jose import create_jwt_instance, create_instance\n\n# create_instance is an alias for create_jwt_instance\njwt = create_jwt_instance(\n    alg=\"RS256\",\n    secret=private_key,  # or secret_key=private_key\n    password=None,\n)\n</code></pre>"},{"location":"usage/jose/jwt/#encode-token_1","title":"Encode token","text":"<p>Method: <code>jwt.encode</code></p> <p>Args:</p> <ul> <li><code>iss</code>: <code>str | None = None</code> - Issuer.</li> <li><code>sub</code>: <code>str | None = None</code> - Subject.</li> <li><code>aud</code>: <code>str | None = None</code> - Audience.</li> <li><code>exp</code>: <code>int | None = None</code> - Lifetime in seconds.</li> <li><code>nbf</code>: <code>int | None = None</code> - Not-before in seconds from now.</li> <li><code>jti</code>: <code>str | None = None</code> - JWT ID.</li> <li><code>header</code>: <code>dict[str, Any] | None = None</code> - Additional header.</li> <li><code>payload</code>: <code>dict[str, Any] | None = None</code> - Custom data.</li> </ul> <p>Returns:</p> <p><code>str</code>: Encoded JWT.</p> <pre><code>token = jwt.encode(\n    exp=3600,\n    sub=\"user@email.com\",\n    payload={\"user_id\": 123}\n)\n</code></pre>"},{"location":"usage/jose/jwt/#decode-token_1","title":"Decode token","text":"<p>Method: <code>jwt.decode</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - JWT token.</li> <li><code>validate_claims</code>: <code>bool = True</code> - Validate exp/nbf.</li> </ul> <p>Returns:</p> <p><code>dict[str, dict[str, Any]]</code>: Dict with <code>header</code> and <code>payload</code> keys.</p> <p>Raises:</p> <ul> <li><code>JamJWTExpired</code> - Token expired.</li> <li><code>JamJWTNotYetValid</code> - Token not yet valid.</li> <li><code>JamJWSVerificationError</code> - Invalid signature or token type.</li> </ul> <pre><code>data = jwt.decode(token, validate_claims=True)\nprint(data[\"payload\"][\"sub\"])\n&gt;&gt;&gt; \"user@email.com\"\n</code></pre>"},{"location":"usage/jose/jwt/#encrypt-token_1","title":"Encrypt token","text":"<p>Method: <code>jwt.encrypt</code></p> <p>Args:</p> <ul> <li><code>payload</code>: <code>dict[str, Any] | str</code> - Data to encrypt.</li> <li><code>header</code>: <code>dict[str, Any] | None = None</code> - Additional header.</li> </ul> <p>Returns:</p> <p><code>str</code>: Encrypted JWT.</p> <pre><code>encrypted = jwt.encrypt(\n    payload={\"data\": \"sensitive\"},\n    header={\"purpose\": \"auth\"}\n)\n</code></pre>"},{"location":"usage/jose/jwt/#decrypt-token_1","title":"Decrypt token","text":"<p>Method: <code>jwt.decrypt</code></p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - Encrypted JWT.</li> </ul> <p>Returns:</p> <p><code>dict[str, Any]</code>: Decrypted data.</p> <pre><code>data = jwt.decrypt(token=encrypted)\nprint(data)\n&gt;&gt;&gt; {\"data\": \"sensitive\"}\n</code></pre>"},{"location":"usage/jose/jwt/#generate-jti","title":"Generate JTI","text":"<p>Property: <code>jwt.jti</code></p> <p>Returns:</p> <p><code>str</code>: Unique UUID for JWT ID.</p> <pre><code>jti = jwt.jti\nprint(jti)\n&gt;&gt;&gt; \"0c2c38d2-5dcb-4294-bb2d-0820f6ff787d\"\n</code></pre>"},{"location":"usage/jose/lists/","title":"Lists","text":""},{"location":"usage/jose/lists/#use-in-instance","title":"Use in instance","text":""},{"location":"usage/jose/lists/#config","title":"Config","text":"<pre><code>[jam.jose.jwt]\nalg = \"$JWT_ALG\"\nsecret_key = \"$JWT_SECRET_KEY\"\n\n[jam.jose.jwt.list]\ntype = \"black\"\nbackend = \"redis\"\nredis_uri = \"redis://localhost:6379\"\nttl = 3600\n</code></pre> <p>Args:</p> <ul> <li><code>type</code>: <code>str</code> - List type: <code>black</code> or <code>white</code>.</li> <li><code>backend</code>: <code>str</code> - Storage backend: <code>redis</code>, <code>json</code>, <code>memory</code>.</li> <li><code>redis_uri</code>: <code>str</code> - Redis connection URI (for redis backend).</li> <li><code>json_path</code>: <code>str</code> - JSON file path (for json backend).</li> <li><code>ttl</code>: <code>int</code> - Time to live in seconds (optional, for redis).</li> <li><code>prefix</code>: <code>str</code> - Key prefix for namespacing.</li> </ul>"},{"location":"usage/jose/lists/#usage","title":"Usage","text":"<pre><code>from jam import Jam\n\njam = Jam(config=\"config.toml\")\n</code></pre>"},{"location":"usage/jose/lists/#add-token-to-list","title":"Add token to list","text":"<p>Method: <code>jam.jwt_list.add</code></p> <p>Adds token to blacklist or whitelist.</p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - JWT token to add.</li> </ul> <pre><code>jam.jwt_list.add(token=token)\n</code></pre>"},{"location":"usage/jose/lists/#check-token-in-list","title":"Check token in list","text":"<p>Method: <code>jam.jwt_list.check</code></p> <p>Checks if token is in list.</p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - JWT token to check.</li> </ul> <p>Returns:</p> <p><code>bool</code>: <code>True</code> if token is in list, <code>False</code> otherwise.</p> <pre><code>is_revoked = jam.jwt_list.check(token=token)\nif is_revoked:\n    print(\"Token is revoked\")\n</code></pre>"},{"location":"usage/jose/lists/#delete-token-from-list","title":"Delete token from list","text":"<p>Method: <code>jam.jwt_list.delete</code></p> <p>Removes token from list.</p> <p>Args:</p> <ul> <li><code>token</code>: <code>str</code> - JWT token to delete.</li> </ul> <pre><code>jam.jwt_list.delete(token=token)\n</code></pre>"},{"location":"usage/jose/lists/#add-multiple-tokens","title":"Add multiple tokens","text":"<p>Method: <code>jam.jwt_list.add_many</code></p> <p>Adds multiple tokens to list.</p> <p>Args:</p> <ul> <li><code>tokens</code>: <code>list[str]</code> - List of JWT tokens.</li> </ul> <pre><code>jam.jwt_list.add_many(tokens=[token1, token2, token3])\n</code></pre>"},{"location":"usage/jose/lists/#check-multiple-tokens","title":"Check multiple tokens","text":"<p>Method: <code>jam.jwt_list.check_many</code></p> <p>Checks multiple tokens in list.</p> <p>Args:</p> <ul> <li><code>tokens</code>: <code>list[str]</code> - List of JWT tokens.</li> </ul> <p>Returns:</p> <p><code>dict[str, bool]</code>: Dict mapping tokens to their presence status.</p> <pre><code>results = jam.jwt_list.check_many(tokens=[token1, token2])\nprint(results)\n&gt;&gt;&gt; {token1: True, token2: False}\n</code></pre>"},{"location":"usage/jose/lists/#delete-multiple-tokens","title":"Delete multiple tokens","text":"<p>Method: <code>jam.jwt_list.delete_many</code></p> <p>Removes multiple tokens from list.</p> <p>Args:</p> <ul> <li><code>tokens</code>: <code>list[str]</code> - List of JWT tokens.</li> </ul> <pre><code>jam.jwt_list.delete_many(tokens=[token1, token2])\n</code></pre>"},{"location":"usage/jose/lists/#use-out-of-instance","title":"Use out of instance","text":""},{"location":"usage/jose/lists/#redislist","title":"RedisList","text":"<p>Redis-based token list. Most optimal for production with TTL support.</p> <p>Module: <code>jam.jose.lists.redis.RedisList</code></p> <p>Args:</p> <ul> <li><code>type</code>: <code>str</code> - List type: <code>black</code> or <code>white</code>.</li> <li><code>prefix</code>: <code>str</code> - Key prefix for namespacing.</li> <li><code>redis_uri</code>: <code>str</code> - Redis connection URI.</li> <li><code>redis</code>: <code>Redis</code> - Pre-configured Redis client (optional).</li> <li><code>ttl</code>: <code>int</code> - Time to live in seconds (optional).</li> <li><code>logger</code>: <code>BaseLogger</code> - Logger instance.</li> </ul> <pre><code>from jam.jose.lists.redis import RedisList\n\nlist = RedisList(\n    type=\"black\",\n    prefix=\"jwt\",\n    redis_uri=\"redis://localhost:6379\",\n    ttl=3600\n)\nlist.add(token)\nlist.check(token)\nlist.delete(token)\n</code></pre> <p>TTL behavior</p> <p>When <code>ttl</code> is set, tokens automatically expire from the list after the specified number of seconds. This is useful for token blacklists where tokens should only be tracked until their natural expiration.</p>"},{"location":"usage/jose/lists/#memorylist","title":"MemoryList","text":"<p>In-memory token list. Simple but not persistent.</p> <p>Module: <code>jam.jose.lists.memory.MemoryList</code></p> <p>Args:</p> <ul> <li><code>type</code>: <code>str</code> - List type: <code>black</code> or <code>white</code>.</li> <li><code>prefix</code>: <code>str</code> - Key prefix for namespacing.</li> <li><code>logger</code>: <code>BaseLogger</code> - Logger instance.</li> </ul> <pre><code>from jam.jose.lists.memory import MemoryList\n\nlist = MemoryList(\n    type=\"black\",\n    prefix=\"jwt\"\n)\nlist.add(token)\nlist.check(token)\nlist.delete(token)\n</code></pre>"},{"location":"usage/jose/lists/#jsonlist","title":"JSONList","text":"<p>JSON file-based token list. Persistent but limited scalability.</p> <p>Module: <code>jam.jose.lists.json.JSONList</code></p> <p>Args:</p> <ul> <li><code>type</code>: <code>str</code> - List type: <code>black</code> or <code>white</code>.</li> <li><code>prefix</code>: <code>str</code> - Key prefix for namespacing.</li> <li><code>json_path</code>: <code>str</code> - Path to JSON file.</li> <li><code>logger</code>: <code>BaseLogger</code> - Logger instance.</li> </ul> <pre><code>from jam.jose.lists.json import JSONList\n\nlist = JSONList(\n    type=\"black\",\n    prefix=\"jwt\",\n    json_path=\"blacklist.json\"\n)\nlist.add(token)\nlist.check(token)\nlist.delete(token)\n</code></pre>"},{"location":"usage/jose/lists/#methods-comparison","title":"Methods comparison","text":"Method MemoryList RedisList JSONList <code>add</code> \u2713 \u2713 \u2713 <code>delete</code> \u2713 \u2713 \u2713 <code>check</code> \u2713 \u2713 \u2713 <code>add_many</code> \u2713 \u2713 \u2713 <code>delete_many</code> \u2713 \u2713 \u2713 <code>check_many</code> \u2713 \u2713 \u2713 TTL support \u2717 \u2713 \u2717 Persistence \u2717 \u2713 \u2713"},{"location":"usage/jose/lists/#blacklist-vs-whitelist","title":"Blacklist vs Whitelist","text":""},{"location":"usage/jose/lists/#blacklist","title":"Blacklist","text":"<p>Tokens in blacklist are rejected.</p> <pre><code>list = RedisList(type=\"black\", prefix=\"jwt\", redis_uri=\"...\")\n\n# Token is in blacklist\nif list.check(token):\n    raise Exception(\"Token has been revoked\")\n</code></pre>"},{"location":"usage/jose/lists/#whitelist","title":"Whitelist","text":"<p>Only tokens in whitelist are accepted.</p> <pre><code>list = RedisList(type=\"white\", prefix=\"jwt\", redis_uri=\"...\")\n\n# Token is not in whitelist\nif not list.check(token):\n    raise Exception(\"Token is not valid\")\n</code></pre>"},{"location":"usage/utils/auth_utils/","title":"Auth utils","text":"<p>For convenience, Jam includes some utilities for authentication mechanisms and simple encryption.</p>"},{"location":"usage/utils/auth_utils/#password-hashing","title":"Password hashing","text":""},{"location":"usage/utils/auth_utils/#create-hash","title":"Create hash","text":"<p>Util: <code>jam.utils.hash_password</code></p> <p>Args:</p> <ul> <li><code>password</code>: <code>str</code> - Password.</li> <li><code>salt</code>: <code>bytes | None = None</code> - Some salt.</li> <li><code>iterations</code>: <code>int = 100_000</code> - Number of iterations for hashing.</li> <li><code>salt_size</code>: <code>int = 16</code> - Size of salt in bytes.</li> </ul> <p>Returns:</p> <p><code>tuple[str, str]</code> - hex salt, hex hash</p> <pre><code>from jam.utils import hash_password\n\nsome_password = \"qwerty1234\"\nsalt, hash_ = hash_password(\n    password=some_password\n)\nprint(salt)\n&gt;&gt;&gt; 92053744b136bed21397f25a7c19bc40\nprint(hash_)\n&gt;&gt;&gt; 0b1ee2ad3bda7c1697ae09733c96449b69c951fe425ef36b7620700f3e77f184\n</code></pre>"},{"location":"usage/utils/auth_utils/#verify-password-hash","title":"Verify password hash","text":"<p>Util: <code>jam.utils.check_password</code></p> <p>Args:</p> <ul> <li><code>password</code>: <code>str</code> - Password to check.</li> <li><code>salt_hex</code>: <code>str</code> - Salt.</li> <li><code>hash_hex</code>: <code>str</code> - Hash.</li> <li><code>iterations</code>: <code>int = 100_000</code> - Number of iterations for hashing.</li> </ul> <p>Returns:</p> <p><code>bool</code> - Whether the password matches the hash.</p> <pre><code>from jam.utils import check_password\n\nvalid = check_password(\n    password=\"qwerty1234\",\n    salt_hex=salt,\n    hash_hex=hash_\n)\nprint(valid)\n&gt;&gt;&gt; True\n</code></pre>"},{"location":"usage/utils/auth_utils/#basic-auth","title":"Basic auth","text":""},{"location":"usage/utils/auth_utils/#encode-header","title":"Encode header","text":"<p>Util: <code>jam.utils.basic_auth_encode</code></p> <p>Args:</p> <ul> <li><code>login</code>: <code>str</code> - Login.</li> <li><code>password</code>: <code>str</code> - Password.</li> </ul> <p>Returns:</p> <p><code>str</code> - Basic auth header value.</p> <pre><code>from jam.utils import basic_auth_encode\n\nauth_header = basic_auth_encode(\n    login=\"user\",\n    password=\"password\"\n)\nprint(auth_header)\n&gt;&gt;&gt; \"dXNlcjpwYXNzd29yZA==\"\n</code></pre>"},{"location":"usage/utils/auth_utils/#decode-header","title":"Decode header","text":"<p>Util: <code>jam.utils.basic_auth_decode</code></p> <p>Args:</p> <ul> <li><code>data</code>: <code>str</code></li> </ul> <p>Returns:</p> <p><code>tuple[str, str]</code> - Login, Password.</p> <pre><code>from jam.utils import basic_auth_decode\n\nlogin, password = basic_auth_decode(\n    data=\"dXNlcjpwYXNzd29yZA==\"\n)\nprint(login)\n&gt;&gt;&gt; \"user\"\nprint(password)\n&gt;&gt;&gt; \"password\"\n</code></pre>"},{"location":"usage/utils/auth_utils/#xor-data","title":"XOR data","text":"<p>Util: <code>jam.utils.xor_my_data</code></p> <p>Args:</p> <ul> <li><code>data</code>: <code>str</code> - Data to XOR.</li> <li><code>key</code>: <code>str</code> - XOR key.</li> </ul> <p>Returns:</p> <p><code>str</code> - XORed data.</p> <pre><code>from jam.utils import xor_my_data\n\ndata = \"secret\"\nkey = \"key\"\nxored = xor_my_data(data=data, key=key)\nprint(xored)\n&gt;&gt;&gt; \"18001a19000d\"\n</code></pre>"},{"location":"usage/utils/key_generators/","title":"Key generators","text":"<p>For your convenience, Jam includes several utilities for generating keys.</p> <p>Tip</p> <p>All of these utilities are available in the Jam CLI. See the documentation.</p>"},{"location":"usage/utils/key_generators/#symmetric-key-generator","title":"Symmetric key generator","text":"<p>Util: <code>jam.utils.generate_symmetric_key</code></p> <p>Args:</p> <ul> <li><code>n</code>: <code>int = 32</code> - Key length in bytes.</li> </ul> <p>Returns:</p> <p><code>str</code> - Symmetric key.</p> <pre><code>from jam.utils import generate_symmetric_key\n\nkey = generate_symmetric_key()\nprint(key)\n&gt;&gt;&gt; KwMcnHqffc-jEKLrCdk93pNBHrTgyFH-MeWu2Z_udco\n</code></pre>"},{"location":"usage/utils/key_generators/#aes-key-generator","title":"AES key generator","text":"<p>Util: <code>jam.utils.generate_aes_key</code></p> <p>Returns:</p> <p><code>bytes</code>: AES key.</p> <pre><code>from jam.utils import generate_aes_key\n\nkey = generate_aes_key()\nprint(key)\n&gt;&gt;&gt; b'NMuwX-O7wgSrOv8NMXkjFrMAwKQsUpWpnkmEnAM0TzE='\n</code></pre>"},{"location":"usage/utils/key_generators/#ed-keypair","title":"ED keypair","text":"<p>Utils:</p> <ul> <li><code>jam.utils.generate_ed25519_keypair</code></li> <li><code>jam.utils.generate_ecdsa_p384_keypair</code></li> </ul> <p>Returns:</p> <p><code>dict[str, str]</code> - Dict in format <code>{\"private\": KEY, \"public\": KEY}</code>.</p> <pre><code>from jam.utils import generate_ed25519_keypair\n\ned25519_keypair = generate_ed25519_keypair()\nprint(ed25519_keypair)\n&gt;&gt;&gt; {'private': '', 'public': ''}\n####\nfrom jam.utils import generate_ecdsa_p384_keypair\n\necdsa_p384_keypair = generate_ecdsa_p384_keypair()\nprint(ecdsa_p384_keypair)\n&gt;&gt;&gt; {'private': '', 'public': ''}\n</code></pre>"},{"location":"usage/utils/key_generators/#otp-specific-key","title":"OTP specific key","text":"<p>Utils:</p> <ul> <li><code>jam.utils.generate_otp_key</code></li> </ul> <p>Args:</p> <ul> <li><code>entropy_bits</code>: <code>int = 128</code> - Number of entropy bits to use for key generation.</li> </ul> <p>Returns:</p> <p><code>str</code> - OTP key.</p> <ul> <li><code>jam.utils.otp_key_from_string</code></li> </ul> <p>Args: </p> <ul> <li><code>s</code>: <code>str</code> - Some string to generate.</li> </ul> <p>Returns:</p> <p><code>str</code> - OTP Key.</p> <pre><code>from jam.utils import generate_otp_key\n\nkey = generate_otp_key()\nprint(key)\n&gt;&gt;&gt; SSDEWUXGHUG3XG53JMVYO555WI\n\n### from string\nfrom jam.utils import otp_key_from_string\n\nkey = otp_key_from_string(\"someusername@example.com\")\nprint(key)\n&gt;&gt;&gt; CUK2QXKSV5DSNPI46DPK46TYYRIMX7QK\n</code></pre>"},{"location":"usage/utils/key_generators/#rsa-keypair","title":"RSA keypair","text":"<p>Util <code>jam.utils.generate_rsa_key_pair</code></p> <p>Args:</p> <ul> <li><code>key_size</code>: <code>int = 2048</code> - Key size in bits.</li> </ul> <p>Returns:</p> <p><code>dict[str, str]</code> - Dict in format <code>{\"private\": KEY, \"public\": KEY}</code></p> <pre><code>from jam.utils import generate_rsa_key_pair\n\nrsa_keypair = generate_rsa_key_pair()\nprint(rsa_keypair)\n&gt;&gt;&gt; {'private': '', 'public': ''}\n</code></pre>"}]}